// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: Product
//     Generated: 2025-12-08 09:21:43
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { productApi } from '../api/productApi';
import type {
  Product,
  CreateProductRequest,
  UpdateProductRequest,
  ProductFilters,
} from '../types/Product.types';
import type { OrderItem } from '../types/OrderItem.types';

/**
 * Hook to fetch single Product by ID.
 */
export const useProduct = (id: number | null) => {
  return useQuery({
    queryKey: ['product', id],
    queryFn: () => productApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Products with optional filters.
 */
export const useProducts = (filters?: ProductFilters) => {
  return useQuery({
    queryKey: ['products', filters],
    queryFn: () => productApi.getAll(filters),
  });
};


/**
 * Hook to fetch orderItems for a specific PRODUCT.
 */
export const useProductOrderItems = (
  productId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['product', productId, 'orderItems', skip, take],
    queryFn: () => productApi.getOrderItems(productId!, skip, take),
    enabled: productId !== null,
  });
};

/**
 * Hook to create Product.
 */
export const useCreateProduct = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateProductRequest) => productApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['products'] });
    },
  });
};


/**
 * Hook to update Product.
 */
export const useUpdateProduct = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateProductRequest }) =>
      productApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['product', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['products'] });
    },
  });
};


/**
 * Hook to delete Product.
 */
export const useDeleteProduct = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => productApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['products'] });
    },
  });
};



// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: vw_ProductSales
//     Generated: 2025-12-08 15:49:50
// </auto-generated>

import React from 'react';
import { useNavigate } from 'react-router-dom';
import ExcelJS from 'exceljs';
import { DataGrid, GridColDef, GridActionsCellItem, GridToolbarContainer, GridToolbarColumnsButton, GridToolbarDensitySelector, useGridApiRef } from '@mui/x-data-grid';
import { Button, Box, CircularProgress, Alert, TextField, Paper, MenuItem, Typography } from '@mui/material';
import { FileDownload as FileDownloadIcon, Clear as ClearIcon } from '@mui/icons-material';
import { useVwProductSaless } from '../../hooks/useVwProductSales';
import type { VwProductSales, VwProductSalesFilters } from '../../types/VwProductSales.types';


export const VwProductSalesList: React.FC = () => {
  const navigate = useNavigate();
  const apiRef = useGridApiRef();
  const [columnFilters, setColumnFilters] = React.useState<Record<string, string>>({});
  const { data: vwProductSaless, isLoading, error } = useVwProductSaless({});

  // Client-side column filters
  const filteredData = React.useMemo(() => {
    if (!vwProductSaless) return vwProductSaless;

    // Get active filters
    const activeFilters = Object.entries(columnFilters).filter(([_, value]) => value && value.trim() !== '');
    if (activeFilters.length === 0) return vwProductSaless;

    // Apply all filters with AND logic
    return vwProductSaless.filter(item => {
      return activeFilters.every(([column, filterValue]) => {
        let itemValue = item[column as keyof typeof item];
        
        // Convert Date objects and ISO date strings to display format
        if (itemValue instanceof Date) {
          itemValue = itemValue.toLocaleDateString();
        } else if (typeof itemValue === 'string' && /^\d{4}-\d{2}-\d{2}/.test(itemValue)) {
          itemValue = new Date(itemValue).toLocaleDateString();
        }
        
        return itemValue?.toString().toLowerCase().includes(filterValue.toLowerCase());
      });
    });
  }, [vwProductSaless, columnFilters]);

  const handleClearAllFilters = () => {
    setColumnFilters({});
  };

  function CustomToolbar() {
    return (
      <GridToolbarContainer>
        <GridToolbarColumnsButton />
        <GridToolbarDensitySelector />
        <Button
          size="small"
          startIcon={<ClearIcon />}
          onClick={handleClearAllFilters}
        >
          Clear All Filters
        </Button>
      </GridToolbarContainer>
    );
  }

  const handleExportToExcel = async () => {
    // Export only the filtered data visible on screen
    const visibleRows = filteredData || [];

    if (!visibleRows || visibleRows.length === 0) {
      alert('No data to export');
      return;
    }

    // Get visible columns from DataGrid (respects column visibility)
    const visibleColumns = apiRef.current
      ? apiRef.current.getVisibleColumns().map(col => col.field).filter(field => field !== 'actions')
      : Object.keys(visibleRows[0] || {});

    // Filter data to only include visible columns
    const exportData = visibleRows.map(row => {
      const filteredRow: any = {};
      visibleColumns.forEach(col => {
        if (col in row) filteredRow[col] = row[col];
      });
      return filteredRow;
    });

    // Create workbook and worksheet
    const workbook = new ExcelJS.Workbook();
    const worksheet = workbook.addWorksheet('VwProductSaless');

    // Get column names from first row (only visible columns)
    const columnNames = exportData.length > 0 ? Object.keys(exportData[0]) : [];
    worksheet.columns = columnNames.map(name => ({ header: name, key: name, width: 15 }));

    // Add data rows
    exportData.forEach(row => worksheet.addRow(row));

    // Style header row
    const headerRow = worksheet.getRow(1);
    headerRow.font = { bold: true, color: { argb: 'FFFFFFFF' } };
    headerRow.fill = { type: 'pattern', pattern: 'solid', fgColor: { argb: 'FF4472C4' } };
    headerRow.alignment = { horizontal: 'center', vertical: 'middle' };
    headerRow.height = 20;

    // Auto-fit columns based on content
    worksheet.columns.forEach(column => {
      let maxLength = column.header?.length || 10;
      column.eachCell?.({ includeEmpty: false }, cell => {
        const cellLength = cell.value ? String(cell.value).length : 0;
        maxLength = Math.max(maxLength, cellLength);
      });
      column.width = Math.min(maxLength + 2, 50);
    });

    // Freeze header row
    worksheet.views = [{ state: 'frozen', ySplit: 1 }];

    // Generate Excel file and download
    const buffer = await workbook.xlsx.writeBuffer();
    const blob = new Blob([buffer], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
    const url = window.URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `VwProductSaless_${new Date().toISOString().split('T')[0]}.xlsx`;
    link.click();
    window.URL.revokeObjectURL(url);
  };

  const columns: GridColDef<VwProductSales>[] = [
    { field: 'productID', headerName: 'ProductID', width: 90 },
    { field: 'productName', headerName: 'ProductName', width: 250 },
    { field: 'category', headerName: 'Category', width: 150 },
    { field: 'currentPrice', headerName: 'CurrentPrice', width: 150 },
    { field: 'stockQuantity', headerName: 'StockQuantity', width: 150 },
    { field: 'isActive', headerName: 'IsActive', width: 100 },
    { field: 'timesSold', headerName: 'TimesSold', width: 150 },
    { field: 'totalQuantitySold', headerName: 'TotalQuantitySold', width: 150 },
    { field: 'totalRevenue', headerName: 'TotalRevenue', width: 150 },
    { field: 'averageSellingPrice', headerName: 'AverageSellingPrice', width: 150 },
  ];


  if (isLoading) {
    return <CircularProgress />;
  }

  if (error) {
    return <Alert severity="error">Failed to load vwProductSaless</Alert>;
  }

  return (
    <Box sx={{ height: 'calc(100vh - 200px)', width: '100%', display: 'flex', flexDirection: 'column' }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<FileDownloadIcon />}
          onClick={handleExportToExcel}
        >
          Export to Excel
        </Button>
      </Box>
      <Paper sx={{ p: 2, mb: 2 }}>
        <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap', alignItems: 'center' }}>
          <TextField
            label="ProductID"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['productID'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              productID: e.target.value
            }))}
          />
          <TextField
            label="ProductName"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['productName'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              productName: e.target.value
            }))}
          />
          <TextField
            label="Category"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['category'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              category: e.target.value
            }))}
          />
          <TextField
            label="CurrentPrice"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['currentPrice'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              currentPrice: e.target.value
            }))}
          />
          <TextField
            label="StockQuantity"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['stockQuantity'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              stockQuantity: e.target.value
            }))}
          />
          <TextField
            label="IsActive"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['isActive'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              isActive: e.target.value
            }))}
          />
          <TextField
            label="TimesSold"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['timesSold'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              timesSold: e.target.value
            }))}
          />
          <TextField
            label="TotalQuantitySold"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['totalQuantitySold'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              totalQuantitySold: e.target.value
            }))}
          />
          <TextField
            label="TotalRevenue"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['totalRevenue'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              totalRevenue: e.target.value
            }))}
          />
          <TextField
            label="AverageSellingPrice"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['averageSellingPrice'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              averageSellingPrice: e.target.value
            }))}
          />
          <Button
            variant="outlined"
            size="small"
            onClick={handleClearAllFilters}
          >
            Clear
          </Button>
          <Box sx={{ ml: 'auto', display: 'flex', alignItems: 'center' }}>
            <Typography variant="body2" color="text.secondary">
              Showing {filteredData?.length || 0} of {vwProductSaless?.length || 0} rows
            </Typography>
          </Box>
        </Box>
      </Paper>

      <Box sx={{ flex: 1, minHeight: 0 }}>
        <DataGrid
          apiRef={apiRef}
          rows={filteredData || []}
          columns={columns}
          getRowId={(row) => row.productID}
          slots={{
            toolbar: CustomToolbar,
          }}
          initialState={{
            pagination: { paginationModel: { pageSize: 10 } },
          }}
          pageSizeOptions={[5, 10, 25, 100]}
          checkboxSelection
          disableRowSelectionOnClick
        />
      </Box>
    </Box>
  );
};


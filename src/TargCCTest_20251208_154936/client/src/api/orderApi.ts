// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Order
//     Generated: 2025-12-08 15:49:50
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  Order,
  CreateOrderRequest,
  UpdateOrderRequest,
  OrderFilters,
} from '../types/Order.types';
import type { OrderItem } from '../types/OrderItem.types';

/**
 * API client for Order operations.
 * Base URL: /Orders
 */
export const orderApi = {
  /**
   * Get Order by ID.
   */
  getById: async (id: number): Promise<Order> => {
    const response = await api.get<Order>(`/Orders/${id}`);
    return response.data;
  },


  /**
   * Get all Orders with optional filters.
   */
  getAll: async (filters?: OrderFilters): Promise<Order[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/Orders/filter' : '/Orders';
    const response = await api.get<Order[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new Order.
   */
  create: async (data: CreateOrderRequest): Promise<Order> => {
    const response = await api.post<Order>('/Orders', data);
    return response.data;
  },


  /**
   * Update existing Order.
   */
  update: async (id: number, data: UpdateOrderRequest): Promise<Order> => {
    const response = await api.put<Order>(`/Orders/${id}`, data);
    return response.data;
  },


  /**
   * Delete Order.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Orders/${id}`);
  },


  /**
   * Get Order by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<Order[]> => {
    const response = await api.get<Order[]>(`/Orders/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get Order by OrderDate.
   */
  getByOrderDate: async (orderDate: Date): Promise<Order[]> => {
    const response = await api.get<Order[]>(`/Orders/by-orderDate/${encodeURIComponent(String(orderDate))}`);
    return response.data;
  },


  /**
   * Get Order by Status.
   */
  getByStatus: async (status: string): Promise<Order[]> => {
    const response = await api.get<Order[]>(`/Orders/by-status/${encodeURIComponent(String(status))}`);
    return response.data;
  },


  /**
   * Get ORDERITEMS for this ORDER.
   */
  getOrderItems: async (id: number, skip?: number, take?: number): Promise<OrderItem[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<OrderItem[]>(`/Orders/${id}/ORDERITEMS${queryString}`);
    return response.data;
  },


};

// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: vw_CustomerOrderSummary
//     Generated: 2025-12-07 22:03:02
// </auto-generated>

import React from 'react';
import { useNavigate } from 'react-router-dom';
import * as XLSX from 'xlsx';
import { DataGrid, GridColDef, GridActionsCellItem, GridToolbarContainer, GridToolbarColumnsButton, GridToolbarDensitySelector, useGridApiRef } from '@mui/x-data-grid';
import { Button, Box, CircularProgress, Alert, TextField, Paper } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, Add as AddIcon, FileDownload as FileDownloadIcon, Clear as ClearIcon } from '@mui/icons-material';
import { useVwCustomerOrderSummarys, useDeleteVwCustomerOrderSummary } from '../../hooks/useVwCustomerOrderSummary';
import type { VwCustomerOrderSummary, VwCustomerOrderSummaryFilters } from '../../types/VwCustomerOrderSummary.types';


export const VwCustomerOrderSummaryList: React.FC = () => {
  const navigate = useNavigate();
  const apiRef = useGridApiRef();
  const [filters, setFilters] = React.useState<VwCustomerOrderSummaryFilters>({});
  const [localFilters, setLocalFilters] = React.useState<VwCustomerOrderSummaryFilters>({});
  const [filterModel, setFilterModel] = React.useState<any>({ items: [] });
  const { data: vwCustomerOrderSummarys, isLoading, error } = useVwCustomerOrderSummarys(filters);
  const { mutate: deleteEntity } = useDeleteVwCustomerOrderSummary();

  const handleApplyFilters = () => {
    setFilters(localFilters);
  };

  const handleClearFilters = () => {
    setLocalFilters({});
    setFilters({});
  };

  const handleClearAllFilters = () => {
    // Clear top panel filters
    setLocalFilters({});
    setFilters({});
    // Clear DataGrid column filters
    setFilterModel({ items: [] });
  };

  function CustomToolbar() {
    return (
      <GridToolbarContainer>
        <GridToolbarColumnsButton />
        <GridToolbarDensitySelector />
        <Button
          size="small"
          startIcon={<ClearIcon />}
          onClick={handleClearAllFilters}
        >
          Clear All Filters
        </Button>
      </GridToolbarContainer>
    );
  }

  const handleExportToExcel = () => {
    if (!vwCustomerOrderSummarys || vwCustomerOrderSummarys.length === 0) {
      alert('No data to export');
      return;
    }

    const ws = XLSX.utils.json_to_sheet(vwCustomerOrderSummarys);
    const wb = XLSX.utils.book_new();
    XLSX.utils.book_append_sheet(wb, ws, 'VwCustomerOrderSummarys');
    XLSX.writeFile(wb, `VwCustomerOrderSummarys_${new Date().toISOString().split('T')[0]}.xlsx`);
  };

  const columns: GridColDef<VwCustomerOrderSummary>[] = [
    { field: 'customerID', headerName: 'CustomerID', width: 90 },
    { field: 'customerName', headerName: 'CustomerName', width: 250 },
    { field: 'email', headerName: 'Email', width: 250 },
    { field: 'status', headerName: 'Status', width: 150 },
    { field: 'totalOrders', headerName: 'TotalOrders', width: 150 },
    { field: 'totalSpent', headerName: 'TotalSpent', width: 150 },
    { field: 'lastOrderDate', headerName: 'LastOrderDate', width: 150, valueFormatter: (value) => value ? new Date(value).toLocaleDateString() : '' },
    { field: 'firstOrderDate', headerName: 'FirstOrderDate', width: 150, valueFormatter: (value) => value ? new Date(value).toLocaleDateString() : '' },
  ];


  if (isLoading) {
    return <CircularProgress />;
  }

  if (error) {
    return <Alert severity="error">Failed to load vwCustomerOrderSummarys</Alert>;
  }

  return (
    <Box sx={{ height: 'calc(100vh - 200px)', width: '100%', display: 'flex', flexDirection: 'column' }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<FileDownloadIcon />}
          onClick={handleExportToExcel}
        >
          Export to Excel
        </Button>
      </Box>
      <Paper sx={{ p: 2, mb: 2 }}>
        <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap', alignItems: 'center' }}>
          <TextField
            label="CustomerName"
            type="text"
            size="small"
            sx={{ minWidth: 200 }}
            value={localFilters.customerName ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              customerName: e.target.value || undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="Email"
            type="text"
            size="small"
            sx={{ minWidth: 200 }}
            value={localFilters.email ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              email: e.target.value || undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="Status"
            type="text"
            size="small"
            sx={{ minWidth: 200 }}
            value={localFilters.status ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              status: e.target.value || undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="TotalOrders"
            type="number"
            size="small"
            sx={{ minWidth: 200 }}
            value={localFilters.totalOrders ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              totalOrders: e.target.value ? Number(e.target.value) : undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="TotalSpent"
            type="number"
            size="small"
            sx={{ minWidth: 200 }}
            value={localFilters.totalSpent ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              totalSpent: e.target.value ? Number(e.target.value) : undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <Button
            variant="contained"
            color="primary"
            onClick={handleApplyFilters}
            size="small"
          >
            Apply Filters
          </Button>
          <Button
            variant="outlined"
            onClick={handleClearFilters}
            size="small"
          >
            Clear
          </Button>
        </Box>
      </Paper>

      <Box sx={{ flex: 1, minHeight: 0 }}>
        <DataGrid
          apiRef={apiRef}
          rows={vwCustomerOrderSummarys || []}
          columns={columns}
          getRowId={(row) => row.customerID}
          filterMode="client"
          filterModel={filterModel}
          onFilterModelChange={setFilterModel}
          slots={{
            toolbar: CustomToolbar,
          }}
          initialState={{
            pagination: { paginationModel: { pageSize: 10 } },
          }}
          pageSizeOptions={[5, 10, 25, 100]}
          checkboxSelection
          disableRowSelectionOnClick
        />
      </Box>
    </Box>
  );
};


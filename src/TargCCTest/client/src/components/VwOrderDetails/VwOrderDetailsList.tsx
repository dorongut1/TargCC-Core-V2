// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: vw_OrderDetails
//     Generated: 2025-12-07 22:03:02
// </auto-generated>

import React from 'react';
import { useNavigate } from 'react-router-dom';
import * as XLSX from 'xlsx';
import { DataGrid, GridColDef, GridActionsCellItem, GridToolbarContainer, GridToolbarColumnsButton, GridToolbarDensitySelector, useGridApiRef } from '@mui/x-data-grid';
import { Button, Box, CircularProgress, Alert, TextField, Paper } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, Add as AddIcon, FileDownload as FileDownloadIcon, Clear as ClearIcon } from '@mui/icons-material';
import { useVwOrderDetailss, useDeleteVwOrderDetails } from '../../hooks/useVwOrderDetails';
import type { VwOrderDetails, VwOrderDetailsFilters } from '../../types/VwOrderDetails.types';


export const VwOrderDetailsList: React.FC = () => {
  const navigate = useNavigate();
  const apiRef = useGridApiRef();
  const [filters, setFilters] = React.useState<VwOrderDetailsFilters>({});
  const [localFilters, setLocalFilters] = React.useState<VwOrderDetailsFilters>({});
  const [filterModel, setFilterModel] = React.useState<any>({ items: [] });
  const { data: vwOrderDetailss, isLoading, error } = useVwOrderDetailss(filters);
  const { mutate: deleteEntity } = useDeleteVwOrderDetails();

  const handleApplyFilters = () => {
    setFilters(localFilters);
  };

  const handleClearFilters = () => {
    setLocalFilters({});
    setFilters({});
  };

  const handleClearAllFilters = () => {
    // Clear top panel filters
    setLocalFilters({});
    setFilters({});
    // Clear DataGrid column filters
    setFilterModel({ items: [] });
  };

  function CustomToolbar() {
    return (
      <GridToolbarContainer>
        <GridToolbarColumnsButton />
        <GridToolbarDensitySelector />
        <Button
          size="small"
          startIcon={<ClearIcon />}
          onClick={handleClearAllFilters}
        >
          Clear All Filters
        </Button>
      </GridToolbarContainer>
    );
  }

  const handleExportToExcel = () => {
    if (!vwOrderDetailss || vwOrderDetailss.length === 0) {
      alert('No data to export');
      return;
    }

    const ws = XLSX.utils.json_to_sheet(vwOrderDetailss);
    const wb = XLSX.utils.book_new();
    XLSX.utils.book_append_sheet(wb, ws, 'VwOrderDetailss');
    XLSX.writeFile(wb, `VwOrderDetailss_${new Date().toISOString().split('T')[0]}.xlsx`);
  };

  const columns: GridColDef<VwOrderDetails>[] = [
    { field: 'orderID', headerName: 'OrderID', width: 90 },
    { field: 'orderDate', headerName: 'OrderDate', width: 150, valueFormatter: (value) => value ? new Date(value).toLocaleDateString() : '' },
    { field: 'totalAmount', headerName: 'TotalAmount', width: 150 },
    { field: 'orderStatus', headerName: 'OrderStatus', width: 150 },
    { field: 'customerID', headerName: 'CustomerID', width: 150 },
    { field: 'customerName', headerName: 'CustomerName', width: 250 },
    { field: 'customerEmail', headerName: 'CustomerEmail', width: 250 },
    { field: 'customerPhone', headerName: 'CustomerPhone', width: 150 },
    { field: 'itemCount', headerName: 'ItemCount', width: 150 },
    { field: 'totalQuantity', headerName: 'TotalQuantity', width: 150 },
  ];


  if (isLoading) {
    return <CircularProgress />;
  }

  if (error) {
    return <Alert severity="error">Failed to load vwOrderDetailss</Alert>;
  }

  return (
    <Box sx={{ height: 'calc(100vh - 200px)', width: '100%', display: 'flex', flexDirection: 'column' }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<FileDownloadIcon />}
          onClick={handleExportToExcel}
        >
          Export to Excel
        </Button>
      </Box>
      <Paper sx={{ p: 2, mb: 2 }}>
        <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap', alignItems: 'center' }}>
          <TextField
            label="OrderDate"
            type="date"
            size="small"
            sx={{ minWidth: 200 }}
            InputLabelProps={{ shrink: true }}
            value={localFilters.orderDate ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              orderDate: e.target.value || undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="TotalAmount"
            type="number"
            size="small"
            sx={{ minWidth: 200 }}
            value={localFilters.totalAmount ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              totalAmount: e.target.value ? Number(e.target.value) : undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="OrderStatus"
            type="text"
            size="small"
            sx={{ minWidth: 200 }}
            value={localFilters.orderStatus ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              orderStatus: e.target.value || undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="CustomerID"
            type="number"
            size="small"
            sx={{ minWidth: 200 }}
            value={localFilters.customerID ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              customerID: e.target.value ? Number(e.target.value) : undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="CustomerName"
            type="text"
            size="small"
            sx={{ minWidth: 200 }}
            value={localFilters.customerName ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              customerName: e.target.value || undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <Button
            variant="contained"
            color="primary"
            onClick={handleApplyFilters}
            size="small"
          >
            Apply Filters
          </Button>
          <Button
            variant="outlined"
            onClick={handleClearFilters}
            size="small"
          >
            Clear
          </Button>
        </Box>
      </Paper>

      <Box sx={{ flex: 1, minHeight: 0 }}>
        <DataGrid
          apiRef={apiRef}
          rows={vwOrderDetailss || []}
          columns={columns}
          getRowId={(row) => row.orderID}
          filterMode="client"
          filterModel={filterModel}
          onFilterModelChange={setFilterModel}
          slots={{
            toolbar: CustomToolbar,
          }}
          initialState={{
            pagination: { paginationModel: { pageSize: 10 } },
          }}
          pageSizeOptions={[5, 10, 25, 100]}
          checkboxSelection
          disableRowSelectionOnClick
        />
      </Box>
    </Box>
  );
};


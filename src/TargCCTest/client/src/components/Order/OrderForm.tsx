// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Order
//     Generated: 2025-12-07 22:03:02
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useOrder, useCreateOrder, useUpdateOrder } from '../../hooks/useOrder';
import type { CreateOrderRequest, UpdateOrderRequest } from '../../types/Order.types';


export const OrderForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const orderId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!orderId;

  const { data: existingOrder, isPending: isLoadingEntity } = useOrder(orderId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateOrderRequest>({
    defaultValues: {
      status: '',
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingOrder) {
      reset(existingOrder);
    }
  }, [existingOrder, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateOrder();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateOrder();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateOrderRequest) => {
    if (isEdit && orderId) {
      updateEntity(
        { id: orderId, data: data as UpdateOrderRequest },
        { onSuccess: () => navigate('/orders') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/orders') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="OrderDate"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('orderDate', { required: 'OrderDate is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.orderDate}
        helperText={errors.orderDate?.message}
      />

      <TextField
        label="TotalAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('totalAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.totalAmount}
        helperText={errors.totalAmount?.message}
      />

      <FormControl fullWidth margin="normal">
        <InputLabel>Status</InputLabel>
        <Select
          label="Status"
          {...register('status', { maxLength: { value: 20, message: 'Maximum length is 20' } })}
          error={!!errors.status}
        >
          {/* TODO: Load enum values for Status */}
          <MenuItem value="">Select...</MenuItem>
        </Select>
      </FormControl>



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/orders')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Product
//     Generated: 2025-12-07 18:18:22
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  Product,
  CreateProductRequest,
  UpdateProductRequest,
  ProductFilters,
} from '../types/Product.types';
import type { OrderItem } from '../types/OrderItem.types';

/**
 * API client for Product operations.
 * Base URL: /products
 */
export const productApi = {
  /**
   * Get Product by ID.
   */
  getById: async (id: number): Promise<Product> => {
    const response = await api.get<Product>(`/products/${id}`);
    return response.data;
  },


  /**
   * Get all Products with optional filters.
   */
  getAll: async (filters?: ProductFilters): Promise<Product[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/products/filter' : '/products';
    const response = await api.get<Product[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new Product.
   */
  create: async (data: CreateProductRequest): Promise<Product> => {
    const response = await api.post<Product>('/products', data);
    return response.data;
  },


  /**
   * Update existing Product.
   */
  update: async (id: number, data: UpdateProductRequest): Promise<Product> => {
    const response = await api.put<Product>(`/products/${id}`, data);
    return response.data;
  },


  /**
   * Delete Product.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/products/${id}`);
  },


  /**
   * Get Product by Category.
   */
  getByCategory: async (category: string): Promise<Product[]> => {
    const response = await api.get<Product[]>(`/products/by-category/${encodeURIComponent(String(category))}`);
    return response.data;
  },


  /**
   * Get Product by Name.
   */
  getByName: async (name: string): Promise<Product[]> => {
    const response = await api.get<Product[]>(`/products/by-name/${encodeURIComponent(String(name))}`);
    return response.data;
  },


  /**
   * Get ORDERITEMS for this PRODUCT.
   */
  getOrderItems: async (id: number, skip?: number, take?: number): Promise<OrderItem[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<OrderItem[]>(`/products/${id}/ORDERITEMS${queryString}`);
    return response.data;
  },


};

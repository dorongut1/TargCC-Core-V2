// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Customer
//     Generated: 2025-12-07 18:03:37
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  Customer,
  CreateCustomerRequest,
  UpdateCustomerRequest,
  CustomerFilters,
} from '../types/Customer.types';
import type { Order } from '../types/Order.types';

/**
 * API client for Customer operations.
 * Base URL: /customers
 */
export const customerApi = {
  /**
   * Get Customer by ID.
   */
  getById: async (id: number): Promise<Customer> => {
    const response = await api.get<Customer>(`/customers/${id}`);
    return response.data;
  },


  /**
   * Get all Customers with optional filters.
   */
  getAll: async (filters?: CustomerFilters): Promise<Customer[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/customers/filter' : '/customers';
    const response = await api.get<Customer[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new Customer.
   */
  create: async (data: CreateCustomerRequest): Promise<Customer> => {
    const response = await api.post<Customer>('/customers', data);
    return response.data;
  },


  /**
   * Update existing Customer.
   */
  update: async (id: number, data: UpdateCustomerRequest): Promise<Customer> => {
    const response = await api.put<Customer>(`/customers/${id}`, data);
    return response.data;
  },


  /**
   * Delete Customer.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/customers/${id}`);
  },


  /**
   * Get Customer by Email.
   */
  getByEmail: async (email: string): Promise<Customer> => {
    const response = await api.get<Customer>(`/customers/by-email/${encodeURIComponent(String(email))}`);
    return response.data;
  },


  /**
   * Get Customer by Status.
   */
  getByStatus: async (status: string): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/customers/by-status/${encodeURIComponent(String(status))}`);
    return response.data;
  },


  /**
   * Get ORDERS for this CUSTOMER.
   */
  getOrders: async (id: number, skip?: number, take?: number): Promise<Order[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Order[]>(`/customers/${id}/ORDERS${queryString}`);
    return response.data;
  },


};

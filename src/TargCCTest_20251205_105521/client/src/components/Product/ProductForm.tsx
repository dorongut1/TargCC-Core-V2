// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Product
//     Generated: 2025-12-06 21:46:58
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useProduct, useCreateProduct, useUpdateProduct } from '../../hooks/useProduct';
import type { CreateProductRequest, UpdateProductRequest } from '../../types/Product.types';


export const ProductForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const productId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!productId;

  const { data: existingProduct, isPending: isLoadingEntity } = useProduct(productId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateProductRequest>({
    defaultValues: {
      category: '',
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingProduct) {
      reset(existingProduct);
    }
  }, [existingProduct, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateProduct();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateProduct();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateProductRequest) => {
    if (isEdit && productId) {
      updateEntity(
        { id: productId, data: data as UpdateProductRequest },
        { onSuccess: () => navigate('/products') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/products') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="Name"
        type="text"
        fullWidth
        margin="normal"
        {...register('name', { required: 'Name is required', maxLength: { value: 400, message: 'Maximum length is 400' } })}
        error={!!errors.name}
        helperText={errors.name?.message}
      />

      <TextField
        label="Description"
        type="text"
        fullWidth
        margin="normal"
        {...register('description', {  })}
        error={!!errors.description}
        helperText={errors.description?.message}
      />

      <TextField
        label="Price"
        type="number"
        fullWidth
        margin="normal"
        {...register('price', { required: 'Price is required', maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.price}
        helperText={errors.price?.message}
      />

      <TextField
        label="StockQuantity"
        type="number"
        fullWidth
        margin="normal"
        {...register('stockQuantity', { required: 'StockQuantity is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.stockQuantity}
        helperText={errors.stockQuantity?.message}
      />

      <FormControl fullWidth margin="normal">
        <InputLabel>Category</InputLabel>
        <Select
          label="Category"
          {...register('category', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
          error={!!errors.category}
        >
          {/* TODO: Load enum values for Category */}
          <MenuItem value="">Select...</MenuItem>
        </Select>
      </FormControl>

      <FormControlLabel
        control={<Checkbox {...register('isActive')} />}
        label="IsActive"
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/products')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


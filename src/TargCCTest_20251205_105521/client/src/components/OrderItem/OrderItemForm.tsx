// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: OrderItem
//     Generated: 2025-12-06 22:12:35
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useOrderItem, useCreateOrderItem, useUpdateOrderItem } from '../../hooks/useOrderItem';
import type { CreateOrderItemRequest, UpdateOrderItemRequest } from '../../types/OrderItem.types';


export const OrderItemForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const orderItemId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!orderItemId;

  const { data: existingOrderItem, isPending: isLoadingEntity } = useOrderItem(orderItemId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateOrderItemRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingOrderItem) {
      reset(existingOrderItem);
    }
  }, [existingOrderItem, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateOrderItem();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateOrderItem();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateOrderItemRequest) => {
    if (isEdit && orderItemId) {
      updateEntity(
        { id: orderItemId, data: data as UpdateOrderItemRequest },
        { onSuccess: () => navigate('/orderItems') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/orderItems') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="OrderID"
        type="number"
        fullWidth
        margin="normal"
        {...register('orderID', { required: 'OrderID is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.orderID}
        helperText={errors.orderID?.message}
      />

      <TextField
        label="ProductID"
        type="number"
        fullWidth
        margin="normal"
        {...register('productID', { required: 'ProductID is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.productID}
        helperText={errors.productID?.message}
      />

      <TextField
        label="Quantity"
        type="number"
        fullWidth
        margin="normal"
        {...register('quantity', { required: 'Quantity is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.quantity}
        helperText={errors.quantity?.message}
      />

      <TextField
        label="UnitPrice"
        type="number"
        fullWidth
        margin="normal"
        {...register('unitPrice', { required: 'UnitPrice is required', maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.unitPrice}
        helperText={errors.unitPrice?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/orderItems')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Customer
//     Generated: 2025-12-06 22:45:29
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useCustomer, useCreateCustomer, useUpdateCustomer } from '../../hooks/useCustomer';
import type { CreateCustomerRequest, UpdateCustomerRequest } from '../../types/Customer.types';


export const CustomerForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const customerId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!customerId;

  const { data: existingCustomer, isPending: isLoadingEntity } = useCustomer(customerId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCustomerRequest>({
    defaultValues: {
      statusCode: '',
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCustomer) {
      reset(existingCustomer);
    }
  }, [existingCustomer, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCustomer();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCustomer();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCustomerRequest) => {
    if (isEdit && customerId) {
      updateEntity(
        { id: customerId, data: data as UpdateCustomerRequest },
        { onSuccess: () => navigate('/customers') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/customers') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="Name"
        type="text"
        fullWidth
        margin="normal"
        {...register('name', { required: 'Name is required', maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.name}
        helperText={errors.name?.message}
      />

      <TextField
        label="Email"
        type="text"
        fullWidth
        margin="normal"
        {...register('email', { required: 'Email is required', maxLength: { value: 200, message: 'Maximum length is 200' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.email}
        helperText={errors.email?.message}
      />

      <TextField
        label="Phone"
        type="text"
        fullWidth
        margin="normal"
        {...register('phone', { maxLength: { value: 20, message: 'Maximum length is 20' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.phone}
        helperText={errors.phone?.message}
      />

      <TextField
        label="CreditCard"
        fullWidth
        margin="normal"
        {...register('creditCard', {  })}
        error={!!errors.creditCard}
        helperText={errors.creditCard?.message}
      />

      <FormControl fullWidth margin="normal">
        <InputLabel>Status</InputLabel>
        <Select
          label="Status"
          {...register('statusCode', { maxLength: { value: 20, message: 'Maximum length is 20' } })}
          error={!!errors.statusCode}
        >
          {/* TODO: Load options from lookup table */}
          <MenuItem value="">Select...</MenuItem>
        </Select>
      </FormControl>



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/customers')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: vw_ProductSales
//     Generated: 2025-12-08 11:08:42
// </auto-generated>

import React from 'react';
import { useNavigate } from 'react-router-dom';
import * as XLSX from 'xlsx-js-style';
import { DataGrid, GridColDef, GridActionsCellItem, GridToolbarContainer, GridToolbarColumnsButton, GridToolbarDensitySelector, useGridApiRef } from '@mui/x-data-grid';
import { Button, Box, CircularProgress, Alert, TextField, Paper } from '@mui/material';
import { FileDownload as FileDownloadIcon, Clear as ClearIcon } from '@mui/icons-material';
import { useVwProductSaless } from '../../hooks/useVwProductSales';
import type { VwProductSales, VwProductSalesFilters } from '../../types/VwProductSales.types';


export const VwProductSalesList: React.FC = () => {
  const navigate = useNavigate();
  // apiRef is used to access filtered/sorted rows for Excel export
  const apiRef = useGridApiRef();
  const [filters, setFilters] = React.useState<VwProductSalesFilters>({});
  const [localFilters, setLocalFilters] = React.useState<VwProductSalesFilters>({});
  const [filterModel, setFilterModel] = React.useState<any>({ items: [] });
  const { data: vwProductSaless, isLoading, error } = useVwProductSaless(filters);

  const handleApplyFilters = () => {
    setFilters(localFilters);
  };

  const handleClearFilters = () => {
    setLocalFilters({});
    setFilters({});
  };

  const handleClearAllFilters = () => {
    // Clear top panel filters
    setLocalFilters({});
    setFilters({});
    // Clear DataGrid column filters
    setFilterModel({ items: [] });
  };

  function CustomToolbar() {
    return (
      <GridToolbarContainer>
        <GridToolbarColumnsButton />
        <GridToolbarDensitySelector />
        <Button
          size="small"
          startIcon={<ClearIcon />}
          onClick={handleClearAllFilters}
        >
          Clear All Filters
        </Button>
      </GridToolbarContainer>
    );
  }

  const handleExportToExcel = () => {
    // Get filtered and sorted rows from DataGrid (respects user's view)
    const visibleRows = apiRef.current
      ? Array.from(apiRef.current.getRowModels().values())
      : vwProductSaless || [];

    if (!visibleRows || visibleRows.length === 0) {
      alert('No data to export');
      return;
    }

    const ws = XLSX.utils.json_to_sheet(visibleRows);

    // Get the range of the worksheet
    const range = XLSX.utils.decode_range(ws['!ref'] || 'A1');

    // Style the header row
    for (let col = range.s.c; col <= range.e.c; col++) {
      const cellAddress = XLSX.utils.encode_cell({ r: 0, c: col });
      if (!ws[cellAddress]) continue;
      ws[cellAddress].s = {
        font: { bold: true, color: { rgb: 'FFFFFF' } },
        fill: { fgColor: { rgb: '4472C4' } },
        alignment: { horizontal: 'center', vertical: 'center' }
      };
    }

    // Set column widths (auto-fit)
    const colWidths = [];
    for (let col = range.s.c; col <= range.e.c; col++) {
      let maxWidth = 10;
      for (let row = range.s.r; row <= range.e.r; row++) {
        const cellAddress = XLSX.utils.encode_cell({ r: row, c: col });
        if (ws[cellAddress] && ws[cellAddress].v) {
          const cellLength = String(ws[cellAddress].v).length;
          maxWidth = Math.max(maxWidth, cellLength);
        }
      }
      colWidths.push({ wch: Math.min(maxWidth + 2, 50) });
    }
    ws['!cols'] = colWidths;

    // Freeze the header row
    ws['!freeze'] = { xSplit: 0, ySplit: 1, state: 'frozen' };

    const wb = XLSX.utils.book_new();
    XLSX.utils.book_append_sheet(wb, ws, 'VwProductSaless');
    XLSX.writeFile(wb, `VwProductSaless_${new Date().toISOString().split('T')[0]}.xlsx`);
  };

  const columns: GridColDef<VwProductSales>[] = [
    { field: 'productID', headerName: 'ProductID', width: 90, filterable: true },
    { field: 'productName', headerName: 'ProductName', width: 250, filterable: true },
    { field: 'category', headerName: 'Category', width: 150, filterable: true },
    { field: 'currentPrice', headerName: 'CurrentPrice', width: 150, filterable: true },
    { field: 'stockQuantity', headerName: 'StockQuantity', width: 150, filterable: true },
    { field: 'isActive', headerName: 'IsActive', width: 100, filterable: true },
    { field: 'timesSold', headerName: 'TimesSold', width: 150, filterable: true },
    { field: 'totalQuantitySold', headerName: 'TotalQuantitySold', width: 150, filterable: true },
    { field: 'totalRevenue', headerName: 'TotalRevenue', width: 150, filterable: true },
    { field: 'averageSellingPrice', headerName: 'AverageSellingPrice', width: 150, filterable: true },
  ];


  if (isLoading) {
    return <CircularProgress />;
  }

  if (error) {
    return <Alert severity="error">Failed to load vwProductSaless</Alert>;
  }

  return (
    <Box sx={{ height: 'calc(100vh - 200px)', width: '100%', display: 'flex', flexDirection: 'column' }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<FileDownloadIcon />}
          onClick={handleExportToExcel}
        >
          Export to Excel
        </Button>
      </Box>
      <Paper sx={{ p: 2, mb: 2 }}>
        <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap', alignItems: 'center' }}>
          <TextField
            label="ProductName"
            type="text"
            size="small"
            sx={{ minWidth: 200 }}
            value={(localFilters as any).productName ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              productName: e.target.value || undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="Category"
            type="text"
            size="small"
            sx={{ minWidth: 200 }}
            value={(localFilters as any).category ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              category: e.target.value || undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="CurrentPrice"
            type="number"
            size="small"
            sx={{ minWidth: 200 }}
            value={(localFilters as any).currentPrice ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              currentPrice: e.target.value ? Number(e.target.value) : undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="StockQuantity"
            type="number"
            size="small"
            sx={{ minWidth: 200 }}
            value={(localFilters as any).stockQuantity ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              stockQuantity: e.target.value ? Number(e.target.value) : undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <TextField
            label="TimesSold"
            type="number"
            size="small"
            sx={{ minWidth: 200 }}
            value={(localFilters as any).timesSold ?? ''}
            onChange={(e) => setLocalFilters(prev => ({
              ...prev,
              timesSold: e.target.value ? Number(e.target.value) : undefined
            }))}
            onKeyDown={(e) => {
              if (e.key === 'Enter') {
                handleApplyFilters();
              }
            }}
          />
          <Button
            variant="contained"
            color="primary"
            onClick={handleApplyFilters}
            size="small"
          >
            Apply Filters
          </Button>
          <Button
            variant="outlined"
            onClick={handleClearFilters}
            size="small"
          >
            Clear
          </Button>
        </Box>
      </Paper>

      <Box sx={{ flex: 1, minHeight: 0 }}>
        <DataGrid
          apiRef={apiRef}
          rows={vwProductSaless || []}
          columns={columns}
          getRowId={(row) => row.productID}
          filterMode="client"
          filterModel={filterModel}
          onFilterModelChange={setFilterModel}
          disableMultipleColumnsFiltering={false}
          slots={{
            toolbar: CustomToolbar,
          }}
          initialState={{
            pagination: { paginationModel: { pageSize: 10 } },
          }}
          pageSizeOptions={[5, 10, 25, 100]}
          checkboxSelection
          disableRowSelectionOnClick
        />
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CardClub
//     Generated: 2025-12-11 21:18:52
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useCardClub, useCreateCardClub, useUpdateCardClub } from '../../hooks/useCardClub';
import type { CreateCardClubRequest, UpdateCardClubRequest } from '../../types/CardClub.types';


export const CardClubForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const cardClubId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!cardClubId;

  const { data: existingCardClub, isPending: isLoadingEntity } = useCardClub(cardClubId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCardClubRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCardClub) {
      reset(existingCardClub);
    }
  }, [existingCardClub, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCardClub();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCardClub();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCardClubRequest) => {
    if (isEdit && cardClubId) {
      updateEntity(
        { id: cardClubId, data: data as UpdateCardClubRequest },
        { onSuccess: () => navigate('/cardClubs') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/cardClubs') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCardClub')} />}
        label="TableNameCardClub"
      />

      <TextField
        label="ClubName"
        type="text"
        fullWidth
        margin="normal"
        {...register('clubName', { required: 'ClubName is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.clubName}
        helperText={errors.clubName?.message}
      />

      <TextField
        label="ClubColour"
        type="text"
        fullWidth
        margin="normal"
        {...register('clubColour', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.clubColour}
        helperText={errors.clubColour?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="Club1Type"
        type="number"
        fullWidth
        margin="normal"
        {...register('club1Type', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.club1Type}
        helperText={errors.club1Type?.message}
      />

      <TextField
        label="Club1Number"
        type="text"
        fullWidth
        margin="normal"
        {...register('club1Number', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.club1Number}
        helperText={errors.club1Number?.message}
      />

      <TextField
        label="Club2Type"
        type="number"
        fullWidth
        margin="normal"
        {...register('club2Type', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.club2Type}
        helperText={errors.club2Type?.message}
      />

      <TextField
        label="Club2Number"
        type="text"
        fullWidth
        margin="normal"
        {...register('club2Number', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.club2Number}
        helperText={errors.club2Number?.message}
      />

      <TextField
        label="Club3Type"
        type="number"
        fullWidth
        margin="normal"
        {...register('club3Type', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.club3Type}
        helperText={errors.club3Type?.message}
      />

      <TextField
        label="Club3Number"
        type="text"
        fullWidth
        margin="normal"
        {...register('club3Number', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.club3Number}
        helperText={errors.club3Number?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isRoot')} />}
        label="IsRoot"
      />

      <TextField
        label="CardIssuerOrgCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardIssuerOrgCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.cardIssuerOrgCode}
        helperText={errors.cardIssuerOrgCode?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('useForCompanyCustomer')} />}
        label="UseForCompanyCustomer"
      />

      <TextField
        label="AssignedCompanyCustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('assignedCompanyCustomerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.assignedCompanyCustomerID}
        helperText={errors.assignedCompanyCustomerID?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('useForUnassignedCustomer')} />}
        label="UseForUnassignedCustomer"
      />

      <TextField
        label="EnmCardBrand"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCardBrand', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCardBrand}
        helperText={errors.enmCardBrand?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/cardClubs')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


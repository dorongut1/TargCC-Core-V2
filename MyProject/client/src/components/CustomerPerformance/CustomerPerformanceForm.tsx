// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerPerformance
//     Generated: 2025-12-18 07:57:43
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCustomerPerformance, useCreateCustomerPerformance, useUpdateCustomerPerformance } from '../../hooks/useCustomerPerformance';
import type { CreateCustomerPerformanceRequest, UpdateCustomerPerformanceRequest } from '../../types/CustomerPerformance.types';


export const CustomerPerformanceForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const customerPerformanceId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!customerPerformanceId;

  const { data: existingCustomerPerformance, isPending: isLoadingEntity } = useCustomerPerformance(customerPerformanceId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCustomerPerformanceRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCustomerPerformance) {
      reset(existingCustomerPerformance);
    }
  }, [existingCustomerPerformance, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCustomerPerformance();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCustomerPerformance();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCustomerPerformanceRequest) => {
    if (isEdit && customerPerformanceId) {
      updateEntity(
        { id: customerPerformanceId, data: data as UpdateCustomerPerformanceRequest },
        { onSuccess: () => navigate('/customerPerformances') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/customerPerformances') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCustomerPerformance')} />}
        label="TableNameCustomerPerformance"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="NumberOfActivePrepaidCards"
        fullWidth
        margin="normal"
        {...register('numberOfActivePrepaidCards', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.numberOfActivePrepaidCards}
        helperText={errors.numberOfActivePrepaidCards?.message}
      />

      <TextField
        label="PrepaidCardBalanceSum"
        fullWidth
        margin="normal"
        {...register('prepaidCardBalanceSum', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.prepaidCardBalanceSum}
        helperText={errors.prepaidCardBalanceSum?.message}
      />

      <TextField
        label="WalletBalanceRaw"
        fullWidth
        margin="normal"
        {...register('walletBalanceRaw', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.walletBalanceRaw}
        helperText={errors.walletBalanceRaw?.message}
      />

      <TextField
        label="WalletBalanceCommitted"
        fullWidth
        margin="normal"
        {...register('walletBalanceCommitted', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.walletBalanceCommitted}
        helperText={errors.walletBalanceCommitted?.message}
      />

      <TextField
        label="LastTimeEnteredCustomerApplication"
        fullWidth
        margin="normal"
        {...register('lastTimeEnteredCustomerApplication', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.lastTimeEnteredCustomerApplication}
        helperText={errors.lastTimeEnteredCustomerApplication?.message}
      />

      <TextField
        label="LastTimeLoadedCard"
        fullWidth
        margin="normal"
        {...register('lastTimeLoadedCard', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.lastTimeLoadedCard}
        helperText={errors.lastTimeLoadedCard?.message}
      />

      <TextField
        label="LastTimeDidWalletTransfer"
        fullWidth
        margin="normal"
        {...register('lastTimeDidWalletTransfer', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.lastTimeDidWalletTransfer}
        helperText={errors.lastTimeDidWalletTransfer?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/customerPerformances')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


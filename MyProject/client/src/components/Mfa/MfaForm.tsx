// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_MFA
//     Generated: 2025-12-18 07:57:40
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useMfa, useCreateMfa, useUpdateMfa } from '../../hooks/useMfa';
import type { CreateMfaRequest, UpdateMfaRequest } from '../../types/Mfa.types';


export const MfaForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mfaId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mfaId;

  const { data: existingMfa, isPending: isLoadingEntity } = useMfa(mfaId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMfaRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMfa) {
      reset(existingMfa);
    }
  }, [existingMfa, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMfa();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMfa();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMfaRequest) => {
    if (isEdit && mfaId) {
      updateEntity(
        { id: mfaId, data: data as UpdateMfaRequest },
        { onSuccess: () => navigate('/mfas') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mfas') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCMfa')} />}
        label="TableNameCMfa"
      />

      <TextField
        label="CellOrEmail"
        type="text"
        fullWidth
        margin="normal"
        {...register('cellOrEmail', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.cellOrEmail}
        helperText={errors.cellOrEmail?.message}
      />

      <TextField
        label="ProtectedFunction"
        type="text"
        fullWidth
        margin="normal"
        {...register('protectedFunction', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.protectedFunction}
        helperText={errors.protectedFunction?.message}
      />

      <TextField
        label="EnoCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('enoCode', { maxLength: { value: 64, message: 'Maximum length is 64' } })}
        error={!!errors.enoCode}
        helperText={errors.enoCode?.message}
      />

      <TextField
        label="AttemptNo"
        type="number"
        fullWidth
        margin="normal"
        {...register('attemptNo', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.attemptNo}
        helperText={errors.attemptNo?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isSuccessful')} />}
        label="IsSuccessful"
      />

      <TextField
        label="LastAccessingIP"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastAccessingIP', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lastAccessingIP}
        helperText={errors.lastAccessingIP?.message}
      />

      <TextField
        label="LastAccessingCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastAccessingCountry', { maxLength: { value: 5, message: 'Maximum length is 5' } })}
        error={!!errors.lastAccessingCountry}
        helperText={errors.lastAccessingCountry?.message}
      />

      <TextField
        label="EnmUILangLanguage"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmUILangLanguage', { maxLength: { value: 5, message: 'Maximum length is 5' } })}
        error={!!errors.enmUILangLanguage}
        helperText={errors.enmUILangLanguage?.message}
      />

      <TextField
        label="WhenCreated"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenCreated', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenCreated}
        helperText={errors.whenCreated?.message}
      />

      <TextField
        label="WhenAccessed"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenAccessed', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenAccessed}
        helperText={errors.whenAccessed?.message}
      />

      <TextField
        label="WhenExpires"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenExpires', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenExpires}
        helperText={errors.whenExpires?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="Details"
        type="text"
        fullWidth
        margin="normal"
        {...register('details', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.details}
        helperText={errors.details?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mfas')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


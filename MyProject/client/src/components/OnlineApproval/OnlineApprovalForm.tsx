// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: OnlineApproval
//     Generated: 2025-12-11 21:18:56
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useOnlineApproval, useCreateOnlineApproval, useUpdateOnlineApproval } from '../../hooks/useOnlineApproval';
import type { CreateOnlineApprovalRequest, UpdateOnlineApprovalRequest } from '../../types/OnlineApproval.types';


export const OnlineApprovalForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const onlineApprovalId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!onlineApprovalId;

  const { data: existingOnlineApproval, isPending: isLoadingEntity } = useOnlineApproval(onlineApprovalId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateOnlineApprovalRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingOnlineApproval) {
      reset(existingOnlineApproval);
    }
  }, [existingOnlineApproval, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateOnlineApproval();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateOnlineApproval();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateOnlineApprovalRequest) => {
    if (isEdit && onlineApprovalId) {
      updateEntity(
        { id: onlineApprovalId, data: data as UpdateOnlineApprovalRequest },
        { onSuccess: () => navigate('/onlineApprovals') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/onlineApprovals') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="Cid"
        type="text"
        fullWidth
        margin="normal"
        {...register('cid', { required: 'Cid is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.cid}
        helperText={errors.cid?.message}
      />

      <TextField
        label="OrgCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('orgCode', { required: 'OrgCode is required', maxLength: { value: 5, message: 'Maximum length is 5' } })}
        error={!!errors.orgCode}
        helperText={errors.orgCode?.message}
      />

      <TextField
        label="CardID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardID}
        helperText={errors.cardID?.message}
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="CurrencyType"
        type="text"
        fullWidth
        margin="normal"
        {...register('currencyType', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.currencyType}
        helperText={errors.currencyType?.message}
      />

      <TextField
        label="AuthCode"
        type="number"
        fullWidth
        margin="normal"
        {...register('authCode', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.authCode}
        helperText={errors.authCode?.message}
      />

      <TextField
        label="ApproveDatetime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('approveDatetime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.approveDatetime}
        helperText={errors.approveDatetime?.message}
      />

      <TextField
        label="ApproveDatetimeGmt"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('approveDatetimeGmt', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.approveDatetimeGmt}
        helperText={errors.approveDatetimeGmt?.message}
      />

      <TextField
        label="DepositDate"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('depositDate', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.depositDate}
        helperText={errors.depositDate?.message}
      />

      <TextField
        label="IndSmartTran"
        type="text"
        fullWidth
        margin="normal"
        {...register('indSmartTran', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.indSmartTran}
        helperText={errors.indSmartTran?.message}
      />

      <TextField
        label="OrigCurrencyCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('origCurrencyCode', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.origCurrencyCode}
        helperText={errors.origCurrencyCode?.message}
      />

      <TextField
        label="OrigAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('origAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.origAmount}
        helperText={errors.origAmount?.message}
      />

      <TextField
        label="ChargeAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('chargeAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.chargeAmount}
        helperText={errors.chargeAmount?.message}
      />

      <TextField
        label="ExchangeRate"
        type="number"
        fullWidth
        margin="normal"
        {...register('exchangeRate', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.exchangeRate}
        helperText={errors.exchangeRate?.message}
      />

      <TextField
        label="MerchantNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('merchantNumber', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.merchantNumber}
        helperText={errors.merchantNumber?.message}
      />

      <TextField
        label="MerchantName"
        type="text"
        fullWidth
        margin="normal"
        {...register('merchantName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.merchantName}
        helperText={errors.merchantName?.message}
      />

      <TextField
        label="Mcc4"
        type="number"
        fullWidth
        margin="normal"
        {...register('mcc4', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.mcc4}
        helperText={errors.mcc4?.message}
      />

      <TextField
        label="MerchantCity"
        type="text"
        fullWidth
        margin="normal"
        {...register('merchantCity', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.merchantCity}
        helperText={errors.merchantCity?.message}
      />

      <TextField
        label="AuthScore"
        type="text"
        fullWidth
        margin="normal"
        {...register('authScore', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.authScore}
        helperText={errors.authScore?.message}
      />

      <TextField
        label="AuthRule"
        type="text"
        fullWidth
        margin="normal"
        {...register('authRule', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.authRule}
        helperText={errors.authRule?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/onlineApprovals')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


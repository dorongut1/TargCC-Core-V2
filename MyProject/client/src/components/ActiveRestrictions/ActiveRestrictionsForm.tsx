// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: ActiveRestrictions
//     Generated: 2025-12-18 07:30:14
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useActiveRestrictions, useCreateActiveRestrictions, useUpdateActiveRestrictions } from '../../hooks/useActiveRestrictions';
import type { CreateActiveRestrictionsRequest, UpdateActiveRestrictionsRequest } from '../../types/ActiveRestrictions.types';


export const ActiveRestrictionsForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const activeRestrictionsId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!activeRestrictionsId;

  const { data: existingActiveRestrictions, isPending: isLoadingEntity } = useActiveRestrictions(activeRestrictionsId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateActiveRestrictionsRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingActiveRestrictions) {
      reset(existingActiveRestrictions);
    }
  }, [existingActiveRestrictions, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateActiveRestrictions();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateActiveRestrictions();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateActiveRestrictionsRequest) => {
    if (isEdit && activeRestrictionsId) {
      updateEntity(
        { id: activeRestrictionsId, data: data as UpdateActiveRestrictionsRequest },
        { onSuccess: () => navigate('/activeRestrictionss') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/activeRestrictionss') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameActiveRestrictions')} />}
        label="TableNameActiveRestrictions"
      />

      <TextField
        label="EnmEntityType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmEntityType', { required: 'EnmEntityType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmEntityType}
        helperText={errors.enmEntityType?.message}
      />

      <TextField
        label="EntityID"
        type="number"
        fullWidth
        margin="normal"
        {...register('entityID', { required: 'EntityID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.entityID}
        helperText={errors.entityID?.message}
      />

      <TextField
        label="EnmBlockedActionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmBlockedActionType', { required: 'EnmBlockedActionType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmBlockedActionType}
        helperText={errors.enmBlockedActionType?.message}
      />

      <TextField
        label="MaxAllowedAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('maxAllowedAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.maxAllowedAmount}
        helperText={errors.maxAllowedAmount?.message}
      />

      <TextField
        label="ValidFrom"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('validFrom', { required: 'ValidFrom is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.validFrom}
        helperText={errors.validFrom?.message}
      />

      <TextField
        label="ValidUntil"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('validUntil', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.validUntil}
        helperText={errors.validUntil?.message}
      />

      <TextField
        label="BlockReason"
        type="text"
        fullWidth
        margin="normal"
        {...register('blockReason', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.blockReason}
        helperText={errors.blockReason?.message}
      />

      <TextField
        label="TriggerRuleID"
        type="number"
        fullWidth
        margin="normal"
        {...register('triggerRuleID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.triggerRuleID}
        helperText={errors.triggerRuleID?.message}
      />

      <TextField
        label="TriggerEventID"
        type="number"
        fullWidth
        margin="normal"
        {...register('triggerEventID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.triggerEventID}
        helperText={errors.triggerEventID?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/activeRestrictionss')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


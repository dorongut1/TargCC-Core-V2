// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: RiskRuleOverride
//     Generated: 2025-12-17 12:56:58
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useRiskRuleOverride, useCreateRiskRuleOverride, useUpdateRiskRuleOverride } from '../../hooks/useRiskRuleOverride';
import type { CreateRiskRuleOverrideRequest, UpdateRiskRuleOverrideRequest } from '../../types/RiskRuleOverride.types';


export const RiskRuleOverrideForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const riskRuleOverrideId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!riskRuleOverrideId;

  const { data: existingRiskRuleOverride, isPending: isLoadingEntity } = useRiskRuleOverride(riskRuleOverrideId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateRiskRuleOverrideRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingRiskRuleOverride) {
      reset(existingRiskRuleOverride);
    }
  }, [existingRiskRuleOverride, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateRiskRuleOverride();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateRiskRuleOverride();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateRiskRuleOverrideRequest) => {
    if (isEdit && riskRuleOverrideId) {
      updateEntity(
        { id: riskRuleOverrideId, data: data as UpdateRiskRuleOverrideRequest },
        { onSuccess: () => navigate('/riskRuleOverrides') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/riskRuleOverrides') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameRiskRuleOverride')} />}
        label="TableNameRiskRuleOverride"
      />

      <TextField
        label="RiskRuleID"
        type="number"
        fullWidth
        margin="normal"
        {...register('riskRuleID', { required: 'RiskRuleID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.riskRuleID}
        helperText={errors.riskRuleID?.message}
      />

      <TextField
        label="EnmEntityType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmEntityType', { required: 'EnmEntityType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmEntityType}
        helperText={errors.enmEntityType?.message}
      />

      <TextField
        label="EntityID"
        type="number"
        fullWidth
        margin="normal"
        {...register('entityID', { required: 'EntityID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.entityID}
        helperText={errors.entityID?.message}
      />

      <TextField
        label="OverrideThresholdValue"
        type="number"
        fullWidth
        margin="normal"
        {...register('overrideThresholdValue', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.overrideThresholdValue}
        helperText={errors.overrideThresholdValue?.message}
      />

      <TextField
        label="OverrideMinAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('overrideMinAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.overrideMinAmount}
        helperText={errors.overrideMinAmount?.message}
      />

      <TextField
        label="OverrideMaxAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('overrideMaxAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.overrideMaxAmount}
        helperText={errors.overrideMaxAmount?.message}
      />

      <TextField
        label="OverrideDurationDays"
        type="number"
        fullWidth
        margin="normal"
        {...register('overrideDurationDays', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.overrideDurationDays}
        helperText={errors.overrideDurationDays?.message}
      />

      <TextField
        label="ValidFrom"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('validFrom', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.validFrom}
        helperText={errors.validFrom?.message}
      />

      <TextField
        label="ValidUntil"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('validUntil', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.validUntil}
        helperText={errors.validUntil?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isActive')} />}
        label="IsActive"
      />

      <TextField
        label="Reason"
        type="text"
        fullWidth
        margin="normal"
        {...register('reason', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.reason}
        helperText={errors.reason?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/riskRuleOverrides')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


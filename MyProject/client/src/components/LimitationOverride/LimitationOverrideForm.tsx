// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: LimitationOverride
//     Generated: 2025-12-16 21:06:06
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useLimitationOverride, useCreateLimitationOverride, useUpdateLimitationOverride } from '../../hooks/useLimitationOverride';
import type { CreateLimitationOverrideRequest, UpdateLimitationOverrideRequest } from '../../types/LimitationOverride.types';


export const LimitationOverrideForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const limitationOverrideId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!limitationOverrideId;

  const { data: existingLimitationOverride, isPending: isLoadingEntity } = useLimitationOverride(limitationOverrideId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateLimitationOverrideRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingLimitationOverride) {
      reset(existingLimitationOverride);
    }
  }, [existingLimitationOverride, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateLimitationOverride();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateLimitationOverride();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateLimitationOverrideRequest) => {
    if (isEdit && limitationOverrideId) {
      updateEntity(
        { id: limitationOverrideId, data: data as UpdateLimitationOverrideRequest },
        { onSuccess: () => navigate('/limitationOverrides') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/limitationOverrides') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameLimitationOverride')} />}
        label="TableNameLimitationOverride"
      />

      <TextField
        label="LimitationID"
        type="number"
        fullWidth
        margin="normal"
        {...register('limitationID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.limitationID}
        helperText={errors.limitationID?.message}
      />

      <TextField
        label="CardClubID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardClubID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardClubID}
        helperText={errors.cardClubID?.message}
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="OverrideAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('overrideAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.overrideAmount}
        helperText={errors.overrideAmount?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="CustomerClubID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerClubID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerClubID}
        helperText={errors.customerClubID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/limitationOverrides')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


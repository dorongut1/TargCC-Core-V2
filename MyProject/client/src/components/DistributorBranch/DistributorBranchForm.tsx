// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: DistributorBranch
//     Generated: 2025-12-11 21:35:46
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useDistributorBranch, useCreateDistributorBranch, useUpdateDistributorBranch } from '../../hooks/useDistributorBranch';
import type { CreateDistributorBranchRequest, UpdateDistributorBranchRequest } from '../../types/DistributorBranch.types';


export const DistributorBranchForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const distributorBranchId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!distributorBranchId;

  const { data: existingDistributorBranch, isPending: isLoadingEntity } = useDistributorBranch(distributorBranchId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateDistributorBranchRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingDistributorBranch) {
      reset(existingDistributorBranch);
    }
  }, [existingDistributorBranch, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateDistributorBranch();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateDistributorBranch();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateDistributorBranchRequest) => {
    if (isEdit && distributorBranchId) {
      updateEntity(
        { id: distributorBranchId, data: data as UpdateDistributorBranchRequest },
        { onSuccess: () => navigate('/distributorBranchs') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/distributorBranchs') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameDistributorBranch')} />}
        label="TableNameDistributorBranch"
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { required: 'DistributorID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="LocName"
        type="text"
        fullWidth
        margin="normal"
        {...register('locName', { required: 'LocName is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.locName}
        helperText={errors.locName?.message}
      />

      <TextField
        label="LocStreetAndNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('locStreetAndNumber', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.locStreetAndNumber}
        helperText={errors.locStreetAndNumber?.message}
      />

      <TextField
        label="LocCity"
        type="text"
        fullWidth
        margin="normal"
        {...register('locCity', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.locCity}
        helperText={errors.locCity?.message}
      />

      <TextField
        label="LkpRegion"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpRegion', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpRegion}
        helperText={errors.lkpRegion?.message}
      />

      <TextField
        label="PhoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('phoneNumber', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.phoneNumber}
        helperText={errors.phoneNumber?.message}
      />

      <TextField
        label="Email"
        type="text"
        fullWidth
        margin="normal"
        {...register('email', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.email}
        helperText={errors.email?.message}
      />

      <TextField
        label="Location"
        type="text"
        fullWidth
        margin="normal"
        {...register('location', {  })}
        error={!!errors.location}
        helperText={errors.location?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/distributorBranchs')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


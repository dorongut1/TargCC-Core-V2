// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_SystemDefault
//     Generated: 2025-12-18 08:27:22
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useSystemDefault, useCreateSystemDefault, useUpdateSystemDefault } from '../../hooks/useSystemDefault';
import type { CreateSystemDefaultRequest, UpdateSystemDefaultRequest } from '../../types/SystemDefault.types';


export const SystemDefaultForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const systemDefaultId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!systemDefaultId;

  const { data: existingSystemDefault, isPending: isLoadingEntity } = useSystemDefault(systemDefaultId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateSystemDefaultRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingSystemDefault) {
      reset(existingSystemDefault);
    }
  }, [existingSystemDefault, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateSystemDefault();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateSystemDefault();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateSystemDefaultRequest) => {
    if (isEdit && systemDefaultId) {
      updateEntity(
        { id: systemDefaultId, data: data as UpdateSystemDefaultRequest },
        { onSuccess: () => navigate('/systemDefaults') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/systemDefaults') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCSystemDefault')} />}
        label="TableNameCSystemDefault"
      />

      <TextField
        label="Group"
        type="text"
        fullWidth
        margin="normal"
        {...register('group', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.group}
        helperText={errors.group?.message}
      />

      <TextField
        label="SettingName"
        type="text"
        fullWidth
        margin="normal"
        {...register('settingName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.settingName}
        helperText={errors.settingName?.message}
      />

      <TextField
        label="SettingValue"
        fullWidth
        margin="normal"
        {...register('settingValue', { maxLength: { value: 8000, message: 'Maximum length is 8000' } })}
        error={!!errors.settingValue}
        helperText={errors.settingValue?.message}
      />

      <TextField
        label="EnmSystemDefaultType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmSystemDefaultType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmSystemDefaultType}
        helperText={errors.enmSystemDefaultType?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="Description"
        type="text"
        fullWidth
        margin="normal"
        {...register('description', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.description}
        helperText={errors.description?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/systemDefaults')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


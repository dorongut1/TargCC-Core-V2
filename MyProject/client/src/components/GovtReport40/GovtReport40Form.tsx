// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: GovtReport40
//     Generated: 2025-12-17 08:16:16
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useGovtReport40, useCreateGovtReport40, useUpdateGovtReport40 } from '../../hooks/useGovtReport40';
import type { CreateGovtReport40Request, UpdateGovtReport40Request } from '../../types/GovtReport40.types';


export const GovtReport40Form: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const govtReport40Id = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!govtReport40Id;

  const { data: existingGovtReport40, isPending: isLoadingEntity } = useGovtReport40(govtReport40Id || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateGovtReport40Request>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingGovtReport40) {
      reset(existingGovtReport40);
    }
  }, [existingGovtReport40, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateGovtReport40();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateGovtReport40();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateGovtReport40Request) => {
    if (isEdit && govtReport40Id) {
      updateEntity(
        { id: govtReport40Id, data: data as UpdateGovtReport40Request },
        { onSuccess: () => navigate('/govtReport40s') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/govtReport40s') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameGovtReport40')} />}
        label="TableNameGovtReport40"
      />

      <TextField
        label="GovtReport00ID"
        type="number"
        fullWidth
        margin="normal"
        {...register('govtReport00ID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.govtReport00ID}
        helperText={errors.govtReport00ID?.message}
      />

      <TextField
        label="ReporterNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterNumber', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.reporterNumber}
        helperText={errors.reporterNumber?.message}
      />

      <TextField
        label="ReporterIDNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterIDNumber', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.reporterIDNumber}
        helperText={errors.reporterIDNumber?.message}
      />

      <TextField
        label="ReportYear"
        type="text"
        fullWidth
        margin="normal"
        {...register('reportYear', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.reportYear}
        helperText={errors.reportYear?.message}
      />

      <TextField
        label="ReportNumber"
        type="number"
        fullWidth
        margin="normal"
        {...register('reportNumber', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.reportNumber}
        helperText={errors.reportNumber?.message}
      />

      <TextField
        label="RecordType"
        type="text"
        fullWidth
        margin="normal"
        {...register('recordType', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.recordType}
        helperText={errors.recordType?.message}
      />

      <TextField
        label="PrimaryActionIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('primaryActionIdentifier', { maxLength: { value: 18, message: 'Maximum length is 18' } })}
        error={!!errors.primaryActionIdentifier}
        helperText={errors.primaryActionIdentifier?.message}
      />

      <TextField
        label="FinancialInstitutionStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('financialInstitutionStatus', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.financialInstitutionStatus}
        helperText={errors.financialInstitutionStatus?.message}
      />

      <TextField
        label="BankNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankNumber', { maxLength: { value: 20, message: 'Maximum length is 20' } })}
        error={!!errors.bankNumber}
        helperText={errors.bankNumber?.message}
      />

      <TextField
        label="BankBranchNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankBranchNumber', { maxLength: { value: 20, message: 'Maximum length is 20' } })}
        error={!!errors.bankBranchNumber}
        helperText={errors.bankBranchNumber?.message}
      />

      <TextField
        label="BankCountryCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankCountryCode', { maxLength: { value: 5, message: 'Maximum length is 5' } })}
        error={!!errors.bankCountryCode}
        helperText={errors.bankCountryCode?.message}
      />

      <TextField
        label="BankName"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankName', { maxLength: { value: 80, message: 'Maximum length is 80' } })}
        error={!!errors.bankName}
        helperText={errors.bankName?.message}
      />

      <TextField
        label="BankAddress"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankAddress', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.bankAddress}
        helperText={errors.bankAddress?.message}
      />

      <TextField
        label="BankAccountNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankAccountNumber', { maxLength: { value: 20, message: 'Maximum length is 20' } })}
        error={!!errors.bankAccountNumber}
        helperText={errors.bankAccountNumber?.message}
      />

      <TextField
        label="ReferenceNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('referenceNumber', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.referenceNumber}
        helperText={errors.referenceNumber?.message}
      />

      <TextField
        label="FinancialInstitutionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('financialInstitutionType', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.financialInstitutionType}
        helperText={errors.financialInstitutionType?.message}
      />

      <TextField
        label="FinancialInstitutionTypeOther"
        type="text"
        fullWidth
        margin="normal"
        {...register('financialInstitutionTypeOther', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.financialInstitutionTypeOther}
        helperText={errors.financialInstitutionTypeOther?.message}
      />

      <TextField
        label="BankAccountOwnerName"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankAccountOwnerName', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.bankAccountOwnerName}
        helperText={errors.bankAccountOwnerName?.message}
      />

      <TextField
        label="BankAccountOwnerAddress"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankAccountOwnerAddress', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.bankAccountOwnerAddress}
        helperText={errors.bankAccountOwnerAddress?.message}
      />

      <TextField
        label="RecipientName"
        type="text"
        fullWidth
        margin="normal"
        {...register('recipientName', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.recipientName}
        helperText={errors.recipientName?.message}
      />

      <TextField
        label="BankAccountOwnerGovernmentID"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankAccountOwnerGovernmentID', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.bankAccountOwnerGovernmentID}
        helperText={errors.bankAccountOwnerGovernmentID?.message}
      />

      <TextField
        label="Telephone1"
        type="text"
        fullWidth
        margin="normal"
        {...register('telephone1', { maxLength: { value: 15, message: 'Maximum length is 15' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.telephone1}
        helperText={errors.telephone1?.message}
      />

      <TextField
        label="SecondaryActionIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('secondaryActionIdentifier', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.secondaryActionIdentifier}
        helperText={errors.secondaryActionIdentifier?.message}
      />

      <TextField
        label="BankAccountOwnerCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankAccountOwnerCountry', { maxLength: { value: 5, message: 'Maximum length is 5' } })}
        error={!!errors.bankAccountOwnerCountry}
        helperText={errors.bankAccountOwnerCountry?.message}
      />

      <TextField
        label="BankAccountOwnerGender"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankAccountOwnerGender', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.bankAccountOwnerGender}
        helperText={errors.bankAccountOwnerGender?.message}
      />

      <TextField
        label="BankAccountBirthdate"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankAccountBirthdate', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.bankAccountBirthdate}
        helperText={errors.bankAccountBirthdate?.message}
      />

      <TextField
        label="RecipientGovernmentID"
        type="text"
        fullWidth
        margin="normal"
        {...register('recipientGovernmentID', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.recipientGovernmentID}
        helperText={errors.recipientGovernmentID?.message}
      />

      <TextField
        label="RecipientAddress"
        type="text"
        fullWidth
        margin="normal"
        {...register('recipientAddress', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.recipientAddress}
        helperText={errors.recipientAddress?.message}
      />

      <TextField
        label="RecipientAccointOpeningDate"
        type="text"
        fullWidth
        margin="normal"
        {...register('recipientAccointOpeningDate', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.recipientAccointOpeningDate}
        helperText={errors.recipientAccointOpeningDate?.message}
      />

      <TextField
        label="MailingAddress"
        type="text"
        fullWidth
        margin="normal"
        {...register('mailingAddress', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.mailingAddress}
        helperText={errors.mailingAddress?.message}
      />

      <TextField
        label="CreditCardType"
        type="text"
        fullWidth
        margin="normal"
        {...register('creditCardType', { maxLength: { value: 14, message: 'Maximum length is 14' } })}
        error={!!errors.creditCardType}
        helperText={errors.creditCardType?.message}
      />

      <TextField
        label="CreditCardNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('creditCardNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.creditCardNumber}
        helperText={errors.creditCardNumber?.message}
      />

      <TextField
        label="CreditCardCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('creditCardCountry', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.creditCardCountry}
        helperText={errors.creditCardCountry?.message}
      />

      <TextField
        label="CreditCardOwnerName"
        type="text"
        fullWidth
        margin="normal"
        {...register('creditCardOwnerName', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.creditCardOwnerName}
        helperText={errors.creditCardOwnerName?.message}
      />

      <TextField
        label="CreditCardOwnerGovernmentID"
        type="text"
        fullWidth
        margin="normal"
        {...register('creditCardOwnerGovernmentID', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.creditCardOwnerGovernmentID}
        helperText={errors.creditCardOwnerGovernmentID?.message}
      />

      <TextField
        label="CreditCardOwnerCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('creditCardOwnerCountry', { maxLength: { value: 5, message: 'Maximum length is 5' } })}
        error={!!errors.creditCardOwnerCountry}
        helperText={errors.creditCardOwnerCountry?.message}
      />

      <TextField
        label="CreditCardClearingHouse"
        type="text"
        fullWidth
        margin="normal"
        {...register('creditCardClearingHouse', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.creditCardClearingHouse}
        helperText={errors.creditCardClearingHouse?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/govtReport40s')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


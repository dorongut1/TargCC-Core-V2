// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: MCCBFPPaymentCard
//     Generated: 2025-12-18 07:30:19
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useMCCBFPPaymentCard, useCreateMCCBFPPaymentCard, useUpdateMCCBFPPaymentCard } from '../../hooks/useMCCBFPPaymentCard';
import type { CreateMCCBFPPaymentCardRequest, UpdateMCCBFPPaymentCardRequest } from '../../types/MCCBFPPaymentCard.types';


export const MCCBFPPaymentCardForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mCCBFPPaymentCardId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mCCBFPPaymentCardId;

  const { data: existingMCCBFPPaymentCard, isPending: isLoadingEntity } = useMCCBFPPaymentCard(mCCBFPPaymentCardId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMCCBFPPaymentCardRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMCCBFPPaymentCard) {
      reset(existingMCCBFPPaymentCard);
    }
  }, [existingMCCBFPPaymentCard, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMCCBFPPaymentCard();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMCCBFPPaymentCard();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMCCBFPPaymentCardRequest) => {
    if (isEdit && mCCBFPPaymentCardId) {
      updateEntity(
        { id: mCCBFPPaymentCardId, data: data as UpdateMCCBFPPaymentCardRequest },
        { onSuccess: () => navigate('/mCCBFPPaymentCards') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mCCBFPPaymentCards') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameMCCBFPPaymentCard')} />}
        label="TableNameMCCBFPPaymentCard"
      />

      <TextField
        label="Region"
        type="text"
        fullWidth
        margin="normal"
        {...register('region', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.region}
        helperText={errors.region?.message}
      />

      <TextField
        label="Country"
        type="text"
        fullWidth
        margin="normal"
        {...register('country', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.country}
        helperText={errors.country?.message}
      />

      <TextField
        label="Brand"
        type="text"
        fullWidth
        margin="normal"
        {...register('brand', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.brand}
        helperText={errors.brand?.message}
      />

      <TextField
        label="PayoutCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('payoutCurrency', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.payoutCurrency}
        helperText={errors.payoutCurrency?.message}
      />

      <TextField
        label="Channel"
        type="text"
        fullWidth
        margin="normal"
        {...register('channel', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.channel}
        helperText={errors.channel?.message}
      />

      <TextField
        label="ReachOfChannel"
        type="text"
        fullWidth
        margin="normal"
        {...register('reachOfChannel', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.reachOfChannel}
        helperText={errors.reachOfChannel?.message}
      />

      <TextField
        label="Corridors"
        type="text"
        fullWidth
        margin="normal"
        {...register('corridors', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.corridors}
        helperText={errors.corridors?.message}
      />

      <TextField
        label="MinimumTransactionAmount"
        type="text"
        fullWidth
        margin="normal"
        {...register('minimumTransactionAmount', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.minimumTransactionAmount}
        helperText={errors.minimumTransactionAmount?.message}
      />

      <TextField
        label="MaximumTransactionAmount"
        type="text"
        fullWidth
        margin="normal"
        {...register('maximumTransactionAmount', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.maximumTransactionAmount}
        helperText={errors.maximumTransactionAmount?.message}
      />

      <TextField
        label="DeliveryTime"
        type="text"
        fullWidth
        margin="normal"
        {...register('deliveryTime', { maxLength: { value: 60, message: 'Maximum length is 60' } })}
        error={!!errors.deliveryTime}
        helperText={errors.deliveryTime?.message}
      />

      <TextField
        label="SpecialNotes"
        type="text"
        fullWidth
        margin="normal"
        {...register('specialNotes', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.specialNotes}
        helperText={errors.specialNotes?.message}
      />

      <TextField
        label="OIPreapprovalRequired"
        type="text"
        fullWidth
        margin="normal"
        {...register('oIPreapprovalRequired', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.oIPreapprovalRequired}
        helperText={errors.oIPreapprovalRequired?.message}
      />

      <TextField
        label="RequiredInformation"
        type="text"
        fullWidth
        margin="normal"
        {...register('requiredInformation', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.requiredInformation}
        helperText={errors.requiredInformation?.message}
      />

      <TextField
        label="AverageResponseTimeFromRSP"
        type="text"
        fullWidth
        margin="normal"
        {...register('averageResponseTimeFromRSP', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.averageResponseTimeFromRSP}
        helperText={errors.averageResponseTimeFromRSP?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mCCBFPPaymentCards')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


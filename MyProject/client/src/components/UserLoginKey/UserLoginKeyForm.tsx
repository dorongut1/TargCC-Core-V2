// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_UserLoginKey
//     Generated: 2025-12-17 08:16:12
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useUserLoginKey, useCreateUserLoginKey, useUpdateUserLoginKey } from '../../hooks/useUserLoginKey';
import type { CreateUserLoginKeyRequest, UpdateUserLoginKeyRequest } from '../../types/UserLoginKey.types';


export const UserLoginKeyForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const userLoginKeyId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!userLoginKeyId;

  const { data: existingUserLoginKey, isPending: isLoadingEntity } = useUserLoginKey(userLoginKeyId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateUserLoginKeyRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingUserLoginKey) {
      reset(existingUserLoginKey);
    }
  }, [existingUserLoginKey, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateUserLoginKey();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateUserLoginKey();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateUserLoginKeyRequest) => {
    if (isEdit && userLoginKeyId) {
      updateEntity(
        { id: userLoginKeyId, data: data as UpdateUserLoginKeyRequest },
        { onSuccess: () => navigate('/userLoginKeys') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/userLoginKeys') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCUserLoginKey')} />}
        label="TableNameCUserLoginKey"
      />

      <TextField
        label="CUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cUserID}
        helperText={errors.cUserID?.message}
      />

      <TextField
        label="ApplicationName"
        type="text"
        fullWidth
        margin="normal"
        {...register('applicationName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.applicationName}
        helperText={errors.applicationName?.message}
      />

      <TextField
        label="ApplicationIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('applicationIdentifier', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.applicationIdentifier}
        helperText={errors.applicationIdentifier?.message}
      />

      <TextField
        label="EnoKey"
        type="text"
        fullWidth
        margin="normal"
        {...register('enoKey', { maxLength: { value: 64, message: 'Maximum length is 64' } })}
        error={!!errors.enoKey}
        helperText={errors.enoKey?.message}
      />

      <TextField
        label="ExternalIPAtCreation"
        type="text"
        fullWidth
        margin="normal"
        {...register('externalIPAtCreation', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.externalIPAtCreation}
        helperText={errors.externalIPAtCreation?.message}
      />

      <TextField
        label="CountryAtCreation"
        type="text"
        fullWidth
        margin="normal"
        {...register('countryAtCreation', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.countryAtCreation}
        helperText={errors.countryAtCreation?.message}
      />

      <TextField
        label="LastAccessTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('lastAccessTime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lastAccessTime}
        helperText={errors.lastAccessTime?.message}
      />

      <TextField
        label="LoggedLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('loggedLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.loggedLoginID}
        helperText={errors.loggedLoginID?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/userLoginKeys')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


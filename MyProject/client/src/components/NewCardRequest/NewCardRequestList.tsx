// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: NewCardRequest
//     Generated: 2025-12-11 22:10:39
// </auto-generated>

import React from 'react';
import { useNavigate } from 'react-router-dom';
import ExcelJS from 'exceljs';
import { DataGrid, GridColDef, GridActionsCellItem, GridToolbarContainer, GridToolbarColumnsButton, GridToolbarDensitySelector, useGridApiRef } from '@mui/x-data-grid';
import { Button, Box, CircularProgress, Alert, TextField, Paper, MenuItem, Typography } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, Add as AddIcon, FileDownload as FileDownloadIcon, Clear as ClearIcon } from '@mui/icons-material';
import { useNewCardRequests, useDeleteNewCardRequest } from '../../hooks/useNewCardRequest';
import type { NewCardRequest, NewCardRequestFilters } from '../../types/NewCardRequest.types';


export const NewCardRequestList: React.FC = () => {
  const navigate = useNavigate();
  const apiRef = useGridApiRef();
  
  // Helper function to format dates as DD/MM/YYYY
  const formatDate = (date: any): string => {
    if (!date) return '';
    const d = new Date(date);
    if (isNaN(d.getTime())) return '';
    const day = d.getDate().toString().padStart(2, '0');
    const month = (d.getMonth() + 1).toString().padStart(2, '0');
    const year = d.getFullYear();
    return `${day}/${month}/${year}`;
  };
  
  const [columnFilters, setColumnFilters] = React.useState<Record<string, string>>({});
  const { data: newCardRequests, isLoading, error } = useNewCardRequests({});
  const { mutate: deleteEntity } = useDeleteNewCardRequest();

  // Client-side column filters
  const filteredData = React.useMemo(() => {
    if (!newCardRequests) return newCardRequests;

    // Get active filters
    const activeFilters = Object.entries(columnFilters).filter(([_, value]) => value && value.trim() !== '');
    if (activeFilters.length === 0) return newCardRequests;

    // Apply all filters with AND logic
    return newCardRequests.filter(item => {
      return activeFilters.every(([column, filterValue]) => {
        // Cast to any to avoid TypeScript union type issues with instanceof
        const itemValue = item[column as keyof typeof item] as any;
        
        // Special handling for date filtering - compare as Date objects
        if (typeof filterValue === 'string' && /^\d{4}-\d{2}-\d{2}$/.test(filterValue)) {
          // Filter value is from date picker (YYYY-MM-DD)
          const filterDate = new Date(filterValue);
          let itemDate: Date | null = null;
          
          if (itemValue instanceof Date) {
            itemDate = itemValue;
          } else if (typeof itemValue === 'string' && /^\d{4}-\d{2}-\d{2}/.test(itemValue)) {
            itemDate = new Date(itemValue);
          }
          
          if (itemDate) {
            // Compare dates (ignore time) - same year, month, and day
            return itemDate.getFullYear() === filterDate.getFullYear() &&
                   itemDate.getMonth() === filterDate.getMonth() &&
                   itemDate.getDate() === filterDate.getDate();
          }
          return false;
        }
        
        // Convert boolean values to readable text
        if (typeof itemValue === 'boolean') {
          itemValue = itemValue ? 'true' : 'false';
        }
        
        return itemValue?.toString().toLowerCase().includes(filterValue.toLowerCase());
      });
    });
  }, [newCardRequests, columnFilters]);

  const handleClearAllFilters = () => {
    setColumnFilters({});
  };

  function CustomToolbar() {
    return (
      <GridToolbarContainer>
        <GridToolbarColumnsButton />
        <GridToolbarDensitySelector />
        <Button
          size="small"
          startIcon={<ClearIcon />}
          onClick={handleClearAllFilters}
        >
          Clear All Filters
        </Button>
      </GridToolbarContainer>
    );
  }

  const handleExportToExcel = async () => {
    // Export only the filtered data visible on screen
    const visibleRows = filteredData || [];

    if (!visibleRows || visibleRows.length === 0) {
      alert('No data to export');
      return;
    }

    // Get visible columns from DataGrid (respects column visibility)
    const visibleColumns = apiRef.current
      ? apiRef.current.getVisibleColumns().map(col => col.field).filter(field => field !== 'actions')
      : Object.keys(visibleRows[0] || {});

    // Filter data to only include visible columns
    const exportData = visibleRows.map(row => {
      const filteredRow: any = {};
      const rowAny = row as any;
      visibleColumns.forEach(col => {
        if (col in rowAny) filteredRow[col] = rowAny[col];
      });
      return filteredRow;
    });

    // Create workbook and worksheet
    const workbook = new ExcelJS.Workbook();
    const worksheet = workbook.addWorksheet('NewCardRequests');

    // Get column names from first row (only visible columns)
    const columnNames = exportData.length > 0 ? Object.keys(exportData[0]) : [];
    worksheet.columns = columnNames.map(name => ({ header: name, key: name, width: 15 }));

    // Add data rows
    exportData.forEach(row => worksheet.addRow(row));

    // Style header row
    const headerRow = worksheet.getRow(1);
    headerRow.font = { bold: true, color: { argb: 'FFFFFFFF' } };
    headerRow.fill = { type: 'pattern', pattern: 'solid', fgColor: { argb: 'FF4472C4' } };
    headerRow.alignment = { horizontal: 'center', vertical: 'middle' };
    headerRow.height = 20;

    // Auto-fit columns based on content
    worksheet.columns.forEach(column => {
      let maxLength = column.header?.length || 10;
      column.eachCell?.({ includeEmpty: false }, cell => {
        const cellLength = cell.value ? String(cell.value).length : 0;
        maxLength = Math.max(maxLength, cellLength);
      });
      column.width = Math.min(maxLength + 2, 50);
    });

    // Freeze header row
    worksheet.views = [{ state: 'frozen', ySplit: 1 }];

    // Generate Excel file and download
    const buffer = await workbook.xlsx.writeBuffer();
    const blob = new Blob([buffer], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
    const url = window.URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `NewCardRequests_${new Date().toISOString().split('T')[0]}.xlsx`;
    link.click();
    window.URL.revokeObjectURL(url);
  };

  const columns: GridColDef<NewCardRequest>[] = [
    { field: 'tableNameNewCardRequest', headerName: 'TableNameNewCardRequest', width: 100 },
    { field: 'id', headerName: 'Id', width: 90 },
    { field: 'enmCurrency', headerName: 'EnmCurrency', width: 150 },
    { field: 'enmReceiptMethod', headerName: 'EnmReceiptMethod', width: 150 },
    { field: 'initialLoad', headerName: 'InitialLoad', width: 150 },
    { field: 'enmInitialLoadMethodLoadMethod', headerName: 'EnmInitialLoadMethodLoadMethod', width: 150 },
    { field: 'customerID', headerName: 'CustomerID', width: 150 },
    { field: 'firstName', headerName: 'FirstName', width: 200 },
    { field: 'lastName', headerName: 'LastName', width: 200 },
    { field: 'birthDate', headerName: 'BirthDate', width: 150, valueFormatter: (value) => formatDate(value) },
    {
      field: 'actions',
      type: 'actions',
      headerName: 'Actions',
      width: 100,
      getActions: (params) => [
        <GridActionsCellItem
          icon={<EditIcon />}
          label="Edit"
          onClick={() => navigate(`/newCardRequests/${params.id}`)}
        />,
        <GridActionsCellItem
          icon={<DeleteIcon />}
          label="Delete"
          onClick={() => {
            if (confirm('Are you sure?')) {
              deleteEntity(params.id as number);
            }
          }}
        />,
      ],
    },
  ];


  if (isLoading) {
    return <CircularProgress />;
  }

  if (error) {
    return <Alert severity="error">Failed to load newCardRequests</Alert>;
  }

  return (
    <Box sx={{ height: 'calc(100vh - 200px)', width: '100%', display: 'flex', flexDirection: 'column' }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="contained"
          startIcon={<AddIcon />}
          onClick={() => navigate('/newCardRequests/new')}
        >
          Create NewCardRequest
        </Button>
        <Button
          variant="outlined"
          startIcon={<FileDownloadIcon />}
          onClick={handleExportToExcel}
        >
          Export to Excel
        </Button>
      </Box>
      <Paper sx={{ p: 2, mb: 2 }}>
        <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap', alignItems: 'center' }}>
          <TextField
            select
            label="TableNameNewCardRequest"
            size="small"
            sx={{ minWidth: 120 }}
            value={columnFilters['tableNameNewCardRequest'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              tableNameNewCardRequest: e.target.value
            }))}
          >
            <MenuItem value="">All</MenuItem>
            <MenuItem value="true">Yes</MenuItem>
            <MenuItem value="false">No</MenuItem>
          </TextField>
          <TextField
            label="Id"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['id'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              id: e.target.value
            }))}
          />
          <TextField
            label="EnmCurrency"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['enmCurrency'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              enmCurrency: e.target.value
            }))}
          />
          <TextField
            label="EnmReceiptMethod"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['enmReceiptMethod'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              enmReceiptMethod: e.target.value
            }))}
          />
          <TextField
            label="InitialLoad"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['initialLoad'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              initialLoad: e.target.value
            }))}
          />
          <TextField
            label="EnmInitialLoadMethodLoadMethod"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['enmInitialLoadMethodLoadMethod'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              enmInitialLoadMethodLoadMethod: e.target.value
            }))}
          />
          <TextField
            label="CustomerID"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['customerID'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              customerID: e.target.value
            }))}
          />
          <TextField
            label="FirstName"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['firstName'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              firstName: e.target.value
            }))}
          />
          <TextField
            label="LastName"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['lastName'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              lastName: e.target.value
            }))}
          />
          <TextField
            label="BirthDate"
            type="date"
            size="small"
            sx={{ minWidth: 180 }}
            InputLabelProps={{ shrink: true }}
            value={columnFilters['birthDate'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              birthDate: e.target.value
            }))}
          />
          <Button
            variant="outlined"
            size="small"
            onClick={handleClearAllFilters}
          >
            Clear
          </Button>
          <Box sx={{ ml: 'auto', display: 'flex', alignItems: 'center' }}>
            <Typography variant="body2" color="text.secondary">
              Showing {filteredData?.length || 0} of {newCardRequests?.length || 0} rows
            </Typography>
          </Box>
        </Box>
      </Paper>

      <Box sx={{ flex: 1, minHeight: 0 }}>
        <DataGrid
          apiRef={apiRef}
          rows={filteredData || []}
          columns={columns}
          getRowId={(row) => row.id}
          slots={{
            toolbar: CustomToolbar,
          }}
          initialState={{
            pagination: { paginationModel: { pageSize: 10 } },
          }}
          pageSizeOptions={[5, 10, 25, 100]}
          checkboxSelection
          disableRowSelectionOnClick
        />
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: NewCardRequest
//     Generated: 2025-12-15 13:48:33
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useNewCardRequest, useCreateNewCardRequest, useUpdateNewCardRequest } from '../../hooks/useNewCardRequest';
import type { CreateNewCardRequestRequest, UpdateNewCardRequestRequest } from '../../types/NewCardRequest.types';


export const NewCardRequestForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const newCardRequestId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!newCardRequestId;

  const { data: existingNewCardRequest, isPending: isLoadingEntity } = useNewCardRequest(newCardRequestId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateNewCardRequestRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingNewCardRequest) {
      reset(existingNewCardRequest);
    }
  }, [existingNewCardRequest, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateNewCardRequest();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateNewCardRequest();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateNewCardRequestRequest) => {
    if (isEdit && newCardRequestId) {
      updateEntity(
        { id: newCardRequestId, data: data as UpdateNewCardRequestRequest },
        { onSuccess: () => navigate('/newCardRequests') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/newCardRequests') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameNewCardRequest')} />}
        label="TableNameNewCardRequest"
      />

      <TextField
        label="EnmCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCurrency', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCurrency}
        helperText={errors.enmCurrency?.message}
      />

      <TextField
        label="EnmReceiptMethod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmReceiptMethod', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmReceiptMethod}
        helperText={errors.enmReceiptMethod?.message}
      />

      <TextField
        label="InitialLoad"
        type="number"
        fullWidth
        margin="normal"
        {...register('initialLoad', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.initialLoad}
        helperText={errors.initialLoad?.message}
      />

      <TextField
        label="EnmInitialLoadMethodLoadMethod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmInitialLoadMethodLoadMethod', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmInitialLoadMethodLoadMethod}
        helperText={errors.enmInitialLoadMethodLoadMethod?.message}
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="FirstName"
        type="text"
        fullWidth
        margin="normal"
        {...register('firstName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.firstName}
        helperText={errors.firstName?.message}
      />

      <TextField
        label="LastName"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lastName}
        helperText={errors.lastName?.message}
      />

      <TextField
        label="BirthDate"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('birthDate', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.birthDate}
        helperText={errors.birthDate?.message}
      />

      <TextField
        label="IsraelIDNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('israelIDNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.israelIDNumber}
        helperText={errors.israelIDNumber?.message}
      />

      <TextField
        label="LkpPassportCountryCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpPassportCountryCountry', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpPassportCountryCountry}
        helperText={errors.lkpPassportCountryCountry?.message}
      />

      <TextField
        label="PassportNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('passportNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.passportNumber}
        helperText={errors.passportNumber?.message}
      />

      <TextField
        label="EnmGender"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmGender', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmGender}
        helperText={errors.enmGender?.message}
      />

      <TextField
        label="ActualAddressNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualAddressNumber', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.actualAddressNumber}
        helperText={errors.actualAddressNumber?.message}
      />

      <TextField
        label="ActualAddressStreet"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualAddressStreet', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.actualAddressStreet}
        helperText={errors.actualAddressStreet?.message}
      />

      <TextField
        label="ActualAddressCity"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualAddressCity', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.actualAddressCity}
        helperText={errors.actualAddressCity?.message}
      />

      <TextField
        label="ActualZipCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualZipCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.actualZipCode}
        helperText={errors.actualZipCode?.message}
      />

      <TextField
        label="CellphoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('cellphoneNumber', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.cellphoneNumber}
        helperText={errors.cellphoneNumber?.message}
      />

      <TextField
        label="Email"
        type="text"
        fullWidth
        margin="normal"
        {...register('email', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.email}
        helperText={errors.email?.message}
      />

      <TextField
        label="Comment"
        fullWidth
        margin="normal"
        {...register('comment', {  })}
        error={!!errors.comment}
        helperText={errors.comment?.message}
      />

      <TextField
        label="WhenOpened"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenOpened', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenOpened}
        helperText={errors.whenOpened?.message}
      />

      <TextField
        label="EnmCardRequestStatus"
        fullWidth
        margin="normal"
        {...register('enmCardRequestStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCardRequestStatus}
        helperText={errors.enmCardRequestStatus?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isCustomerDefined')} />}
        label="IsCustomerDefined"
      />

      <FormControlLabel
        control={<Checkbox {...register('isKYCDefined')} />}
        label="IsKYCDefined"
      />

      <TextField
        label="DeliveryFeeAdjustment"
        type="number"
        fullWidth
        margin="normal"
        {...register('deliveryFeeAdjustment', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.deliveryFeeAdjustment}
        helperText={errors.deliveryFeeAdjustment?.message}
      />

      <TextField
        label="DistributorSentToID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorSentToID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorSentToID}
        helperText={errors.distributorSentToID?.message}
      />

      <TextField
        label="InterviewedByUser"
        fullWidth
        margin="normal"
        {...register('interviewedByUser', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.interviewedByUser}
        helperText={errors.interviewedByUser?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="EnmLastStatusBeforeCancelCardRequestStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLastStatusBeforeCancelCardRequestStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmLastStatusBeforeCancelCardRequestStatus}
        helperText={errors.enmLastStatusBeforeCancelCardRequestStatus?.message}
      />

      <TextField
        label="NameOnCard"
        type="text"
        fullWidth
        margin="normal"
        {...register('nameOnCard', { required: 'NameOnCard is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.nameOnCard}
        helperText={errors.nameOnCard?.message}
      />

      <TextField
        label="EnmPendingLoadMethodLoadMethod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmPendingLoadMethodLoadMethod', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmPendingLoadMethodLoadMethod}
        helperText={errors.enmPendingLoadMethodLoadMethod?.message}
      />

      <TextField
        label="PendingLoadAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('pendingLoadAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.pendingLoadAmount}
        helperText={errors.pendingLoadAmount?.message}
      />

      <TextField
        label="PendingLoadReference"
        type="text"
        fullWidth
        margin="normal"
        {...register('pendingLoadReference', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.pendingLoadReference}
        helperText={errors.pendingLoadReference?.message}
      />

      <TextField
        label="PendingLoadUserResponsible"
        type="text"
        fullWidth
        margin="normal"
        {...register('pendingLoadUserResponsible', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.pendingLoadUserResponsible}
        helperText={errors.pendingLoadUserResponsible?.message}
      />

      <TextField
        label="CardActivationID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardActivationID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardActivationID}
        helperText={errors.cardActivationID?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('initialLoadComplete')} />}
        label="InitialLoadComplete"
      />

      <TextField
        label="CustomerWalletID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerWalletID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerWalletID}
        helperText={errors.customerWalletID?.message}
      />

      <TextField
        label="InitialLoadNumberOfPayments"
        type="number"
        fullWidth
        margin="normal"
        {...register('initialLoadNumberOfPayments', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.initialLoadNumberOfPayments}
        helperText={errors.initialLoadNumberOfPayments?.message}
      />

      <TextField
        label="IntendedClubID"
        type="number"
        fullWidth
        margin="normal"
        {...register('intendedClubID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.intendedClubID}
        helperText={errors.intendedClubID?.message}
      />

      <TextField
        label="CommittedLoadAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('committedLoadAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.committedLoadAmount}
        helperText={errors.committedLoadAmount?.message}
      />

      <TextField
        label="CommittedPurchaseExchangeRateToILS"
        type="number"
        fullWidth
        margin="normal"
        {...register('committedPurchaseExchangeRateToILS', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.committedPurchaseExchangeRateToILS}
        helperText={errors.committedPurchaseExchangeRateToILS?.message}
      />

      <TextField
        label="CommittedLoadFee"
        type="number"
        fullWidth
        margin="normal"
        {...register('committedLoadFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.committedLoadFee}
        helperText={errors.committedLoadFee?.message}
      />

      <TextField
        label="CommittedDeliveryFee"
        type="number"
        fullWidth
        margin="normal"
        {...register('committedDeliveryFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.committedDeliveryFee}
        helperText={errors.committedDeliveryFee?.message}
      />

      <TextField
        label="CommittedNewCardFee"
        type="number"
        fullWidth
        margin="normal"
        {...register('committedNewCardFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.committedNewCardFee}
        helperText={errors.committedNewCardFee?.message}
      />

      <TextField
        label="EnmCustomerType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCustomerType', { required: 'EnmCustomerType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCustomerType}
        helperText={errors.enmCustomerType?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/newCardRequests')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerPartnership
//     Generated: 2025-12-11 21:35:46
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useCustomerPartnership, useCreateCustomerPartnership, useUpdateCustomerPartnership } from '../../hooks/useCustomerPartnership';
import type { CreateCustomerPartnershipRequest, UpdateCustomerPartnershipRequest } from '../../types/CustomerPartnership.types';


export const CustomerPartnershipForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const customerPartnershipId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!customerPartnershipId;

  const { data: existingCustomerPartnership, isPending: isLoadingEntity } = useCustomerPartnership(customerPartnershipId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCustomerPartnershipRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCustomerPartnership) {
      reset(existingCustomerPartnership);
    }
  }, [existingCustomerPartnership, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCustomerPartnership();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCustomerPartnership();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCustomerPartnershipRequest) => {
    if (isEdit && customerPartnershipId) {
      updateEntity(
        { id: customerPartnershipId, data: data as UpdateCustomerPartnershipRequest },
        { onSuccess: () => navigate('/customerPartnerships') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/customerPartnerships') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCustomerPartnership')} />}
        label="TableNameCustomerPartnership"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="EnmCustomerPartnership"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCustomerPartnership', { required: 'EnmCustomerPartnership is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.enmCustomerPartnership}
        helperText={errors.enmCustomerPartnership?.message}
      />

      <TextField
        label="PartnershipCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('partnershipCode', { required: 'PartnershipCode is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.partnershipCode}
        helperText={errors.partnershipCode?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/customerPartnerships')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


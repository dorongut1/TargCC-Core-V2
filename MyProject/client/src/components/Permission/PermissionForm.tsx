// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Permission
//     Generated: 2025-12-17 08:16:12
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { usePermission, useCreatePermission, useUpdatePermission } from '../../hooks/usePermission';
import type { CreatePermissionRequest, UpdatePermissionRequest } from '../../types/Permission.types';


export const PermissionForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const permissionId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!permissionId;

  const { data: existingPermission, isPending: isLoadingEntity } = usePermission(permissionId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreatePermissionRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingPermission) {
      reset(existingPermission);
    }
  }, [existingPermission, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreatePermission();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdatePermission();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreatePermissionRequest) => {
    if (isEdit && permissionId) {
      updateEntity(
        { id: permissionId, data: data as UpdatePermissionRequest },
        { onSuccess: () => navigate('/permissions') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/permissions') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCPermission')} />}
        label="TableNameCPermission"
      />

      <TextField
        label="CRoleID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cRoleID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cRoleID}
        helperText={errors.cRoleID?.message}
      />

      <TextField
        label="CProcessID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cProcessID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cProcessID}
        helperText={errors.cProcessID?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('canDo')} />}
        label="CanDo"
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/permissions')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


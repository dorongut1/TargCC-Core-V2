// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_LoggedRequest
//     Generated: 2025-12-16 09:50:43
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress } from '@mui/material';
import { useLoggedRequest, useCreateLoggedRequest, useUpdateLoggedRequest } from '../../hooks/useLoggedRequest';
import type { CreateLoggedRequestRequest, UpdateLoggedRequestRequest } from '../../types/LoggedRequest.types';


export const LoggedRequestForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const loggedRequestId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!loggedRequestId;

  const { data: existingLoggedRequest, isPending: isLoadingEntity } = useLoggedRequest(loggedRequestId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateLoggedRequestRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingLoggedRequest) {
      reset(existingLoggedRequest);
    }
  }, [existingLoggedRequest, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateLoggedRequest();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateLoggedRequest();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateLoggedRequestRequest) => {
    if (isEdit && loggedRequestId) {
      updateEntity(
        { id: loggedRequestId, data: data as UpdateLoggedRequestRequest },
        { onSuccess: () => navigate('/loggedRequests') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/loggedRequests') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="CLoggedLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cLoggedLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cLoggedLoginID}
        helperText={errors.cLoggedLoginID?.message}
      />

      <TextField
        label="TimeAccessed"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('timeAccessed', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.timeAccessed}
        helperText={errors.timeAccessed?.message}
      />

      <TextField
        label="CUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cUserID}
        helperText={errors.cUserID?.message}
      />

      <TextField
        label="CallingFunctionWithinApplication"
        type="text"
        fullWidth
        margin="normal"
        {...register('callingFunctionWithinApplication', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.callingFunctionWithinApplication}
        helperText={errors.callingFunctionWithinApplication?.message}
      />

      <TextField
        label="EntryPoint"
        type="text"
        fullWidth
        margin="normal"
        {...register('entryPoint', { maxLength: { value: 255, message: 'Maximum length is 255' } })}
        error={!!errors.entryPoint}
        helperText={errors.entryPoint?.message}
      />

      <TextField
        label="Process"
        type="text"
        fullWidth
        margin="normal"
        {...register('process', { maxLength: { value: 75, message: 'Maximum length is 75' } })}
        error={!!errors.process}
        helperText={errors.process?.message}
      />

      <TextField
        label="Thread"
        type="text"
        fullWidth
        margin="normal"
        {...register('thread', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.thread}
        helperText={errors.thread?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/loggedRequests')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


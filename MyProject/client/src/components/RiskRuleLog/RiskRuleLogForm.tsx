// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: RiskRuleLog
//     Generated: 2025-12-17 13:13:01
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useRiskRuleLog, useCreateRiskRuleLog, useUpdateRiskRuleLog } from '../../hooks/useRiskRuleLog';
import type { CreateRiskRuleLogRequest, UpdateRiskRuleLogRequest } from '../../types/RiskRuleLog.types';


export const RiskRuleLogForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const riskRuleLogId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!riskRuleLogId;

  const { data: existingRiskRuleLog, isPending: isLoadingEntity } = useRiskRuleLog(riskRuleLogId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateRiskRuleLogRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingRiskRuleLog) {
      reset(existingRiskRuleLog);
    }
  }, [existingRiskRuleLog, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateRiskRuleLog();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateRiskRuleLog();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateRiskRuleLogRequest) => {
    if (isEdit && riskRuleLogId) {
      updateEntity(
        { id: riskRuleLogId, data: data as UpdateRiskRuleLogRequest },
        { onSuccess: () => navigate('/riskRuleLogs') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/riskRuleLogs') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameRiskRuleLog')} />}
        label="TableNameRiskRuleLog"
      />

      <TextField
        label="RiskRuleID"
        type="number"
        fullWidth
        margin="normal"
        {...register('riskRuleID', { required: 'RiskRuleID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.riskRuleID}
        helperText={errors.riskRuleID?.message}
      />

      <TextField
        label="ChangeType"
        type="text"
        fullWidth
        margin="normal"
        {...register('changeType', { required: 'ChangeType is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.changeType}
        helperText={errors.changeType?.message}
      />

      <TextField
        label="FieldName"
        type="text"
        fullWidth
        margin="normal"
        {...register('fieldName', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.fieldName}
        helperText={errors.fieldName?.message}
      />

      <TextField
        label="OldValue"
        type="text"
        fullWidth
        margin="normal"
        {...register('oldValue', {  })}
        error={!!errors.oldValue}
        helperText={errors.oldValue?.message}
      />

      <TextField
        label="NewValue"
        type="text"
        fullWidth
        margin="normal"
        {...register('newValue', {  })}
        error={!!errors.newValue}
        helperText={errors.newValue?.message}
      />

      <TextField
        label="ChangeReason"
        type="text"
        fullWidth
        margin="normal"
        {...register('changeReason', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.changeReason}
        helperText={errors.changeReason?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/riskRuleLogs')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: ImportTransactionLoad
//     Generated: 2025-12-17 08:16:17
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useImportTransactionLoad, useCreateImportTransactionLoad, useUpdateImportTransactionLoad } from '../../hooks/useImportTransactionLoad';
import type { CreateImportTransactionLoadRequest, UpdateImportTransactionLoadRequest } from '../../types/ImportTransactionLoad.types';


export const ImportTransactionLoadForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const importTransactionLoadId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!importTransactionLoadId;

  const { data: existingImportTransactionLoad, isPending: isLoadingEntity } = useImportTransactionLoad(importTransactionLoadId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateImportTransactionLoadRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingImportTransactionLoad) {
      reset(existingImportTransactionLoad);
    }
  }, [existingImportTransactionLoad, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateImportTransactionLoad();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateImportTransactionLoad();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateImportTransactionLoadRequest) => {
    if (isEdit && importTransactionLoadId) {
      updateEntity(
        { id: importTransactionLoadId, data: data as UpdateImportTransactionLoadRequest },
        { onSuccess: () => navigate('/importTransactionLoads') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/importTransactionLoads') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameImportTransactionLoad')} />}
        label="TableNameImportTransactionLoad"
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { required: 'DistributorID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="WhenInitialized"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenInitialized', { required: 'WhenInitialized is required', maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenInitialized}
        helperText={errors.whenInitialized?.message}
      />

      <TextField
        label="InitiatedByUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('initiatedByUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.initiatedByUserID}
        helperText={errors.initiatedByUserID?.message}
      />

      <TextField
        label="WhenCompleted"
        fullWidth
        margin="normal"
        {...register('whenCompleted', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenCompleted}
        helperText={errors.whenCompleted?.message}
      />

      <TextField
        label="LoadsRequested"
        type="number"
        fullWidth
        margin="normal"
        {...register('loadsRequested', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.loadsRequested}
        helperText={errors.loadsRequested?.message}
      />

      <TextField
        label="LoadsApproved"
        type="number"
        fullWidth
        margin="normal"
        {...register('loadsApproved', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.loadsApproved}
        helperText={errors.loadsApproved?.message}
      />

      <TextField
        label="Comments"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 4000, message: 'Maximum length is 4000' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/importTransactionLoads')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Limitation
//     Generated: 2025-12-15 22:52:43
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useLimitation, useCreateLimitation, useUpdateLimitation } from '../../hooks/useLimitation';
import type { CreateLimitationRequest, UpdateLimitationRequest } from '../../types/Limitation.types';


export const LimitationForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const limitationId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!limitationId;

  const { data: existingLimitation, isPending: isLoadingEntity } = useLimitation(limitationId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateLimitationRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingLimitation) {
      reset(existingLimitation);
    }
  }, [existingLimitation, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateLimitation();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateLimitation();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateLimitationRequest) => {
    if (isEdit && limitationId) {
      updateEntity(
        { id: limitationId, data: data as UpdateLimitationRequest },
        { onSuccess: () => navigate('/limitations') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/limitations') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameLimitation')} />}
        label="TableNameLimitation"
      />

      <TextField
        label="EnmLimitation"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLimitation', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmLimitation}
        helperText={errors.enmLimitation?.message}
      />

      <TextField
        label="EnmLimitedEntityEntity"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLimitedEntityEntity', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmLimitedEntityEntity}
        helperText={errors.enmLimitedEntityEntity?.message}
      />

      <TextField
        label="Amount"
        type="number"
        fullWidth
        margin="normal"
        {...register('amount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amount}
        helperText={errors.amount?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/limitations')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


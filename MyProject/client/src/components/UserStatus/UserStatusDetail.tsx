// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_UserStatus
//     Generated: 2025-12-11 21:35:44
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { useUserStatus, useDeleteUserStatus } from '../../hooks/useUserStatus';
import type { UserStatus } from '../../types/UserStatus.types';


export const UserStatusDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useUserStatus(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteUserStatus();

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/userStatuss'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load userStatus</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/userStatuss')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/userStatuss/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            UserStatus Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCUserStatus
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCUserStatus ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CUserID
          </Typography>
          <Typography variant="body1">
            {entity.cUserID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ApplicationName
          </Typography>
          <Typography variant="body1">
            {entity.applicationName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LastLoggedLoginID
          </Typography>
          <Typography variant="body1">
            {entity.lastLoggedLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoginTime
          </Typography>
          <Typography variant="body1">
            {(entity.loginTime ? (entity.loginTime instanceof Date ? entity.loginTime.toLocaleDateString() : new Date(entity.loginTime).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LogoutTime
          </Typography>
          <Typography variant="body1">
            {(entity.logoutTime ? (entity.logoutTime instanceof Date ? entity.logoutTime.toLocaleDateString() : new Date(entity.logoutTime).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? (entity.deletedOn instanceof Date ? entity.deletedOn.toLocaleDateString() : new Date(entity.deletedOn).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>
    </Box>
  );
};


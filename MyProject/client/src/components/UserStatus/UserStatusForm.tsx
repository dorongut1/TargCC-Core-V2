// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_UserStatus
//     Generated: 2025-12-17 21:20:37
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useUserStatus, useCreateUserStatus, useUpdateUserStatus } from '../../hooks/useUserStatus';
import type { CreateUserStatusRequest, UpdateUserStatusRequest } from '../../types/UserStatus.types';


export const UserStatusForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const userStatusId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!userStatusId;

  const { data: existingUserStatus, isPending: isLoadingEntity } = useUserStatus(userStatusId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateUserStatusRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingUserStatus) {
      reset(existingUserStatus);
    }
  }, [existingUserStatus, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateUserStatus();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateUserStatus();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateUserStatusRequest) => {
    if (isEdit && userStatusId) {
      updateEntity(
        { id: userStatusId, data: data as UpdateUserStatusRequest },
        { onSuccess: () => navigate('/userStatuss') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/userStatuss') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCUserStatus')} />}
        label="TableNameCUserStatus"
      />

      <TextField
        label="CUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cUserID}
        helperText={errors.cUserID?.message}
      />

      <TextField
        label="ApplicationName"
        type="text"
        fullWidth
        margin="normal"
        {...register('applicationName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.applicationName}
        helperText={errors.applicationName?.message}
      />

      <TextField
        label="LastLoggedLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('lastLoggedLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lastLoggedLoginID}
        helperText={errors.lastLoggedLoginID?.message}
      />

      <TextField
        label="LoginTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('loginTime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.loginTime}
        helperText={errors.loginTime?.message}
      />

      <TextField
        label="LogoutTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('logoutTime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.logoutTime}
        helperText={errors.logoutTime?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/userStatuss')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


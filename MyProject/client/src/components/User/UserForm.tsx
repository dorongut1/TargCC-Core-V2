// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_User
//     Generated: 2025-12-17 07:52:20
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useUser, useCreateUser, useUpdateUser } from '../../hooks/useUser';
import type { CreateUserRequest, UpdateUserRequest } from '../../types/User.types';


export const UserForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const userId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!userId;

  const { data: existingUser, isPending: isLoadingEntity } = useUser(userId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateUserRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingUser) {
      reset(existingUser);
    }
  }, [existingUser, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateUser();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateUser();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateUserRequest) => {
    if (isEdit && userId) {
      updateEntity(
        { id: userId, data: data as UpdateUserRequest },
        { onSuccess: () => navigate('/users') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/users') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCUser')} />}
        label="TableNameCUser"
      />

      <TextField
        label="UserName"
        type="text"
        fullWidth
        margin="normal"
        {...register('userName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.userName}
        helperText={errors.userName?.message}
      />

      <TextField
        label="LastName"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lastName}
        helperText={errors.lastName?.message}
      />

      <TextField
        label="FirstName"
        type="text"
        fullWidth
        margin="normal"
        {...register('firstName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.firstName}
        helperText={errors.firstName?.message}
      />

      <TextField
        label="NationalIDNo"
        type="text"
        fullWidth
        margin="normal"
        {...register('nationalIDNo', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.nationalIDNo}
        helperText={errors.nationalIDNo?.message}
      />

      <TextField
        label="Address"
        type="text"
        fullWidth
        margin="normal"
        {...register('address', { maxLength: { value: 500, message: 'Maximum length is 500' } })}
        error={!!errors.address}
        helperText={errors.address?.message}
      />

      <TextField
        label="City"
        type="text"
        fullWidth
        margin="normal"
        {...register('city', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.city}
        helperText={errors.city?.message}
      />

      <TextField
        label="ProvinceState"
        type="text"
        fullWidth
        margin="normal"
        {...register('provinceState', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.provinceState}
        helperText={errors.provinceState?.message}
      />

      <TextField
        label="PostalCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('postalCode', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.postalCode}
        helperText={errors.postalCode?.message}
      />

      <TextField
        label="Country"
        type="text"
        fullWidth
        margin="normal"
        {...register('country', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.country}
        helperText={errors.country?.message}
      />

      <TextField
        label="PhoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('phoneNumber', { maxLength: { value: 100, message: 'Maximum length is 100' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.phoneNumber}
        helperText={errors.phoneNumber?.message}
      />

      <TextField
        label="Email"
        type="text"
        fullWidth
        margin="normal"
        {...register('email', { maxLength: { value: 100, message: 'Maximum length is 100' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.email}
        helperText={errors.email?.message}
      />

      <TextField
        label="EnoPassword"
        type="text"
        fullWidth
        margin="normal"
        {...register('enoPassword', { maxLength: { value: 64, message: 'Maximum length is 64' } })}
        error={!!errors.enoPassword}
        helperText={errors.enoPassword?.message}
      />

      <TextField
        label="DatePasswordChanged"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('datePasswordChanged', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.datePasswordChanged}
        helperText={errors.datePasswordChanged?.message}
      />

      <TextField
        label="EnmTypeUserIdentityType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmTypeUserIdentityType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmTypeUserIdentityType}
        helperText={errors.enmTypeUserIdentityType?.message}
      />

      <TextField
        label="IDinType"
        type="number"
        fullWidth
        margin="normal"
        {...register('iDinType', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.iDinType}
        helperText={errors.iDinType?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('requiresComputerIdentification')} />}
        label="RequiresComputerIdentification"
      />

      <FormControlLabel
        control={<Checkbox {...register('enableSimultaneousLogins')} />}
        label="EnableSimultaneousLogins"
      />

      <FormControlLabel
        control={<Checkbox {...register('isDisabled')} />}
        label="IsDisabled"
      />

      <TextField
        label="ExpiryDate"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('expiryDate', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.expiryDate}
        helperText={errors.expiryDate?.message}
      />

      <TextField
        label="Comments"
        type="text"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 500, message: 'Maximum length is 500' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="LastPasswords"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastPasswords', { maxLength: { value: 350, message: 'Maximum length is 350' } })}
        error={!!errors.lastPasswords}
        helperText={errors.lastPasswords?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="Applications (comma-separated)"
        fullWidth
        margin="normal"
        {...register('applications', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.applications}
        helperText={errors.applications?.message || 'Enter values separated by commas'}
      />

      <TextField
        label="EnmLanguage"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLanguage', { required: 'EnmLanguage is required', maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.enmLanguage}
        helperText={errors.enmLanguage?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isLockedOut')} />}
        label="IsLockedOut"
      />

      <TextField
        label="RoleID"
        type="number"
        fullWidth
        margin="normal"
        {...register('roleID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.roleID}
        helperText={errors.roleID?.message}
      />

      <TextField
        label="EnmAuthenticationMethod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmAuthenticationMethod', { required: 'EnmAuthenticationMethod is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmAuthenticationMethod}
        helperText={errors.enmAuthenticationMethod?.message}
      />

      <TextField
        label="EnmMessagingMode"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmMessagingMode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmMessagingMode}
        helperText={errors.enmMessagingMode?.message}
      />

      <TextField
        label="LoggedInIP"
        fullWidth
        margin="normal"
        {...register('loggedInIP', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.loggedInIP}
        helperText={errors.loggedInIP?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('requiresFixedIP')} />}
        label="RequiresFixedIP"
      />

      <TextField
        label="EnoApprovalCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('enoApprovalCode', { maxLength: { value: 64, message: 'Maximum length is 64' } })}
        error={!!errors.enoApprovalCode}
        helperText={errors.enoApprovalCode?.message}
      />

      <TextField
        label="ApprovalFunctionName"
        type="text"
        fullWidth
        margin="normal"
        {...register('approvalFunctionName', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.approvalFunctionName}
        helperText={errors.approvalFunctionName?.message}
      />

      <TextField
        label="ApprovalTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('approvalTime', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.approvalTime}
        helperText={errors.approvalTime?.message}
      />

      <TextField
        label="LastSuccessfulLogin"
        fullWidth
        margin="normal"
        {...register('lastSuccessfulLogin', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.lastSuccessfulLogin}
        helperText={errors.lastSuccessfulLogin?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('passwordNeverExpires')} />}
        label="PasswordNeverExpires"
      />

      <TextField
        label="LkpSecurityQuestion1SecurityQuestion"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpSecurityQuestion1SecurityQuestion', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpSecurityQuestion1SecurityQuestion}
        helperText={errors.lkpSecurityQuestion1SecurityQuestion?.message}
      />

      <TextField
        label="EntSecurityQuestion1Response"
        type="text"
        fullWidth
        margin="normal"
        {...register('entSecurityQuestion1Response', {  })}
        error={!!errors.entSecurityQuestion1Response}
        helperText={errors.entSecurityQuestion1Response?.message}
      />

      <TextField
        label="LkpSecurityQuestion2SecurityQuestion"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpSecurityQuestion2SecurityQuestion', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpSecurityQuestion2SecurityQuestion}
        helperText={errors.lkpSecurityQuestion2SecurityQuestion?.message}
      />

      <TextField
        label="EntSecurityQuestion2Response"
        type="text"
        fullWidth
        margin="normal"
        {...register('entSecurityQuestion2Response', {  })}
        error={!!errors.entSecurityQuestion2Response}
        helperText={errors.entSecurityQuestion2Response?.message}
      />

      <TextField
        label="LkpSecurityQuestion3SecurityQuestion"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpSecurityQuestion3SecurityQuestion', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpSecurityQuestion3SecurityQuestion}
        helperText={errors.lkpSecurityQuestion3SecurityQuestion?.message}
      />

      <TextField
        label="EntSecurityQuestion3Response"
        type="text"
        fullWidth
        margin="normal"
        {...register('entSecurityQuestion3Response', {  })}
        error={!!errors.entSecurityQuestion3Response}
        helperText={errors.entSecurityQuestion3Response?.message}
      />

      <TextField
        label="EntPIN"
        type="text"
        fullWidth
        margin="normal"
        {...register('entPIN', {  })}
        error={!!errors.entPIN}
        helperText={errors.entPIN?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/users')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


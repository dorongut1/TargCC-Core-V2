// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_IndexFragmentationData
//     Generated: 2025-12-11 11:52:10
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import type { IndexFragmentationData } from '../../types/IndexFragmentationData.types';


export const IndexFragmentationDataDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  // VIEWs are read-only - Detail page not applicable
  const isLoading = false;
  const error = null;
  const entity = null;

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load indexFragmentationData</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/indexFragmentationDatas')}
        >
          Back
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            IndexFragmentationData Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableName
          </Typography>
          <Typography variant="body1">
            {entity.tableName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IndexName
          </Typography>
          <Typography variant="body1">
            {entity.indexName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IndexType
          </Typography>
          <Typography variant="body1">
            {entity.indexType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            FragmentationPct
          </Typography>
          <Typography variant="body1">
            {entity.fragmentationPct?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PageCount
          </Typography>
          <Typography variant="body1">
            {entity.pageCount}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>
    </Box>
  );
};


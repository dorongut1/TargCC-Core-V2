// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: UserApproval
//     Generated: 2025-12-17 21:49:15
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useUserApproval, useCreateUserApproval, useUpdateUserApproval } from '../../hooks/useUserApproval';
import type { CreateUserApprovalRequest, UpdateUserApprovalRequest } from '../../types/UserApproval.types';


export const UserApprovalForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const userApprovalId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!userApprovalId;

  const { data: existingUserApproval, isPending: isLoadingEntity } = useUserApproval(userApprovalId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateUserApprovalRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingUserApproval) {
      reset(existingUserApproval);
    }
  }, [existingUserApproval, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateUserApproval();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateUserApproval();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateUserApprovalRequest) => {
    if (isEdit && userApprovalId) {
      updateEntity(
        { id: userApprovalId, data: data as UpdateUserApprovalRequest },
        { onSuccess: () => navigate('/userApprovals') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/userApprovals') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameUserApproval')} />}
        label="TableNameUserApproval"
      />

      <TextField
        label="UserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('userID', { required: 'UserID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.userID}
        helperText={errors.userID?.message}
      />

      <TextField
        label="FunctionName"
        type="text"
        fullWidth
        margin="normal"
        {...register('functionName', { required: 'FunctionName is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.functionName}
        helperText={errors.functionName?.message}
      />

      <TextField
        label="EnoInteriorCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('enoInteriorCode', { required: 'EnoInteriorCode is required', maxLength: { value: 64, message: 'Maximum length is 64' } })}
        error={!!errors.enoInteriorCode}
        helperText={errors.enoInteriorCode?.message}
      />

      <TextField
        label="EnoAuthorizationCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('enoAuthorizationCode', { required: 'EnoAuthorizationCode is required', maxLength: { value: 64, message: 'Maximum length is 64' } })}
        error={!!errors.enoAuthorizationCode}
        helperText={errors.enoAuthorizationCode?.message}
      />

      <TextField
        label="EntDetails"
        type="text"
        fullWidth
        margin="normal"
        {...register('entDetails', {  })}
        error={!!errors.entDetails}
        helperText={errors.entDetails?.message}
      />

      <TextField
        label="ValidUntil"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('validUntil', { required: 'ValidUntil is required', maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.validUntil}
        helperText={errors.validUntil?.message}
      />

      <TextField
        label="WasUsed"
        fullWidth
        margin="normal"
        {...register('wasUsed', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.wasUsed}
        helperText={errors.wasUsed?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/userApprovals')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


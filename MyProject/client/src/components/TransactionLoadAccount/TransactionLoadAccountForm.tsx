// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: TransactionLoadAccount
//     Generated: 2025-12-18 08:11:23
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useTransactionLoadAccount, useCreateTransactionLoadAccount, useUpdateTransactionLoadAccount } from '../../hooks/useTransactionLoadAccount';
import type { CreateTransactionLoadAccountRequest, UpdateTransactionLoadAccountRequest } from '../../types/TransactionLoadAccount.types';


export const TransactionLoadAccountForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const transactionLoadAccountId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!transactionLoadAccountId;

  const { data: existingTransactionLoadAccount, isPending: isLoadingEntity } = useTransactionLoadAccount(transactionLoadAccountId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateTransactionLoadAccountRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingTransactionLoadAccount) {
      reset(existingTransactionLoadAccount);
    }
  }, [existingTransactionLoadAccount, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateTransactionLoadAccount();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateTransactionLoadAccount();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateTransactionLoadAccountRequest) => {
    if (isEdit && transactionLoadAccountId) {
      updateEntity(
        { id: transactionLoadAccountId, data: data as UpdateTransactionLoadAccountRequest },
        { onSuccess: () => navigate('/transactionLoadAccounts') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/transactionLoadAccounts') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameTransactionLoadAccount')} />}
        label="TableNameTransactionLoadAccount"
      />

      <TextField
        label="Account"
        type="text"
        fullWidth
        margin="normal"
        {...register('account', { required: 'Account is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.account}
        helperText={errors.account?.message}
      />

      <TextField
        label="EnmCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCurrency', { required: 'EnmCurrency is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.enmCurrency}
        helperText={errors.enmCurrency?.message}
      />

      <TextField
        label="Amount"
        type="number"
        fullWidth
        margin="normal"
        {...register('amount', { required: 'Amount is required', maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amount}
        helperText={errors.amount?.message}
      />

      <TextField
        label="BankReference"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankReference', { required: 'BankReference is required', maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.bankReference}
        helperText={errors.bankReference?.message}
      />

      <TextField
        label="CardIssuerRequestNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardIssuerRequestNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.cardIssuerRequestNumber}
        helperText={errors.cardIssuerRequestNumber?.message}
      />

      <TextField
        label="ResponseCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('responseCode', { required: 'ResponseCode is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.responseCode}
        helperText={errors.responseCode?.message}
      />

      <TextField
        label="ResponseText"
        type="text"
        fullWidth
        margin="normal"
        {...register('responseText', { required: 'ResponseText is required' })}
        error={!!errors.responseText}
        helperText={errors.responseText?.message}
      />

      <TextField
        label="LoggedAlertID"
        type="number"
        fullWidth
        margin="normal"
        {...register('loggedAlertID', { required: 'LoggedAlertID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.loggedAlertID}
        helperText={errors.loggedAlertID?.message}
      />

      <TextField
        label="WhenRequested"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenRequested', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenRequested}
        helperText={errors.whenRequested?.message}
      />

      <TextField
        label="WhenResponded"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenResponded', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenResponded}
        helperText={errors.whenResponded?.message}
      />

      <TextField
        label="WhenConfirmed"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenConfirmed', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenConfirmed}
        helperText={errors.whenConfirmed?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="EnmCardBrand"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCardBrand', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCardBrand}
        helperText={errors.enmCardBrand?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/transactionLoadAccounts')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_AlertMessage
//     Generated: 2025-12-17 08:54:21
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useAlertMessage, useCreateAlertMessage, useUpdateAlertMessage } from '../../hooks/useAlertMessage';
import type { CreateAlertMessageRequest, UpdateAlertMessageRequest } from '../../types/AlertMessage.types';


export const AlertMessageForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const alertMessageId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!alertMessageId;

  const { data: existingAlertMessage, isPending: isLoadingEntity } = useAlertMessage(alertMessageId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateAlertMessageRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingAlertMessage) {
      reset(existingAlertMessage);
    }
  }, [existingAlertMessage, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateAlertMessage();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateAlertMessage();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateAlertMessageRequest) => {
    if (isEdit && alertMessageId) {
      updateEntity(
        { id: alertMessageId, data: data as UpdateAlertMessageRequest },
        { onSuccess: () => navigate('/alertMessages') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/alertMessages') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCAlertMessage')} />}
        label="TableNameCAlertMessage"
      />

      <TextField
        label="Number"
        type="number"
        fullWidth
        margin="normal"
        {...register('number', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.number}
        helperText={errors.number?.message}
      />

      <TextField
        label="Description"
        type="text"
        fullWidth
        margin="normal"
        {...register('description', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.description}
        helperText={errors.description?.message}
      />

      <TextField
        label="EnmTypeFaultType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmTypeFaultType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmTypeFaultType}
        helperText={errors.enmTypeFaultType?.message}
      />

      <TextField
        label="EnmSeverityFaultSeverity"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmSeverityFaultSeverity', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmSeverityFaultSeverity}
        helperText={errors.enmSeverityFaultSeverity?.message}
      />

      <TextField
        label="LocMessage"
        type="text"
        fullWidth
        margin="normal"
        {...register('locMessage', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.locMessage}
        helperText={errors.locMessage?.message}
      />

      <TextField
        label="LocAction"
        type="text"
        fullWidth
        margin="normal"
        {...register('locAction', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.locAction}
        helperText={errors.locAction?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/alertMessages')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


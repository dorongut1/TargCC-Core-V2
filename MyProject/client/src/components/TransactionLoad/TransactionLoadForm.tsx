// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: TransactionLoad
//     Generated: 2025-12-17 21:11:17
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useTransactionLoad, useCreateTransactionLoad, useUpdateTransactionLoad } from '../../hooks/useTransactionLoad';
import type { CreateTransactionLoadRequest, UpdateTransactionLoadRequest } from '../../types/TransactionLoad.types';


export const TransactionLoadForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const transactionLoadId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!transactionLoadId;

  const { data: existingTransactionLoad, isPending: isLoadingEntity } = useTransactionLoad(transactionLoadId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateTransactionLoadRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingTransactionLoad) {
      reset(existingTransactionLoad);
    }
  }, [existingTransactionLoad, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateTransactionLoad();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateTransactionLoad();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateTransactionLoadRequest) => {
    if (isEdit && transactionLoadId) {
      updateEntity(
        { id: transactionLoadId, data: data as UpdateTransactionLoadRequest },
        { onSuccess: () => navigate('/transactionLoads') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/transactionLoads') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameTransactionLoad')} />}
        label="TableNameTransactionLoad"
      />

      <TextField
        label="UserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('userID', { required: 'UserID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.userID}
        helperText={errors.userID?.message}
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="EnmLoadType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLoadType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmLoadType}
        helperText={errors.enmLoadType?.message}
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="CardID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardID', { required: 'CardID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardID}
        helperText={errors.cardID?.message}
      />

      <TextField
        label="EnmCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCurrency', { required: 'EnmCurrency is required', maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.enmCurrency}
        helperText={errors.enmCurrency?.message}
      />

      <TextField
        label="Amount"
        type="number"
        fullWidth
        margin="normal"
        {...register('amount', { required: 'Amount is required', maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amount}
        helperText={errors.amount?.message}
      />

      <TextField
        label="WhenRequested"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenRequested', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenRequested}
        helperText={errors.whenRequested?.message}
      />

      <TextField
        label="ExchangeRateUsed"
        type="number"
        fullWidth
        margin="normal"
        {...register('exchangeRateUsed', { required: 'ExchangeRateUsed is required', maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.exchangeRateUsed}
        helperText={errors.exchangeRateUsed?.message}
      />

      <TextField
        label="AmountInILS"
        type="number"
        fullWidth
        margin="normal"
        {...register('amountInILS', { required: 'AmountInILS is required', maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amountInILS}
        helperText={errors.amountInILS?.message}
      />

      <TextField
        label="PaymentDebitResult"
        type="text"
        fullWidth
        margin="normal"
        {...register('paymentDebitResult', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.paymentDebitResult}
        helperText={errors.paymentDebitResult?.message}
      />

      <TextField
        label="PaymentDebitAuthorizationCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('paymentDebitAuthorizationCode', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.paymentDebitAuthorizationCode}
        helperText={errors.paymentDebitAuthorizationCode?.message}
      />

      <TextField
        label="PaymentDebitDetails"
        type="text"
        fullWidth
        margin="normal"
        {...register('paymentDebitDetails', { maxLength: { value: 2000, message: 'Maximum length is 2000' } })}
        error={!!errors.paymentDebitDetails}
        helperText={errors.paymentDebitDetails?.message}
      />

      <TextField
        label="DebitElapsedTimeSec"
        type="text"
        fullWidth
        margin="normal"
        {...register('debitElapsedTimeSec', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.debitElapsedTimeSec}
        helperText={errors.debitElapsedTimeSec?.message}
      />

      <TextField
        label="EnmLoadApprovedApprovalType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLoadApprovedApprovalType', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.enmLoadApprovedApprovalType}
        helperText={errors.enmLoadApprovedApprovalType?.message}
      />

      <TextField
        label="LoadResponseCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('loadResponseCode', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.loadResponseCode}
        helperText={errors.loadResponseCode?.message}
      />

      <TextField
        label="LoadResponse"
        type="text"
        fullWidth
        margin="normal"
        {...register('loadResponse', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.loadResponse}
        helperText={errors.loadResponse?.message}
      />

      <TextField
        label="LoadElapsedTimeSec"
        type="text"
        fullWidth
        margin="normal"
        {...register('loadElapsedTimeSec', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.loadElapsedTimeSec}
        helperText={errors.loadElapsedTimeSec?.message}
      />

      <TextField
        label="PaymentCreditResult"
        type="text"
        fullWidth
        margin="normal"
        {...register('paymentCreditResult', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.paymentCreditResult}
        helperText={errors.paymentCreditResult?.message}
      />

      <TextField
        label="PaymentCreditAuthorizationCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('paymentCreditAuthorizationCode', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.paymentCreditAuthorizationCode}
        helperText={errors.paymentCreditAuthorizationCode?.message}
      />

      <TextField
        label="PaymentCreditDetails"
        type="text"
        fullWidth
        margin="normal"
        {...register('paymentCreditDetails', { maxLength: { value: 2000, message: 'Maximum length is 2000' } })}
        error={!!errors.paymentCreditDetails}
        helperText={errors.paymentCreditDetails?.message}
      />

      <TextField
        label="CreditElapsedTimeSec"
        type="text"
        fullWidth
        margin="normal"
        {...register('creditElapsedTimeSec', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.creditElapsedTimeSec}
        helperText={errors.creditElapsedTimeSec?.message}
      />

      <TextField
        label="LoggedAlertID"
        type="number"
        fullWidth
        margin="normal"
        {...register('loggedAlertID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.loggedAlertID}
        helperText={errors.loggedAlertID?.message}
      />

      <TextField
        label="FaultNo"
        type="number"
        fullWidth
        margin="normal"
        {...register('faultNo', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.faultNo}
        helperText={errors.faultNo?.message}
      />

      <TextField
        label="EnmLoadResult"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLoadResult', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmLoadResult}
        helperText={errors.enmLoadResult?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="EnmCoveredBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCoveredBy', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCoveredBy}
        helperText={errors.enmCoveredBy?.message}
      />

      <TextField
        label="Details"
        type="text"
        fullWidth
        margin="normal"
        {...register('details', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.details}
        helperText={errors.details?.message}
      />

      <TextField
        label="CustomerReference"
        type="text"
        fullWidth
        margin="normal"
        {...register('customerReference', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.customerReference}
        helperText={errors.customerReference?.message}
      />

      <TextField
        label="RepresentativeExchangeRate"
        type="number"
        fullWidth
        margin="normal"
        {...register('representativeExchangeRate', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.representativeExchangeRate}
        helperText={errors.representativeExchangeRate?.message}
      />

      <TextField
        label="RequestedTransactionID"
        type="number"
        fullWidth
        margin="normal"
        {...register('requestedTransactionID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.requestedTransactionID}
        helperText={errors.requestedTransactionID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/transactionLoads')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


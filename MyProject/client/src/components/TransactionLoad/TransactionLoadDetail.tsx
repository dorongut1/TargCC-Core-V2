// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: TransactionLoad
//     Generated: 2025-12-15 22:18:53
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useTransactionLoad, useDeleteTransactionLoad } from '../../hooks/useTransactionLoad';
import { useTransactionLoadBackofficeTransactions } from '../../hooks/useTransactionLoad';
import { useTransactionLoadCardActivations } from '../../hooks/useTransactionLoad';
import { useTransactionLoadImportTransactionLoadRows } from '../../hooks/useTransactionLoad';


export const TransactionLoadDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useTransactionLoad(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteTransactionLoad();
  const { data: backofficeTransactions, isLoading: backofficeTransactionsLoading } = useTransactionLoadBackofficeTransactions(id ? parseInt(id, 10) : null);
  const { data: cardActivations, isLoading: cardActivationsLoading } = useTransactionLoadCardActivations(id ? parseInt(id, 10) : null);
  const { data: importTransactionLoadRows, isLoading: importTransactionLoadRowsLoading } = useTransactionLoadImportTransactionLoadRows(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/transactionLoads'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load transactionLoad</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/transactionLoads')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/transactionLoads/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            TransactionLoad Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameTransactionLoad
          </Typography>
          <Typography variant="body1">
            {entity.tableNameTransactionLoad ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UserID
          </Typography>
          <Typography variant="body1">
            {entity.userID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DistributorID
          </Typography>
          <Typography variant="body1">
            {entity.distributorID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmLoadType
          </Typography>
          <Typography variant="body1">
            {entity.enmLoadType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerID
          </Typography>
          <Typography variant="body1">
            {entity.customerID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardID
          </Typography>
          <Typography variant="body1">
            {entity.cardID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmCurrency
          </Typography>
          <Typography variant="body1">
            {entity.enmCurrency}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Amount
          </Typography>
          <Typography variant="body1">
            {entity.amount?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenRequested
          </Typography>
          <Typography variant="body1">
            {(entity.whenRequested ? new Date(entity.whenRequested).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ExchangeRateUsed
          </Typography>
          <Typography variant="body1">
            {entity.exchangeRateUsed?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AmountInILS
          </Typography>
          <Typography variant="body1">
            {entity.amountInILS?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentDebitResult
          </Typography>
          <Typography variant="body1">
            {entity.paymentDebitResult}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentDebitAuthorizationCode
          </Typography>
          <Typography variant="body1">
            {entity.paymentDebitAuthorizationCode}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentDebitDetails
          </Typography>
          <Typography variant="body1">
            {entity.paymentDebitDetails}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DebitElapsedTimeSec
          </Typography>
          <Typography variant="body1">
            {entity.debitElapsedTimeSec}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmLoadApprovedApprovalType
          </Typography>
          <Typography variant="body1">
            {entity.enmLoadApprovedApprovalType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoadResponseCode
          </Typography>
          <Typography variant="body1">
            {entity.loadResponseCode}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoadResponse
          </Typography>
          <Typography variant="body1">
            {entity.loadResponse}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoadElapsedTimeSec
          </Typography>
          <Typography variant="body1">
            {entity.loadElapsedTimeSec}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentCreditResult
          </Typography>
          <Typography variant="body1">
            {entity.paymentCreditResult}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentCreditAuthorizationCode
          </Typography>
          <Typography variant="body1">
            {entity.paymentCreditAuthorizationCode}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentCreditDetails
          </Typography>
          <Typography variant="body1">
            {entity.paymentCreditDetails}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CreditElapsedTimeSec
          </Typography>
          <Typography variant="body1">
            {entity.creditElapsedTimeSec}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoggedAlertID
          </Typography>
          <Typography variant="body1">
            {entity.loggedAlertID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            FaultNo
          </Typography>
          <Typography variant="body1">
            {entity.faultNo}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmLoadResult
          </Typography>
          <Typography variant="body1">
            {entity.enmLoadResult}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? new Date(entity.deletedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Month
          </Typography>
          <Typography variant="body1">
            {(entity.month ? new Date(entity.month).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmCoveredBy
          </Typography>
          <Typography variant="body1">
            {entity.enmCoveredBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Details
          </Typography>
          <Typography variant="body1">
            {entity.details}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerReference
          </Typography>
          <Typography variant="body1">
            {entity.customerReference}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RepresentativeExchangeRate
          </Typography>
          <Typography variant="body1">
            {entity.representativeExchangeRate?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Day
          </Typography>
          <Typography variant="body1">
            {(entity.day ? new Date(entity.day).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RequestedTransactionID
          </Typography>
          <Typography variant="body1">
            {entity.requestedTransactionID}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            BackofficeTransactions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={backofficeTransactions || []}
              columns={[
                { field: 'tableNameBackofficeTransaction', headerName: 'TableNameBackofficeTransaction', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'distributorID', headerName: 'DistributorID', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'transactionLoadID', headerName: 'TransactionLoadID', width: 100 },
              ]}
              loading={backofficeTransactionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            CardActivations
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={cardActivations || []}
              columns={[
                { field: 'tableNameCardActivation', headerName: 'TableNameCardActivation', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'distributorID', headerName: 'DistributorID', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'cardID', headerName: 'CardID', width: 100 },
              ]}
              loading={cardActivationsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            ImportTransactionLoadRows
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={importTransactionLoadRows || []}
              columns={[
                { field: 'tableNameImportTransactionLoadRow', headerName: 'TableNameImportTransactionLoadRow', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'importTransactionLoadID', headerName: 'ImportTransactionLoadID', width: 100 },
                { field: 'customerPhoneNumber', headerName: 'CustomerPhoneNumber', width: 150 },
                { field: 'enmCurrency', headerName: 'EnmCurrency', width: 150 },
              ]}
              loading={importTransactionLoadRowsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


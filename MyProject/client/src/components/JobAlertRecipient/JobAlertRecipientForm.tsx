// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_JobAlertRecipient
//     Generated: 2025-12-17 21:32:11
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useJobAlertRecipient, useCreateJobAlertRecipient, useUpdateJobAlertRecipient } from '../../hooks/useJobAlertRecipient';
import type { CreateJobAlertRecipientRequest, UpdateJobAlertRecipientRequest } from '../../types/JobAlertRecipient.types';


export const JobAlertRecipientForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const jobAlertRecipientId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!jobAlertRecipientId;

  const { data: existingJobAlertRecipient, isPending: isLoadingEntity } = useJobAlertRecipient(jobAlertRecipientId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateJobAlertRecipientRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingJobAlertRecipient) {
      reset(existingJobAlertRecipient);
    }
  }, [existingJobAlertRecipient, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateJobAlertRecipient();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateJobAlertRecipient();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateJobAlertRecipientRequest) => {
    if (isEdit && jobAlertRecipientId) {
      updateEntity(
        { id: jobAlertRecipientId, data: data as UpdateJobAlertRecipientRequest },
        { onSuccess: () => navigate('/jobAlertRecipients') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/jobAlertRecipients') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCJobAlertRecipient')} />}
        label="TableNameCJobAlertRecipient"
      />

      <TextField
        label="CJobID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cJobID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cJobID}
        helperText={errors.cJobID?.message}
      />

      <TextField
        label="CUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cUserID}
        helperText={errors.cUserID?.message}
      />

      <TextField
        label="EnmJobAlertType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmJobAlertType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmJobAlertType}
        helperText={errors.enmJobAlertType?.message}
      />

      <TextField
        label="OverrideName"
        type="text"
        fullWidth
        margin="normal"
        {...register('overrideName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.overrideName}
        helperText={errors.overrideName?.message}
      />

      <TextField
        label="OverrideEmailOrPhone"
        type="text"
        fullWidth
        margin="normal"
        {...register('overrideEmailOrPhone', { maxLength: { value: 100, message: 'Maximum length is 100' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.overrideEmailOrPhone}
        helperText={errors.overrideEmailOrPhone?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/jobAlertRecipients')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


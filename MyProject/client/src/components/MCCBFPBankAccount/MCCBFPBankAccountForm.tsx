// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: MCCBFPBankAccount
//     Generated: 2025-12-17 21:11:16
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useMCCBFPBankAccount, useCreateMCCBFPBankAccount, useUpdateMCCBFPBankAccount } from '../../hooks/useMCCBFPBankAccount';
import type { CreateMCCBFPBankAccountRequest, UpdateMCCBFPBankAccountRequest } from '../../types/MCCBFPBankAccount.types';


export const MCCBFPBankAccountForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mCCBFPBankAccountId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mCCBFPBankAccountId;

  const { data: existingMCCBFPBankAccount, isPending: isLoadingEntity } = useMCCBFPBankAccount(mCCBFPBankAccountId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMCCBFPBankAccountRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMCCBFPBankAccount) {
      reset(existingMCCBFPBankAccount);
    }
  }, [existingMCCBFPBankAccount, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMCCBFPBankAccount();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMCCBFPBankAccount();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMCCBFPBankAccountRequest) => {
    if (isEdit && mCCBFPBankAccountId) {
      updateEntity(
        { id: mCCBFPBankAccountId, data: data as UpdateMCCBFPBankAccountRequest },
        { onSuccess: () => navigate('/mCCBFPBankAccounts') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mCCBFPBankAccounts') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameMCCBFPBankAccount')} />}
        label="TableNameMCCBFPBankAccount"
      />

      <TextField
        label="Region"
        type="text"
        fullWidth
        margin="normal"
        {...register('region', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.region}
        helperText={errors.region?.message}
      />

      <TextField
        label="Country"
        type="text"
        fullWidth
        margin="normal"
        {...register('country', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.country}
        helperText={errors.country?.message}
      />

      <TextField
        label="PayoutCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('payoutCurrency', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.payoutCurrency}
        helperText={errors.payoutCurrency?.message}
      />

      <TextField
        label="ReachOfChannel"
        type="text"
        fullWidth
        margin="normal"
        {...register('reachOfChannel', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.reachOfChannel}
        helperText={errors.reachOfChannel?.message}
      />

      <TextField
        label="Corridors"
        type="text"
        fullWidth
        margin="normal"
        {...register('corridors', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.corridors}
        helperText={errors.corridors?.message}
      />

      <TextField
        label="MinimumTransactionAmount"
        type="text"
        fullWidth
        margin="normal"
        {...register('minimumTransactionAmount', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.minimumTransactionAmount}
        helperText={errors.minimumTransactionAmount?.message}
      />

      <TextField
        label="MaximumTransactionAmount"
        type="text"
        fullWidth
        margin="normal"
        {...register('maximumTransactionAmount', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.maximumTransactionAmount}
        helperText={errors.maximumTransactionAmount?.message}
      />

      <TextField
        label="DeliveryTime"
        type="text"
        fullWidth
        margin="normal"
        {...register('deliveryTime', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.deliveryTime}
        helperText={errors.deliveryTime?.message}
      />

      <TextField
        label="CutoffTimeForDelivery"
        type="text"
        fullWidth
        margin="normal"
        {...register('cutoffTimeForDelivery', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.cutoffTimeForDelivery}
        helperText={errors.cutoffTimeForDelivery?.message}
      />

      <TextField
        label="SpecialNotes"
        type="text"
        fullWidth
        margin="normal"
        {...register('specialNotes', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.specialNotes}
        helperText={errors.specialNotes?.message}
      />

      <TextField
        label="DocumentationRequirements"
        type="text"
        fullWidth
        margin="normal"
        {...register('documentationRequirements', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.documentationRequirements}
        helperText={errors.documentationRequirements?.message}
      />

      <TextField
        label="DocumentationCollectionProcess"
        type="text"
        fullWidth
        margin="normal"
        {...register('documentationCollectionProcess', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.documentationCollectionProcess}
        helperText={errors.documentationCollectionProcess?.message}
      />

      <TextField
        label="ThresholdForDocumentationRequirements"
        type="text"
        fullWidth
        margin="normal"
        {...register('thresholdForDocumentationRequirements', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.thresholdForDocumentationRequirements}
        helperText={errors.thresholdForDocumentationRequirements?.message}
      />

      <TextField
        label="RegistrationRequirements"
        type="text"
        fullWidth
        margin="normal"
        {...register('registrationRequirements', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.registrationRequirements}
        helperText={errors.registrationRequirements?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mCCBFPBankAccounts')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


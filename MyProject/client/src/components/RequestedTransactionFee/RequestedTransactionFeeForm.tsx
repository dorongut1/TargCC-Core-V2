// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: RequestedTransactionFee
//     Generated: 2025-12-18 07:42:15
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useRequestedTransactionFee, useCreateRequestedTransactionFee, useUpdateRequestedTransactionFee } from '../../hooks/useRequestedTransactionFee';
import type { CreateRequestedTransactionFeeRequest, UpdateRequestedTransactionFeeRequest } from '../../types/RequestedTransactionFee.types';


export const RequestedTransactionFeeForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const requestedTransactionFeeId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!requestedTransactionFeeId;

  const { data: existingRequestedTransactionFee, isPending: isLoadingEntity } = useRequestedTransactionFee(requestedTransactionFeeId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateRequestedTransactionFeeRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingRequestedTransactionFee) {
      reset(existingRequestedTransactionFee);
    }
  }, [existingRequestedTransactionFee, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateRequestedTransactionFee();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateRequestedTransactionFee();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateRequestedTransactionFeeRequest) => {
    if (isEdit && requestedTransactionFeeId) {
      updateEntity(
        { id: requestedTransactionFeeId, data: data as UpdateRequestedTransactionFeeRequest },
        { onSuccess: () => navigate('/requestedTransactionFees') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/requestedTransactionFees') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameRequestedTransactionFee')} />}
        label="TableNameRequestedTransactionFee"
      />

      <TextField
        label="RequestedTransactionID"
        type="number"
        fullWidth
        margin="normal"
        {...register('requestedTransactionID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.requestedTransactionID}
        helperText={errors.requestedTransactionID?.message}
      />

      <TextField
        label="EnmTypeFeeType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmTypeFeeType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmTypeFeeType}
        helperText={errors.enmTypeFeeType?.message}
      />

      <TextField
        label="EnmPayer"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmPayer', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmPayer}
        helperText={errors.enmPayer?.message}
      />

      <TextField
        label="AmountToBaseFeeOn"
        type="number"
        fullWidth
        margin="normal"
        {...register('amountToBaseFeeOn', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amountToBaseFeeOn}
        helperText={errors.amountToBaseFeeOn?.message}
      />

      <TextField
        label="FeeDefinitionAmt"
        type="number"
        fullWidth
        margin="normal"
        {...register('feeDefinitionAmt', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.feeDefinitionAmt}
        helperText={errors.feeDefinitionAmt?.message}
      />

      <TextField
        label="FeeDefinitionPct"
        type="number"
        fullWidth
        margin="normal"
        {...register('feeDefinitionPct', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.feeDefinitionPct}
        helperText={errors.feeDefinitionPct?.message}
      />

      <TextField
        label="FeeDefinitionMax"
        type="number"
        fullWidth
        margin="normal"
        {...register('feeDefinitionMax', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.feeDefinitionMax}
        helperText={errors.feeDefinitionMax?.message}
      />

      <TextField
        label="TotalFee"
        type="number"
        fullWidth
        margin="normal"
        {...register('totalFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.totalFee}
        helperText={errors.totalFee?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="CustomerWalletID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerWalletID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerWalletID}
        helperText={errors.customerWalletID?.message}
      />

      <TextField
        label="FeeDefinitionMin"
        type="number"
        fullWidth
        margin="normal"
        {...register('feeDefinitionMin', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.feeDefinitionMin}
        helperText={errors.feeDefinitionMin?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/requestedTransactionFees')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


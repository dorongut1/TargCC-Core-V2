// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Job
//     Generated: 2025-12-18 07:30:14
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useJob, useDeleteJob } from '../../hooks/useJob';
import { useJobJobAlertRecipients } from '../../hooks/useJob';
import { useJobLoggedJobs } from '../../hooks/useJob';


export const JobDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useJob(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteJob();
  const { data: jobAlertRecipients, isLoading: jobAlertRecipientsLoading } = useJobJobAlertRecipients(id ? parseInt(id, 10) : null);
  const { data: loggedJobs, isLoading: loggedJobsLoading } = useJobLoggedJobs(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/jobs'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load job</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/jobs')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/jobs/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            Job Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCJob
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCJob ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpJob
          </Typography>
          <Typography variant="body1">
            {entity.lkpJob}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpJobRunner
          </Typography>
          <Typography variant="body1">
            {entity.lkpJobRunner}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Description
          </Typography>
          <Typography variant="body1">
            {entity.description}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Instructions
          </Typography>
          <Typography variant="body1">
            {entity.instructions}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmJobType
          </Typography>
          <Typography variant="body1">
            {entity.enmJobType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenToRun
          </Typography>
          <Typography variant="body1">
            {(entity.whenToRun ? new Date(entity.whenToRun).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CyclicCount
          </Typography>
          <Typography variant="body1">
            {entity.cyclicCount}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SendNotificationOnSuccess
          </Typography>
          <Typography variant="body1">
            {entity.sendNotificationOnSuccess ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SendAlarmOnMissed
          </Typography>
          <Typography variant="body1">
            {entity.sendAlarmOnMissed ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TimeOutSec
          </Typography>
          <Typography variant="body1">
            {entity.timeOutSec}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Active
          </Typography>
          <Typography variant="body1">
            {entity.active ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ActivatingUser
          </Typography>
          <Typography variant="body1">
            {entity.activatingUser}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            NextRunTime
          </Typography>
          <Typography variant="body1">
            {(entity.nextRunTime ? new Date(entity.nextRunTime).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LastRunTime
          </Typography>
          <Typography variant="body1">
            {(entity.lastRunTime ? new Date(entity.lastRunTime).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmJobStatus
          </Typography>
          <Typography variant="body1">
            {entity.enmJobStatus}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WarningMailSent
          </Typography>
          <Typography variant="body1">
            {entity.warningMailSent ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsManaged
          </Typography>
          <Typography variant="body1">
            {entity.isManaged ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LastRunBy
          </Typography>
          <Typography variant="body1">
            {entity.lastRunBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? new Date(entity.deletedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            JobAlertRecipients
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={jobAlertRecipients || []}
              columns={[
                { field: 'tableNameCJobAlertRecipient', headerName: 'TableNameCJobAlertRecipient', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'cJobID', headerName: 'CJobID', width: 100 },
                { field: 'cUserID', headerName: 'CUserID', width: 100 },
                { field: 'enmJobAlertType', headerName: 'EnmJobAlertType', width: 150 },
              ]}
              loading={jobAlertRecipientsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            LoggedJobs
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={loggedJobs || []}
              columns={[
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'cJobID', headerName: 'CJobID', width: 100 },
                { field: 'whenStarted', headerName: 'WhenStarted', width: 150 },
                { field: 'activatingUser', headerName: 'ActivatingUser', width: 150 },
                { field: 'lastRunBy', headerName: 'LastRunBy', width: 150 },
              ]}
              loading={loggedJobsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Job
//     Generated: 2025-12-15 13:48:29
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useJob, useCreateJob, useUpdateJob } from '../../hooks/useJob';
import type { CreateJobRequest, UpdateJobRequest } from '../../types/Job.types';


export const JobForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const jobId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!jobId;

  const { data: existingJob, isPending: isLoadingEntity } = useJob(jobId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateJobRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingJob) {
      reset(existingJob);
    }
  }, [existingJob, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateJob();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateJob();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateJobRequest) => {
    if (isEdit && jobId) {
      updateEntity(
        { id: jobId, data: data as UpdateJobRequest },
        { onSuccess: () => navigate('/jobs') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/jobs') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCJob')} />}
        label="TableNameCJob"
      />

      <TextField
        label="LkpJob"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpJob', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpJob}
        helperText={errors.lkpJob?.message}
      />

      <TextField
        label="LkpJobRunner"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpJobRunner', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpJobRunner}
        helperText={errors.lkpJobRunner?.message}
      />

      <TextField
        label="Description"
        type="text"
        fullWidth
        margin="normal"
        {...register('description', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.description}
        helperText={errors.description?.message}
      />

      <TextField
        label="Instructions"
        type="text"
        fullWidth
        margin="normal"
        {...register('instructions', { maxLength: { value: 2000, message: 'Maximum length is 2000' } })}
        error={!!errors.instructions}
        helperText={errors.instructions?.message}
      />

      <TextField
        label="EnmJobType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmJobType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmJobType}
        helperText={errors.enmJobType?.message}
      />

      <TextField
        label="WhenToRun"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenToRun', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.whenToRun}
        helperText={errors.whenToRun?.message}
      />

      <TextField
        label="CyclicCount"
        type="number"
        fullWidth
        margin="normal"
        {...register('cyclicCount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.cyclicCount}
        helperText={errors.cyclicCount?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('sendNotificationOnSuccess')} />}
        label="SendNotificationOnSuccess"
      />

      <FormControlLabel
        control={<Checkbox {...register('sendAlarmOnMissed')} />}
        label="SendAlarmOnMissed"
      />

      <TextField
        label="TimeOutSec"
        type="number"
        fullWidth
        margin="normal"
        {...register('timeOutSec', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.timeOutSec}
        helperText={errors.timeOutSec?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('active')} />}
        label="Active"
      />

      <TextField
        label="ActivatingUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('activatingUser', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.activatingUser}
        helperText={errors.activatingUser?.message}
      />

      <TextField
        label="NextRunTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('nextRunTime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.nextRunTime}
        helperText={errors.nextRunTime?.message}
      />

      <TextField
        label="LastRunTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('lastRunTime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lastRunTime}
        helperText={errors.lastRunTime?.message}
      />

      <TextField
        label="EnmJobStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmJobStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmJobStatus}
        helperText={errors.enmJobStatus?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('warningMailSent')} />}
        label="WarningMailSent"
      />

      <FormControlLabel
        control={<Checkbox {...register('isManaged')} />}
        label="IsManaged"
      />

      <TextField
        label="LastRunBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastRunBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lastRunBy}
        helperText={errors.lastRunBy?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/jobs')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: ImportTransactionLoadRow
//     Generated: 2025-12-17 21:20:41
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useImportTransactionLoadRow, useCreateImportTransactionLoadRow, useUpdateImportTransactionLoadRow } from '../../hooks/useImportTransactionLoadRow';
import type { CreateImportTransactionLoadRowRequest, UpdateImportTransactionLoadRowRequest } from '../../types/ImportTransactionLoadRow.types';


export const ImportTransactionLoadRowForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const importTransactionLoadRowId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!importTransactionLoadRowId;

  const { data: existingImportTransactionLoadRow, isPending: isLoadingEntity } = useImportTransactionLoadRow(importTransactionLoadRowId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateImportTransactionLoadRowRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingImportTransactionLoadRow) {
      reset(existingImportTransactionLoadRow);
    }
  }, [existingImportTransactionLoadRow, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateImportTransactionLoadRow();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateImportTransactionLoadRow();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateImportTransactionLoadRowRequest) => {
    if (isEdit && importTransactionLoadRowId) {
      updateEntity(
        { id: importTransactionLoadRowId, data: data as UpdateImportTransactionLoadRowRequest },
        { onSuccess: () => navigate('/importTransactionLoadRows') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/importTransactionLoadRows') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameImportTransactionLoadRow')} />}
        label="TableNameImportTransactionLoadRow"
      />

      <TextField
        label="ImportTransactionLoadID"
        type="number"
        fullWidth
        margin="normal"
        {...register('importTransactionLoadID', { required: 'ImportTransactionLoadID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.importTransactionLoadID}
        helperText={errors.importTransactionLoadID?.message}
      />

      <TextField
        label="CustomerPhoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('customerPhoneNumber', { required: 'CustomerPhoneNumber is required', maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.customerPhoneNumber}
        helperText={errors.customerPhoneNumber?.message}
      />

      <TextField
        label="EnmCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCurrency', { required: 'EnmCurrency is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.enmCurrency}
        helperText={errors.enmCurrency?.message}
      />

      <TextField
        label="Amount"
        type="number"
        fullWidth
        margin="normal"
        {...register('amount', { required: 'Amount is required', maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amount}
        helperText={errors.amount?.message}
      />

      <TextField
        label="CardCid"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardCid', { required: 'CardCid is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.cardCid}
        helperText={errors.cardCid?.message}
      />

      <TextField
        label="CardLast4Numbers"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardLast4Numbers', { required: 'CardLast4Numbers is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.cardLast4Numbers}
        helperText={errors.cardLast4Numbers?.message}
      />

      <TextField
        label="LoadReference"
        type="text"
        fullWidth
        margin="normal"
        {...register('loadReference', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.loadReference}
        helperText={errors.loadReference?.message}
      />

      <TextField
        label="CustomerFirstName"
        type="text"
        fullWidth
        margin="normal"
        {...register('customerFirstName', { required: 'CustomerFirstName is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.customerFirstName}
        helperText={errors.customerFirstName?.message}
      />

      <TextField
        label="CustomerLastName"
        type="text"
        fullWidth
        margin="normal"
        {...register('customerLastName', { required: 'CustomerLastName is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.customerLastName}
        helperText={errors.customerLastName?.message}
      />

      <TextField
        label="CustomerIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('customerIdentifier', { required: 'CustomerIdentifier is required', maxLength: { value: 104, message: 'Maximum length is 104' } })}
        error={!!errors.customerIdentifier}
        helperText={errors.customerIdentifier?.message}
      />

      <TextField
        label="LgCustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('lgCustomerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lgCustomerID}
        helperText={errors.lgCustomerID?.message}
      />

      <TextField
        label="LgCardID"
        type="number"
        fullWidth
        margin="normal"
        {...register('lgCardID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lgCardID}
        helperText={errors.lgCardID?.message}
      />

      <TextField
        label="WhenInitialized"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenInitialized', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenInitialized}
        helperText={errors.whenInitialized?.message}
      />

      <TextField
        label="TransactionLoadID"
        type="number"
        fullWidth
        margin="normal"
        {...register('transactionLoadID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.transactionLoadID}
        helperText={errors.transactionLoadID?.message}
      />

      <TextField
        label="WhenCompleted"
        fullWidth
        margin="normal"
        {...register('whenCompleted', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenCompleted}
        helperText={errors.whenCompleted?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isSuccessful')} />}
        label="IsSuccessful"
      />

      <TextField
        label="LgLoggedAlertID"
        type="number"
        fullWidth
        margin="normal"
        {...register('lgLoggedAlertID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lgLoggedAlertID}
        helperText={errors.lgLoggedAlertID?.message}
      />

      <TextField
        label="Comments"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 4000, message: 'Maximum length is 4000' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/importTransactionLoadRows')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


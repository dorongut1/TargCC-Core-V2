// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_ObjectToTranslate
//     Generated: 2025-12-15 20:38:12
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useObjectToTranslate, useDeleteObjectToTranslate } from '../../hooks/useObjectToTranslate';
import { useObjectToTranslateObjectTranslations } from '../../hooks/useObjectToTranslate';


export const ObjectToTranslateDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useObjectToTranslate(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteObjectToTranslate();
  const { data: objectTranslations, isLoading: objectTranslationsLoading } = useObjectToTranslateObjectTranslations(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/objectToTranslates'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load objectToTranslate</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/objectToTranslates')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/objectToTranslates/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            ObjectToTranslate Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCObjectToTranslate
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCObjectToTranslate ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmObjectType
          </Typography>
          <Typography variant="body1">
            {entity.enmObjectType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Object
          </Typography>
          <Typography variant="body1">
            {entity.object}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Item
          </Typography>
          <Typography variant="body1">
            {entity.item}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? new Date(entity.deletedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            ObjectTranslations
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={objectTranslations || []}
              columns={[
                { field: 'tableNameCObjectTranslation', headerName: 'TableNameCObjectTranslation', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'cObjectToTranslateID', headerName: 'CObjectToTranslateID', width: 100 },
                { field: 'instance', headerName: 'Instance', width: 100 },
                { field: 'defaultText', headerName: 'DefaultText', width: 150 },
              ]}
              loading={objectTranslationsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


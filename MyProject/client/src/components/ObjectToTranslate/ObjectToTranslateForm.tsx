// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_ObjectToTranslate
//     Generated: 2025-12-18 07:30:14
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useObjectToTranslate, useCreateObjectToTranslate, useUpdateObjectToTranslate } from '../../hooks/useObjectToTranslate';
import type { CreateObjectToTranslateRequest, UpdateObjectToTranslateRequest } from '../../types/ObjectToTranslate.types';


export const ObjectToTranslateForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const objectToTranslateId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!objectToTranslateId;

  const { data: existingObjectToTranslate, isPending: isLoadingEntity } = useObjectToTranslate(objectToTranslateId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateObjectToTranslateRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingObjectToTranslate) {
      reset(existingObjectToTranslate);
    }
  }, [existingObjectToTranslate, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateObjectToTranslate();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateObjectToTranslate();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateObjectToTranslateRequest) => {
    if (isEdit && objectToTranslateId) {
      updateEntity(
        { id: objectToTranslateId, data: data as UpdateObjectToTranslateRequest },
        { onSuccess: () => navigate('/objectToTranslates') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/objectToTranslates') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCObjectToTranslate')} />}
        label="TableNameCObjectToTranslate"
      />

      <TextField
        label="EnmObjectType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmObjectType', { required: 'EnmObjectType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmObjectType}
        helperText={errors.enmObjectType?.message}
      />

      <TextField
        label="Object"
        type="text"
        fullWidth
        margin="normal"
        {...register('object', { required: 'Object is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.object}
        helperText={errors.object?.message}
      />

      <TextField
        label="Item"
        type="text"
        fullWidth
        margin="normal"
        {...register('item', { maxLength: { value: 510, message: 'Maximum length is 510' } })}
        error={!!errors.item}
        helperText={errors.item?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/objectToTranslates')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: NewCardRequestAPI
//     Generated: 2025-12-17 21:32:15
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useNewCardRequestAPI, useCreateNewCardRequestAPI, useUpdateNewCardRequestAPI } from '../../hooks/useNewCardRequestAPI';
import type { CreateNewCardRequestAPIRequest, UpdateNewCardRequestAPIRequest } from '../../types/NewCardRequestAPI.types';


export const NewCardRequestAPIForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const newCardRequestAPIId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!newCardRequestAPIId;

  const { data: existingNewCardRequestAPI, isPending: isLoadingEntity } = useNewCardRequestAPI(newCardRequestAPIId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateNewCardRequestAPIRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingNewCardRequestAPI) {
      reset(existingNewCardRequestAPI);
    }
  }, [existingNewCardRequestAPI, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateNewCardRequestAPI();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateNewCardRequestAPI();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateNewCardRequestAPIRequest) => {
    if (isEdit && newCardRequestAPIId) {
      updateEntity(
        { id: newCardRequestAPIId, data: data as UpdateNewCardRequestAPIRequest },
        { onSuccess: () => navigate('/newCardRequestAPIs') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/newCardRequestAPIs') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameNewCardRequestAPI')} />}
        label="TableNameNewCardRequestAPI"
      />

      <TextField
        label="EnmRequestedCurrencyCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmRequestedCurrencyCurrency', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmRequestedCurrencyCurrency}
        helperText={errors.enmRequestedCurrencyCurrency?.message}
      />

      <TextField
        label="FirstName"
        type="text"
        fullWidth
        margin="normal"
        {...register('firstName', { required: 'FirstName is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.firstName}
        helperText={errors.firstName?.message}
      />

      <TextField
        label="LastName"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastName', { required: 'LastName is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lastName}
        helperText={errors.lastName?.message}
      />

      <TextField
        label="FirstNameLatin"
        type="text"
        fullWidth
        margin="normal"
        {...register('firstNameLatin', { required: 'FirstNameLatin is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.firstNameLatin}
        helperText={errors.firstNameLatin?.message}
      />

      <TextField
        label="LastNameLatin"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastNameLatin', { required: 'LastNameLatin is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lastNameLatin}
        helperText={errors.lastNameLatin?.message}
      />

      <TextField
        label="Email"
        type="text"
        fullWidth
        margin="normal"
        {...register('email', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.email}
        helperText={errors.email?.message}
      />

      <TextField
        label="BirthDate"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('birthDate', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.birthDate}
        helperText={errors.birthDate?.message}
      />

      <TextField
        label="IsraelIDNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('israelIDNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.israelIDNumber}
        helperText={errors.israelIDNumber?.message}
      />

      <TextField
        label="LkpPassportCountryCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpPassportCountryCountry', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.lkpPassportCountryCountry}
        helperText={errors.lkpPassportCountryCountry?.message}
      />

      <TextField
        label="PassportNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('passportNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.passportNumber}
        helperText={errors.passportNumber?.message}
      />

      <TextField
        label="DateIssued"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('dateIssued', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.dateIssued}
        helperText={errors.dateIssued?.message}
      />

      <TextField
        label="ExpiryDate"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('expiryDate', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.expiryDate}
        helperText={errors.expiryDate?.message}
      />

      <TextField
        label="CellphoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('cellphoneNumber', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.cellphoneNumber}
        helperText={errors.cellphoneNumber?.message}
      />

      <TextField
        label="HomePhoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('homePhoneNumber', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.homePhoneNumber}
        helperText={errors.homePhoneNumber?.message}
      />

      <TextField
        label="ActualAddressNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualAddressNumber', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.actualAddressNumber}
        helperText={errors.actualAddressNumber?.message}
      />

      <TextField
        label="ActualAddressStreet"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualAddressStreet', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.actualAddressStreet}
        helperText={errors.actualAddressStreet?.message}
      />

      <TextField
        label="ActualAddressCity"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualAddressCity', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.actualAddressCity}
        helperText={errors.actualAddressCity?.message}
      />

      <TextField
        label="ActualZipCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualZipCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.actualZipCode}
        helperText={errors.actualZipCode?.message}
      />

      <TextField
        label="MailAddressNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('mailAddressNumber', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.mailAddressNumber}
        helperText={errors.mailAddressNumber?.message}
      />

      <TextField
        label="MailAddressStreet"
        type="text"
        fullWidth
        margin="normal"
        {...register('mailAddressStreet', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.mailAddressStreet}
        helperText={errors.mailAddressStreet?.message}
      />

      <TextField
        label="MailAddressCity"
        type="text"
        fullWidth
        margin="normal"
        {...register('mailAddressCity', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.mailAddressCity}
        helperText={errors.mailAddressCity?.message}
      />

      <TextField
        label="MailZipCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('mailZipCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.mailZipCode}
        helperText={errors.mailZipCode?.message}
      />

      <TextField
        label="EnmGender"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmGender', { required: 'EnmGender is required', maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.enmGender}
        helperText={errors.enmGender?.message}
      />

      <TextField
        label="EnmLanguage"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLanguage', { required: 'EnmLanguage is required', maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.enmLanguage}
        helperText={errors.enmLanguage?.message}
      />

      <TextField
        label="EnmMessagingMode"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmMessagingMode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmMessagingMode}
        helperText={errors.enmMessagingMode?.message}
      />

      <TextField
        label="Profession"
        type="text"
        fullWidth
        margin="normal"
        {...register('profession', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.profession}
        helperText={errors.profession?.message}
      />

      <TextField
        label="LkpEmploymentStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpEmploymentStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpEmploymentStatus}
        helperText={errors.lkpEmploymentStatus?.message}
      />

      <TextField
        label="Employer"
        type="text"
        fullWidth
        margin="normal"
        {...register('employer', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.employer}
        helperText={errors.employer?.message}
      />

      <TextField
        label="LkpCardUse"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpCardUse', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpCardUse}
        helperText={errors.lkpCardUse?.message}
      />

      <TextField
        label="PublicPosition"
        type="text"
        fullWidth
        margin="normal"
        {...register('publicPosition', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.publicPosition}
        helperText={errors.publicPosition?.message}
      />

      <TextField
        label="ConnectionWithPublicPerson"
        type="text"
        fullWidth
        margin="normal"
        {...register('connectionWithPublicPerson', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.connectionWithPublicPerson}
        helperText={errors.connectionWithPublicPerson?.message}
      />

      <TextField
        label="LkpAdditionalCitizenshipCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpAdditionalCitizenshipCountry', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lkpAdditionalCitizenshipCountry}
        helperText={errors.lkpAdditionalCitizenshipCountry?.message}
      />

      <TextField
        label="LkpConnectionWithIsrael"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpConnectionWithIsrael', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpConnectionWithIsrael}
        helperText={errors.lkpConnectionWithIsrael?.message}
      />

      <TextField
        label="ConnectionWithIsraelDetails"
        type="text"
        fullWidth
        margin="normal"
        {...register('connectionWithIsraelDetails', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.connectionWithIsraelDetails}
        helperText={errors.connectionWithIsraelDetails?.message}
      />

      <TextField
        label="LkpMainUseOfCardCardUsage"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpMainUseOfCardCardUsage', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpMainUseOfCardCardUsage}
        helperText={errors.lkpMainUseOfCardCardUsage?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isExpectedToReceiveDeposits')} />}
        label="IsExpectedToReceiveDeposits"
      />

      <TextField
        label="IsExpectedToReceiveDepositsDetails"
        type="text"
        fullWidth
        margin="normal"
        {...register('isExpectedToReceiveDepositsDetails', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.isExpectedToReceiveDepositsDetails}
        helperText={errors.isExpectedToReceiveDepositsDetails?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('wasPreviouslyDeniedBankService')} />}
        label="WasPreviouslyDeniedBankService"
      />

      <TextField
        label="WitnessFirstName"
        type="text"
        fullWidth
        margin="normal"
        {...register('witnessFirstName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.witnessFirstName}
        helperText={errors.witnessFirstName?.message}
      />

      <TextField
        label="WitnessLastName"
        type="text"
        fullWidth
        margin="normal"
        {...register('witnessLastName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.witnessLastName}
        helperText={errors.witnessLastName?.message}
      />

      <TextField
        label="WitnessIsraelID"
        type="text"
        fullWidth
        margin="normal"
        {...register('witnessIsraelID', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.witnessIsraelID}
        helperText={errors.witnessIsraelID?.message}
      />

      <TextField
        label="EnmIdentificationMethod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmIdentificationMethod', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmIdentificationMethod}
        helperText={errors.enmIdentificationMethod?.message}
      />

      <TextField
        label="InitialLoad"
        type="number"
        fullWidth
        margin="normal"
        {...register('initialLoad', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.initialLoad}
        helperText={errors.initialLoad?.message}
      />

      <TextField
        label="EnmInitialLoadMethodLoadMethod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmInitialLoadMethodLoadMethod', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmInitialLoadMethodLoadMethod}
        helperText={errors.enmInitialLoadMethodLoadMethod?.message}
      />

      <TextField
        label="CustomerReference"
        type="text"
        fullWidth
        margin="normal"
        {...register('customerReference', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.customerReference}
        helperText={errors.customerReference?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('amountIncludesLoadFee')} />}
        label="AmountIncludesLoadFee"
      />

      <TextField
        label="CardAllocationFee"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardAllocationFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.cardAllocationFee}
        helperText={errors.cardAllocationFee?.message}
      />

      <TextField
        label="MessageIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('messageIdentifier', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.messageIdentifier}
        helperText={errors.messageIdentifier?.message}
      />

      <TextField
        label="IDFrontURL"
        type="text"
        fullWidth
        margin="normal"
        {...register('iDFrontURL', { maxLength: { value: 4096, message: 'Maximum length is 4096' }, pattern: { value: /^https?:\/\/.+/, message: 'Invalid URL' } })}
        error={!!errors.iDFrontURL}
        helperText={errors.iDFrontURL?.message}
      />

      <TextField
        label="IDBackURL"
        type="text"
        fullWidth
        margin="normal"
        {...register('iDBackURL', { maxLength: { value: 4096, message: 'Maximum length is 4096' }, pattern: { value: /^https?:\/\/.+/, message: 'Invalid URL' } })}
        error={!!errors.iDBackURL}
        helperText={errors.iDBackURL?.message}
      />

      <TextField
        label="IDAppendixURL"
        type="text"
        fullWidth
        margin="normal"
        {...register('iDAppendixURL', { maxLength: { value: 4096, message: 'Maximum length is 4096' }, pattern: { value: /^https?:\/\/.+/, message: 'Invalid URL' } })}
        error={!!errors.iDAppendixURL}
        helperText={errors.iDAppendixURL?.message}
      />

      <TextField
        label="PassportURL"
        type="text"
        fullWidth
        margin="normal"
        {...register('passportURL', { maxLength: { value: 4096, message: 'Maximum length is 4096' }, pattern: { value: /^https?:\/\/.+/, message: 'Invalid URL' } })}
        error={!!errors.passportURL}
        helperText={errors.passportURL?.message}
      />

      <TextField
        label="Comments"
        fullWidth
        margin="normal"
        {...register('comments', {  })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <Box sx={{ my: 2 }}>
        <Button variant="outlined" component="label">
          Upload TZFront
          <input type="file" hidden {...register('tZFront')} />
        </Button>
      </Box>

      <Box sx={{ my: 2 }}>
        <Button variant="outlined" component="label">
          Upload TZBack
          <input type="file" hidden {...register('tZBack')} />
        </Button>
      </Box>

      <Box sx={{ my: 2 }}>
        <Button variant="outlined" component="label">
          Upload Passport
          <input type="file" hidden {...register('passport')} />
        </Button>
      </Box>



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/newCardRequestAPIs')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


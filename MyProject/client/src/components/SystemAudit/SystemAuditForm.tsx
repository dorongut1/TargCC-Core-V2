// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_SystemAudit
//     Generated: 2025-12-17 21:49:09
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress } from '@mui/material';
import { useSystemAudit, useCreateSystemAudit, useUpdateSystemAudit } from '../../hooks/useSystemAudit';
import type { CreateSystemAuditRequest, UpdateSystemAuditRequest } from '../../types/SystemAudit.types';


export const SystemAuditForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const systemAuditId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!systemAuditId;

  const { data: existingSystemAudit, isPending: isLoadingEntity } = useSystemAudit(systemAuditId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateSystemAuditRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingSystemAudit) {
      reset(existingSystemAudit);
    }
  }, [existingSystemAudit, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateSystemAudit();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateSystemAudit();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateSystemAuditRequest) => {
    if (isEdit && systemAuditId) {
      updateEntity(
        { id: systemAuditId, data: data as UpdateSystemAuditRequest },
        { onSuccess: () => navigate('/systemAudits') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/systemAudits') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="TableName"
        type="text"
        fullWidth
        margin="normal"
        {...register('tableName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.tableName}
        helperText={errors.tableName?.message}
      />

      <TextField
        label="RowId"
        type="number"
        fullWidth
        margin="normal"
        {...register('rowId', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.rowId}
        helperText={errors.rowId?.message}
      />

      <TextField
        label="Operation"
        type="text"
        fullWidth
        margin="normal"
        {...register('operation', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.operation}
        helperText={errors.operation?.message}
      />

      <TextField
        label="OccurredAt"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('occurredAt', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.occurredAt}
        helperText={errors.occurredAt?.message}
      />

      <TextField
        label="SqlCurrentUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('sqlCurrentUser', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.sqlCurrentUser}
        helperText={errors.sqlCurrentUser?.message}
      />

      <TextField
        label="ChangedByUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('changedByUser', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.changedByUser}
        helperText={errors.changedByUser?.message}
      />

      <TextField
        label="ActiveLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('activeLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.activeLoginID}
        helperText={errors.activeLoginID?.message}
      />

      <TextField
        label="SqlSystemUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('sqlSystemUser', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.sqlSystemUser}
        helperText={errors.sqlSystemUser?.message}
      />

      <TextField
        label="SqlAppName"
        type="text"
        fullWidth
        margin="normal"
        {...register('sqlAppName', { maxLength: { value: 500, message: 'Maximum length is 500' } })}
        error={!!errors.sqlAppName}
        helperText={errors.sqlAppName?.message}
      />

      <TextField
        label="SqlHostName"
        type="text"
        fullWidth
        margin="normal"
        {...register('sqlHostName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.sqlHostName}
        helperText={errors.sqlHostName?.message}
      />

      <TextField
        label="Changes"
        type="text"
        fullWidth
        margin="normal"
        {...register('changes', {  })}
        error={!!errors.changes}
        helperText={errors.changes?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/systemAudits')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


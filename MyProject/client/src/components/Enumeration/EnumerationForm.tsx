// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Enumeration
//     Generated: 2025-12-17 07:22:06
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useEnumeration, useCreateEnumeration, useUpdateEnumeration } from '../../hooks/useEnumeration';
import type { CreateEnumerationRequest, UpdateEnumerationRequest } from '../../types/Enumeration.types';


export const EnumerationForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const enumerationId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!enumerationId;

  const { data: existingEnumeration, isPending: isLoadingEntity } = useEnumeration(enumerationId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateEnumerationRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingEnumeration) {
      reset(existingEnumeration);
    }
  }, [existingEnumeration, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateEnumeration();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateEnumeration();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateEnumerationRequest) => {
    if (isEdit && enumerationId) {
      updateEntity(
        { id: enumerationId, data: data as UpdateEnumerationRequest },
        { onSuccess: () => navigate('/enumerations') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/enumerations') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('isSystem')} />}
        label="IsSystem"
      />

      <TextField
        label="EnumType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enumType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enumType}
        helperText={errors.enumType?.message}
      />

      <TextField
        label="EnumValue"
        type="text"
        fullWidth
        margin="normal"
        {...register('enumValue', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enumValue}
        helperText={errors.enumValue?.message}
      />

      <TextField
        label="LocText"
        type="text"
        fullWidth
        margin="normal"
        {...register('locText', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.locText}
        helperText={errors.locText?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('tableNameCEnumeration')} />}
        label="TableNameCEnumeration"
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/enumerations')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


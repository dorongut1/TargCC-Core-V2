// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Task
//     Generated: 2025-12-11 20:26:17
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useTask, useCreateTask, useUpdateTask } from '../../hooks/useTask';
import type { CreateTaskRequest, UpdateTaskRequest } from '../../types/Task.types';


export const TaskForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const taskId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!taskId;

  const { data: existingTask, isPending: isLoadingEntity } = useTask(taskId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateTaskRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingTask) {
      reset(existingTask);
    }
  }, [existingTask, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateTask();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateTask();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateTaskRequest) => {
    if (isEdit && taskId) {
      updateEntity(
        { id: taskId, data: data as UpdateTaskRequest },
        { onSuccess: () => navigate('/tasks') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/tasks') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameTask')} />}
        label="TableNameTask"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="EnmTask"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmTask', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmTask}
        helperText={errors.enmTask?.message}
      />

      <TextField
        label="EnmStatus"
        fullWidth
        margin="normal"
        {...register('enmStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmStatus}
        helperText={errors.enmStatus?.message}
      />

      <TextField
        label="Description"
        type="text"
        fullWidth
        margin="normal"
        {...register('description', { maxLength: { value: 500, message: 'Maximum length is 500' } })}
        error={!!errors.description}
        helperText={errors.description?.message}
      />

      <TextField
        label="HandlerComments"
        fullWidth
        margin="normal"
        {...register('handlerComments', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.handlerComments}
        helperText={errors.handlerComments?.message}
      />

      <TextField
        label="ReferenceTable"
        type="text"
        fullWidth
        margin="normal"
        {...register('referenceTable', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.referenceTable}
        helperText={errors.referenceTable?.message}
      />

      <TextField
        label="ReferenceTableID"
        type="number"
        fullWidth
        margin="normal"
        {...register('referenceTableID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.referenceTableID}
        helperText={errors.referenceTableID?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="HandledByUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('handledByUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.handledByUserID}
        helperText={errors.handledByUserID?.message}
      />

      <TextField
        label="WhenStatusChanged"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenStatusChanged', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.whenStatusChanged}
        helperText={errors.whenStatusChanged?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/tasks')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: GovtReport10
//     Generated: 2025-12-17 21:32:15
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useGovtReport10, useCreateGovtReport10, useUpdateGovtReport10 } from '../../hooks/useGovtReport10';
import type { CreateGovtReport10Request, UpdateGovtReport10Request } from '../../types/GovtReport10.types';


export const GovtReport10Form: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const govtReport10Id = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!govtReport10Id;

  const { data: existingGovtReport10, isPending: isLoadingEntity } = useGovtReport10(govtReport10Id || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateGovtReport10Request>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingGovtReport10) {
      reset(existingGovtReport10);
    }
  }, [existingGovtReport10, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateGovtReport10();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateGovtReport10();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateGovtReport10Request) => {
    if (isEdit && govtReport10Id) {
      updateEntity(
        { id: govtReport10Id, data: data as UpdateGovtReport10Request },
        { onSuccess: () => navigate('/govtReport10s') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/govtReport10s') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameGovtReport10')} />}
        label="TableNameGovtReport10"
      />

      <TextField
        label="GovtReport00ID"
        type="number"
        fullWidth
        margin="normal"
        {...register('govtReport00ID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.govtReport00ID}
        helperText={errors.govtReport00ID?.message}
      />

      <TextField
        label="ReporterNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterNumber', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.reporterNumber}
        helperText={errors.reporterNumber?.message}
      />

      <TextField
        label="ReporterIDNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterIDNumber', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.reporterIDNumber}
        helperText={errors.reporterIDNumber?.message}
      />

      <TextField
        label="ReportYear"
        type="text"
        fullWidth
        margin="normal"
        {...register('reportYear', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.reportYear}
        helperText={errors.reportYear?.message}
      />

      <TextField
        label="ReportNumber"
        type="number"
        fullWidth
        margin="normal"
        {...register('reportNumber', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.reportNumber}
        helperText={errors.reportNumber?.message}
      />

      <TextField
        label="RecordType"
        type="text"
        fullWidth
        margin="normal"
        {...register('recordType', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.recordType}
        helperText={errors.recordType?.message}
      />

      <TextField
        label="BranchNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('branchNumber', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.branchNumber}
        helperText={errors.branchNumber?.message}
      />

      <TextField
        label="PrimaryActionIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('primaryActionIdentifier', { maxLength: { value: 18, message: 'Maximum length is 18' } })}
        error={!!errors.primaryActionIdentifier}
        helperText={errors.primaryActionIdentifier?.message}
      />

      <TextField
        label="ActionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('actionType', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.actionType}
        helperText={errors.actionType?.message}
      />

      <TextField
        label="DateExecutedAtReporter"
        type="text"
        fullWidth
        margin="normal"
        {...register('dateExecutedAtReporter', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.dateExecutedAtReporter}
        helperText={errors.dateExecutedAtReporter?.message}
      />

      <TextField
        label="FinancialInstitutionCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('financialInstitutionCode', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.financialInstitutionCode}
        helperText={errors.financialInstitutionCode?.message}
      />

      <TextField
        label="OtherFinancialCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('otherFinancialCode', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.otherFinancialCode}
        helperText={errors.otherFinancialCode?.message}
      />

      <TextField
        label="FinancialInstitutionCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('financialInstitutionCountry', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.financialInstitutionCountry}
        helperText={errors.financialInstitutionCountry?.message}
      />

      <TextField
        label="CurrencyCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('currencyCode', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.currencyCode}
        helperText={errors.currencyCode?.message}
      />

      <TextField
        label="TransactionAmountILS"
        type="text"
        fullWidth
        margin="normal"
        {...register('transactionAmountILS', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.transactionAmountILS}
        helperText={errors.transactionAmountILS?.message}
      />

      <TextField
        label="SecondaryActionIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('secondaryActionIdentifier', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.secondaryActionIdentifier}
        helperText={errors.secondaryActionIdentifier?.message}
      />

      <TextField
        label="TransactionAmountOriginalCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('transactionAmountOriginalCurrency', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.transactionAmountOriginalCurrency}
        helperText={errors.transactionAmountOriginalCurrency?.message}
      />

      <TextField
        label="DateExecutedAtSupplier"
        type="text"
        fullWidth
        margin="normal"
        {...register('dateExecutedAtSupplier', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.dateExecutedAtSupplier}
        helperText={errors.dateExecutedAtSupplier?.message}
      />

      <TextField
        label="TransactionTypeAtSupplier"
        type="text"
        fullWidth
        margin="normal"
        {...register('transactionTypeAtSupplier', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.transactionTypeAtSupplier}
        helperText={errors.transactionTypeAtSupplier?.message}
      />

      <TextField
        label="Comments"
        type="text"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="TimeExecutedAtSupplier"
        type="text"
        fullWidth
        margin="normal"
        {...register('timeExecutedAtSupplier', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.timeExecutedAtSupplier}
        helperText={errors.timeExecutedAtSupplier?.message}
      />

      <TextField
        label="ExchangeRate"
        type="text"
        fullWidth
        margin="normal"
        {...register('exchangeRate', { maxLength: { value: 34, message: 'Maximum length is 34' } })}
        error={!!errors.exchangeRate}
        helperText={errors.exchangeRate?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="Ip"
        type="text"
        fullWidth
        margin="normal"
        {...register('ip', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.ip}
        helperText={errors.ip?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/govtReport10s')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: MCCBFPCashOut
//     Generated: 2025-12-11 21:18:55
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useMCCBFPCashOut, useCreateMCCBFPCashOut, useUpdateMCCBFPCashOut } from '../../hooks/useMCCBFPCashOut';
import type { CreateMCCBFPCashOutRequest, UpdateMCCBFPCashOutRequest } from '../../types/MCCBFPCashOut.types';


export const MCCBFPCashOutForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mCCBFPCashOutId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mCCBFPCashOutId;

  const { data: existingMCCBFPCashOut, isPending: isLoadingEntity } = useMCCBFPCashOut(mCCBFPCashOutId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMCCBFPCashOutRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMCCBFPCashOut) {
      reset(existingMCCBFPCashOut);
    }
  }, [existingMCCBFPCashOut, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMCCBFPCashOut();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMCCBFPCashOut();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMCCBFPCashOutRequest) => {
    if (isEdit && mCCBFPCashOutId) {
      updateEntity(
        { id: mCCBFPCashOutId, data: data as UpdateMCCBFPCashOutRequest },
        { onSuccess: () => navigate('/mCCBFPCashOuts') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mCCBFPCashOuts') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameMCCBFPCashOut')} />}
        label="TableNameMCCBFPCashOut"
      />

      <TextField
        label="Region"
        type="text"
        fullWidth
        margin="normal"
        {...register('region', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.region}
        helperText={errors.region?.message}
      />

      <TextField
        label="Country"
        type="text"
        fullWidth
        margin="normal"
        {...register('country', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.country}
        helperText={errors.country?.message}
      />

      <TextField
        label="Brand"
        type="text"
        fullWidth
        margin="normal"
        {...register('brand', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.brand}
        helperText={errors.brand?.message}
      />

      <TextField
        label="PayoutCapability"
        type="text"
        fullWidth
        margin="normal"
        {...register('payoutCapability', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.payoutCapability}
        helperText={errors.payoutCapability?.message}
      />

      <TextField
        label="PayoutCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('payoutCurrency', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.payoutCurrency}
        helperText={errors.payoutCurrency?.message}
      />

      <TextField
        label="ReachOfChannel"
        type="text"
        fullWidth
        margin="normal"
        {...register('reachOfChannel', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.reachOfChannel}
        helperText={errors.reachOfChannel?.message}
      />

      <TextField
        label="Corridors"
        type="text"
        fullWidth
        margin="normal"
        {...register('corridors', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.corridors}
        helperText={errors.corridors?.message}
      />

      <TextField
        label="MinimumTransactionAmount"
        type="text"
        fullWidth
        margin="normal"
        {...register('minimumTransactionAmount', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.minimumTransactionAmount}
        helperText={errors.minimumTransactionAmount?.message}
      />

      <TextField
        label="MaximumTransactionAmount"
        type="text"
        fullWidth
        margin="normal"
        {...register('maximumTransactionAmount', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.maximumTransactionAmount}
        helperText={errors.maximumTransactionAmount?.message}
      />

      <TextField
        label="AvailabilityForCashPickup"
        type="text"
        fullWidth
        margin="normal"
        {...register('availabilityForCashPickup', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.availabilityForCashPickup}
        helperText={errors.availabilityForCashPickup?.message}
      />

      <TextField
        label="MaximumTimeAllowedForCashToBePickedUp"
        type="text"
        fullWidth
        margin="normal"
        {...register('maximumTimeAllowedForCashToBePickedUp', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.maximumTimeAllowedForCashToBePickedUp}
        helperText={errors.maximumTimeAllowedForCashToBePickedUp?.message}
      />

      <TextField
        label="OIPreapprovalRequired"
        type="text"
        fullWidth
        margin="normal"
        {...register('oIPreapprovalRequired', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.oIPreapprovalRequired}
        helperText={errors.oIPreapprovalRequired?.message}
      />

      <TextField
        label="RequiredInformation"
        type="text"
        fullWidth
        margin="normal"
        {...register('requiredInformation', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.requiredInformation}
        helperText={errors.requiredInformation?.message}
      />

      <TextField
        label="AverageResponseTimeFromRSP"
        type="text"
        fullWidth
        margin="normal"
        {...register('averageResponseTimeFromRSP', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.averageResponseTimeFromRSP}
        helperText={errors.averageResponseTimeFromRSP?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mCCBFPCashOuts')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: vwCustomerCardStatus
//     Generated: 2025-12-15 09:25:40
// </auto-generated>

import React from 'react';
import { useNavigate } from 'react-router-dom';
import ExcelJS from 'exceljs';
import { DataGrid, GridColDef, GridToolbarContainer, GridToolbarColumnsButton, GridToolbarDensitySelector, useGridApiRef } from '@mui/x-data-grid';
import { Button, Box, CircularProgress, Alert, TextField, Paper, Typography } from '@mui/material';
import { FileDownload as FileDownloadIcon, Clear as ClearIcon } from '@mui/icons-material';
import { useVwCustomerCardStatuss } from '../../hooks/useVwCustomerCardStatus';
import type { VwCustomerCardStatus, VwCustomerCardStatusFilters } from '../../types/VwCustomerCardStatus.types';


export const VwCustomerCardStatusList: React.FC = () => {
  const navigate = useNavigate();
  const apiRef = useGridApiRef();
  
  const [columnFilters, setColumnFilters] = React.useState<Record<string, string>>({});
  const { data: vwCustomerCardStatuss, isLoading, error } = useVwCustomerCardStatuss({});

  // Client-side column filters
  const filteredData = React.useMemo(() => {
    if (!vwCustomerCardStatuss) return vwCustomerCardStatuss;

    // Get active filters
    const activeFilters = Object.entries(columnFilters).filter(([_, value]) => value && value.trim() !== '');
    if (activeFilters.length === 0) return vwCustomerCardStatuss;

    // Apply all filters with AND logic
    return vwCustomerCardStatuss.filter(item => {
      return activeFilters.every(([column, filterValue]) => {
        // Cast to any to avoid TypeScript union type issues with instanceof
        let itemValue = item[column as keyof typeof item] as any;
        
        // Special handling for date filtering - compare as Date objects
        if (typeof filterValue === 'string' && /^\d{4}-\d{2}-\d{2}$/.test(filterValue)) {
          // Filter value is from date picker (YYYY-MM-DD)
          const filterDate = new Date(filterValue);
          let itemDate: Date | null = null;
          
          if (itemValue instanceof Date) {
            itemDate = itemValue;
          } else if (typeof itemValue === 'string' && /^\d{4}-\d{2}-\d{2}/.test(itemValue)) {
            itemDate = new Date(itemValue);
          }
          
          if (itemDate) {
            // Compare dates (ignore time) - same year, month, and day
            return itemDate.getFullYear() === filterDate.getFullYear() &&
                   itemDate.getMonth() === filterDate.getMonth() &&
                   itemDate.getDate() === filterDate.getDate();
          }
          return false;
        }
        
        // Convert boolean values to readable text
        if (typeof itemValue === 'boolean') {
          itemValue = itemValue ? 'true' : 'false';
        }
        
        return itemValue?.toString().toLowerCase().includes(filterValue.toLowerCase());
      });
    });
  }, [vwCustomerCardStatuss, columnFilters]);

  const handleClearAllFilters = () => {
    setColumnFilters({});
  };

  function CustomToolbar() {
    return (
      <GridToolbarContainer>
        <GridToolbarColumnsButton />
        <GridToolbarDensitySelector />
        <Button
          size="small"
          startIcon={<ClearIcon />}
          onClick={handleClearAllFilters}
        >
          Clear All Filters
        </Button>
      </GridToolbarContainer>
    );
  }

  const handleExportToExcel = async () => {
    // Export only the filtered data visible on screen
    const visibleRows = filteredData || [];

    if (!visibleRows || visibleRows.length === 0) {
      alert('No data to export');
      return;
    }

    // Get visible columns from DataGrid (respects column visibility)
    const visibleColumns = apiRef.current
      ? apiRef.current.getVisibleColumns().map(col => col.field).filter(field => field !== 'actions')
      : Object.keys(visibleRows[0] || {});

    // Filter data to only include visible columns
    const exportData = visibleRows.map(row => {
      const filteredRow: any = {};
      const rowAny = row as any;
      visibleColumns.forEach(col => {
        if (col in rowAny) filteredRow[col] = rowAny[col];
      });
      return filteredRow;
    });

    // Create workbook and worksheet
    const workbook = new ExcelJS.Workbook();
    const worksheet = workbook.addWorksheet('VwCustomerCardStatuss');

    // Get column names from first row (only visible columns)
    const columnNames = exportData.length > 0 ? Object.keys(exportData[0]) : [];
    worksheet.columns = columnNames.map(name => ({ header: name, key: name, width: 15 }));

    // Add data rows
    exportData.forEach(row => worksheet.addRow(row));

    // Style header row
    const headerRow = worksheet.getRow(1);
    headerRow.font = { bold: true, color: { argb: 'FFFFFFFF' } };
    headerRow.fill = { type: 'pattern', pattern: 'solid', fgColor: { argb: 'FF4472C4' } };
    headerRow.alignment = { horizontal: 'center', vertical: 'middle' };
    headerRow.height = 20;

    // Auto-fit columns based on content
    worksheet.columns.forEach(column => {
      let maxLength = column.header?.length || 10;
      column.eachCell?.({ includeEmpty: false }, cell => {
        const cellLength = cell.value ? String(cell.value).length : 0;
        maxLength = Math.max(maxLength, cellLength);
      });
      column.width = Math.min(maxLength + 2, 50);
    });

    // Freeze header row
    worksheet.views = [{ state: 'frozen', ySplit: 1 }];

    // Generate Excel file and download
    const buffer = await workbook.xlsx.writeBuffer();
    const blob = new Blob([buffer], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
    const url = window.URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `VwCustomerCardStatuss_${new Date().toISOString().split('T')[0]}.xlsx`;
    link.click();
    window.URL.revokeObjectURL(url);
  };

  const columns: GridColDef<VwCustomerCardStatus>[] = [
    { field: 'id', headerName: 'Id', width: 90 },
    { field: 'customerID', headerName: 'CustomerID', width: 150 },
    { field: 'hasCard', headerName: 'HasCard', width: 100 },
    { field: 'israelIDNumber', headerName: 'IsraelIDNumber', width: 150 },
    { field: 'email', headerName: 'Email', width: 250 },
    { field: 'cellphoneNumber', headerName: 'CellphoneNumber', width: 150 },
    { field: 'cardClubID', headerName: 'CardClubID', width: 150 },
    { field: 'cid', headerName: 'Cid', width: 200 },
    { field: 'cardLast4Numbers', headerName: 'CardLast4Numbers', width: 150 },
  ];


  if (isLoading) {
    return <CircularProgress />;
  }

  if (error) {
    return <Alert severity="error">Failed to load vwCustomerCardStatuss</Alert>;
  }

  return (
    <Box sx={{ height: 'calc(100vh - 200px)', width: '100%', display: 'flex', flexDirection: 'column' }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<FileDownloadIcon />}
          onClick={handleExportToExcel}
        >
          Export to Excel
        </Button>
      </Box>
      <Paper sx={{ p: 2, mb: 2 }}>
        <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap', alignItems: 'center' }}>
          <TextField
            label="Id"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['id'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              id: e.target.value
            }))}
          />
          <TextField
            label="CustomerID"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['customerID'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              customerID: e.target.value
            }))}
          />
          <TextField
            select
            label="HasCard"
            size="small"
            sx={{ minWidth: 120 }}
            value={columnFilters['hasCard'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              hasCard: e.target.value
            }))}
          >
            <MenuItem value="">All</MenuItem>
            <MenuItem value="true">Yes</MenuItem>
            <MenuItem value="false">No</MenuItem>
          </TextField>
          <TextField
            label="IsraelIDNumber"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['israelIDNumber'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              israelIDNumber: e.target.value
            }))}
          />
          <TextField
            label="Email"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['email'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              email: e.target.value
            }))}
          />
          <TextField
            label="CellphoneNumber"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['cellphoneNumber'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              cellphoneNumber: e.target.value
            }))}
          />
          <TextField
            label="CardClubID"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['cardClubID'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              cardClubID: e.target.value
            }))}
          />
          <TextField
            label="Cid"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['cid'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              cid: e.target.value
            }))}
          />
          <TextField
            label="CardLast4Numbers"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['cardLast4Numbers'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              cardLast4Numbers: e.target.value
            }))}
          />
          <Button
            variant="outlined"
            size="small"
            onClick={handleClearAllFilters}
          >
            Clear
          </Button>
          <Box sx={{ ml: 'auto', display: 'flex', alignItems: 'center' }}>
            <Typography variant="body2" color="text.secondary">
              Showing {filteredData?.length || 0} of {vwCustomerCardStatuss?.length || 0} rows
            </Typography>
          </Box>
        </Box>
      </Paper>

      <Box sx={{ flex: 1, minHeight: 0 }}>
        <DataGrid
          apiRef={apiRef}
          rows={filteredData || []}
          columns={columns}
          getRowId={(row) => row.id}
          slots={{
            toolbar: CustomToolbar,
          }}
          initialState={{
            pagination: { paginationModel: { pageSize: 10 } },
          }}
          pageSizeOptions={[5, 10, 25, 100]}
          checkboxSelection
          disableRowSelectionOnClick
        />
      </Box>
    </Box>
  );
};


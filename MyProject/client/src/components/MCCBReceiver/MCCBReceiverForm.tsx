// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: MCCBReceiver
//     Generated: 2025-12-17 21:20:41
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useMCCBReceiver, useCreateMCCBReceiver, useUpdateMCCBReceiver } from '../../hooks/useMCCBReceiver';
import type { CreateMCCBReceiverRequest, UpdateMCCBReceiverRequest } from '../../types/MCCBReceiver.types';


export const MCCBReceiverForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mCCBReceiverId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mCCBReceiverId;

  const { data: existingMCCBReceiver, isPending: isLoadingEntity } = useMCCBReceiver(mCCBReceiverId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMCCBReceiverRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMCCBReceiver) {
      reset(existingMCCBReceiver);
    }
  }, [existingMCCBReceiver, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMCCBReceiver();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMCCBReceiver();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMCCBReceiverRequest) => {
    if (isEdit && mCCBReceiverId) {
      updateEntity(
        { id: mCCBReceiverId, data: data as UpdateMCCBReceiverRequest },
        { onSuccess: () => navigate('/mCCBReceivers') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mCCBReceivers') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameMCCBReceiver')} />}
        label="TableNameMCCBReceiver"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="FirstName"
        type="text"
        fullWidth
        margin="normal"
        {...register('firstName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.firstName}
        helperText={errors.firstName?.message}
      />

      <TextField
        label="LastName"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lastName}
        helperText={errors.lastName?.message}
      />

      <TextField
        label="PhoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('phoneNumber', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.phoneNumber}
        helperText={errors.phoneNumber?.message}
      />

      <TextField
        label="BirthDate"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('birthDate', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.birthDate}
        helperText={errors.birthDate?.message}
      />

      <TextField
        label="BirthCountryISO3166Alpha2"
        type="text"
        fullWidth
        margin="normal"
        {...register('birthCountryISO3166Alpha2', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.birthCountryISO3166Alpha2}
        helperText={errors.birthCountryISO3166Alpha2?.message}
      />

      <TextField
        label="CitizenshipCountryISO3166Alpha2"
        type="text"
        fullWidth
        margin="normal"
        {...register('citizenshipCountryISO3166Alpha2', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.citizenshipCountryISO3166Alpha2}
        helperText={errors.citizenshipCountryISO3166Alpha2?.message}
      />

      <TextField
        label="ResidenceCountryISO3166Alpha2"
        type="text"
        fullWidth
        margin="normal"
        {...register('residenceCountryISO3166Alpha2', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.residenceCountryISO3166Alpha2}
        helperText={errors.residenceCountryISO3166Alpha2?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="EnmGender"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmGender', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmGender}
        helperText={errors.enmGender?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isBusiness')} />}
        label="IsBusiness"
      />

      <TextField
        label="EnmIdentificationMethod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmIdentificationMethod', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmIdentificationMethod}
        helperText={errors.enmIdentificationMethod?.message}
      />

      <TextField
        label="IdentificationNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('identificationNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.identificationNumber}
        helperText={errors.identificationNumber?.message}
      />

      <TextField
        label="Expiration"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('expiration', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.expiration}
        helperText={errors.expiration?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mCCBReceivers')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


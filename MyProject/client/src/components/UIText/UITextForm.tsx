// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: UIText
//     Generated: 2025-12-17 08:03:54
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useUIText, useCreateUIText, useUpdateUIText } from '../../hooks/useUIText';
import type { CreateUITextRequest, UpdateUITextRequest } from '../../types/UIText.types';


export const UITextForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const uITextId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!uITextId;

  const { data: existingUIText, isPending: isLoadingEntity } = useUIText(uITextId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateUITextRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingUIText) {
      reset(existingUIText);
    }
  }, [existingUIText, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateUIText();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateUIText();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateUITextRequest) => {
    if (isEdit && uITextId) {
      updateEntity(
        { id: uITextId, data: data as UpdateUITextRequest },
        { onSuccess: () => navigate('/uITexts') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/uITexts') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameUIText')} />}
        label="TableNameUIText"
      />

      <TextField
        label="Page"
        type="text"
        fullWidth
        margin="normal"
        {...register('page', { required: 'Page is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.page}
        helperText={errors.page?.message}
      />

      <TextField
        label="Name"
        type="text"
        fullWidth
        margin="normal"
        {...register('name', { required: 'Name is required', maxLength: { value: 150, message: 'Maximum length is 150' } })}
        error={!!errors.name}
        helperText={errors.name?.message}
      />

      <TextField
        label="LocText"
        type="text"
        fullWidth
        margin="normal"
        {...register('locText', {  })}
        error={!!errors.locText}
        helperText={errors.locText?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/uITexts')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: ccvwComboList_CardActivationForCard
//     Generated: 2025-12-11 11:52:11
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import type { CcvwComboListCardActivationForCard } from '../../types/CcvwComboListCardActivationForCard.types';


export const CcvwComboListCardActivationForCardDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  // VIEWs are read-only - Detail page not applicable
  const isLoading = false;
  const error = null;
  const entity = null;

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load ccvwComboListCardActivationForCard</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/ccvwComboListCardActivationForCards')}
        >
          Back
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            CcvwComboListCardActivationForCard Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ParentID
          </Typography>
          <Typography variant="body1">
            {entity.parentID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TextNS
          </Typography>
          <Typography variant="body1">
            {entity.textNS}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Text
          </Typography>
          <Typography variant="body1">
            {entity.text}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>
    </Box>
  );
};


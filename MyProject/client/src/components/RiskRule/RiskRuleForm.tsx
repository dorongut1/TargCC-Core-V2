// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: RiskRule
//     Generated: 2025-12-17 09:34:38
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useRiskRule, useCreateRiskRule, useUpdateRiskRule } from '../../hooks/useRiskRule';
import type { CreateRiskRuleRequest, UpdateRiskRuleRequest } from '../../types/RiskRule.types';


export const RiskRuleForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const riskRuleId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!riskRuleId;

  const { data: existingRiskRule, isPending: isLoadingEntity } = useRiskRule(riskRuleId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateRiskRuleRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingRiskRule) {
      reset(existingRiskRule);
    }
  }, [existingRiskRule, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateRiskRule();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateRiskRule();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateRiskRuleRequest) => {
    if (isEdit && riskRuleId) {
      updateEntity(
        { id: riskRuleId, data: data as UpdateRiskRuleRequest },
        { onSuccess: () => navigate('/riskRules') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/riskRules') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameRiskRule')} />}
        label="TableNameRiskRule"
      />

      <TextField
        label="RuleName"
        type="text"
        fullWidth
        margin="normal"
        {...register('ruleName', { required: 'RuleName is required', maxLength: { value: 400, message: 'Maximum length is 400' } })}
        error={!!errors.ruleName}
        helperText={errors.ruleName?.message}
      />

      <TextField
        label="RuleDescription"
        type="text"
        fullWidth
        margin="normal"
        {...register('ruleDescription', { maxLength: { value: 2000, message: 'Maximum length is 2000' } })}
        error={!!errors.ruleDescription}
        helperText={errors.ruleDescription?.message}
      />

      <TextField
        label="EnmMonitoredEntity"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmMonitoredEntity', { required: 'EnmMonitoredEntity is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmMonitoredEntity}
        helperText={errors.enmMonitoredEntity?.message}
      />

      <TextField
        label="ReferenceTable"
        type="text"
        fullWidth
        margin="normal"
        {...register('referenceTable', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.referenceTable}
        helperText={errors.referenceTable?.message}
      />

      <TextField
        label="ReferenceTransactionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('referenceTransactionType', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.referenceTransactionType}
        helperText={errors.referenceTransactionType?.message}
      />

      <TextField
        label="EnmThresholdType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmThresholdType', { required: 'EnmThresholdType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmThresholdType}
        helperText={errors.enmThresholdType?.message}
      />

      <TextField
        label="ThresholdValue"
        type="number"
        fullWidth
        margin="normal"
        {...register('thresholdValue', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.thresholdValue}
        helperText={errors.thresholdValue?.message}
      />

      <TextField
        label="MinimumTransactionAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('minimumTransactionAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.minimumTransactionAmount}
        helperText={errors.minimumTransactionAmount?.message}
      />

      <TextField
        label="MaximumTransactionAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('maximumTransactionAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.maximumTransactionAmount}
        helperText={errors.maximumTransactionAmount?.message}
      />

      <TextField
        label="EnmBaselineScope"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmBaselineScope', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmBaselineScope}
        helperText={errors.enmBaselineScope?.message}
      />

      <TextField
        label="BaselineReferenceTable"
        type="text"
        fullWidth
        margin="normal"
        {...register('baselineReferenceTable', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.baselineReferenceTable}
        helperText={errors.baselineReferenceTable?.message}
      />

      <TextField
        label="MinimumBaselineCount"
        type="number"
        fullWidth
        margin="normal"
        {...register('minimumBaselineCount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.minimumBaselineCount}
        helperText={errors.minimumBaselineCount?.message}
      />

      <TextField
        label="EnmTimePeriod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmTimePeriod', { required: 'EnmTimePeriod is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmTimePeriod}
        helperText={errors.enmTimePeriod?.message}
      />

      <TextField
        label="EnmAction"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmAction', { required: 'EnmAction is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmAction}
        helperText={errors.enmAction?.message}
      />

      <TextField
        label="ActionDurationDays"
        type="number"
        fullWidth
        margin="normal"
        {...register('actionDurationDays', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.actionDurationDays}
        helperText={errors.actionDurationDays?.message}
      />

      <TextField
        label="EnmBlockedActionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmBlockedActionType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmBlockedActionType}
        helperText={errors.enmBlockedActionType?.message}
      />

      <TextField
        label="ActionColorID"
        type="text"
        fullWidth
        margin="normal"
        {...register('actionColorID', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.actionColorID}
        helperText={errors.actionColorID?.message}
      />

      <TextField
        label="ActionMaxAllowedAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('actionMaxAllowedAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.actionMaxAllowedAmount}
        helperText={errors.actionMaxAllowedAmount?.message}
      />

      <TextField
        label="AlertEmailAddresses"
        type="text"
        fullWidth
        margin="normal"
        {...register('alertEmailAddresses', { maxLength: { value: 1000, message: 'Maximum length is 1000' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.alertEmailAddresses}
        helperText={errors.alertEmailAddresses?.message}
      />

      <TextField
        label="AlertSMSNumbers"
        type="text"
        fullWidth
        margin="normal"
        {...register('alertSMSNumbers', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.alertSMSNumbers}
        helperText={errors.alertSMSNumbers?.message}
      />

      <TextField
        label="CreateTaskForUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('createTaskForUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.createTaskForUserID}
        helperText={errors.createTaskForUserID?.message}
      />

      <TextField
        label="EnmSeverity"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmSeverity', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmSeverity}
        helperText={errors.enmSeverity?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isActive')} />}
        label="IsActive"
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/riskRules')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Card
//     Generated: 2025-12-11 22:10:36
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid, GridColDef } from '@mui/x-data-grid';
import { useCard, useDeleteCard } from '../../hooks/useCard';
import { useCardBackofficeTransactions } from '../../hooks/useCard';
import { useCardCardActivations } from '../../hooks/useCard';
import { useCardCustomerCardTransactions } from '../../hooks/useCard';
import { useCardImportTransactionLoadRows } from '../../hooks/useCard';
import { useCardOnlineApprovals } from '../../hooks/useCard';
import { useCardRequestedTransactions } from '../../hooks/useCard';
import { useCardTransactionLoads } from '../../hooks/useCard';
import type { Card } from '../../types/Card.types';
import type { BackofficeTransaction } from '../../types/BackofficeTransaction.types';
import type { CardActivation } from '../../types/CardActivation.types';
import type { CustomerCardTransaction } from '../../types/CustomerCardTransaction.types';
import type { ImportTransactionLoadRow } from '../../types/ImportTransactionLoadRow.types';
import type { OnlineApproval } from '../../types/OnlineApproval.types';
import type { RequestedTransaction } from '../../types/RequestedTransaction.types';
import type { TransactionLoad } from '../../types/TransactionLoad.types';


export const CardDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useCard(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteCard();
  const { data: backofficeTransactions, isLoading: backofficeTransactionsLoading } = useCardBackofficeTransactions(id ? parseInt(id, 10) : null);
  const { data: cardActivations, isLoading: cardActivationsLoading } = useCardCardActivations(id ? parseInt(id, 10) : null);
  const { data: customerCardTransactions, isLoading: customerCardTransactionsLoading } = useCardCustomerCardTransactions(id ? parseInt(id, 10) : null);
  const { data: importTransactionLoadRows, isLoading: importTransactionLoadRowsLoading } = useCardImportTransactionLoadRows(id ? parseInt(id, 10) : null);
  const { data: onlineApprovals, isLoading: onlineApprovalsLoading } = useCardOnlineApprovals(id ? parseInt(id, 10) : null);
  const { data: requestedTransactions, isLoading: requestedTransactionsLoading } = useCardRequestedTransactions(id ? parseInt(id, 10) : null);
  const { data: transactionLoads, isLoading: transactionLoadsLoading } = useCardTransactionLoads(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/cards'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load card</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/cards')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/cards/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            Card Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCard
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCard ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardClubID
          </Typography>
          <Typography variant="body1">
            {entity.cardClubID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Cid
          </Typography>
          <Typography variant="body1">
            {entity.cid}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardLast4Numbers
          </Typography>
          <Typography variant="body1">
            {entity.cardLast4Numbers}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Expiry
          </Typography>
          <Typography variant="body1">
            {(entity.expiry ? (entity.expiry instanceof Date ? entity.expiry.toLocaleDateString() : new Date(entity.expiry).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerID
          </Typography>
          <Typography variant="body1">
            {entity.customerID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmCurrency
          </Typography>
          <Typography variant="body1">
            {entity.enmCurrency}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            OwningDistributorID
          </Typography>
          <Typography variant="body1">
            {entity.owningDistributorID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardOrderFromIssuerID
          </Typography>
          <Typography variant="body1">
            {entity.cardOrderFromIssuerID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardOrderByDistributorID
          </Typography>
          <Typography variant="body1">
            {entity.cardOrderByDistributorID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ActivationDate
          </Typography>
          <Typography variant="body1">
            {(entity.activationDate ? (entity.activationDate instanceof Date ? entity.activationDate.toLocaleDateString() : new Date(entity.activationDate).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsLost
          </Typography>
          <Typography variant="body1">
            {entity.isLost ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ReportedLostWhen
          </Typography>
          <Typography variant="body1">
            {(entity.reportedLostWhen ? (entity.reportedLostWhen instanceof Date ? entity.reportedLostWhen.toLocaleDateString() : new Date(entity.reportedLostWhen).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LastBalance
          </Typography>
          <Typography variant="body1">
            {entity.lastBalance?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LastBalanceTime
          </Typography>
          <Typography variant="body1">
            {(entity.lastBalanceTime ? (entity.lastBalanceTime instanceof Date ? entity.lastBalanceTime.toLocaleDateString() : new Date(entity.lastBalanceTime).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmStatusAtCardIssuer
          </Typography>
          <Typography variant="body1">
            {entity.enmStatusAtCardIssuer}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoadsTodayAggregate
          </Typography>
          <Typography variant="body1">
            {entity.loadsTodayAggregate}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoadsThisMonthAggregate
          </Typography>
          <Typography variant="body1">
            {entity.loadsThisMonthAggregate}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AmountLoadedTodayAggregate
          </Typography>
          <Typography variant="body1">
            {entity.amountLoadedTodayAggregate?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AmountLoadedThisMonthAggregate
          </Typography>
          <Typography variant="body1">
            {entity.amountLoadedThisMonthAggregate?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmStatusAtICP
          </Typography>
          <Typography variant="body1">
            {entity.enmStatusAtICP}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenStatusAtICPChanged
          </Typography>
          <Typography variant="body1">
            {(entity.whenStatusAtICPChanged ? (entity.whenStatusAtICPChanged instanceof Date ? entity.whenStatusAtICPChanged.toLocaleDateString() : new Date(entity.whenStatusAtICPChanged).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            StatusAtICPChangedBy
          </Typography>
          <Typography variant="body1">
            {entity.statusAtICPChangedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Comment
          </Typography>
          <Typography variant="body1">
            {entity.comment}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? (entity.deletedOn instanceof Date ? entity.deletedOn.toLocaleDateString() : new Date(entity.deletedOn).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PreviousDistributorID
          </Typography>
          <Typography variant="body1">
            {entity.previousDistributorID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EmbossedText
          </Typography>
          <Typography variant="body1">
            {entity.embossedText}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LogCellPhone
          </Typography>
          <Typography variant="body1">
            {entity.logCellPhone}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AmountLoadedTotalAggregate
          </Typography>
          <Typography variant="body1">
            {entity.amountLoadedTotalAggregate?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoadsTotalAggregate
          </Typography>
          <Typography variant="body1">
            {entity.loadsTotalAggregate}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsCashWithdrawalAllowed
          </Typography>
          <Typography variant="body1">
            {entity.isCashWithdrawalAllowed ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LastMonthlyFeeDatePaid
          </Typography>
          <Typography variant="body1">
            {(entity.lastMonthlyFeeDatePaid ? (entity.lastMonthlyFeeDatePaid instanceof Date ? entity.lastMonthlyFeeDatePaid.toLocaleDateString() : new Date(entity.lastMonthlyFeeDatePaid).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardHolderName
          </Typography>
          <Typography variant="body1">
            {entity.cardHolderName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardHolderPhoneNumberOrEmail
          </Typography>
          <Typography variant="body1">
            {entity.cardHolderPhoneNumberOrEmail}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardHolderIdentifier
          </Typography>
          <Typography variant="body1">
            {entity.cardHolderIdentifier}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmIsOnTerrorList
          </Typography>
          <Typography variant="body1">
            {entity.enmIsOnTerrorList}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmIsOnTerrorListChangedBy
          </Typography>
          <Typography variant="body1">
            {entity.enmIsOnTerrorListChangedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TerrorListUpdatedOn
          </Typography>
          <Typography variant="body1">
            {(entity.terrorListUpdatedOn ? (entity.terrorListUpdatedOn instanceof Date ? entity.terrorListUpdatedOn.toLocaleDateString() : new Date(entity.terrorListUpdatedOn).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            BackofficeTransactions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={backofficeTransactions || []}
              columns={[
                { field: 'tableNameBackofficeTransaction', headerName: 'TableNameBackofficeTransaction', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'distributorID', headerName: 'DistributorID', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'transactionLoadID', headerName: 'TransactionLoadID', width: 100 },
              ]}
              loading={backofficeTransactionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            CardActivations
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={cardActivations || []}
              columns={[
                { field: 'tableNameCardActivation', headerName: 'TableNameCardActivation', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'distributorID', headerName: 'DistributorID', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'cardID', headerName: 'CardID', width: 100 },
              ]}
              loading={cardActivationsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            CustomerCardTransactions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={customerCardTransactions || []}
              columns={[
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'fileName', headerName: 'FileName', width: 150 },
                { field: 'rowNumberInFile', headerName: 'RowNumberInFile', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'cardID', headerName: 'CardID', width: 100 },
              ]}
              loading={customerCardTransactionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            ImportTransactionLoadRows
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={importTransactionLoadRows || []}
              columns={[
                { field: 'tableNameImportTransactionLoadRow', headerName: 'TableNameImportTransactionLoadRow', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'importTransactionLoadID', headerName: 'ImportTransactionLoadID', width: 100 },
                { field: 'customerPhoneNumber', headerName: 'CustomerPhoneNumber', width: 150 },
                { field: 'enmCurrency', headerName: 'EnmCurrency', width: 150 },
              ]}
              loading={importTransactionLoadRowsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            OnlineApprovals
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={onlineApprovals || []}
              columns={[
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'cid', headerName: 'Cid', width: 150 },
                { field: 'orgCode', headerName: 'OrgCode', width: 150 },
                { field: 'cardID', headerName: 'CardID', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
              ]}
              loading={onlineApprovalsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            RequestedTransactions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={requestedTransactions || []}
              columns={[
                { field: 'tableNameRequestedTransaction', headerName: 'TableNameRequestedTransaction', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'initiatingCustomerID', headerName: 'InitiatingCustomerID', width: 100 },
                { field: 'targetCustomerID', headerName: 'TargetCustomerID', width: 100 },
                { field: 'targetCustomerEmailOrPhone', headerName: 'TargetCustomerEmailOrPhone', width: 150 },
              ]}
              loading={requestedTransactionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            TransactionLoads
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={transactionLoads || []}
              columns={[
                { field: 'tableNameTransactionLoad', headerName: 'TableNameTransactionLoad', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'userID', headerName: 'UserID', width: 100 },
                { field: 'distributorID', headerName: 'DistributorID', width: 100 },
                { field: 'enmLoadType', headerName: 'EnmLoadType', width: 150 },
              ]}
              loading={transactionLoadsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>
    </Box>
  );
};


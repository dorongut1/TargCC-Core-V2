// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Card
//     Generated: 2025-12-15 22:18:48
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCard, useCreateCard, useUpdateCard } from '../../hooks/useCard';
import type { CreateCardRequest, UpdateCardRequest } from '../../types/Card.types';


export const CardForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const cardId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!cardId;

  const { data: existingCard, isPending: isLoadingEntity } = useCard(cardId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCardRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCard) {
      reset(existingCard);
    }
  }, [existingCard, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCard();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCard();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCardRequest) => {
    if (isEdit && cardId) {
      updateEntity(
        { id: cardId, data: data as UpdateCardRequest },
        { onSuccess: () => navigate('/cards') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/cards') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCard')} />}
        label="TableNameCard"
      />

      <TextField
        label="CardClubID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardClubID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardClubID}
        helperText={errors.cardClubID?.message}
      />

      <TextField
        label="Cid"
        type="text"
        fullWidth
        margin="normal"
        {...register('cid', { required: 'Cid is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.cid}
        helperText={errors.cid?.message}
      />

      <TextField
        label="CardLast4Numbers"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardLast4Numbers', { required: 'CardLast4Numbers is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.cardLast4Numbers}
        helperText={errors.cardLast4Numbers?.message}
      />

      <TextField
        label="Expiry"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('expiry', { required: 'Expiry is required', maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.expiry}
        helperText={errors.expiry?.message}
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="EnmCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCurrency', { required: 'EnmCurrency is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.enmCurrency}
        helperText={errors.enmCurrency?.message}
      />

      <TextField
        label="OwningDistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('owningDistributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.owningDistributorID}
        helperText={errors.owningDistributorID?.message}
      />

      <TextField
        label="CardOrderFromIssuerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardOrderFromIssuerID', { required: 'CardOrderFromIssuerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardOrderFromIssuerID}
        helperText={errors.cardOrderFromIssuerID?.message}
      />

      <TextField
        label="CardOrderByDistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardOrderByDistributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardOrderByDistributorID}
        helperText={errors.cardOrderByDistributorID?.message}
      />

      <TextField
        label="ActivationDate"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('activationDate', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.activationDate}
        helperText={errors.activationDate?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isLost')} />}
        label="IsLost"
      />

      <TextField
        label="ReportedLostWhen"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('reportedLostWhen', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.reportedLostWhen}
        helperText={errors.reportedLostWhen?.message}
      />

      <TextField
        label="LastBalance"
        type="number"
        fullWidth
        margin="normal"
        {...register('lastBalance', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.lastBalance}
        helperText={errors.lastBalance?.message}
      />

      <TextField
        label="LastBalanceTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('lastBalanceTime', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.lastBalanceTime}
        helperText={errors.lastBalanceTime?.message}
      />

      <TextField
        label="EnmStatusAtCardIssuer"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmStatusAtCardIssuer', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmStatusAtCardIssuer}
        helperText={errors.enmStatusAtCardIssuer?.message}
      />

      <TextField
        label="EnmStatusAtICP"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmStatusAtICP', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmStatusAtICP}
        helperText={errors.enmStatusAtICP?.message}
      />

      <TextField
        label="WhenStatusAtICPChanged"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenStatusAtICPChanged', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenStatusAtICPChanged}
        helperText={errors.whenStatusAtICPChanged?.message}
      />

      <TextField
        label="StatusAtICPChangedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('statusAtICPChangedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.statusAtICPChangedBy}
        helperText={errors.statusAtICPChangedBy?.message}
      />

      <TextField
        label="Comment"
        fullWidth
        margin="normal"
        {...register('comment', {  })}
        error={!!errors.comment}
        helperText={errors.comment?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="PreviousDistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('previousDistributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.previousDistributorID}
        helperText={errors.previousDistributorID?.message}
      />

      <TextField
        label="EmbossedText"
        type="text"
        fullWidth
        margin="normal"
        {...register('embossedText', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.embossedText}
        helperText={errors.embossedText?.message}
      />

      <TextField
        label="LogCellPhone"
        type="text"
        fullWidth
        margin="normal"
        {...register('logCellPhone', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.logCellPhone}
        helperText={errors.logCellPhone?.message}
      />

      <TextField
        label="IsCashWithdrawalAllowed"
        fullWidth
        margin="normal"
        {...register('isCashWithdrawalAllowed', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.isCashWithdrawalAllowed}
        helperText={errors.isCashWithdrawalAllowed?.message}
      />

      <TextField
        label="LastMonthlyFeeDatePaid"
        fullWidth
        margin="normal"
        {...register('lastMonthlyFeeDatePaid', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.lastMonthlyFeeDatePaid}
        helperText={errors.lastMonthlyFeeDatePaid?.message}
      />

      <TextField
        label="CardHolderName"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardHolderName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.cardHolderName}
        helperText={errors.cardHolderName?.message}
      />

      <TextField
        label="CardHolderPhoneNumberOrEmail"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardHolderPhoneNumberOrEmail', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.cardHolderPhoneNumberOrEmail}
        helperText={errors.cardHolderPhoneNumberOrEmail?.message}
      />

      <TextField
        label="CardHolderIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardHolderIdentifier', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.cardHolderIdentifier}
        helperText={errors.cardHolderIdentifier?.message}
      />

      <TextField
        label="EnmIsOnTerrorList"
        fullWidth
        margin="normal"
        {...register('enmIsOnTerrorList', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmIsOnTerrorList}
        helperText={errors.enmIsOnTerrorList?.message}
      />

      <TextField
        label="TerrorListUpdatedOn"
        fullWidth
        margin="normal"
        {...register('terrorListUpdatedOn', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.terrorListUpdatedOn}
        helperText={errors.terrorListUpdatedOn?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/cards')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


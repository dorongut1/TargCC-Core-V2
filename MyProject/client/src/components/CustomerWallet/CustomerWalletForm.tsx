// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerWallet
//     Generated: 2025-12-17 07:52:23
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCustomerWallet, useCreateCustomerWallet, useUpdateCustomerWallet } from '../../hooks/useCustomerWallet';
import type { CreateCustomerWalletRequest, UpdateCustomerWalletRequest } from '../../types/CustomerWallet.types';


export const CustomerWalletForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const customerWalletId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!customerWalletId;

  const { data: existingCustomerWallet, isPending: isLoadingEntity } = useCustomerWallet(customerWalletId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCustomerWalletRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCustomerWallet) {
      reset(existingCustomerWallet);
    }
  }, [existingCustomerWallet, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCustomerWallet();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCustomerWallet();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCustomerWalletRequest) => {
    if (isEdit && customerWalletId) {
      updateEntity(
        { id: customerWalletId, data: data as UpdateCustomerWalletRequest },
        { onSuccess: () => navigate('/customerWallets') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/customerWallets') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCustomerWallet')} />}
        label="TableNameCustomerWallet"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="WhenExecuted"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenExecuted', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenExecuted}
        helperText={errors.whenExecuted?.message}
      />

      <TextField
        label="EnmTypeWalletTransactionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmTypeWalletTransactionType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmTypeWalletTransactionType}
        helperText={errors.enmTypeWalletTransactionType?.message}
      />

      <TextField
        label="Description"
        type="text"
        fullWidth
        margin="normal"
        {...register('description', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.description}
        helperText={errors.description?.message}
      />

      <TextField
        label="DescriptionInCustLang"
        type="text"
        fullWidth
        margin="normal"
        {...register('descriptionInCustLang', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.descriptionInCustLang}
        helperText={errors.descriptionInCustLang?.message}
      />

      <TextField
        label="Reference"
        type="text"
        fullWidth
        margin="normal"
        {...register('reference', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.reference}
        helperText={errors.reference?.message}
      />

      <TextField
        label="ReferenceTable"
        type="text"
        fullWidth
        margin="normal"
        {...register('referenceTable', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.referenceTable}
        helperText={errors.referenceTable?.message}
      />

      <TextField
        label="ReferenceID"
        type="number"
        fullWidth
        margin="normal"
        {...register('referenceID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.referenceID}
        helperText={errors.referenceID?.message}
      />

      <TextField
        label="Amount"
        type="number"
        fullWidth
        margin="normal"
        {...register('amount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amount}
        helperText={errors.amount?.message}
      />

      <TextField
        label="WhenApproved"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenApproved', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenApproved}
        helperText={errors.whenApproved?.message}
      />

      <TextField
        label="WhoInitiatedID"
        type="number"
        fullWidth
        margin="normal"
        {...register('whoInitiatedID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.whoInitiatedID}
        helperText={errors.whoInitiatedID?.message}
      />

      <TextField
        label="Comments"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 4000, message: 'Maximum length is 4000' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('addedToBackOffice')} />}
        label="AddedToBackOffice"
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="InvoiceURL"
        type="text"
        fullWidth
        margin="normal"
        {...register('invoiceURL', { maxLength: { value: 254, message: 'Maximum length is 254' }, pattern: { value: /^https?:\/\/.+/, message: 'Invalid URL' } })}
        error={!!errors.invoiceURL}
        helperText={errors.invoiceURL?.message}
      />

      <TextField
        label="RequestedTransactionID"
        type="number"
        fullWidth
        margin="normal"
        {...register('requestedTransactionID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.requestedTransactionID}
        helperText={errors.requestedTransactionID?.message}
      />

      <TextField
        label="CounterpartCustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('counterpartCustomerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.counterpartCustomerID}
        helperText={errors.counterpartCustomerID?.message}
      />

      <TextField
        label="TransferText"
        type="text"
        fullWidth
        margin="normal"
        {...register('transferText', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.transferText}
        helperText={errors.transferText?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/customerWallets')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


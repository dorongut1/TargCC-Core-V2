// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerWallet
//     Generated: 2025-12-11 11:52:13
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid, GridColDef } from '@mui/x-data-grid';
import { useCustomerWallet, useDeleteCustomerWallet } from '../../hooks/useCustomerWallet';
import { useCustomerWalletBackofficeTransactions } from '../../hooks/useCustomerWallet';
import { useCustomerWalletExternalTransactions } from '../../hooks/useCustomerWallet';
import { useCustomerWalletRequestedTransactionFees } from '../../hooks/useCustomerWallet';
import { useCustomerWalletTransactionBits } from '../../hooks/useCustomerWallet';
import { useCustomerWalletTransactionCreditCards } from '../../hooks/useCustomerWallet';
import type { CustomerWallet } from '../../types/CustomerWallet.types';
import type { BackofficeTransaction } from '../../types/BackofficeTransaction.types';
import type { ExternalTransaction } from '../../types/ExternalTransaction.types';
import type { RequestedTransactionFee } from '../../types/RequestedTransactionFee.types';
import type { TransactionBit } from '../../types/TransactionBit.types';
import type { TransactionCreditCard } from '../../types/TransactionCreditCard.types';


export const CustomerWalletDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useCustomerWallet(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteCustomerWallet();
  const { data: backofficeTransactions, isLoading: backofficeTransactionsLoading } = useCustomerWalletBackofficeTransactions(id ? parseInt(id, 10) : null);
  const { data: externalTransactions, isLoading: externalTransactionsLoading } = useCustomerWalletExternalTransactions(id ? parseInt(id, 10) : null);
  const { data: requestedTransactionFees, isLoading: requestedTransactionFeesLoading } = useCustomerWalletRequestedTransactionFees(id ? parseInt(id, 10) : null);
  const { data: transactionBits, isLoading: transactionBitsLoading } = useCustomerWalletTransactionBits(id ? parseInt(id, 10) : null);
  const { data: transactionCreditCards, isLoading: transactionCreditCardsLoading } = useCustomerWalletTransactionCreditCards(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/customerWallets'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load customerWallet</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/customerWallets')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/customerWallets/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            CustomerWallet Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCustomerWallet
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCustomerWallet ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerID
          </Typography>
          <Typography variant="body1">
            {entity.customerID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenExecuted
          </Typography>
          <Typography variant="body1">
            {new Date(entity.whenExecuted).toLocaleDateString()}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmTypeWalletTransactionType
          </Typography>
          <Typography variant="body1">
            {entity.enmTypeWalletTransactionType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Description
          </Typography>
          <Typography variant="body1">
            {entity.description}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DescriptionInCustLang
          </Typography>
          <Typography variant="body1">
            {entity.descriptionInCustLang}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Reference
          </Typography>
          <Typography variant="body1">
            {entity.reference}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ReferenceTable
          </Typography>
          <Typography variant="body1">
            {entity.referenceTable}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ReferenceID
          </Typography>
          <Typography variant="body1">
            {entity.referenceID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Amount
          </Typography>
          <Typography variant="body1">
            {entity.amount?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Balance
          </Typography>
          <Typography variant="body1">
            {entity.balance?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenApproved
          </Typography>
          <Typography variant="body1">
            {new Date(entity.whenApproved).toLocaleDateString()}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhoInitiatedID
          </Typography>
          <Typography variant="body1">
            {entity.whoInitiatedID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Comments
          </Typography>
          <Typography variant="body1">
            {entity.comments}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AddedToBackOffice
          </Typography>
          <Typography variant="body1">
            {entity.addedToBackOffice ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            InvoiceURL
          </Typography>
          <Typography variant="body1">
            {entity.invoiceURL}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RequestedTransactionID
          </Typography>
          <Typography variant="body1">
            {entity.requestedTransactionID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CounterpartCustomerID
          </Typography>
          <Typography variant="body1">
            {entity.counterpartCustomerID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TransferText
          </Typography>
          <Typography variant="body1">
            {entity.transferText}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            BackofficeTransactions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={backofficeTransactions || []}
              columns={[
                { field: 'tableNameBackofficeTransaction', headerName: 'TableNameBackofficeTransaction', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'distributorID', headerName: 'DistributorID', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'transactionLoadID', headerName: 'TransactionLoadID', width: 100 },
              ]}
              loading={backofficeTransactionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            ExternalTransactions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={externalTransactions || []}
              columns={[
                { field: 'tableNameExternalTransaction', headerName: 'TableNameExternalTransaction', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'enmExternalService', headerName: 'EnmExternalService', width: 150 },
                { field: 'whenInitialized', headerName: 'WhenInitialized', width: 150 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
              ]}
              loading={externalTransactionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            RequestedTransactionFees
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={requestedTransactionFees || []}
              columns={[
                { field: 'tableNameRequestedTransactionFee', headerName: 'TableNameRequestedTransactionFee', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'requestedTransactionID', headerName: 'RequestedTransactionID', width: 100 },
                { field: 'enmTypeFeeType', headerName: 'EnmTypeFeeType', width: 150 },
                { field: 'enmPayer', headerName: 'EnmPayer', width: 150 },
              ]}
              loading={requestedTransactionFeesLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            TransactionBits
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={transactionBits || []}
              columns={[
                { field: 'tableNameTransactionBit', headerName: 'TableNameTransactionBit', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'uniqueIdentifier', headerName: 'UniqueIdentifier', width: 150 },
                { field: 'requestedTransactionID', headerName: 'RequestedTransactionID', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
              ]}
              loading={transactionBitsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            TransactionCreditCards
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={transactionCreditCards || []}
              columns={[
                { field: 'tableNameTransactionCreditCard', headerName: 'TableNameTransactionCreditCard', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'uniqueIdentifier', headerName: 'UniqueIdentifier', width: 150 },
                { field: 'whenInitialized', headerName: 'WhenInitialized', width: 150 },
                { field: 'whenValidated', headerName: 'WhenValidated', width: 150 },
              ]}
              loading={transactionCreditCardsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>
    </Box>
  );
};


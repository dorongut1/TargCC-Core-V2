// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_LoggedLogin
//     Generated: 2025-12-17 09:08:22
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useLoggedLogin, useDeleteLoggedLogin } from '../../hooks/useLoggedLogin';
import { useLoggedLoginLoggedAlerts } from '../../hooks/useLoggedLogin';
import { useLoggedLoginLoggedRequests } from '../../hooks/useLoggedLogin';


export const LoggedLoginDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useLoggedLogin(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteLoggedLogin();
  const { data: loggedAlerts, isLoading: loggedAlertsLoading } = useLoggedLoginLoggedAlerts(id ? parseInt(id, 10) : null);
  const { data: loggedRequests, isLoading: loggedRequestsLoading } = useLoggedLoginLoggedRequests(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/loggedLogins'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load loggedLogin</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/loggedLogins')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/loggedLogins/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            LoggedLogin Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UserName
          </Typography>
          <Typography variant="body1">
            {entity.userName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UserFullName
          </Typography>
          <Typography variant="body1">
            {entity.userFullName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TimeLoggedIn
          </Typography>
          <Typography variant="body1">
            {(entity.timeLoggedIn ? new Date(entity.timeLoggedIn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ApplicationName
          </Typography>
          <Typography variant="body1">
            {entity.applicationName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpUserIdentityType
          </Typography>
          <Typography variant="body1">
            {entity.lkpUserIdentityType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpUserIdentityTypeName
          </Typography>
          <Typography variant="body1">
            {entity.lkpUserIdentityTypeName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Roles
          </Typography>
          <Typography variant="body1">
            {entity.roles}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TimeLoggedOut
          </Typography>
          <Typography variant="body1">
            {(entity.timeLoggedOut ? new Date(entity.timeLoggedOut).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoginFaultNumber
          </Typography>
          <Typography variant="body1">
            {entity.loginFaultNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnvironmentUserName
          </Typography>
          <Typography variant="body1">
            {entity.environmentUserName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnvironmentMachineName
          </Typography>
          <Typography variant="body1">
            {entity.environmentMachineName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnvironmentUserDomainName
          </Typography>
          <Typography variant="body1">
            {entity.environmentUserDomainName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DnsGetHostName
          </Typography>
          <Typography variant="body1">
            {entity.dnsGetHostName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AddressList
          </Typography>
          <Typography variant="body1">
            {entity.addressList}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ComputerMACAddress
          </Typography>
          <Typography variant="body1">
            {entity.computerMACAddress}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SystemDiskVolumeSerialNo
          </Typography>
          <Typography variant="body1">
            {entity.systemDiskVolumeSerialNo}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LocalTime
          </Typography>
          <Typography variant="body1">
            {(entity.localTime ? new Date(entity.localTime).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            GmtTime
          </Typography>
          <Typography variant="body1">
            {(entity.gmtTime ? new Date(entity.gmtTime).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AccessingComputerDetails
          </Typography>
          <Typography variant="body1">
            {entity.accessingComputerDetails}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UICulture
          </Typography>
          <Typography variant="body1">
            {entity.uICulture}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TotalPhysicalMemoryKb
          </Typography>
          <Typography variant="body1">
            {entity.totalPhysicalMemoryKb}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AvailablePhysicalMemoryKb
          </Typography>
          <Typography variant="body1">
            {entity.availablePhysicalMemoryKb}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ApplicationVersion
          </Typography>
          <Typography variant="body1">
            {entity.applicationVersion}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            OriginatingIP
          </Typography>
          <Typography variant="body1">
            {entity.originatingIP}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmLanguage
          </Typography>
          <Typography variant="body1">
            {entity.enmLanguage}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            HostingAssembly
          </Typography>
          <Typography variant="body1">
            {entity.hostingAssembly}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            OriginatingCountry
          </Typography>
          <Typography variant="body1">
            {entity.originatingCountry}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DateLoggedIn
          </Typography>
          <Typography variant="body1">
            {(entity.dateLoggedIn ? new Date(entity.dateLoggedIn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            MonthLoggedIn
          </Typography>
          <Typography variant="body1">
            {(entity.monthLoggedIn ? new Date(entity.monthLoggedIn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ClientReportedIP
          </Typography>
          <Typography variant="body1">
            {entity.clientReportedIP}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ClientReportedCountry
          </Typography>
          <Typography variant="body1">
            {entity.clientReportedCountry}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IPAdditionalDetails
          </Typography>
          <Typography variant="body1">
            {entity.iPAdditionalDetails}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            LoggedAlerts
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={loggedAlerts || []}
              columns={[
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'timeOccurred', headerName: 'TimeOccurred', width: 150 },
                { field: 'faultNumber', headerName: 'FaultNumber', width: 100 },
                { field: 'systemName', headerName: 'SystemName', width: 150 },
                { field: 'callingApplication', headerName: 'CallingApplication', width: 150 },
              ]}
              loading={loggedAlertsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            LoggedRequests
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={loggedRequests || []}
              columns={[
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'cLoggedLoginID', headerName: 'CLoggedLoginID', width: 100 },
                { field: 'timeAccessed', headerName: 'TimeAccessed', width: 150 },
                { field: 'cUserID', headerName: 'CUserID', width: 100 },
                { field: 'callingFunctionWithinApplication', headerName: 'CallingFunctionWithinApplication', width: 150 },
              ]}
              loading={loggedRequestsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_UserPermission
//     Generated: 2025-12-16 09:50:43
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useUserPermission, useCreateUserPermission, useUpdateUserPermission } from '../../hooks/useUserPermission';
import type { CreateUserPermissionRequest, UpdateUserPermissionRequest } from '../../types/UserPermission.types';


export const UserPermissionForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const userPermissionId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!userPermissionId;

  const { data: existingUserPermission, isPending: isLoadingEntity } = useUserPermission(userPermissionId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateUserPermissionRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingUserPermission) {
      reset(existingUserPermission);
    }
  }, [existingUserPermission, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateUserPermission();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateUserPermission();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateUserPermissionRequest) => {
    if (isEdit && userPermissionId) {
      updateEntity(
        { id: userPermissionId, data: data as UpdateUserPermissionRequest },
        { onSuccess: () => navigate('/userPermissions') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/userPermissions') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCUserPermission')} />}
        label="TableNameCUserPermission"
      />

      <TextField
        label="CUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cUserID}
        helperText={errors.cUserID?.message}
      />

      <TextField
        label="ComputerIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('computerIdentifier', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.computerIdentifier}
        helperText={errors.computerIdentifier?.message}
      />

      <TextField
        label="ApplicationName"
        type="text"
        fullWidth
        margin="normal"
        {...register('applicationName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.applicationName}
        helperText={errors.applicationName?.message}
      />

      <TextField
        label="ComputerName"
        type="text"
        fullWidth
        margin="normal"
        {...register('computerName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.computerName}
        helperText={errors.computerName?.message}
      />

      <TextField
        label="ExternalIP"
        type="text"
        fullWidth
        margin="normal"
        {...register('externalIP', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.externalIP}
        helperText={errors.externalIP?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('hasPermission')} />}
        label="HasPermission"
      />

      <TextField
        label="Comments"
        type="text"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 400, message: 'Maximum length is 400' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="LastAccessTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('lastAccessTime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lastAccessTime}
        helperText={errors.lastAccessTime?.message}
      />

      <TextField
        label="LoggedLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('loggedLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.loggedLoginID}
        helperText={errors.loggedLoginID?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/userPermissions')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


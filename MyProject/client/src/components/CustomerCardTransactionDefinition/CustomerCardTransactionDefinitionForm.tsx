// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerCardTransactionDefinition
//     Generated: 2025-12-17 20:20:32
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCustomerCardTransactionDefinition, useCreateCustomerCardTransactionDefinition, useUpdateCustomerCardTransactionDefinition } from '../../hooks/useCustomerCardTransactionDefinition';
import type { CreateCustomerCardTransactionDefinitionRequest, UpdateCustomerCardTransactionDefinitionRequest } from '../../types/CustomerCardTransactionDefinition.types';


export const CustomerCardTransactionDefinitionForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const customerCardTransactionDefinitionId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!customerCardTransactionDefinitionId;

  const { data: existingCustomerCardTransactionDefinition, isPending: isLoadingEntity } = useCustomerCardTransactionDefinition(customerCardTransactionDefinitionId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCustomerCardTransactionDefinitionRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCustomerCardTransactionDefinition) {
      reset(existingCustomerCardTransactionDefinition);
    }
  }, [existingCustomerCardTransactionDefinition, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCustomerCardTransactionDefinition();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCustomerCardTransactionDefinition();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCustomerCardTransactionDefinitionRequest) => {
    if (isEdit && customerCardTransactionDefinitionId) {
      updateEntity(
        { id: customerCardTransactionDefinitionId, data: data as UpdateCustomerCardTransactionDefinitionRequest },
        { onSuccess: () => navigate('/customerCardTransactionDefinitions') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/customerCardTransactionDefinitions') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCustomerCardTransactionDefinition')} />}
        label="TableNameCustomerCardTransactionDefinition"
      />

      <TextField
        label="LeumiCardTransactionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('leumiCardTransactionType', { required: 'LeumiCardTransactionType is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.leumiCardTransactionType}
        helperText={errors.leumiCardTransactionType?.message}
      />

      <TextField
        label="EnmUpayCardTransactionTypeCustomerTransactionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmUpayCardTransactionTypeCustomerTransactionType', { required: 'EnmUpayCardTransactionTypeCustomerTransactionType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmUpayCardTransactionTypeCustomerTransactionType}
        helperText={errors.enmUpayCardTransactionTypeCustomerTransactionType?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/customerCardTransactionDefinitions')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


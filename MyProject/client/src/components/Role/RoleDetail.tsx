// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Role
//     Generated: 2025-12-18 08:27:22
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useRole, useDeleteRole } from '../../hooks/useRole';
import { useRolePermissions } from '../../hooks/useRole';
import { useRoleRoles } from '../../hooks/useRole';
import { useRoleUsers } from '../../hooks/useRole';


export const RoleDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useRole(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteRole();
  const { data: permissions, isLoading: permissionsLoading } = useRolePermissions(id ? parseInt(id, 10) : null);
  const { data: roles, isLoading: rolesLoading } = useRoleRoles(id ? parseInt(id, 10) : null);
  const { data: users, isLoading: usersLoading } = useRoleUsers(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/roles'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load role</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/roles')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/roles/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            Role Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCRole
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCRole ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Name
          </Typography>
          <Typography variant="body1">
            {entity.name}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? new Date(entity.deletedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            BaseRoleID
          </Typography>
          <Typography variant="body1">
            {entity.baseRoleID}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            Permissions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={permissions || []}
              columns={[
                { field: 'tableNameCPermission', headerName: 'TableNameCPermission', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'cRoleID', headerName: 'CRoleID', width: 100 },
                { field: 'cProcessID', headerName: 'CProcessID', width: 100 },
                { field: 'canDo', headerName: 'CanDo', width: 150 },
              ]}
              loading={permissionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            Roles
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={roles || []}
              columns={[
                { field: 'tableNameCRole', headerName: 'TableNameCRole', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'name', headerName: 'Name', width: 150 },
                { field: 'deletedBy', headerName: 'DeletedBy', width: 150 },
                { field: 'deletedOn', headerName: 'DeletedOn', width: 150 },
              ]}
              loading={rolesLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            Users
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={users || []}
              columns={[
                { field: 'tableNameCUser', headerName: 'TableNameCUser', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'userName', headerName: 'UserName', width: 150 },
                { field: 'lastName', headerName: 'LastName', width: 150 },
                { field: 'firstName', headerName: 'FirstName', width: 150 },
              ]}
              loading={usersLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


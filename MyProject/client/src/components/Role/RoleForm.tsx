// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Role
//     Generated: 2025-12-16 09:50:43
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useRole, useCreateRole, useUpdateRole } from '../../hooks/useRole';
import type { CreateRoleRequest, UpdateRoleRequest } from '../../types/Role.types';


export const RoleForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const roleId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!roleId;

  const { data: existingRole, isPending: isLoadingEntity } = useRole(roleId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateRoleRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingRole) {
      reset(existingRole);
    }
  }, [existingRole, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateRole();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateRole();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateRoleRequest) => {
    if (isEdit && roleId) {
      updateEntity(
        { id: roleId, data: data as UpdateRoleRequest },
        { onSuccess: () => navigate('/roles') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/roles') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCRole')} />}
        label="TableNameCRole"
      />

      <TextField
        label="Name"
        type="text"
        fullWidth
        margin="normal"
        {...register('name', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.name}
        helperText={errors.name?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="BaseRoleID"
        type="number"
        fullWidth
        margin="normal"
        {...register('baseRoleID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.baseRoleID}
        helperText={errors.baseRoleID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/roles')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: EntityRiskColor
//     Generated: 2025-12-17 09:34:37
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useEntityRiskColor, useCreateEntityRiskColor, useUpdateEntityRiskColor } from '../../hooks/useEntityRiskColor';
import type { CreateEntityRiskColorRequest, UpdateEntityRiskColorRequest } from '../../types/EntityRiskColor.types';


export const EntityRiskColorForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const entityRiskColorId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!entityRiskColorId;

  const { data: existingEntityRiskColor, isPending: isLoadingEntity } = useEntityRiskColor(entityRiskColorId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateEntityRiskColorRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingEntityRiskColor) {
      reset(existingEntityRiskColor);
    }
  }, [existingEntityRiskColor, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateEntityRiskColor();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateEntityRiskColor();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateEntityRiskColorRequest) => {
    if (isEdit && entityRiskColorId) {
      updateEntity(
        { id: entityRiskColorId, data: data as UpdateEntityRiskColorRequest },
        { onSuccess: () => navigate('/entityRiskColors') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/entityRiskColors') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameEntityRiskColor')} />}
        label="TableNameEntityRiskColor"
      />

      <TextField
        label="EnmEntityType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmEntityType', { required: 'EnmEntityType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmEntityType}
        helperText={errors.enmEntityType?.message}
      />

      <TextField
        label="EntityID"
        type="number"
        fullWidth
        margin="normal"
        {...register('entityID', { required: 'EntityID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.entityID}
        helperText={errors.entityID?.message}
      />

      <TextField
        label="EnmCurrentColor"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCurrentColor', { required: 'EnmCurrentColor is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCurrentColor}
        helperText={errors.enmCurrentColor?.message}
      />

      <TextField
        label="PreviousColorID"
        type="text"
        fullWidth
        margin="normal"
        {...register('previousColorID', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.previousColorID}
        helperText={errors.previousColorID?.message}
      />

      <TextField
        label="LastUpdated"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('lastUpdated', { required: 'LastUpdated is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lastUpdated}
        helperText={errors.lastUpdated?.message}
      />

      <TextField
        label="UpdatedByRuleID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatedByRuleID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatedByRuleID}
        helperText={errors.updatedByRuleID?.message}
      />

      <TextField
        label="UpdatedByUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatedByUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatedByUserID}
        helperText={errors.updatedByUserID?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/entityRiskColors')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


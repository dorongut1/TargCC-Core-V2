// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_TableFields
//     Generated: 2025-12-11 21:58:09
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useTableFields, useCreateTableFields, useUpdateTableFields } from '../../hooks/useTableFields';
import type { CreateTableFieldsRequest, UpdateTableFieldsRequest } from '../../types/TableFields.types';


export const TableFieldsForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const tableFieldsId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!tableFieldsId;

  const { data: existingTableFields, isPending: isLoadingEntity } = useTableFields(tableFieldsId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateTableFieldsRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingTableFields) {
      reset(existingTableFields);
    }
  }, [existingTableFields, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateTableFields();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateTableFields();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateTableFieldsRequest) => {
    if (isEdit && tableFieldsId) {
      updateEntity(
        { id: tableFieldsId, data: data as UpdateTableFieldsRequest },
        { onSuccess: () => navigate('/tableFieldss') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/tableFieldss') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="GroupName"
        type="text"
        fullWidth
        margin="normal"
        {...register('groupName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.groupName}
        helperText={errors.groupName?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('showInWinF')} />}
        label="ShowInWinF"
      />

      <TextField
        label="OrdinalOnScreen"
        type="number"
        fullWidth
        margin="normal"
        {...register('ordinalOnScreen', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.ordinalOnScreen}
        helperText={errors.ordinalOnScreen?.message}
      />

      <TextField
        label="TableName"
        type="text"
        fullWidth
        margin="normal"
        {...register('tableName', { required: 'TableName is required', maxLength: { value: 256, message: 'Maximum length is 256' } })}
        error={!!errors.tableName}
        helperText={errors.tableName?.message}
      />

      <TextField
        label="TableType"
        type="text"
        fullWidth
        margin="normal"
        {...register('tableType', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.tableType}
        helperText={errors.tableType?.message}
      />

      <TextField
        label="ColumnName"
        type="text"
        fullWidth
        margin="normal"
        {...register('columnName', { maxLength: { value: 256, message: 'Maximum length is 256' } })}
        error={!!errors.columnName}
        helperText={errors.columnName?.message}
      />

      <TextField
        label="DataType"
        type="text"
        fullWidth
        margin="normal"
        {...register('dataType', { maxLength: { value: 256, message: 'Maximum length is 256' } })}
        error={!!errors.dataType}
        helperText={errors.dataType?.message}
      />

      <TextField
        label="CharacterMaximumLength"
        type="number"
        fullWidth
        margin="normal"
        {...register('characterMaximumLength', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.characterMaximumLength}
        helperText={errors.characterMaximumLength?.message}
      />

      <TextField
        label="NumericPrecision"
        type="number"
        fullWidth
        margin="normal"
        {...register('numericPrecision', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.numericPrecision}
        helperText={errors.numericPrecision?.message}
      />

      <TextField
        label="ColumnDefault"
        type="text"
        fullWidth
        margin="normal"
        {...register('columnDefault', { maxLength: { value: 2000, message: 'Maximum length is 2000' } })}
        error={!!errors.columnDefault}
        helperText={errors.columnDefault?.message}
      />

      <TextField
        label="IsNullable"
        type="text"
        fullWidth
        margin="normal"
        {...register('isNullable', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.isNullable}
        helperText={errors.isNullable?.message}
      />

      <TextField
        label="Ordinalindatabase"
        type="number"
        fullWidth
        margin="normal"
        {...register('ordinalindatabase', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.ordinalindatabase}
        helperText={errors.ordinalindatabase?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('useForCustomWinFormProject')} />}
        label="UseForCustomWinFormProject"
      />

      <TextField
        label="DtoForWebAPI"
        type="text"
        fullWidth
        margin="normal"
        {...register('dtoForWebAPI', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.dtoForWebAPI}
        helperText={errors.dtoForWebAPI?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/tableFieldss')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


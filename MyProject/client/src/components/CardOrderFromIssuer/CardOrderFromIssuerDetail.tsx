// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CardOrderFromIssuer
//     Generated: 2025-12-17 21:49:10
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useCardOrderFromIssuer, useDeleteCardOrderFromIssuer } from '../../hooks/useCardOrderFromIssuer';
import { useCardOrderFromIssuerBackofficeTransactions } from '../../hooks/useCardOrderFromIssuer';
import { useCardOrderFromIssuerCards } from '../../hooks/useCardOrderFromIssuer';


export const CardOrderFromIssuerDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useCardOrderFromIssuer(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteCardOrderFromIssuer();
  const { data: backofficeTransactions, isLoading: backofficeTransactionsLoading } = useCardOrderFromIssuerBackofficeTransactions(id ? parseInt(id, 10) : null);
  const { data: cards, isLoading: cardsLoading } = useCardOrderFromIssuerCards(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/cardOrderFromIssuers'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load cardOrderFromIssuer</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/cardOrderFromIssuers')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/cardOrderFromIssuers/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            CardOrderFromIssuer Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCardOrderFromIssuer
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCardOrderFromIssuer ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            NumberOfCards
          </Typography>
          <Typography variant="body1">
            {entity.numberOfCards}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmCurrency
          </Typography>
          <Typography variant="body1">
            {entity.enmCurrency}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardClubID
          </Typography>
          <Typography variant="body1">
            {entity.cardClubID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EmbossedText
          </Typography>
          <Typography variant="body1">
            {entity.embossedText}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PackageType
          </Typography>
          <Typography variant="body1">
            {entity.packageType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ICPOrderCardsFee
          </Typography>
          <Typography variant="body1">
            {entity.iCPOrderCardsFee?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardIssuerOrderNumber
          </Typography>
          <Typography variant="body1">
            {entity.cardIssuerOrderNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ResponseCodeFromCardIssuer
          </Typography>
          <Typography variant="body1">
            {entity.responseCodeFromCardIssuer}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ResponseDescriptionFromCardIssuer
          </Typography>
          <Typography variant="body1">
            {entity.responseDescriptionFromCardIssuer}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoggedAlertID
          </Typography>
          <Typography variant="body1">
            {entity.loggedAlertID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Comment
          </Typography>
          <Typography variant="body1">
            {entity.comment}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            OrderedByUserID
          </Typography>
          <Typography variant="body1">
            {entity.orderedByUserID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenOrdered
          </Typography>
          <Typography variant="body1">
            {(entity.whenOrdered ? new Date(entity.whenOrdered).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmStatusCardOrderStatus
          </Typography>
          <Typography variant="body1">
            {entity.enmStatusCardOrderStatus}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenReceived
          </Typography>
          <Typography variant="body1">
            {(entity.whenReceived ? new Date(entity.whenReceived).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? new Date(entity.deletedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ResponseCodeOnImport
          </Typography>
          <Typography variant="body1">
            {entity.responseCodeOnImport}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ResponseDescriptionOnImport
          </Typography>
          <Typography variant="body1">
            {entity.responseDescriptionOnImport}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            BackofficeTransactions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={backofficeTransactions || []}
              columns={[
                { field: 'tableNameBackofficeTransaction', headerName: 'TableNameBackofficeTransaction', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'distributorID', headerName: 'DistributorID', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'transactionLoadID', headerName: 'TransactionLoadID', width: 100 },
              ]}
              loading={backofficeTransactionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            Cards
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={cards || []}
              columns={[
                { field: 'tableNameCard', headerName: 'TableNameCard', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'cardClubID', headerName: 'CardClubID', width: 100 },
                { field: 'cid', headerName: 'Cid', width: 150 },
                { field: 'cardLast4Numbers', headerName: 'CardLast4Numbers', width: 150 },
              ]}
              loading={cardsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


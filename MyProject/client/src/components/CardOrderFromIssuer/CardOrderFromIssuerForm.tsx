// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CardOrderFromIssuer
//     Generated: 2025-12-17 14:08:22
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCardOrderFromIssuer, useCreateCardOrderFromIssuer, useUpdateCardOrderFromIssuer } from '../../hooks/useCardOrderFromIssuer';
import type { CreateCardOrderFromIssuerRequest, UpdateCardOrderFromIssuerRequest } from '../../types/CardOrderFromIssuer.types';


export const CardOrderFromIssuerForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const cardOrderFromIssuerId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!cardOrderFromIssuerId;

  const { data: existingCardOrderFromIssuer, isPending: isLoadingEntity } = useCardOrderFromIssuer(cardOrderFromIssuerId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCardOrderFromIssuerRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCardOrderFromIssuer) {
      reset(existingCardOrderFromIssuer);
    }
  }, [existingCardOrderFromIssuer, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCardOrderFromIssuer();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCardOrderFromIssuer();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCardOrderFromIssuerRequest) => {
    if (isEdit && cardOrderFromIssuerId) {
      updateEntity(
        { id: cardOrderFromIssuerId, data: data as UpdateCardOrderFromIssuerRequest },
        { onSuccess: () => navigate('/cardOrderFromIssuers') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/cardOrderFromIssuers') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCardOrderFromIssuer')} />}
        label="TableNameCardOrderFromIssuer"
      />

      <TextField
        label="NumberOfCards"
        type="number"
        fullWidth
        margin="normal"
        {...register('numberOfCards', { required: 'NumberOfCards is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.numberOfCards}
        helperText={errors.numberOfCards?.message}
      />

      <TextField
        label="EnmCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCurrency', { required: 'EnmCurrency is required', maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.enmCurrency}
        helperText={errors.enmCurrency?.message}
      />

      <TextField
        label="CardClubID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardClubID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardClubID}
        helperText={errors.cardClubID?.message}
      />

      <TextField
        label="EmbossedText"
        type="text"
        fullWidth
        margin="normal"
        {...register('embossedText', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.embossedText}
        helperText={errors.embossedText?.message}
      />

      <TextField
        label="PackageType"
        type="text"
        fullWidth
        margin="normal"
        {...register('packageType', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.packageType}
        helperText={errors.packageType?.message}
      />

      <TextField
        label="ICPOrderCardsFee"
        type="number"
        fullWidth
        margin="normal"
        {...register('iCPOrderCardsFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.iCPOrderCardsFee}
        helperText={errors.iCPOrderCardsFee?.message}
      />

      <TextField
        label="CardIssuerOrderNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardIssuerOrderNumber', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.cardIssuerOrderNumber}
        helperText={errors.cardIssuerOrderNumber?.message}
      />

      <TextField
        label="ResponseCodeFromCardIssuer"
        type="text"
        fullWidth
        margin="normal"
        {...register('responseCodeFromCardIssuer', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.responseCodeFromCardIssuer}
        helperText={errors.responseCodeFromCardIssuer?.message}
      />

      <TextField
        label="ResponseDescriptionFromCardIssuer"
        type="text"
        fullWidth
        margin="normal"
        {...register('responseDescriptionFromCardIssuer', {  })}
        error={!!errors.responseDescriptionFromCardIssuer}
        helperText={errors.responseDescriptionFromCardIssuer?.message}
      />

      <TextField
        label="LoggedAlertID"
        type="number"
        fullWidth
        margin="normal"
        {...register('loggedAlertID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.loggedAlertID}
        helperText={errors.loggedAlertID?.message}
      />

      <TextField
        label="Comment"
        fullWidth
        margin="normal"
        {...register('comment', {  })}
        error={!!errors.comment}
        helperText={errors.comment?.message}
      />

      <TextField
        label="OrderedByUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('orderedByUserID', { required: 'OrderedByUserID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.orderedByUserID}
        helperText={errors.orderedByUserID?.message}
      />

      <TextField
        label="WhenOrdered"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenOrdered', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenOrdered}
        helperText={errors.whenOrdered?.message}
      />

      <TextField
        label="EnmStatusCardOrderStatus"
        fullWidth
        margin="normal"
        {...register('enmStatusCardOrderStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmStatusCardOrderStatus}
        helperText={errors.enmStatusCardOrderStatus?.message}
      />

      <TextField
        label="WhenReceived"
        fullWidth
        margin="normal"
        {...register('whenReceived', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenReceived}
        helperText={errors.whenReceived?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="ResponseCodeOnImport"
        type="text"
        fullWidth
        margin="normal"
        {...register('responseCodeOnImport', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.responseCodeOnImport}
        helperText={errors.responseCodeOnImport?.message}
      />

      <TextField
        label="ResponseDescriptionOnImport"
        type="text"
        fullWidth
        margin="normal"
        {...register('responseDescriptionOnImport', {  })}
        error={!!errors.responseDescriptionOnImport}
        helperText={errors.responseDescriptionOnImport?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/cardOrderFromIssuers')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


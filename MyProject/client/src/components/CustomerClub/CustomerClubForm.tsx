// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerClub
//     Generated: 2025-12-11 13:48:46
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useCustomerClub, useCreateCustomerClub, useUpdateCustomerClub } from '../../hooks/useCustomerClub';
import type { CreateCustomerClubRequest, UpdateCustomerClubRequest } from '../../types/CustomerClub.types';


export const CustomerClubForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const customerClubId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!customerClubId;

  const { data: existingCustomerClub, isPending: isLoadingEntity } = useCustomerClub(customerClubId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCustomerClubRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCustomerClub) {
      reset(existingCustomerClub);
    }
  }, [existingCustomerClub, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCustomerClub();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCustomerClub();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCustomerClubRequest) => {
    if (isEdit && customerClubId) {
      updateEntity(
        { id: customerClubId, data: data as UpdateCustomerClubRequest },
        { onSuccess: () => navigate('/customerClubs') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/customerClubs') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCustomerClub')} />}
        label="TableNameCustomerClub"
      />

      <TextField
        label="ClubName"
        type="text"
        fullWidth
        margin="normal"
        {...register('clubName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.clubName}
        helperText={errors.clubName?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isRoot')} />}
        label="IsRoot"
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isForBusinessCustomer')} />}
        label="IsForBusinessCustomer"
      />

      <FormControlLabel
        control={<Checkbox {...register('useForUnassignedCustomer')} />}
        label="UseForUnassignedCustomer"
      />

      <FormControlLabel
        control={<Checkbox {...register('useForUnassignedBusinessCustomer')} />}
        label="UseForUnassignedBusinessCustomer"
      />

      <TextField
        label="AppsFlyerFlag"
        type="text"
        fullWidth
        margin="normal"
        {...register('appsFlyerFlag', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.appsFlyerFlag}
        helperText={errors.appsFlyerFlag?.message}
      />

      <TextField
        label="LocMarketingHeader"
        type="text"
        fullWidth
        margin="normal"
        {...register('locMarketingHeader', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.locMarketingHeader}
        helperText={errors.locMarketingHeader?.message}
      />

      <TextField
        label="LocMarketingDetails"
        type="text"
        fullWidth
        margin="normal"
        {...register('locMarketingDetails', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.locMarketingDetails}
        helperText={errors.locMarketingDetails?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/customerClubs')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


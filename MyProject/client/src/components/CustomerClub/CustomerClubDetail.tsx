// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerClub
//     Generated: 2025-12-16 08:57:49
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useCustomerClub, useDeleteCustomerClub } from '../../hooks/useCustomerClub';
import { useCustomerClubCustomers } from '../../hooks/useCustomerClub';
import { useCustomerClubFeeOverrides } from '../../hooks/useCustomerClub';
import { useCustomerClubLimitationOverrides } from '../../hooks/useCustomerClub';


export const CustomerClubDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useCustomerClub(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteCustomerClub();
  const { data: customers, isLoading: customersLoading } = useCustomerClubCustomers(id ? parseInt(id, 10) : null);
  const { data: feeOverrides, isLoading: feeOverridesLoading } = useCustomerClubFeeOverrides(id ? parseInt(id, 10) : null);
  const { data: limitationOverrides, isLoading: limitationOverridesLoading } = useCustomerClubLimitationOverrides(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/customerClubs'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load customerClub</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/customerClubs')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/customerClubs/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            CustomerClub Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCustomerClub
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCustomerClub ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ClubName
          </Typography>
          <Typography variant="body1">
            {entity.clubName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsRoot
          </Typography>
          <Typography variant="body1">
            {entity.isRoot ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? new Date(entity.deletedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsForBusinessCustomer
          </Typography>
          <Typography variant="body1">
            {entity.isForBusinessCustomer ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UseForUnassignedCustomer
          </Typography>
          <Typography variant="body1">
            {entity.useForUnassignedCustomer ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UseForUnassignedBusinessCustomer
          </Typography>
          <Typography variant="body1">
            {entity.useForUnassignedBusinessCustomer ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AppsFlyerFlag
          </Typography>
          <Typography variant="body1">
            {entity.appsFlyerFlag}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LocMarketingHeader
          </Typography>
          <Typography variant="body1">
            {entity.locMarketingHeader}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LocMarketingDetails
          </Typography>
          <Typography variant="body1">
            {entity.locMarketingDetails}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            Customers
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={customers || []}
              columns={[
                { field: 'tableNameCustomer', headerName: 'TableNameCustomer', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'firstName', headerName: 'FirstName', width: 150 },
                { field: 'lastName', headerName: 'LastName', width: 150 },
                { field: 'firstNameLatin', headerName: 'FirstNameLatin', width: 150 },
              ]}
              loading={customersLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            FeeOverrides
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={feeOverrides || []}
              columns={[
                { field: 'tableNameFeeOverride', headerName: 'TableNameFeeOverride', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'feeID', headerName: 'FeeID', width: 100 },
                { field: 'cardClubID', headerName: 'CardClubID', width: 100 },
                { field: 'distributorID', headerName: 'DistributorID', width: 100 },
              ]}
              loading={feeOverridesLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            LimitationOverrides
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={limitationOverrides || []}
              columns={[
                { field: 'tableNameLimitationOverride', headerName: 'TableNameLimitationOverride', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'limitationID', headerName: 'LimitationID', width: 100 },
                { field: 'cardClubID', headerName: 'CardClubID', width: 100 },
                { field: 'distributorID', headerName: 'DistributorID', width: 100 },
              ]}
              loading={limitationOverridesLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: vwReportMonthlyCardFee
//     Generated: 2025-12-11 13:48:48
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import type { VwReportMonthlyCardFee } from '../../types/VwReportMonthlyCardFee.types';


export const VwReportMonthlyCardFeeDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  // VIEWs are read-only - Detail page not applicable
  const isLoading = false;
  const error = null;
  const entity = null;

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load vwReportMonthlyCardFee</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/vwReportMonthlyCardFees')}
        >
          Back
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            VwReportMonthlyCardFee Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Month
          </Typography>
          <Typography variant="body1">
            {new Date(entity.month).toLocaleDateString()}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerIdentifier
          </Typography>
          <Typography variant="body1">
            {entity.customerIdentifier}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerName
          </Typography>
          <Typography variant="body1">
            {entity.customerName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardCid
          </Typography>
          <Typography variant="body1">
            {entity.cardCid}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardLast4Numbers
          </Typography>
          <Typography variant="body1">
            {entity.cardLast4Numbers}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardCurrency
          </Typography>
          <Typography variant="body1">
            {entity.cardCurrency}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardClubName
          </Typography>
          <Typography variant="body1">
            {entity.cardClubName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardIssuerOrgCode
          </Typography>
          <Typography variant="body1">
            {entity.cardIssuerOrgCode}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            FeeAmount
          </Typography>
          <Typography variant="body1">
            {entity.feeAmount?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenRequested
          </Typography>
          <Typography variant="body1">
            {new Date(entity.whenRequested).toLocaleDateString()}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RequestingUser
          </Typography>
          <Typography variant="body1">
            {entity.requestingUser}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerEmail
          </Typography>
          <Typography variant="body1">
            {entity.customerEmail}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerCellphoneNumber
          </Typography>
          <Typography variant="body1">
            {entity.customerCellphoneNumber}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: ExternalCustomer
//     Generated: 2025-12-15 22:04:10
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useExternalCustomer, useCreateExternalCustomer, useUpdateExternalCustomer } from '../../hooks/useExternalCustomer';
import type { CreateExternalCustomerRequest, UpdateExternalCustomerRequest } from '../../types/ExternalCustomer.types';


export const ExternalCustomerForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const externalCustomerId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!externalCustomerId;

  const { data: existingExternalCustomer, isPending: isLoadingEntity } = useExternalCustomer(externalCustomerId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateExternalCustomerRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingExternalCustomer) {
      reset(existingExternalCustomer);
    }
  }, [existingExternalCustomer, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateExternalCustomer();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateExternalCustomer();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateExternalCustomerRequest) => {
    if (isEdit && externalCustomerId) {
      updateEntity(
        { id: externalCustomerId, data: data as UpdateExternalCustomerRequest },
        { onSuccess: () => navigate('/externalCustomers') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/externalCustomers') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameExternalCustomer')} />}
        label="TableNameExternalCustomer"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="EnmExternalService"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmExternalService', { required: 'EnmExternalService is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmExternalService}
        helperText={errors.enmExternalService?.message}
      />

      <TextField
        label="ExternalCustomerIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('externalCustomerIdentifier', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.externalCustomerIdentifier}
        helperText={errors.externalCustomerIdentifier?.message}
      />

      <TextField
        label="WhenCustomerDetailsUpdatedAtExternalService"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenCustomerDetailsUpdatedAtExternalService', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenCustomerDetailsUpdatedAtExternalService}
        helperText={errors.whenCustomerDetailsUpdatedAtExternalService?.message}
      />

      <TextField
        label="WhenDocumentationRecdFromExternalService"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenDocumentationRecdFromExternalService', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenDocumentationRecdFromExternalService}
        helperText={errors.whenDocumentationRecdFromExternalService?.message}
      />

      <TextField
        label="EnmExternalCustomerStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmExternalCustomerStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmExternalCustomerStatus}
        helperText={errors.enmExternalCustomerStatus?.message}
      />

      <TextField
        label="StatusDescription"
        type="text"
        fullWidth
        margin="normal"
        {...register('statusDescription', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.statusDescription}
        helperText={errors.statusDescription?.message}
      />

      <TextField
        label="LastLoggedAlertID"
        type="number"
        fullWidth
        margin="normal"
        {...register('lastLoggedAlertID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lastLoggedAlertID}
        helperText={errors.lastLoggedAlertID?.message}
      />

      <TextField
        label="Comments"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 500, message: 'Maximum length is 500' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="WhenLastUsed"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenLastUsed', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenLastUsed}
        helperText={errors.whenLastUsed?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/externalCustomers')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


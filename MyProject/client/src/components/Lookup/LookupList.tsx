// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Lookup
//     Generated: 2025-12-16 10:13:17
// </auto-generated>

import React from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import ExcelJS from 'exceljs';
import { DataGrid, GridColDef, GridActionsCellItem, GridToolbarContainer, GridToolbarColumnsButton, GridToolbarDensitySelector, useGridApiRef } from '@mui/x-data-grid';
import { Button, Box, CircularProgress, Alert, TextField, Paper, MenuItem, Typography } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, Add as AddIcon, FileDownload as FileDownloadIcon, Clear as ClearIcon } from '@mui/icons-material';
import { useLookups, useDeleteLookup } from '../../hooks/useLookup';
import type { Lookup } from '../../types/Lookup.types';


export const LookupList: React.FC = () => {
  const navigate = useNavigate();
  const [searchParams, setSearchParams] = useSearchParams();
  const apiRef = useGridApiRef();
  
  // Helper function to format dates as DD/MM/YYYY
  const formatDate = (date: any): string => {
    if (!date) return '';
    const d = new Date(date);
    if (isNaN(d.getTime())) return '';
    const day = d.getDate().toString().padStart(2, '0');
    const month = (d.getMonth() + 1).toString().padStart(2, '0');
    const year = d.getFullYear();
    return `${day}/${month}/${year}`;
  };
  
  // Extract pagination, sorting, and filters from URL
  const page = parseInt(searchParams.get('page') || '1', 10);
  const pageSize = parseInt(searchParams.get('pageSize') || '10', 10);
  const sortBy = searchParams.get('sortBy') || 'id';
  const sortDirection = (searchParams.get('sortDirection') || 'asc') as 'asc' | 'desc';

  // Extract filters from URL
  const filters = React.useMemo(() => {
    const filtersObj: Record<string, string> = {};
    searchParams.forEach((value, key) => {
      if (!['page', 'pageSize', 'sortBy', 'sortDirection'].includes(key)) {
        filtersObj[key] = value;
      }
    });
    return filtersObj;
  }, [searchParams]);

  // Fetch data with server-side options
  const { data, isLoading, error } = useLookups({
    page,
    pageSize,
    sortBy,
    sortDirection,
    filters: Object.keys(filters).length > 0 ? filters : undefined,
  });

  const lookups = data?.items || [];
  const totalCount = data?.totalCount || 0;

  const { mutate: deleteEntity } = useDeleteLookup();

  // URL update handlers
  const updateUrlParam = (key: string, value: string) => {
    const newParams = new URLSearchParams(searchParams);
    if (value && value.trim() !== '') {
      newParams.set(key, value);
    } else {
      newParams.delete(key);
    }
    // Reset to page 1 when filter changes
    if (key !== 'page' && key !== 'pageSize') {
      newParams.set('page', '1');
    }
    setSearchParams(newParams);
  };

  const handlePaginationChange = (newPage: number, newPageSize: number) => {
    const newParams = new URLSearchParams(searchParams);
    newParams.set('page', (newPage + 1).toString()); // MUI DataGrid is 0-based
    newParams.set('pageSize', newPageSize.toString());
    setSearchParams(newParams);
  };

  const handleSortChange = (field: string, direction: 'asc' | 'desc' | null) => {
    const newParams = new URLSearchParams(searchParams);
    if (field && direction) {
      newParams.set('sortBy', field);
      newParams.set('sortDirection', direction);
    } else {
      newParams.delete('sortBy');
      newParams.delete('sortDirection');
    }
    setSearchParams(newParams);
  };

  const handleClearAllFilters = () => {
    // Keep only page and pageSize, remove all filters and sorting
    const newParams = new URLSearchParams();
    newParams.set('page', '1');
    newParams.set('pageSize', pageSize.toString());
    setSearchParams(newParams);
  };

  function CustomToolbar() {
    return (
      <GridToolbarContainer>
        <GridToolbarColumnsButton />
        <GridToolbarDensitySelector />
        <Button
          size="small"
          startIcon={<ClearIcon />}
          onClick={handleClearAllFilters}
        >
          Clear All Filters
        </Button>
      </GridToolbarContainer>
    );
  }

  const handleExportToExcel = async () => {
    // Export the current page data (server-side filtered)
    const visibleRows = lookups || [];

    if (!visibleRows || visibleRows.length === 0) {
      alert('No data to export');
      return;
    }

    // Get visible columns from DataGrid (respects column visibility)
    const visibleColumns = apiRef.current
      ? apiRef.current.getVisibleColumns().map(col => col.field).filter(field => field !== 'actions')
      : Object.keys(visibleRows[0] || {});

    // Filter data to only include visible columns
    const exportData = visibleRows.map(row => {
      const filteredRow: any = {};
      const rowAny = row as any;
      visibleColumns.forEach(col => {
        if (col in rowAny) filteredRow[col] = rowAny[col];
      });
      return filteredRow;
    });

    // Create workbook and worksheet
    const workbook = new ExcelJS.Workbook();
    const worksheet = workbook.addWorksheet('Lookups');

    // Get column names from first row (only visible columns)
    const columnNames = exportData.length > 0 ? Object.keys(exportData[0]) : [];
    worksheet.columns = columnNames.map(name => ({ header: name, key: name, width: 15 }));

    // Add data rows
    exportData.forEach(row => worksheet.addRow(row));

    // Style header row
    const headerRow = worksheet.getRow(1);
    headerRow.font = { bold: true, color: { argb: 'FFFFFFFF' } };
    headerRow.fill = { type: 'pattern', pattern: 'solid', fgColor: { argb: 'FF4472C4' } };
    headerRow.alignment = { horizontal: 'center', vertical: 'middle' };
    headerRow.height = 20;

    // Auto-fit columns based on content
    worksheet.columns.forEach(column => {
      let maxLength = column.header?.length || 10;
      column.eachCell?.({ includeEmpty: false }, cell => {
        const cellLength = cell.value ? String(cell.value).length : 0;
        maxLength = Math.max(maxLength, cellLength);
      });
      column.width = Math.min(maxLength + 2, 50);
    });

    // Freeze header row
    worksheet.views = [{ state: 'frozen', ySplit: 1 }];

    // Generate Excel file and download
    const buffer = await workbook.xlsx.writeBuffer();
    const blob = new Blob([buffer], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
    const url = window.URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `Lookups_${new Date().toISOString().split('T')[0]}.xlsx`;
    link.click();
    window.URL.revokeObjectURL(url);
  };

  const columns: GridColDef<Lookup>[] = [
    { field: 'tableNameCLookup', headerName: 'TableNameCLookup', width: 100 },
    { field: 'id', headerName: 'Id', width: 90 },
    { field: 'enmParentLookupTypeLookup', headerName: 'EnmParentLookupTypeLookup', width: 150 },
    { field: 'parentCode', headerName: 'ParentCode', width: 150 },
    { field: 'enmLookupTypeLookup', headerName: 'EnmLookupTypeLookup', width: 150 },
    { field: 'code', headerName: 'Code', width: 150 },
    { field: 'locText', headerName: 'LocText', width: 250 },
    { field: 'deletedBy', headerName: 'DeletedBy', width: 200 },
    { field: 'deletedOn', headerName: 'DeletedOn', width: 150, valueFormatter: (value) => formatDate(value) },
    { field: 'updatingLoginID', headerName: 'UpdatingLoginID', width: 150 },
    {
      field: 'actions',
      type: 'actions',
      headerName: 'Actions',
      width: 100,
      getActions: (params) => [
        <GridActionsCellItem
          icon={<EditIcon />}
          label="Edit"
          onClick={() => navigate(`/lookups/${params.id}`)}
        />,
        <GridActionsCellItem
          icon={<DeleteIcon />}
          label="Delete"
          onClick={() => {
            if (confirm('Are you sure?')) {
              deleteEntity(params.id as number);
            }
          }}
        />,
      ],
    },
  ];


  if (isLoading) {
    return <CircularProgress />;
  }

  if (error) {
    return <Alert severity="error">Failed to load lookups</Alert>;
  }

  return (
    <Box sx={{ height: 'calc(100vh - 200px)', width: '100%', display: 'flex', flexDirection: 'column' }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="contained"
          startIcon={<AddIcon />}
          onClick={() => navigate('/lookups/new')}
        >
          Create Lookup
        </Button>
        <Button
          variant="outlined"
          startIcon={<FileDownloadIcon />}
          onClick={handleExportToExcel}
        >
          Export to Excel
        </Button>
      </Box>
      <Paper sx={{ p: 2, mb: 2 }}>
        <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap', alignItems: 'center' }}>
          <TextField
            select
            label="TableNameCLookup"
            size="small"
            sx={{ minWidth: 120 }}
            value={filters['tableNameCLookup'] || ''}
            onChange={(e) => updateUrlParam('tableNameCLookup', e.target.value)}
          >
            <MenuItem value="">All</MenuItem>
            <MenuItem value="true">Yes</MenuItem>
            <MenuItem value="false">No</MenuItem>
          </TextField>
          <TextField
            label="Id"
            size="small"
            sx={{ minWidth: 150 }}
            value={filters['id'] || ''}
            onChange={(e) => updateUrlParam('id', e.target.value)}
          />
          <TextField
            label="EnmParentLookupTypeLookup"
            size="small"
            sx={{ minWidth: 150 }}
            value={filters['enmParentLookupTypeLookup'] || ''}
            onChange={(e) => updateUrlParam('enmParentLookupTypeLookup', e.target.value)}
          />
          <TextField
            label="ParentCode"
            size="small"
            sx={{ minWidth: 150 }}
            value={filters['parentCode'] || ''}
            onChange={(e) => updateUrlParam('parentCode', e.target.value)}
          />
          <TextField
            label="EnmLookupTypeLookup"
            size="small"
            sx={{ minWidth: 150 }}
            value={filters['enmLookupTypeLookup'] || ''}
            onChange={(e) => updateUrlParam('enmLookupTypeLookup', e.target.value)}
          />
          <TextField
            label="Code"
            size="small"
            sx={{ minWidth: 150 }}
            value={filters['code'] || ''}
            onChange={(e) => updateUrlParam('code', e.target.value)}
          />
          <TextField
            label="LocText"
            size="small"
            sx={{ minWidth: 150 }}
            value={filters['locText'] || ''}
            onChange={(e) => updateUrlParam('locText', e.target.value)}
          />
          <TextField
            label="DeletedBy"
            size="small"
            sx={{ minWidth: 150 }}
            value={filters['deletedBy'] || ''}
            onChange={(e) => updateUrlParam('deletedBy', e.target.value)}
          />
          <TextField
            label="DeletedOn"
            type="date"
            size="small"
            sx={{ minWidth: 180 }}
            InputLabelProps={{ shrink: true }}
            value={filters['deletedOn'] || ''}
            onChange={(e) => updateUrlParam('deletedOn', e.target.value)}
          />
          <TextField
            label="UpdatingLoginID"
            size="small"
            sx={{ minWidth: 150 }}
            value={filters['updatingLoginID'] || ''}
            onChange={(e) => updateUrlParam('updatingLoginID', e.target.value)}
          />
          <Button
            variant="outlined"
            size="small"
            onClick={handleClearAllFilters}
          >
            Clear
          </Button>
          <Box sx={{ ml: 'auto', display: 'flex', alignItems: 'center' }}>
            <Typography variant="body2" color="text.secondary">
              Showing {lookups?.length || 0} of {totalCount} rows
            </Typography>
          </Box>
        </Box>
      </Paper>

      <Box sx={{ flex: 1, minHeight: 0 }}>
        <DataGrid
          apiRef={apiRef}
          rows={lookups || []}
          columns={columns}
          getRowId={(row) => row.id ?? 0}
          // Server-side pagination
          paginationMode="server"
          rowCount={totalCount}
          paginationModel={{
            page: page - 1, // MUI DataGrid is 0-based, our API is 1-based
            pageSize: pageSize,
          }}
          onPaginationModelChange={(model) => handlePaginationChange(model.page, model.pageSize)}
          pageSizeOptions={[5, 10, 25, 50, 100]}
          // Server-side sorting
          sortingMode="server"
          sortModel={[{
            field: sortBy,
            sort: sortDirection,
          }]}
          onSortModelChange={(model) => {
            if (model.length > 0) {
              handleSortChange(model[0].field, model[0].sort);
            } else {
              handleSortChange('id', 'asc');
            }
          }}
          slots={{
            toolbar: CustomToolbar,
          }}
          checkboxSelection
          disableRowSelectionOnClick
        />
      </Box>
    </Box>
  );
};


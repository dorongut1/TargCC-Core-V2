// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Lookup
//     Generated: 2025-12-16 21:06:01
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useLookup, useCreateLookup, useUpdateLookup } from '../../hooks/useLookup';
import type { CreateLookupRequest, UpdateLookupRequest } from '../../types/Lookup.types';


export const LookupForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const lookupId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!lookupId;

  const { data: existingLookup, isPending: isLoadingEntity } = useLookup(lookupId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateLookupRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingLookup) {
      reset(existingLookup);
    }
  }, [existingLookup, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateLookup();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateLookup();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateLookupRequest) => {
    if (isEdit && lookupId) {
      updateEntity(
        { id: lookupId, data: data as UpdateLookupRequest },
        { onSuccess: () => navigate('/lookups') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/lookups') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCLookup')} />}
        label="TableNameCLookup"
      />

      <TextField
        label="EnmParentLookupTypeLookup"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmParentLookupTypeLookup', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmParentLookupTypeLookup}
        helperText={errors.enmParentLookupTypeLookup?.message}
      />

      <TextField
        label="ParentCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('parentCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.parentCode}
        helperText={errors.parentCode?.message}
      />

      <TextField
        label="EnmLookupTypeLookup"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLookupTypeLookup', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmLookupTypeLookup}
        helperText={errors.enmLookupTypeLookup?.message}
      />

      <TextField
        label="Code"
        type="text"
        fullWidth
        margin="normal"
        {...register('code', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.code}
        helperText={errors.code?.message}
      />

      <TextField
        label="LocText"
        type="text"
        fullWidth
        margin="normal"
        {...register('locText', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.locText}
        helperText={errors.locText?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="LocDescription"
        type="text"
        fullWidth
        margin="normal"
        {...register('locDescription', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.locDescription}
        helperText={errors.locDescription?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/lookups')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: DistributorObligo
//     Generated: 2025-12-17 09:02:39
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useDistributorObligo, useCreateDistributorObligo, useUpdateDistributorObligo } from '../../hooks/useDistributorObligo';
import type { CreateDistributorObligoRequest, UpdateDistributorObligoRequest } from '../../types/DistributorObligo.types';


export const DistributorObligoForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const distributorObligoId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!distributorObligoId;

  const { data: existingDistributorObligo, isPending: isLoadingEntity } = useDistributorObligo(distributorObligoId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateDistributorObligoRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingDistributorObligo) {
      reset(existingDistributorObligo);
    }
  }, [existingDistributorObligo, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateDistributorObligo();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateDistributorObligo();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateDistributorObligoRequest) => {
    if (isEdit && distributorObligoId) {
      updateEntity(
        { id: distributorObligoId, data: data as UpdateDistributorObligoRequest },
        { onSuccess: () => navigate('/distributorObligos') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/distributorObligos') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameDistributorObligo')} />}
        label="TableNameDistributorObligo"
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="WhenExecuted"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenExecuted', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenExecuted}
        helperText={errors.whenExecuted?.message}
      />

      <TextField
        label="EnmObligoTransactionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmObligoTransactionType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmObligoTransactionType}
        helperText={errors.enmObligoTransactionType?.message}
      />

      <TextField
        label="Amount"
        type="number"
        fullWidth
        margin="normal"
        {...register('amount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amount}
        helperText={errors.amount?.message}
      />

      <TextField
        label="Reference"
        type="text"
        fullWidth
        margin="normal"
        {...register('reference', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.reference}
        helperText={errors.reference?.message}
      />

      <TextField
        label="Authoriser"
        type="text"
        fullWidth
        margin="normal"
        {...register('authoriser', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.authoriser}
        helperText={errors.authoriser?.message}
      />

      <TextField
        label="Balance"
        type="number"
        fullWidth
        margin="normal"
        {...register('balance', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.balance}
        helperText={errors.balance?.message}
      />

      <TextField
        label="Comments"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 500, message: 'Maximum length is 500' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/distributorObligos')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


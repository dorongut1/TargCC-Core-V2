// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: DistributorObligo
//     Generated: 2025-12-11 21:58:12
// </auto-generated>

import React from 'react';
import { useNavigate } from 'react-router-dom';
import ExcelJS from 'exceljs';
import { DataGrid, GridColDef, GridActionsCellItem, GridToolbarContainer, GridToolbarColumnsButton, GridToolbarDensitySelector, useGridApiRef } from '@mui/x-data-grid';
import { Button, Box, CircularProgress, Alert, TextField, Paper, MenuItem, Typography } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, Add as AddIcon, FileDownload as FileDownloadIcon, Clear as ClearIcon } from '@mui/icons-material';
import { useDistributorObligos, useDeleteDistributorObligo } from '../../hooks/useDistributorObligo';
import type { DistributorObligo, DistributorObligoFilters } from '../../types/DistributorObligo.types';


export const DistributorObligoList: React.FC = () => {
  const navigate = useNavigate();
  const apiRef = useGridApiRef();
  
  // Helper function to format dates as DD/MM/YYYY
  const formatDate = (date: any): string => {
    if (!date) return '';
    const d = new Date(date);
    if (isNaN(d.getTime())) return '';
    const day = d.getDate().toString().padStart(2, '0');
    const month = (d.getMonth() + 1).toString().padStart(2, '0');
    const year = d.getFullYear();
    return `${day}/${month}/${year}`;
  };
  
  const [columnFilters, setColumnFilters] = React.useState<Record<string, string>>({});
  const { data: distributorObligos, isLoading, error } = useDistributorObligos({});
  const { mutate: deleteEntity } = useDeleteDistributorObligo();

  // Client-side column filters
  const filteredData = React.useMemo(() => {
    if (!distributorObligos) return distributorObligos;

    // Get active filters
    const activeFilters = Object.entries(columnFilters).filter(([_, value]) => value && value.trim() !== '');
    if (activeFilters.length === 0) return distributorObligos;

    // Apply all filters with AND logic
    return distributorObligos.filter(item => {
      return activeFilters.every(([column, filterValue]) => {
        let itemValue = item[column as keyof typeof item];
        
        // Special handling for date filtering - compare as Date objects
        if (typeof filterValue === 'string' && /^\d{4}-\d{2}-\d{2}$/.test(filterValue)) {
          // Filter value is from date picker (YYYY-MM-DD)
          const filterDate = new Date(filterValue);
          let itemDate: Date | null = null;
          
          if (itemValue instanceof Date) {
            itemDate = itemValue;
          } else if (typeof itemValue === 'string' && /^\d{4}-\d{2}-\d{2}/.test(itemValue)) {
            itemDate = new Date(itemValue);
          }
          
          if (itemDate) {
            // Compare dates (ignore time) - same year, month, and day
            return itemDate.getFullYear() === filterDate.getFullYear() &&
                   itemDate.getMonth() === filterDate.getMonth() &&
                   itemDate.getDate() === filterDate.getDate();
          }
          return false;
        }
        
        // Convert boolean values to readable text
        if (typeof itemValue === 'boolean') {
          itemValue = itemValue ? 'true' : 'false';
        }
        
        return itemValue?.toString().toLowerCase().includes(filterValue.toLowerCase());
      });
    });
  }, [distributorObligos, columnFilters]);

  const handleClearAllFilters = () => {
    setColumnFilters({});
  };

  function CustomToolbar() {
    return (
      <GridToolbarContainer>
        <GridToolbarColumnsButton />
        <GridToolbarDensitySelector />
        <Button
          size="small"
          startIcon={<ClearIcon />}
          onClick={handleClearAllFilters}
        >
          Clear All Filters
        </Button>
      </GridToolbarContainer>
    );
  }

  const handleExportToExcel = async () => {
    // Export only the filtered data visible on screen
    const visibleRows = filteredData || [];

    if (!visibleRows || visibleRows.length === 0) {
      alert('No data to export');
      return;
    }

    // Get visible columns from DataGrid (respects column visibility)
    const visibleColumns = apiRef.current
      ? apiRef.current.getVisibleColumns().map(col => col.field).filter(field => field !== 'actions')
      : Object.keys(visibleRows[0] || {});

    // Filter data to only include visible columns
    const exportData = visibleRows.map(row => {
      const filteredRow: any = {};
      const rowAny = row as any;
      visibleColumns.forEach(col => {
        if (col in rowAny) filteredRow[col] = rowAny[col];
      });
      return filteredRow;
    });

    // Create workbook and worksheet
    const workbook = new ExcelJS.Workbook();
    const worksheet = workbook.addWorksheet('DistributorObligos');

    // Get column names from first row (only visible columns)
    const columnNames = exportData.length > 0 ? Object.keys(exportData[0]) : [];
    worksheet.columns = columnNames.map(name => ({ header: name, key: name, width: 15 }));

    // Add data rows
    exportData.forEach(row => worksheet.addRow(row));

    // Style header row
    const headerRow = worksheet.getRow(1);
    headerRow.font = { bold: true, color: { argb: 'FFFFFFFF' } };
    headerRow.fill = { type: 'pattern', pattern: 'solid', fgColor: { argb: 'FF4472C4' } };
    headerRow.alignment = { horizontal: 'center', vertical: 'middle' };
    headerRow.height = 20;

    // Auto-fit columns based on content
    worksheet.columns.forEach(column => {
      let maxLength = column.header?.length || 10;
      column.eachCell?.({ includeEmpty: false }, cell => {
        const cellLength = cell.value ? String(cell.value).length : 0;
        maxLength = Math.max(maxLength, cellLength);
      });
      column.width = Math.min(maxLength + 2, 50);
    });

    // Freeze header row
    worksheet.views = [{ state: 'frozen', ySplit: 1 }];

    // Generate Excel file and download
    const buffer = await workbook.xlsx.writeBuffer();
    const blob = new Blob([buffer], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
    const url = window.URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `DistributorObligos_${new Date().toISOString().split('T')[0]}.xlsx`;
    link.click();
    window.URL.revokeObjectURL(url);
  };

  const columns: GridColDef<DistributorObligo>[] = [
    { field: 'tableNameDistributorObligo', headerName: 'TableNameDistributorObligo', width: 100 },
    { field: 'id', headerName: 'Id', width: 90 },
    { field: 'distributorID', headerName: 'DistributorID', width: 150 },
    { field: 'whenExecuted', headerName: 'WhenExecuted', width: 150, valueFormatter: (value) => formatDate(value) },
    { field: 'enmObligoTransactionType', headerName: 'EnmObligoTransactionType', width: 150 },
    { field: 'amount', headerName: 'Amount', width: 150 },
    { field: 'reference', headerName: 'Reference', width: 250 },
    { field: 'authoriser', headerName: 'Authoriser', width: 250 },
    { field: 'balance', headerName: 'Balance', width: 150 },
    { field: 'comments', headerName: 'Comments', width: 250 },
    {
      field: 'actions',
      type: 'actions',
      headerName: 'Actions',
      width: 100,
      getActions: (params) => [
        <GridActionsCellItem
          icon={<EditIcon />}
          label="Edit"
          onClick={() => navigate(`/distributorObligos/${params.id}`)}
        />,
        <GridActionsCellItem
          icon={<DeleteIcon />}
          label="Delete"
          onClick={() => {
            if (confirm('Are you sure?')) {
              deleteEntity(params.id as number);
            }
          }}
        />,
      ],
    },
  ];


  if (isLoading) {
    return <CircularProgress />;
  }

  if (error) {
    return <Alert severity="error">Failed to load distributorObligos</Alert>;
  }

  return (
    <Box sx={{ height: 'calc(100vh - 200px)', width: '100%', display: 'flex', flexDirection: 'column' }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="contained"
          startIcon={<AddIcon />}
          onClick={() => navigate('/distributorObligos/new')}
        >
          Create DistributorObligo
        </Button>
        <Button
          variant="outlined"
          startIcon={<FileDownloadIcon />}
          onClick={handleExportToExcel}
        >
          Export to Excel
        </Button>
      </Box>
      <Paper sx={{ p: 2, mb: 2 }}>
        <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap', alignItems: 'center' }}>
          <TextField
            select
            label="TableNameDistributorObligo"
            size="small"
            sx={{ minWidth: 120 }}
            value={columnFilters['tableNameDistributorObligo'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              tableNameDistributorObligo: e.target.value
            }))}
          >
            <MenuItem value="">All</MenuItem>
            <MenuItem value="true">Yes</MenuItem>
            <MenuItem value="false">No</MenuItem>
          </TextField>
          <TextField
            label="Id"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['id'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              id: e.target.value
            }))}
          />
          <TextField
            label="DistributorID"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['distributorID'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              distributorID: e.target.value
            }))}
          />
          <TextField
            label="WhenExecuted"
            type="date"
            size="small"
            sx={{ minWidth: 180 }}
            InputLabelProps={{ shrink: true }}
            value={columnFilters['whenExecuted'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              whenExecuted: e.target.value
            }))}
          />
          <TextField
            select
            label="EnmObligoTransactionType"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['enmObligoTransactionType'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              enmObligoTransactionType: e.target.value
            }))}
          >
            <MenuItem value="">All</MenuItem>
            <MenuItem value="Active">Active</MenuItem>
            <MenuItem value="Inactive">Inactive</MenuItem>
            <MenuItem value="Pending">Pending</MenuItem>
          </TextField>
          <TextField
            label="Amount"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['amount'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              amount: e.target.value
            }))}
          />
          <TextField
            label="Reference"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['reference'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              reference: e.target.value
            }))}
          />
          <TextField
            label="Authoriser"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['authoriser'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              authoriser: e.target.value
            }))}
          />
          <TextField
            label="Balance"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['balance'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              balance: e.target.value
            }))}
          />
          <TextField
            label="Comments"
            size="small"
            sx={{ minWidth: 150 }}
            value={columnFilters['comments'] || ''}
            onChange={(e) => setColumnFilters(prev => ({
              ...prev,
              comments: e.target.value
            }))}
          />
          <Button
            variant="outlined"
            size="small"
            onClick={handleClearAllFilters}
          >
            Clear
          </Button>
          <Box sx={{ ml: 'auto', display: 'flex', alignItems: 'center' }}>
            <Typography variant="body2" color="text.secondary">
              Showing {filteredData?.length || 0} of {distributorObligos?.length || 0} rows
            </Typography>
          </Box>
        </Box>
      </Paper>

      <Box sx={{ flex: 1, minHeight: 0 }}>
        <DataGrid
          apiRef={apiRef}
          rows={filteredData || []}
          columns={columns}
          getRowId={(row) => row.id}
          slots={{
            toolbar: CustomToolbar,
          }}
          initialState={{
            pagination: { paginationModel: { pageSize: 10 } },
          }}
          pageSizeOptions={[5, 10, 25, 100]}
          checkboxSelection
          disableRowSelectionOnClick
        />
      </Box>
    </Box>
  );
};


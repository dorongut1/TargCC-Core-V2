// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: vwCustomerSecurityStatusCheck
//     Generated: 2025-12-11 20:26:17
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import type { VwCustomerSecurityStatusCheck } from '../../types/VwCustomerSecurityStatusCheck.types';


export const VwCustomerSecurityStatusCheckDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  // VIEWs are read-only - Detail page not applicable
  const isLoading = false;
  const error = null;
  const entity = null;

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load vwCustomerSecurityStatusCheck</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/vwCustomerSecurityStatusChecks')}
        >
          Back
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            VwCustomerSecurityStatusCheck Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerID
          </Typography>
          <Typography variant="body1">
            {entity.customerID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerName
          </Typography>
          <Typography variant="body1">
            {entity.customerName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerIdentifier
          </Typography>
          <Typography variant="body1">
            {entity.customerIdentifier}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerPhone
          </Typography>
          <Typography variant="body1">
            {entity.customerPhone}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerEmail
          </Typography>
          <Typography variant="body1">
            {entity.customerEmail}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmCustomerType
          </Typography>
          <Typography variant="body1">
            {entity.enmCustomerType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmSecurityTier
          </Typography>
          <Typography variant="body1">
            {entity.enmSecurityTier}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsBusiness
          </Typography>
          <Typography variant="body1">
            {entity.isBusiness ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WalletBalanceWorking
          </Typography>
          <Typography variant="body1">
            {entity.walletBalanceWorking?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TotalTransferredToWallet
          </Typography>
          <Typography variant="body1">
            {entity.totalTransferredToWallet?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TotalTransferredToWallet6Months
          </Typography>
          <Typography variant="body1">
            {entity.totalTransferredToWallet6Months?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TotalTransferredToWallet1Month
          </Typography>
          <Typography variant="body1">
            {entity.totalTransferredToWallet1Month?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            HasSignedServiceRequest
          </Typography>
          <Typography variant="body1">
            {entity.hasSignedServiceRequest ? 'Yes' : 'No'}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>
    </Box>
  );
};


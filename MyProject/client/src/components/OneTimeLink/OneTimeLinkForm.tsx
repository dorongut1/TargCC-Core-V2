// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: OneTimeLink
//     Generated: 2025-12-17 21:20:42
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useOneTimeLink, useCreateOneTimeLink, useUpdateOneTimeLink } from '../../hooks/useOneTimeLink';
import type { CreateOneTimeLinkRequest, UpdateOneTimeLinkRequest } from '../../types/OneTimeLink.types';


export const OneTimeLinkForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const oneTimeLinkId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!oneTimeLinkId;

  const { data: existingOneTimeLink, isPending: isLoadingEntity } = useOneTimeLink(oneTimeLinkId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateOneTimeLinkRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingOneTimeLink) {
      reset(existingOneTimeLink);
    }
  }, [existingOneTimeLink, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateOneTimeLink();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateOneTimeLink();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateOneTimeLinkRequest) => {
    if (isEdit && oneTimeLinkId) {
      updateEntity(
        { id: oneTimeLinkId, data: data as UpdateOneTimeLinkRequest },
        { onSuccess: () => navigate('/oneTimeLinks') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/oneTimeLinks') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameOneTimeLink')} />}
        label="TableNameOneTimeLink"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="EnmLanguage"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLanguage', { required: 'EnmLanguage is required', maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.enmLanguage}
        helperText={errors.enmLanguage?.message}
      />

      <TextField
        label="EnmLinkType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLinkType', { required: 'EnmLinkType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmLinkType}
        helperText={errors.enmLinkType?.message}
      />

      <TextField
        label="EntData"
        type="text"
        fullWidth
        margin="normal"
        {...register('entData', {  })}
        error={!!errors.entData}
        helperText={errors.entData?.message}
      />

      <TextField
        label="WhenCreated"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenCreated', { required: 'WhenCreated is required', maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenCreated}
        helperText={errors.whenCreated?.message}
      />

      <TextField
        label="WhenExpires"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenExpires', { required: 'WhenExpires is required', maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenExpires}
        helperText={errors.whenExpires?.message}
      />

      <TextField
        label="WhenAccessed"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenAccessed', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenAccessed}
        helperText={errors.whenAccessed?.message}
      />

      <TextField
        label="AccessingIP"
        type="text"
        fullWidth
        margin="normal"
        {...register('accessingIP', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.accessingIP}
        helperText={errors.accessingIP?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/oneTimeLinks')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


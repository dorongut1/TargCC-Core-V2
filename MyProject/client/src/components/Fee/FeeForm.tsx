// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Fee
//     Generated: 2025-12-17 09:02:39
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useFee, useCreateFee, useUpdateFee } from '../../hooks/useFee';
import type { CreateFeeRequest, UpdateFeeRequest } from '../../types/Fee.types';


export const FeeForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const feeId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!feeId;

  const { data: existingFee, isPending: isLoadingEntity } = useFee(feeId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateFeeRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingFee) {
      reset(existingFee);
    }
  }, [existingFee, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateFee();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateFee();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateFeeRequest) => {
    if (isEdit && feeId) {
      updateEntity(
        { id: feeId, data: data as UpdateFeeRequest },
        { onSuccess: () => navigate('/fees') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/fees') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameFee')} />}
        label="TableNameFee"
      />

      <TextField
        label="EnmTypeFeeType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmTypeFeeType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmTypeFeeType}
        helperText={errors.enmTypeFeeType?.message}
      />

      <TextField
        label="EnmPayerEntity"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmPayerEntity', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmPayerEntity}
        helperText={errors.enmPayerEntity?.message}
      />

      <TextField
        label="EnmRecipientEntity"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmRecipientEntity', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmRecipientEntity}
        helperText={errors.enmRecipientEntity?.message}
      />

      <TextField
        label="DefaultAmt"
        type="number"
        fullWidth
        margin="normal"
        {...register('defaultAmt', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.defaultAmt}
        helperText={errors.defaultAmt?.message}
      />

      <TextField
        label="DefaultPctOfTrigger"
        type="number"
        fullWidth
        margin="normal"
        {...register('defaultPctOfTrigger', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.defaultPctOfTrigger}
        helperText={errors.defaultPctOfTrigger?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="DefaultMax"
        type="number"
        fullWidth
        margin="normal"
        {...register('defaultMax', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.defaultMax}
        helperText={errors.defaultMax?.message}
      />

      <TextField
        label="DefaultMin"
        type="number"
        fullWidth
        margin="normal"
        {...register('defaultMin', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.defaultMin}
        helperText={errors.defaultMin?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/fees')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: MCCBTransaction
//     Generated: 2025-12-11 22:10:39
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid, GridColDef } from '@mui/x-data-grid';
import { useMCCBTransaction, useDeleteMCCBTransaction } from '../../hooks/useMCCBTransaction';
import { useMCCBTransactionMCCBSecurityChecks } from '../../hooks/useMCCBTransaction';
import type { MCCBTransaction } from '../../types/MCCBTransaction.types';
import type { MCCBSecurityCheck } from '../../types/MCCBSecurityCheck.types';


export const MCCBTransactionDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useMCCBTransaction(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteMCCBTransaction();
  const { data: mCCBSecurityChecks, isLoading: mCCBSecurityChecksLoading } = useMCCBTransactionMCCBSecurityChecks(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/mCCBTransactions'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load mCCBTransaction</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/mCCBTransactions')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/mCCBTransactions/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            MCCBTransaction Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameMCCBTransaction
          </Typography>
          <Typography variant="body1">
            {entity.tableNameMCCBTransaction ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerID
          </Typography>
          <Typography variant="body1">
            {entity.customerID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmPaymentMode
          </Typography>
          <Typography variant="body1">
            {entity.enmPaymentMode}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AmountILSDisplayed
          </Typography>
          <Typography variant="body1">
            {entity.amountILSDisplayed?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AmountILSCalculated
          </Typography>
          <Typography variant="body1">
            {entity.amountILSCalculated?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            AmountILSAddedToFee
          </Typography>
          <Typography variant="body1">
            {entity.amountILSAddedToFee?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PayoutAmount
          </Typography>
          <Typography variant="body1">
            {entity.payoutAmount?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PayoutCurrency
          </Typography>
          <Typography variant="body1">
            {entity.payoutCurrency}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DisplayedFee
          </Typography>
          <Typography variant="body1">
            {entity.displayedFee?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DisplayedExchangeRate
          </Typography>
          <Typography variant="body1">
            {entity.displayedExchangeRate?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TransactionReference
          </Typography>
          <Typography variant="body1">
            {entity.transactionReference}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ICPTransactionFee
          </Typography>
          <Typography variant="body1">
            {entity.iCPTransactionFee?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmSettlementCurrencyCurrency
          </Typography>
          <Typography variant="body1">
            {entity.enmSettlementCurrencyCurrency}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ICPILSToSettlementER
          </Typography>
          <Typography variant="body1">
            {entity.iCPILSToSettlementER?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            MCFeeInSettlementCurrency
          </Typography>
          <Typography variant="body1">
            {entity.mCFeeInSettlementCurrency?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            MCPayoutToSettlementER
          </Typography>
          <Typography variant="body1">
            {entity.mCPayoutToSettlementER?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            MCChargedAmount
          </Typography>
          <Typography variant="body1">
            {entity.mCChargedAmount?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            MCPrincipleAmount
          </Typography>
          <Typography variant="body1">
            {entity.mCPrincipleAmount?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ProposalExpiry
          </Typography>
          <Typography variant="body1">
            {(entity.proposalExpiry ? (entity.proposalExpiry instanceof Date ? entity.proposalExpiry.toLocaleDateString() : new Date(entity.proposalExpiry).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentId
          </Typography>
          <Typography variant="body1">
            {entity.paymentId}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentPurpose
          </Typography>
          <Typography variant="body1">
            {entity.paymentPurpose}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentStatus
          </Typography>
          <Typography variant="body1">
            {entity.paymentStatus}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentStatusTimestamp
          </Typography>
          <Typography variant="body1">
            {(entity.paymentStatusTimestamp ? (entity.paymentStatusTimestamp instanceof Date ? entity.paymentStatusTimestamp.toLocaleDateString() : new Date(entity.paymentStatusTimestamp).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentPendingStage
          </Typography>
          <Typography variant="body1">
            {entity.paymentPendingStage}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentPendingMaxCompletionDate
          </Typography>
          <Typography variant="body1">
            {(entity.paymentPendingMaxCompletionDate ? (entity.paymentPendingMaxCompletionDate instanceof Date ? entity.paymentPendingMaxCompletionDate.toLocaleDateString() : new Date(entity.paymentPendingMaxCompletionDate).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RawDataPayment
          </Typography>
          <Typography variant="body1">
            {entity.rawDataPayment}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RawDataQuote
          </Typography>
          <Typography variant="body1">
            {entity.rawDataQuote}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Comments
          </Typography>
          <Typography variant="body1">
            {entity.comments}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? (entity.deletedOn instanceof Date ? entity.deletedOn.toLocaleDateString() : new Date(entity.deletedOn).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            MCCBReceiverID
          </Typography>
          <Typography variant="body1">
            {entity.mCCBReceiverID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RequestedTransactionID
          </Typography>
          <Typography variant="body1">
            {entity.requestedTransactionID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ProposalId
          </Typography>
          <Typography variant="body1">
            {entity.proposalId}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            MCCBReceiverAccountID
          </Typography>
          <Typography variant="body1">
            {entity.mCCBReceiverAccountID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RawDataStatus
          </Typography>
          <Typography variant="body1">
            {entity.rawDataStatus}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            FundsTakenByICP
          </Typography>
          <Typography variant="body1">
            {entity.fundsTakenByICP ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            FundsRefundedByICP
          </Typography>
          <Typography variant="body1">
            {entity.fundsRefundedByICP ? 'Yes' : 'No'}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            MCCBSecurityChecks
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={mCCBSecurityChecks || []}
              columns={[
                { field: 'tableNameMCCBSecurityCheck', headerName: 'TableNameMCCBSecurityCheck', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'mCCBReceiverID', headerName: 'MCCBReceiverID', width: 100 },
                { field: 'destinationCountryISO3166Alpha2', headerName: 'DestinationCountryISO3166Alpha2', width: 150 },
              ]}
              loading={mCCBSecurityChecksLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>
    </Box>
  );
};


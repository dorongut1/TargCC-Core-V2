// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Table
//     Generated: 2025-12-17 13:12:57
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useTable, useCreateTable, useUpdateTable } from '../../hooks/useTable';
import type { CreateTableRequest, UpdateTableRequest } from '../../types/Table.types';


export const TableForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const tableId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!tableId;

  const { data: existingTable, isPending: isLoadingEntity } = useTable(tableId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateTableRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingTable) {
      reset(existingTable);
    }
  }, [existingTable, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateTable();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateTable();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateTableRequest) => {
    if (isEdit && tableId) {
      updateEntity(
        { id: tableId, data: data as UpdateTableRequest },
        { onSuccess: () => navigate('/tables') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/tables') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="Name"
        type="text"
        fullWidth
        margin="normal"
        {...register('name', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.name}
        helperText={errors.name?.message}
      />

      <TextField
        label="DefaultTextFields"
        type="text"
        fullWidth
        margin="normal"
        {...register('defaultTextFields', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.defaultTextFields}
        helperText={errors.defaultTextFields?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('usedForIdentity')} />}
        label="UsedForIdentity"
      />

      <FormControlLabel
        control={<Checkbox {...register('isSingleRow')} />}
        label="IsSingleRow"
      />

      <TextField
        label="CanAdd"
        type="text"
        fullWidth
        margin="normal"
        {...register('canAdd', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.canAdd}
        helperText={errors.canAdd?.message}
      />

      <TextField
        label="CanEdit"
        type="text"
        fullWidth
        margin="normal"
        {...register('canEdit', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.canEdit}
        helperText={errors.canEdit?.message}
      />

      <TextField
        label="CanDelete"
        type="text"
        fullWidth
        margin="normal"
        {...register('canDelete', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.canDelete}
        helperText={errors.canDelete?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('auditAdd')} />}
        label="AuditAdd"
      />

      <FormControlLabel
        control={<Checkbox {...register('auditEdit')} />}
        label="AuditEdit"
      />

      <FormControlLabel
        control={<Checkbox {...register('auditDelete')} />}
        label="AuditDelete"
      />

      <FormControlLabel
        control={<Checkbox {...register('trackRowChangers')} />}
        label="TrackRowChangers"
      />

      <FormControlLabel
        control={<Checkbox {...register('createUIMenu')} />}
        label="CreateUIMenu"
      />

      <FormControlLabel
        control={<Checkbox {...register('createUICollection')} />}
        label="CreateUICollection"
      />

      <FormControlLabel
        control={<Checkbox {...register('createUIEntity')} />}
        label="CreateUIEntity"
      />

      <TextField
        label="SortOrder"
        type="number"
        fullWidth
        margin="normal"
        {...register('sortOrder', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.sortOrder}
        helperText={errors.sortOrder?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/tables')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


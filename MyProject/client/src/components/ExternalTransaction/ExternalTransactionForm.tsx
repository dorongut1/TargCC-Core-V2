// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: ExternalTransaction
//     Generated: 2025-12-17 21:49:13
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useExternalTransaction, useCreateExternalTransaction, useUpdateExternalTransaction } from '../../hooks/useExternalTransaction';
import type { CreateExternalTransactionRequest, UpdateExternalTransactionRequest } from '../../types/ExternalTransaction.types';


export const ExternalTransactionForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const externalTransactionId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!externalTransactionId;

  const { data: existingExternalTransaction, isPending: isLoadingEntity } = useExternalTransaction(externalTransactionId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateExternalTransactionRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingExternalTransaction) {
      reset(existingExternalTransaction);
    }
  }, [existingExternalTransaction, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateExternalTransaction();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateExternalTransaction();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateExternalTransactionRequest) => {
    if (isEdit && externalTransactionId) {
      updateEntity(
        { id: externalTransactionId, data: data as UpdateExternalTransactionRequest },
        { onSuccess: () => navigate('/externalTransactions') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/externalTransactions') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameExternalTransaction')} />}
        label="TableNameExternalTransaction"
      />

      <TextField
        label="EnmExternalService"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmExternalService', { required: 'EnmExternalService is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmExternalService}
        helperText={errors.enmExternalService?.message}
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="ExternalTransactionIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('externalTransactionIdentifier', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.externalTransactionIdentifier}
        helperText={errors.externalTransactionIdentifier?.message}
      />

      <TextField
        label="TransactionCreditCardID"
        type="number"
        fullWidth
        margin="normal"
        {...register('transactionCreditCardID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.transactionCreditCardID}
        helperText={errors.transactionCreditCardID?.message}
      />

      <TextField
        label="CustomerWalletID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerWalletID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerWalletID}
        helperText={errors.customerWalletID?.message}
      />

      <TextField
        label="MarginUsed"
        type="number"
        fullWidth
        margin="normal"
        {...register('marginUsed', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.marginUsed}
        helperText={errors.marginUsed?.message}
      />

      <TextField
        label="ExecutingCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('executingCurrency', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.executingCurrency}
        helperText={errors.executingCurrency?.message}
      />

      <TextField
        label="ExecutingCurrencyExchangeRate"
        type="number"
        fullWidth
        margin="normal"
        {...register('executingCurrencyExchangeRate', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.executingCurrencyExchangeRate}
        helperText={errors.executingCurrencyExchangeRate?.message}
      />

      <TextField
        label="AmountILS"
        type="number"
        fullWidth
        margin="normal"
        {...register('amountILS', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amountILS}
        helperText={errors.amountILS?.message}
      />

      <TextField
        label="Description"
        type="text"
        fullWidth
        margin="normal"
        {...register('description', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.description}
        helperText={errors.description?.message}
      />

      <TextField
        label="StatusFromExternalService"
        type="text"
        fullWidth
        margin="normal"
        {...register('statusFromExternalService', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.statusFromExternalService}
        helperText={errors.statusFromExternalService?.message}
      />

      <TextField
        label="EnmTransactionStatusExternalTransactionStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmTransactionStatusExternalTransactionStatus', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.enmTransactionStatusExternalTransactionStatus}
        helperText={errors.enmTransactionStatusExternalTransactionStatus?.message}
      />

      <TextField
        label="PaymentApprovalCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('paymentApprovalCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.paymentApprovalCode}
        helperText={errors.paymentApprovalCode?.message}
      />

      <TextField
        label="WhenPaymentCompleted"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenPaymentCompleted', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenPaymentCompleted}
        helperText={errors.whenPaymentCompleted?.message}
      />

      <TextField
        label="WhenTransactionCancelled"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenTransactionCancelled', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenTransactionCancelled}
        helperText={errors.whenTransactionCancelled?.message}
      />

      <TextField
        label="WhenTransactionCompleted"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenTransactionCompleted', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenTransactionCompleted}
        helperText={errors.whenTransactionCompleted?.message}
      />

      <TextField
        label="Comments"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 300, message: 'Maximum length is 300' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="AmountRefunded"
        type="number"
        fullWidth
        margin="normal"
        {...register('amountRefunded', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amountRefunded}
        helperText={errors.amountRefunded?.message}
      />

      <TextField
        label="WhenRefunded"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenRefunded', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenRefunded}
        helperText={errors.whenRefunded?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="ExternalReferenceNo"
        type="text"
        fullWidth
        margin="normal"
        {...register('externalReferenceNo', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.externalReferenceNo}
        helperText={errors.externalReferenceNo?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/externalTransactions')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


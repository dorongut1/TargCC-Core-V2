// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: GovtReport00
//     Generated: 2025-12-17 08:24:03
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useGovtReport00, useCreateGovtReport00, useUpdateGovtReport00 } from '../../hooks/useGovtReport00';
import type { CreateGovtReport00Request, UpdateGovtReport00Request } from '../../types/GovtReport00.types';


export const GovtReport00Form: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const govtReport00Id = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!govtReport00Id;

  const { data: existingGovtReport00, isPending: isLoadingEntity } = useGovtReport00(govtReport00Id || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateGovtReport00Request>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingGovtReport00) {
      reset(existingGovtReport00);
    }
  }, [existingGovtReport00, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateGovtReport00();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateGovtReport00();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateGovtReport00Request) => {
    if (isEdit && govtReport00Id) {
      updateEntity(
        { id: govtReport00Id, data: data as UpdateGovtReport00Request },
        { onSuccess: () => navigate('/govtReport00s') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/govtReport00s') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameGovtReport00')} />}
        label="TableNameGovtReport00"
      />

      <TextField
        label="Name"
        type="text"
        fullWidth
        margin="normal"
        {...register('name', { required: 'Name is required', maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.name}
        helperText={errors.name?.message}
      />

      <TextField
        label="ReporterNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterNumber', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.reporterNumber}
        helperText={errors.reporterNumber?.message}
      />

      <TextField
        label="ReporterIDNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterIDNumber', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.reporterIDNumber}
        helperText={errors.reporterIDNumber?.message}
      />

      <TextField
        label="ReportYear"
        type="text"
        fullWidth
        margin="normal"
        {...register('reportYear', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.reportYear}
        helperText={errors.reportYear?.message}
      />

      <TextField
        label="RecordType"
        type="text"
        fullWidth
        margin="normal"
        {...register('recordType', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.recordType}
        helperText={errors.recordType?.message}
      />

      <TextField
        label="ReportMonth"
        type="text"
        fullWidth
        margin="normal"
        {...register('reportMonth', { maxLength: { value: 6, message: 'Maximum length is 6' } })}
        error={!!errors.reportMonth}
        helperText={errors.reportMonth?.message}
      />

      <TextField
        label="ReportCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('reportCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.reportCode}
        helperText={errors.reportCode?.message}
      />

      <TextField
        label="ReporterName"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterName', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.reporterName}
        helperText={errors.reporterName?.message}
      />

      <TextField
        label="ReporterAddress01"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterAddress01', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.reporterAddress01}
        helperText={errors.reporterAddress01?.message}
      />

      <TextField
        label="ReporterAddress02"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterAddress02', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.reporterAddress02}
        helperText={errors.reporterAddress02?.message}
      />

      <TextField
        label="PhoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('phoneNumber', { maxLength: { value: 15, message: 'Maximum length is 15' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.phoneNumber}
        helperText={errors.phoneNumber?.message}
      />

      <TextField
        label="PhoneNumber2"
        type="text"
        fullWidth
        margin="normal"
        {...register('phoneNumber2', { maxLength: { value: 15, message: 'Maximum length is 15' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.phoneNumber2}
        helperText={errors.phoneNumber2?.message}
      />

      <TextField
        label="Fax"
        type="text"
        fullWidth
        margin="normal"
        {...register('fax', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.fax}
        helperText={errors.fax?.message}
      />

      <TextField
        label="FileDescription"
        type="text"
        fullWidth
        margin="normal"
        {...register('fileDescription', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.fileDescription}
        helperText={errors.fileDescription?.message}
      />

      <TextField
        label="BranchNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('branchNumber', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.branchNumber}
        helperText={errors.branchNumber?.message}
      />

      <TextField
        label="VersionNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('versionNumber', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.versionNumber}
        helperText={errors.versionNumber?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/govtReport00s')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


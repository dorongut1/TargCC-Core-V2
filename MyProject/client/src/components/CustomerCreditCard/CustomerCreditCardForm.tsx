// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerCreditCard
//     Generated: 2025-12-17 21:37:03
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCustomerCreditCard, useCreateCustomerCreditCard, useUpdateCustomerCreditCard } from '../../hooks/useCustomerCreditCard';
import type { CreateCustomerCreditCardRequest, UpdateCustomerCreditCardRequest } from '../../types/CustomerCreditCard.types';


export const CustomerCreditCardForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const customerCreditCardId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!customerCreditCardId;

  const { data: existingCustomerCreditCard, isPending: isLoadingEntity } = useCustomerCreditCard(customerCreditCardId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCustomerCreditCardRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCustomerCreditCard) {
      reset(existingCustomerCreditCard);
    }
  }, [existingCustomerCreditCard, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCustomerCreditCard();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCustomerCreditCard();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCustomerCreditCardRequest) => {
    if (isEdit && customerCreditCardId) {
      updateEntity(
        { id: customerCreditCardId, data: data as UpdateCustomerCreditCardRequest },
        { onSuccess: () => navigate('/customerCreditCards') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/customerCreditCards') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCustomerCreditCard')} />}
        label="TableNameCustomerCreditCard"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="CardToken"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardToken', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.cardToken}
        helperText={errors.cardToken?.message}
      />

      <TextField
        label="Expiry"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('expiry', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.expiry}
        helperText={errors.expiry?.message}
      />

      <TextField
        label="Last4Numbers"
        type="text"
        fullWidth
        margin="normal"
        {...register('last4Numbers', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.last4Numbers}
        helperText={errors.last4Numbers?.message}
      />

      <TextField
        label="LastSuccessfulDate"
        fullWidth
        margin="normal"
        {...register('lastSuccessfulDate', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.lastSuccessfulDate}
        helperText={errors.lastSuccessfulDate?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="LkpCardBrand"
        type="number"
        fullWidth
        margin="normal"
        {...register('lkpCardBrand', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.lkpCardBrand}
        helperText={errors.lkpCardBrand?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('cardIsForeign')} />}
        label="CardIsForeign"
      />

      <FormControlLabel
        control={<Checkbox {...register('isDefault')} />}
        label="IsDefault"
      />

      <FormControlLabel
        control={<Checkbox {...register('isForShow')} />}
        label="IsForShow"
      />

      <TextField
        label="Bin"
        type="text"
        fullWidth
        margin="normal"
        {...register('bin', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.bin}
        helperText={errors.bin?.message}
      />

      <TextField
        label="PaymentType"
        type="text"
        fullWidth
        margin="normal"
        {...register('paymentType', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.paymentType}
        helperText={errors.paymentType?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/customerCreditCards')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


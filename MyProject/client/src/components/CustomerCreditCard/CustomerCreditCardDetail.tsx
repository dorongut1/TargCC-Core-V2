// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerCreditCard
//     Generated: 2025-12-17 07:22:10
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useCustomerCreditCard, useDeleteCustomerCreditCard } from '../../hooks/useCustomerCreditCard';
import { useCustomerCreditCardRequestedTransactions } from '../../hooks/useCustomerCreditCard';
import { useCustomerCreditCardTransactionCreditCards } from '../../hooks/useCustomerCreditCard';


export const CustomerCreditCardDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useCustomerCreditCard(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteCustomerCreditCard();
  const { data: requestedTransactions, isLoading: requestedTransactionsLoading } = useCustomerCreditCardRequestedTransactions(id ? parseInt(id, 10) : null);
  const { data: transactionCreditCards, isLoading: transactionCreditCardsLoading } = useCustomerCreditCardTransactionCreditCards(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/customerCreditCards'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load customerCreditCard</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/customerCreditCards')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/customerCreditCards/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            CustomerCreditCard Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCustomerCreditCard
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCustomerCreditCard ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerID
          </Typography>
          <Typography variant="body1">
            {entity.customerID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardToken
          </Typography>
          <Typography variant="body1">
            {entity.cardToken}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Expiry
          </Typography>
          <Typography variant="body1">
            {(entity.expiry ? new Date(entity.expiry).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Last4Numbers
          </Typography>
          <Typography variant="body1">
            {entity.last4Numbers}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LastSuccessfulDate
          </Typography>
          <Typography variant="body1">
            {(entity.lastSuccessfulDate ? new Date(entity.lastSuccessfulDate).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenAdded
          </Typography>
          <Typography variant="body1">
            {(entity.whenAdded ? new Date(entity.whenAdded).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? new Date(entity.deletedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpCardBrand
          </Typography>
          <Typography variant="body1">
            {entity.lkpCardBrand}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardIsForeign
          </Typography>
          <Typography variant="body1">
            {entity.cardIsForeign ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsDefault
          </Typography>
          <Typography variant="body1">
            {entity.isDefault ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsForShow
          </Typography>
          <Typography variant="body1">
            {entity.isForShow ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Bin
          </Typography>
          <Typography variant="body1">
            {entity.bin}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentType
          </Typography>
          <Typography variant="body1">
            {entity.paymentType}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            RequestedTransactions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={requestedTransactions || []}
              columns={[
                { field: 'tableNameRequestedTransaction', headerName: 'TableNameRequestedTransaction', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'initiatingCustomerID', headerName: 'InitiatingCustomerID', width: 100 },
                { field: 'targetCustomerID', headerName: 'TargetCustomerID', width: 100 },
                { field: 'targetCustomerEmailOrPhone', headerName: 'TargetCustomerEmailOrPhone', width: 150 },
              ]}
              loading={requestedTransactionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            TransactionCreditCards
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={transactionCreditCards || []}
              columns={[
                { field: 'tableNameTransactionCreditCard', headerName: 'TableNameTransactionCreditCard', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'uniqueIdentifier', headerName: 'UniqueIdentifier', width: 150 },
                { field: 'whenInitialized', headerName: 'WhenInitialized', width: 150 },
                { field: 'whenValidated', headerName: 'WhenValidated', width: 150 },
              ]}
              loading={transactionCreditCardsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: MCCBReceiverAccount
//     Generated: 2025-12-17 13:13:01
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useMCCBReceiverAccount, useCreateMCCBReceiverAccount, useUpdateMCCBReceiverAccount } from '../../hooks/useMCCBReceiverAccount';
import type { CreateMCCBReceiverAccountRequest, UpdateMCCBReceiverAccountRequest } from '../../types/MCCBReceiverAccount.types';


export const MCCBReceiverAccountForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mCCBReceiverAccountId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mCCBReceiverAccountId;

  const { data: existingMCCBReceiverAccount, isPending: isLoadingEntity } = useMCCBReceiverAccount(mCCBReceiverAccountId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMCCBReceiverAccountRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMCCBReceiverAccount) {
      reset(existingMCCBReceiverAccount);
    }
  }, [existingMCCBReceiverAccount, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMCCBReceiverAccount();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMCCBReceiverAccount();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMCCBReceiverAccountRequest) => {
    if (isEdit && mCCBReceiverAccountId) {
      updateEntity(
        { id: mCCBReceiverAccountId, data: data as UpdateMCCBReceiverAccountRequest },
        { onSuccess: () => navigate('/mCCBReceiverAccounts') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mCCBReceiverAccounts') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameMCCBReceiverAccount')} />}
        label="TableNameMCCBReceiverAccount"
      />

      <TextField
        label="ReceiverID"
        type="number"
        fullWidth
        margin="normal"
        {...register('receiverID', { required: 'ReceiverID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.receiverID}
        helperText={errors.receiverID?.message}
      />

      <TextField
        label="EnmPaymentMode"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmPaymentMode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmPaymentMode}
        helperText={errors.enmPaymentMode?.message}
      />

      <TextField
        label="AccountCountryISO3166Alpha2"
        type="text"
        fullWidth
        margin="normal"
        {...register('accountCountryISO3166Alpha2', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.accountCountryISO3166Alpha2}
        helperText={errors.accountCountryISO3166Alpha2?.message}
      />

      <TextField
        label="Swift"
        type="text"
        fullWidth
        margin="normal"
        {...register('swift', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.swift}
        helperText={errors.swift?.message}
      />

      <TextField
        label="Account"
        type="text"
        fullWidth
        margin="normal"
        {...register('account', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.account}
        helperText={errors.account?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="Provider"
        type="text"
        fullWidth
        margin="normal"
        {...register('provider', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.provider}
        helperText={errors.provider?.message}
      />

      <TextField
        label="AccountType"
        type="text"
        fullWidth
        margin="normal"
        {...register('accountType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.accountType}
        helperText={errors.accountType?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mCCBReceiverAccounts')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


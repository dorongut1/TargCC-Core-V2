// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: sysdiagrams
//     Generated: 2025-12-15 13:48:33
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress } from '@mui/material';
import { useSysdiagrams, useCreateSysdiagrams, useUpdateSysdiagrams } from '../../hooks/useSysdiagrams';
import type { CreateSysdiagramsRequest, UpdateSysdiagramsRequest } from '../../types/Sysdiagrams.types';


export const SysdiagramsForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const sysdiagramsId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!sysdiagramsId;

  const { data: existingSysdiagrams, isPending: isLoadingEntity } = useSysdiagrams(sysdiagramsId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateSysdiagramsRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingSysdiagrams) {
      reset(existingSysdiagrams);
    }
  }, [existingSysdiagrams, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateSysdiagrams();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateSysdiagrams();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateSysdiagramsRequest) => {
    if (isEdit && sysdiagramsId) {
      updateEntity(
        { id: sysdiagramsId, data: data as UpdateSysdiagramsRequest },
        { onSuccess: () => navigate('/sysdiagramss') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/sysdiagramss') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="Name"
        type="text"
        fullWidth
        margin="normal"
        {...register('name', { required: 'Name is required', maxLength: { value: 256, message: 'Maximum length is 256' } })}
        error={!!errors.name}
        helperText={errors.name?.message}
      />

      <TextField
        label="PrincipalId"
        type="number"
        fullWidth
        margin="normal"
        {...register('principalId', { required: 'PrincipalId is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.principalId}
        helperText={errors.principalId?.message}
      />

      <TextField
        label="Version"
        type="number"
        fullWidth
        margin="normal"
        {...register('version', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.version}
        helperText={errors.version?.message}
      />

      <TextField
        label="Definition"
        type="text"
        fullWidth
        margin="normal"
        {...register('definition', {  })}
        error={!!errors.definition}
        helperText={errors.definition?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/sysdiagramss')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


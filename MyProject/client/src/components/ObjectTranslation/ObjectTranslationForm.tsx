// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_ObjectTranslation
//     Generated: 2025-12-17 09:08:22
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useObjectTranslation, useCreateObjectTranslation, useUpdateObjectTranslation } from '../../hooks/useObjectTranslation';
import type { CreateObjectTranslationRequest, UpdateObjectTranslationRequest } from '../../types/ObjectTranslation.types';


export const ObjectTranslationForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const objectTranslationId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!objectTranslationId;

  const { data: existingObjectTranslation, isPending: isLoadingEntity } = useObjectTranslation(objectTranslationId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateObjectTranslationRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingObjectTranslation) {
      reset(existingObjectTranslation);
    }
  }, [existingObjectTranslation, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateObjectTranslation();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateObjectTranslation();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateObjectTranslationRequest) => {
    if (isEdit && objectTranslationId) {
      updateEntity(
        { id: objectTranslationId, data: data as UpdateObjectTranslationRequest },
        { onSuccess: () => navigate('/objectTranslations') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/objectTranslations') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCObjectTranslation')} />}
        label="TableNameCObjectTranslation"
      />

      <TextField
        label="CObjectToTranslateID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cObjectToTranslateID', { required: 'CObjectToTranslateID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cObjectToTranslateID}
        helperText={errors.cObjectToTranslateID?.message}
      />

      <TextField
        label="Instance"
        type="number"
        fullWidth
        margin="normal"
        {...register('instance', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.instance}
        helperText={errors.instance?.message}
      />

      <TextField
        label="DefaultText"
        type="text"
        fullWidth
        margin="normal"
        {...register('defaultText', {  })}
        error={!!errors.defaultText}
        helperText={errors.defaultText?.message}
      />

      <TextField
        label="EnmLanguage"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmLanguage', { required: 'EnmLanguage is required', maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.enmLanguage}
        helperText={errors.enmLanguage?.message}
      />

      <TextField
        label="Text"
        type="text"
        fullWidth
        margin="normal"
        {...register('text', { required: 'Text is required' })}
        error={!!errors.text}
        helperText={errors.text?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="InstanceUniqueText"
        type="text"
        fullWidth
        margin="normal"
        {...register('instanceUniqueText', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.instanceUniqueText}
        helperText={errors.instanceUniqueText?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/objectTranslations')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


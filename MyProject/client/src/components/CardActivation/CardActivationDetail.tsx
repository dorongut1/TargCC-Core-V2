// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CardActivation
//     Generated: 2025-12-17 09:34:34
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { useCardActivation, useDeleteCardActivation } from '../../hooks/useCardActivation';


export const CardActivationDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useCardActivation(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteCardActivation();

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/cardActivations'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load cardActivation</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/cardActivations')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/cardActivations/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            CardActivation Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCardActivation
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCardActivation ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DistributorID
          </Typography>
          <Typography variant="body1">
            {entity.distributorID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CustomerID
          </Typography>
          <Typography variant="body1">
            {entity.customerID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardID
          </Typography>
          <Typography variant="body1">
            {entity.cardID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenInitiated
          </Typography>
          <Typography variant="body1">
            {(entity.whenInitiated ? new Date(entity.whenInitiated).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Profession
          </Typography>
          <Typography variant="body1">
            {entity.profession}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpEmploymentStatus
          </Typography>
          <Typography variant="body1">
            {entity.lkpEmploymentStatus}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Employer
          </Typography>
          <Typography variant="body1">
            {entity.employer}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpCardUse
          </Typography>
          <Typography variant="body1">
            {entity.lkpCardUse}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PublicPosition
          </Typography>
          <Typography variant="body1">
            {entity.publicPosition}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ConnectionWithPublicPerson
          </Typography>
          <Typography variant="body1">
            {entity.connectionWithPublicPerson}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpAdditionalCitizenshipCountry
          </Typography>
          <Typography variant="body1">
            {entity.lkpAdditionalCitizenshipCountry}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpConnectionWithIsrael
          </Typography>
          <Typography variant="body1">
            {entity.lkpConnectionWithIsrael}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ConnectionWithIsraelDetails
          </Typography>
          <Typography variant="body1">
            {entity.connectionWithIsraelDetails}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LkpMainUseOfCardCardUsage
          </Typography>
          <Typography variant="body1">
            {entity.lkpMainUseOfCardCardUsage}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsExpectedToReceiveDeposits
          </Typography>
          <Typography variant="body1">
            {entity.isExpectedToReceiveDeposits ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsExpectedToReceiveDepositsDetails
          </Typography>
          <Typography variant="body1">
            {entity.isExpectedToReceiveDepositsDetails}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WasPreviouslyDeniedBankService
          </Typography>
          <Typography variant="body1">
            {entity.wasPreviouslyDeniedBankService ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WitnessFirstName
          </Typography>
          <Typography variant="body1">
            {entity.witnessFirstName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WitnessLastName
          </Typography>
          <Typography variant="body1">
            {entity.witnessLastName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WitnessIsraelID
          </Typography>
          <Typography variant="body1">
            {entity.witnessIsraelID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmIDTypeForWitnessIdentificationMethod
          </Typography>
          <Typography variant="body1">
            {entity.enmIDTypeForWitnessIdentificationMethod}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmApprovedApprovalType
          </Typography>
          <Typography variant="body1">
            {entity.enmApprovedApprovalType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ResponseCode
          </Typography>
          <Typography variant="body1">
            {entity.responseCode}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ResponseText
          </Typography>
          <Typography variant="body1">
            {entity.responseText}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoggedAlertID
          </Typography>
          <Typography variant="body1">
            {entity.loggedAlertID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenRequested
          </Typography>
          <Typography variant="body1">
            {(entity.whenRequested ? new Date(entity.whenRequested).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenResponded
          </Typography>
          <Typography variant="body1">
            {(entity.whenResponded ? new Date(entity.whenResponded).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            InitialLoadID
          </Typography>
          <Typography variant="body1">
            {entity.initialLoadID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DocumentionLoadDate
          </Typography>
          <Typography variant="body1">
            {(entity.documentionLoadDate ? new Date(entity.documentionLoadDate).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsDocumentationOK
          </Typography>
          <Typography variant="body1">
            {entity.isDocumentationOK ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LgInterviewedByUser
          </Typography>
          <Typography variant="body1">
            {entity.lgInterviewedByUser}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmRequestedCurrencyCurrency
          </Typography>
          <Typography variant="body1">
            {entity.enmRequestedCurrencyCurrency}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? new Date(entity.deletedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LgAddedBy
          </Typography>
          <Typography variant="body1">
            {entity.lgAddedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmCardActivationNextTask
          </Typography>
          <Typography variant="body1">
            {entity.enmCardActivationNextTask}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Comment
          </Typography>
          <Typography variant="body1">
            {entity.comment}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LogCellPhone
          </Typography>
          <Typography variant="body1">
            {entity.logCellPhone}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            OnHold
          </Typography>
          <Typography variant="body1">
            {entity.onHold ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WantsNameOnCard
          </Typography>
          <Typography variant="body1">
            {entity.wantsNameOnCard ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CardClubID
          </Typography>
          <Typography variant="body1">
            {entity.cardClubID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CreatingApp
          </Typography>
          <Typography variant="body1">
            {entity.creatingApp}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeliveryFee
          </Typography>
          <Typography variant="body1">
            {entity.deliveryFee?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            NewCardFee
          </Typography>
          <Typography variant="body1">
            {entity.newCardFee?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RequestedInitialLoad
          </Typography>
          <Typography variant="body1">
            {entity.requestedInitialLoad?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EmbossFee
          </Typography>
          <Typography variant="body1">
            {entity.embossFee?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>
    </Box>
  );
};


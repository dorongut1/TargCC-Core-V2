// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CardActivation
//     Generated: 2025-12-17 12:56:54
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCardActivation, useCreateCardActivation, useUpdateCardActivation } from '../../hooks/useCardActivation';
import type { CreateCardActivationRequest, UpdateCardActivationRequest } from '../../types/CardActivation.types';


export const CardActivationForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const cardActivationId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!cardActivationId;

  const { data: existingCardActivation, isPending: isLoadingEntity } = useCardActivation(cardActivationId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCardActivationRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCardActivation) {
      reset(existingCardActivation);
    }
  }, [existingCardActivation, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCardActivation();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCardActivation();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCardActivationRequest) => {
    if (isEdit && cardActivationId) {
      updateEntity(
        { id: cardActivationId, data: data as UpdateCardActivationRequest },
        { onSuccess: () => navigate('/cardActivations') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/cardActivations') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCardActivation')} />}
        label="TableNameCardActivation"
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="CardID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardID}
        helperText={errors.cardID?.message}
      />

      <TextField
        label="Profession"
        type="text"
        fullWidth
        margin="normal"
        {...register('profession', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.profession}
        helperText={errors.profession?.message}
      />

      <TextField
        label="LkpEmploymentStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpEmploymentStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpEmploymentStatus}
        helperText={errors.lkpEmploymentStatus?.message}
      />

      <TextField
        label="Employer"
        type="text"
        fullWidth
        margin="normal"
        {...register('employer', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.employer}
        helperText={errors.employer?.message}
      />

      <TextField
        label="LkpCardUse"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpCardUse', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpCardUse}
        helperText={errors.lkpCardUse?.message}
      />

      <TextField
        label="PublicPosition"
        type="text"
        fullWidth
        margin="normal"
        {...register('publicPosition', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.publicPosition}
        helperText={errors.publicPosition?.message}
      />

      <TextField
        label="ConnectionWithPublicPerson"
        type="text"
        fullWidth
        margin="normal"
        {...register('connectionWithPublicPerson', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.connectionWithPublicPerson}
        helperText={errors.connectionWithPublicPerson?.message}
      />

      <TextField
        label="LkpAdditionalCitizenshipCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpAdditionalCitizenshipCountry', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lkpAdditionalCitizenshipCountry}
        helperText={errors.lkpAdditionalCitizenshipCountry?.message}
      />

      <TextField
        label="LkpConnectionWithIsrael"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpConnectionWithIsrael', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpConnectionWithIsrael}
        helperText={errors.lkpConnectionWithIsrael?.message}
      />

      <TextField
        label="ConnectionWithIsraelDetails"
        type="text"
        fullWidth
        margin="normal"
        {...register('connectionWithIsraelDetails', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.connectionWithIsraelDetails}
        helperText={errors.connectionWithIsraelDetails?.message}
      />

      <TextField
        label="LkpMainUseOfCardCardUsage"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpMainUseOfCardCardUsage', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpMainUseOfCardCardUsage}
        helperText={errors.lkpMainUseOfCardCardUsage?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isExpectedToReceiveDeposits')} />}
        label="IsExpectedToReceiveDeposits"
      />

      <TextField
        label="IsExpectedToReceiveDepositsDetails"
        type="text"
        fullWidth
        margin="normal"
        {...register('isExpectedToReceiveDepositsDetails', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.isExpectedToReceiveDepositsDetails}
        helperText={errors.isExpectedToReceiveDepositsDetails?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('wasPreviouslyDeniedBankService')} />}
        label="WasPreviouslyDeniedBankService"
      />

      <TextField
        label="WitnessFirstName"
        type="text"
        fullWidth
        margin="normal"
        {...register('witnessFirstName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.witnessFirstName}
        helperText={errors.witnessFirstName?.message}
      />

      <TextField
        label="WitnessLastName"
        type="text"
        fullWidth
        margin="normal"
        {...register('witnessLastName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.witnessLastName}
        helperText={errors.witnessLastName?.message}
      />

      <TextField
        label="WitnessIsraelID"
        type="text"
        fullWidth
        margin="normal"
        {...register('witnessIsraelID', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.witnessIsraelID}
        helperText={errors.witnessIsraelID?.message}
      />

      <TextField
        label="EnmIDTypeForWitnessIdentificationMethod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmIDTypeForWitnessIdentificationMethod', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmIDTypeForWitnessIdentificationMethod}
        helperText={errors.enmIDTypeForWitnessIdentificationMethod?.message}
      />

      <TextField
        label="EnmApprovedApprovalType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmApprovedApprovalType', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.enmApprovedApprovalType}
        helperText={errors.enmApprovedApprovalType?.message}
      />

      <TextField
        label="ResponseCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('responseCode', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.responseCode}
        helperText={errors.responseCode?.message}
      />

      <TextField
        label="ResponseText"
        type="text"
        fullWidth
        margin="normal"
        {...register('responseText', {  })}
        error={!!errors.responseText}
        helperText={errors.responseText?.message}
      />

      <TextField
        label="LoggedAlertID"
        type="number"
        fullWidth
        margin="normal"
        {...register('loggedAlertID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.loggedAlertID}
        helperText={errors.loggedAlertID?.message}
      />

      <TextField
        label="WhenRequested"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenRequested', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenRequested}
        helperText={errors.whenRequested?.message}
      />

      <TextField
        label="WhenResponded"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenResponded', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenResponded}
        helperText={errors.whenResponded?.message}
      />

      <TextField
        label="InitialLoadID"
        type="number"
        fullWidth
        margin="normal"
        {...register('initialLoadID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.initialLoadID}
        helperText={errors.initialLoadID?.message}
      />

      <TextField
        label="DocumentionLoadDate"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('documentionLoadDate', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.documentionLoadDate}
        helperText={errors.documentionLoadDate?.message}
      />

      <TextField
        label="IsDocumentationOK"
        fullWidth
        margin="normal"
        {...register('isDocumentationOK', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.isDocumentationOK}
        helperText={errors.isDocumentationOK?.message}
      />

      <TextField
        label="LgInterviewedByUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('lgInterviewedByUser', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lgInterviewedByUser}
        helperText={errors.lgInterviewedByUser?.message}
      />

      <TextField
        label="EnmRequestedCurrencyCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmRequestedCurrencyCurrency', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmRequestedCurrencyCurrency}
        helperText={errors.enmRequestedCurrencyCurrency?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="LgAddedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('lgAddedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lgAddedBy}
        helperText={errors.lgAddedBy?.message}
      />

      <TextField
        label="EnmCardActivationNextTask"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCardActivationNextTask', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCardActivationNextTask}
        helperText={errors.enmCardActivationNextTask?.message}
      />

      <TextField
        label="Comment"
        fullWidth
        margin="normal"
        {...register('comment', {  })}
        error={!!errors.comment}
        helperText={errors.comment?.message}
      />

      <TextField
        label="LogCellPhone"
        type="text"
        fullWidth
        margin="normal"
        {...register('logCellPhone', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.logCellPhone}
        helperText={errors.logCellPhone?.message}
      />

      <TextField
        label="OnHold"
        fullWidth
        margin="normal"
        {...register('onHold', { required: 'OnHold is required', maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.onHold}
        helperText={errors.onHold?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('wantsNameOnCard')} />}
        label="WantsNameOnCard"
      />

      <TextField
        label="CardClubID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardClubID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardClubID}
        helperText={errors.cardClubID?.message}
      />

      <TextField
        label="CreatingApp"
        type="text"
        fullWidth
        margin="normal"
        {...register('creatingApp', { maxLength: { value: 60, message: 'Maximum length is 60' } })}
        error={!!errors.creatingApp}
        helperText={errors.creatingApp?.message}
      />

      <TextField
        label="DeliveryFee"
        fullWidth
        margin="normal"
        {...register('deliveryFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.deliveryFee}
        helperText={errors.deliveryFee?.message}
      />

      <TextField
        label="NewCardFee"
        fullWidth
        margin="normal"
        {...register('newCardFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.newCardFee}
        helperText={errors.newCardFee?.message}
      />

      <TextField
        label="RequestedInitialLoad"
        type="number"
        fullWidth
        margin="normal"
        {...register('requestedInitialLoad', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.requestedInitialLoad}
        helperText={errors.requestedInitialLoad?.message}
      />

      <TextField
        label="EmbossFee"
        fullWidth
        margin="normal"
        {...register('embossFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.embossFee}
        helperText={errors.embossFee?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/cardActivations')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


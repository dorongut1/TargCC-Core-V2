// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: FeeOverride
//     Generated: 2025-12-17 09:08:26
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useFeeOverride, useCreateFeeOverride, useUpdateFeeOverride } from '../../hooks/useFeeOverride';
import type { CreateFeeOverrideRequest, UpdateFeeOverrideRequest } from '../../types/FeeOverride.types';


export const FeeOverrideForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const feeOverrideId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!feeOverrideId;

  const { data: existingFeeOverride, isPending: isLoadingEntity } = useFeeOverride(feeOverrideId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateFeeOverrideRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingFeeOverride) {
      reset(existingFeeOverride);
    }
  }, [existingFeeOverride, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateFeeOverride();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateFeeOverride();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateFeeOverrideRequest) => {
    if (isEdit && feeOverrideId) {
      updateEntity(
        { id: feeOverrideId, data: data as UpdateFeeOverrideRequest },
        { onSuccess: () => navigate('/feeOverrides') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/feeOverrides') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameFeeOverride')} />}
        label="TableNameFeeOverride"
      />

      <TextField
        label="FeeID"
        type="number"
        fullWidth
        margin="normal"
        {...register('feeID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.feeID}
        helperText={errors.feeID?.message}
      />

      <TextField
        label="CardClubID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardClubID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardClubID}
        helperText={errors.cardClubID?.message}
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="OverrideAmt"
        type="number"
        fullWidth
        margin="normal"
        {...register('overrideAmt', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.overrideAmt}
        helperText={errors.overrideAmt?.message}
      />

      <TextField
        label="OverridePctOfTrigger"
        type="number"
        fullWidth
        margin="normal"
        {...register('overridePctOfTrigger', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.overridePctOfTrigger}
        helperText={errors.overridePctOfTrigger?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="OverrideMax"
        type="number"
        fullWidth
        margin="normal"
        {...register('overrideMax', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.overrideMax}
        helperText={errors.overrideMax?.message}
      />

      <TextField
        label="CustomerClubID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerClubID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerClubID}
        helperText={errors.customerClubID?.message}
      />

      <TextField
        label="OverrideMin"
        type="number"
        fullWidth
        margin="normal"
        {...register('overrideMin', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.overrideMin}
        helperText={errors.overrideMin?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/feeOverrides')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


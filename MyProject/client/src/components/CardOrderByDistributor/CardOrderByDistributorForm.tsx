// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CardOrderByDistributor
//     Generated: 2025-12-17 21:11:13
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCardOrderByDistributor, useCreateCardOrderByDistributor, useUpdateCardOrderByDistributor } from '../../hooks/useCardOrderByDistributor';
import type { CreateCardOrderByDistributorRequest, UpdateCardOrderByDistributorRequest } from '../../types/CardOrderByDistributor.types';


export const CardOrderByDistributorForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const cardOrderByDistributorId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!cardOrderByDistributorId;

  const { data: existingCardOrderByDistributor, isPending: isLoadingEntity } = useCardOrderByDistributor(cardOrderByDistributorId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCardOrderByDistributorRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCardOrderByDistributor) {
      reset(existingCardOrderByDistributor);
    }
  }, [existingCardOrderByDistributor, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCardOrderByDistributor();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCardOrderByDistributor();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCardOrderByDistributorRequest) => {
    if (isEdit && cardOrderByDistributorId) {
      updateEntity(
        { id: cardOrderByDistributorId, data: data as UpdateCardOrderByDistributorRequest },
        { onSuccess: () => navigate('/cardOrderByDistributors') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/cardOrderByDistributors') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCardOrderByDistributor')} />}
        label="TableNameCardOrderByDistributor"
      />

      <TextField
        label="NumberOfCards"
        type="number"
        fullWidth
        margin="normal"
        {...register('numberOfCards', { required: 'NumberOfCards is required', maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.numberOfCards}
        helperText={errors.numberOfCards?.message}
      />

      <TextField
        label="EnmCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCurrency', { required: 'EnmCurrency is required', maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.enmCurrency}
        helperText={errors.enmCurrency?.message}
      />

      <TextField
        label="CardClubID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardClubID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardClubID}
        helperText={errors.cardClubID?.message}
      />

      <TextField
        label="OrderedByUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('orderedByUserID', { required: 'OrderedByUserID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.orderedByUserID}
        helperText={errors.orderedByUserID?.message}
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { required: 'DistributorID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="DistributorOrderCardsFee"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorOrderCardsFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.distributorOrderCardsFee}
        helperText={errors.distributorOrderCardsFee?.message}
      />

      <TextField
        label="WhenOrdered"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenOrdered', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenOrdered}
        helperText={errors.whenOrdered?.message}
      />

      <TextField
        label="EnmStatusCardOrderStatus"
        fullWidth
        margin="normal"
        {...register('enmStatusCardOrderStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmStatusCardOrderStatus}
        helperText={errors.enmStatusCardOrderStatus?.message}
      />

      <TextField
        label="WhenReceived"
        fullWidth
        margin="normal"
        {...register('whenReceived', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenReceived}
        helperText={errors.whenReceived?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="EnmCustomerCardActivationStatusStatus"
        fullWidth
        margin="normal"
        {...register('enmCustomerCardActivationStatusStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCustomerCardActivationStatusStatus}
        helperText={errors.enmCustomerCardActivationStatusStatus?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/cardOrderByDistributors')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


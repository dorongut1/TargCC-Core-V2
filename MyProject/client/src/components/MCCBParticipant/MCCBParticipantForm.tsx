// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: MCCBParticipant
//     Generated: 2025-12-17 21:37:04
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useMCCBParticipant, useCreateMCCBParticipant, useUpdateMCCBParticipant } from '../../hooks/useMCCBParticipant';
import type { CreateMCCBParticipantRequest, UpdateMCCBParticipantRequest } from '../../types/MCCBParticipant.types';


export const MCCBParticipantForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mCCBParticipantId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mCCBParticipantId;

  const { data: existingMCCBParticipant, isPending: isLoadingEntity } = useMCCBParticipant(mCCBParticipantId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMCCBParticipantRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMCCBParticipant) {
      reset(existingMCCBParticipant);
    }
  }, [existingMCCBParticipant, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMCCBParticipant();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMCCBParticipant();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMCCBParticipantRequest) => {
    if (isEdit && mCCBParticipantId) {
      updateEntity(
        { id: mCCBParticipantId, data: data as UpdateMCCBParticipantRequest },
        { onSuccess: () => navigate('/mCCBParticipants') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mCCBParticipants') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameMCCBParticipant')} />}
        label="TableNameMCCBParticipant"
      />

      <TextField
        label="EnmCorridor"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCorridor', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmCorridor}
        helperText={errors.enmCorridor?.message}
      />

      <TextField
        label="CountryISO3166Alpha2"
        type="text"
        fullWidth
        margin="normal"
        {...register('countryISO3166Alpha2', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.countryISO3166Alpha2}
        helperText={errors.countryISO3166Alpha2?.message}
      />

      <TextField
        label="PayoutCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('payoutCurrency', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.payoutCurrency}
        helperText={errors.payoutCurrency?.message}
      />

      <TextField
        label="EnmPaymentMode"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmPaymentMode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmPaymentMode}
        helperText={errors.enmPaymentMode?.message}
      />

      <TextField
        label="EnmSettlementCurrencyCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmSettlementCurrencyCurrency', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmSettlementCurrencyCurrency}
        helperText={errors.enmSettlementCurrencyCurrency?.message}
      />

      <TextField
        label="Comments"
        type="text"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 510, message: 'Maximum length is 510' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isParticipant')} />}
        label="IsParticipant"
      />

      <TextField
        label="SortOrder"
        type="number"
        fullWidth
        margin="normal"
        {...register('sortOrder', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.sortOrder}
        helperText={errors.sortOrder?.message}
      />

      <TextField
        label="FlatFee"
        type="number"
        fullWidth
        margin="normal"
        {...register('flatFee', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.flatFee}
        helperText={errors.flatFee?.message}
      />

      <TextField
        label="FeeBaseAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('feeBaseAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.feeBaseAmount}
        helperText={errors.feeBaseAmount?.message}
      />

      <TextField
        label="FeePctOfTrigger"
        type="number"
        fullWidth
        margin="normal"
        {...register('feePctOfTrigger', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.feePctOfTrigger}
        helperText={errors.feePctOfTrigger?.message}
      />

      <TextField
        label="FeeMax"
        type="number"
        fullWidth
        margin="normal"
        {...register('feeMax', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.feeMax}
        helperText={errors.feeMax?.message}
      />

      <TextField
        label="FeeMin"
        type="number"
        fullWidth
        margin="normal"
        {...register('feeMin', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.feeMin}
        helperText={errors.feeMin?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mCCBParticipants')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


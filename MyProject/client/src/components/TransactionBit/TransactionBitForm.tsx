// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: TransactionBit
//     Generated: 2025-12-17 20:20:35
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useTransactionBit, useCreateTransactionBit, useUpdateTransactionBit } from '../../hooks/useTransactionBit';
import type { CreateTransactionBitRequest, UpdateTransactionBitRequest } from '../../types/TransactionBit.types';


export const TransactionBitForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const transactionBitId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!transactionBitId;

  const { data: existingTransactionBit, isPending: isLoadingEntity } = useTransactionBit(transactionBitId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateTransactionBitRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingTransactionBit) {
      reset(existingTransactionBit);
    }
  }, [existingTransactionBit, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateTransactionBit();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateTransactionBit();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateTransactionBitRequest) => {
    if (isEdit && transactionBitId) {
      updateEntity(
        { id: transactionBitId, data: data as UpdateTransactionBitRequest },
        { onSuccess: () => navigate('/transactionBits') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/transactionBits') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameTransactionBit')} />}
        label="TableNameTransactionBit"
      />

      <TextField
        label="UniqueIdentifier"
        type="text"
        fullWidth
        margin="normal"
        {...register('uniqueIdentifier', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.uniqueIdentifier}
        helperText={errors.uniqueIdentifier?.message}
      />

      <TextField
        label="RequestedTransactionID"
        type="number"
        fullWidth
        margin="normal"
        {...register('requestedTransactionID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.requestedTransactionID}
        helperText={errors.requestedTransactionID?.message}
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="RequestAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('requestAmount', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.requestAmount}
        helperText={errors.requestAmount?.message}
      />

      <TextField
        label="CurrencyTypeCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('currencyTypeCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.currencyTypeCode}
        helperText={errors.currencyTypeCode?.message}
      />

      <TextField
        label="RequestSubjectDescription"
        type="text"
        fullWidth
        margin="normal"
        {...register('requestSubjectDescription', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.requestSubjectDescription}
        helperText={errors.requestSubjectDescription?.message}
      />

      <TextField
        label="EventExpirationTimestamp"
        type="text"
        fullWidth
        margin="normal"
        {...register('eventExpirationTimestamp', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.eventExpirationTimestamp}
        helperText={errors.eventExpirationTimestamp?.message}
      />

      <TextField
        label="PaymentInitiationReference"
        type="text"
        fullWidth
        margin="normal"
        {...register('paymentInitiationReference', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.paymentInitiationReference}
        helperText={errors.paymentInitiationReference?.message}
      />

      <TextField
        label="ExternalSystemReference"
        type="text"
        fullWidth
        margin="normal"
        {...register('externalSystemReference', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.externalSystemReference}
        helperText={errors.externalSystemReference?.message}
      />

      <TextField
        label="DebitMethodCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('debitMethodCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.debitMethodCode}
        helperText={errors.debitMethodCode?.message}
      />

      <TextField
        label="TransactionSerialCode"
        type="number"
        fullWidth
        margin="normal"
        {...register('transactionSerialCode', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.transactionSerialCode}
        helperText={errors.transactionSerialCode?.message}
      />

      <TextField
        label="ProviderNbr"
        type="text"
        fullWidth
        margin="normal"
        {...register('providerNbr', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.providerNbr}
        helperText={errors.providerNbr?.message}
      />

      <TextField
        label="WhenInitialized"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenInitialized', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenInitialized}
        helperText={errors.whenInitialized?.message}
      />

      <TextField
        label="WhenValidated"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenValidated', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenValidated}
        helperText={errors.whenValidated?.message}
      />

      <TextField
        label="WhenCaptured"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenCaptured', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenCaptured}
        helperText={errors.whenCaptured?.message}
      />

      <TextField
        label="RequestStatusCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('requestStatusCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.requestStatusCode}
        helperText={errors.requestStatusCode?.message}
      />

      <TextField
        label="RequestStatusDescription"
        type="text"
        fullWidth
        margin="normal"
        {...register('requestStatusDescription', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.requestStatusDescription}
        helperText={errors.requestStatusDescription?.message}
      />

      <TextField
        label="IssuerAuthorizationNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('issuerAuthorizationNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.issuerAuthorizationNumber}
        helperText={errors.issuerAuthorizationNumber?.message}
      />

      <TextField
        label="LoggedAlertID"
        type="number"
        fullWidth
        margin="normal"
        {...register('loggedAlertID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.loggedAlertID}
        helperText={errors.loggedAlertID?.message}
      />

      <TextField
        label="ErrorText"
        type="text"
        fullWidth
        margin="normal"
        {...register('errorText', { maxLength: { value: 4000, message: 'Maximum length is 4000' } })}
        error={!!errors.errorText}
        helperText={errors.errorText?.message}
      />

      <TextField
        label="Comment"
        fullWidth
        margin="normal"
        {...register('comment', {  })}
        error={!!errors.comment}
        helperText={errors.comment?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="ExecutionTimeSec"
        type="number"
        fullWidth
        margin="normal"
        {...register('executionTimeSec', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.executionTimeSec}
        helperText={errors.executionTimeSec?.message}
      />

      <TextField
        label="Status"
        type="text"
        fullWidth
        margin="normal"
        {...register('status', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.status}
        helperText={errors.status?.message}
      />

      <TextField
        label="TransactionSerial"
        type="text"
        fullWidth
        margin="normal"
        {...register('transactionSerial', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.transactionSerial}
        helperText={errors.transactionSerial?.message}
      />

      <TextField
        label="NumberOfTries"
        type="number"
        fullWidth
        margin="normal"
        {...register('numberOfTries', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.numberOfTries}
        helperText={errors.numberOfTries?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isSuccessful')} />}
        label="IsSuccessful"
      />

      <TextField
        label="CustomerWalletID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerWalletID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerWalletID}
        helperText={errors.customerWalletID?.message}
      />

      <TextField
        label="CardLast4Numbers"
        type="text"
        fullWidth
        margin="normal"
        {...register('cardLast4Numbers', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.cardLast4Numbers}
        helperText={errors.cardLast4Numbers?.message}
      />

      <TextField
        label="ApprovalCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('approvalCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.approvalCode}
        helperText={errors.approvalCode?.message}
      />

      <TextField
        label="AllInfo"
        type="text"
        fullWidth
        margin="normal"
        {...register('allInfo', {  })}
        error={!!errors.allInfo}
        helperText={errors.allInfo?.message}
      />

      <TextField
        label="NonCustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('nonCustomerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.nonCustomerID}
        helperText={errors.nonCustomerID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/transactionBits')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


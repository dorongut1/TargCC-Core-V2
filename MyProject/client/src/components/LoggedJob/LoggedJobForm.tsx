// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_LoggedJob
//     Generated: 2025-12-17 08:54:21
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress } from '@mui/material';
import { useLoggedJob, useCreateLoggedJob, useUpdateLoggedJob } from '../../hooks/useLoggedJob';
import type { CreateLoggedJobRequest, UpdateLoggedJobRequest } from '../../types/LoggedJob.types';


export const LoggedJobForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const loggedJobId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!loggedJobId;

  const { data: existingLoggedJob, isPending: isLoadingEntity } = useLoggedJob(loggedJobId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateLoggedJobRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingLoggedJob) {
      reset(existingLoggedJob);
    }
  }, [existingLoggedJob, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateLoggedJob();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateLoggedJob();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateLoggedJobRequest) => {
    if (isEdit && loggedJobId) {
      updateEntity(
        { id: loggedJobId, data: data as UpdateLoggedJobRequest },
        { onSuccess: () => navigate('/loggedJobs') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/loggedJobs') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="CJobID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cJobID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cJobID}
        helperText={errors.cJobID?.message}
      />

      <TextField
        label="WhenStarted"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenStarted', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.whenStarted}
        helperText={errors.whenStarted?.message}
      />

      <TextField
        label="ActivatingUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('activatingUser', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.activatingUser}
        helperText={errors.activatingUser?.message}
      />

      <TextField
        label="LastRunBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastRunBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lastRunBy}
        helperText={errors.lastRunBy?.message}
      />

      <TextField
        label="ExecutionTimeSec"
        type="number"
        fullWidth
        margin="normal"
        {...register('executionTimeSec', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.executionTimeSec}
        helperText={errors.executionTimeSec?.message}
      />

      <TextField
        label="EnmRunStatusJobStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmRunStatusJobStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmRunStatusJobStatus}
        helperText={errors.enmRunStatusJobStatus?.message}
      />

      <TextField
        label="Remarks"
        type="text"
        fullWidth
        margin="normal"
        {...register('remarks', {  })}
        error={!!errors.remarks}
        helperText={errors.remarks?.message}
      />

      <TextField
        label="CLoggedAlertID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cLoggedAlertID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cLoggedAlertID}
        helperText={errors.cLoggedAlertID?.message}
      />

      <TextField
        label="SuccessCount"
        type="number"
        fullWidth
        margin="normal"
        {...register('successCount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.successCount}
        helperText={errors.successCount?.message}
      />

      <TextField
        label="FailureCount"
        type="number"
        fullWidth
        margin="normal"
        {...register('failureCount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.failureCount}
        helperText={errors.failureCount?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/loggedJobs')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: BackofficeTransaction
//     Generated: 2025-12-17 09:08:22
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useBackofficeTransaction, useCreateBackofficeTransaction, useUpdateBackofficeTransaction } from '../../hooks/useBackofficeTransaction';
import type { CreateBackofficeTransactionRequest, UpdateBackofficeTransactionRequest } from '../../types/BackofficeTransaction.types';


export const BackofficeTransactionForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const backofficeTransactionId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!backofficeTransactionId;

  const { data: existingBackofficeTransaction, isPending: isLoadingEntity } = useBackofficeTransaction(backofficeTransactionId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateBackofficeTransactionRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingBackofficeTransaction) {
      reset(existingBackofficeTransaction);
    }
  }, [existingBackofficeTransaction, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateBackofficeTransaction();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateBackofficeTransaction();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateBackofficeTransactionRequest) => {
    if (isEdit && backofficeTransactionId) {
      updateEntity(
        { id: backofficeTransactionId, data: data as UpdateBackofficeTransactionRequest },
        { onSuccess: () => navigate('/backofficeTransactions') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/backofficeTransactions') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameBackofficeTransaction')} />}
        label="TableNameBackofficeTransaction"
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="TransactionLoadID"
        type="number"
        fullWidth
        margin="normal"
        {...register('transactionLoadID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.transactionLoadID}
        helperText={errors.transactionLoadID?.message}
      />

      <TextField
        label="CardOrderFromIssuerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardOrderFromIssuerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardOrderFromIssuerID}
        helperText={errors.cardOrderFromIssuerID?.message}
      />

      <TextField
        label="CardOrderByDistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardOrderByDistributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardOrderByDistributorID}
        helperText={errors.cardOrderByDistributorID?.message}
      />

      <TextField
        label="CustomerWalletID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerWalletID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerWalletID}
        helperText={errors.customerWalletID?.message}
      />

      <TextField
        label="EnmBackofficeTransactionType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmBackofficeTransactionType', { required: 'EnmBackofficeTransactionType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmBackofficeTransactionType}
        helperText={errors.enmBackofficeTransactionType?.message}
      />

      <TextField
        label="AmountToAccountOwner"
        type="number"
        fullWidth
        margin="normal"
        {...register('amountToAccountOwner', { required: 'AmountToAccountOwner is required', maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amountToAccountOwner}
        helperText={errors.amountToAccountOwner?.message}
      />

      <TextField
        label="EnmAccountOwner"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmAccountOwner', { required: 'EnmAccountOwner is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmAccountOwner}
        helperText={errors.enmAccountOwner?.message}
      />

      <TextField
        label="WhenRequested"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenRequested', { required: 'WhenRequested is required', maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenRequested}
        helperText={errors.whenRequested?.message}
      />

      <TextField
        label="WhenToExecute"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenToExecute', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenToExecute}
        helperText={errors.whenToExecute?.message}
      />

      <TextField
        label="RequestingUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('requestingUser', { required: 'RequestingUser is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.requestingUser}
        helperText={errors.requestingUser?.message}
      />

      <TextField
        label="CodeForInvoice"
        type="text"
        fullWidth
        margin="normal"
        {...register('codeForInvoice', { required: 'CodeForInvoice is required', maxLength: { value: 300, message: 'Maximum length is 300' } })}
        error={!!errors.codeForInvoice}
        helperText={errors.codeForInvoice?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="WhenSentToSAP"
        fullWidth
        margin="normal"
        {...register('whenSentToSAP', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenSentToSAP}
        helperText={errors.whenSentToSAP?.message}
      />

      <TextField
        label="CardID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cardID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cardID}
        helperText={errors.cardID?.message}
      />

      <TextField
        label="TransactionCreditCardID"
        type="number"
        fullWidth
        margin="normal"
        {...register('transactionCreditCardID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.transactionCreditCardID}
        helperText={errors.transactionCreditCardID?.message}
      />

      <TextField
        label="TransactionBitID"
        type="number"
        fullWidth
        margin="normal"
        {...register('transactionBitID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.transactionBitID}
        helperText={errors.transactionBitID?.message}
      />

      <TextField
        label="RequestedTransactionID"
        type="number"
        fullWidth
        margin="normal"
        {...register('requestedTransactionID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.requestedTransactionID}
        helperText={errors.requestedTransactionID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/backofficeTransactions')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


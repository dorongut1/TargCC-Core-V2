// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: _report2
//     Generated: 2025-12-15 09:25:36
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { useReport2, useDeleteReport2 } from '../../hooks/useReport2';
import type { Report2 } from '../../types/Report2.types';


export const Report2Detail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useReport2(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteReport2();

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/report2s'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load report2</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/report2s')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/report2s/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            Report2 Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RowNumber
          </Typography>
          <Typography variant="body1">
            {entity.rowNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EventClass
          </Typography>
          <Typography variant="body1">
            {entity.eventClass}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TextData
          </Typography>
          <Typography variant="body1">
            {entity.textData}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ApplicationName
          </Typography>
          <Typography variant="body1">
            {entity.applicationName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            NTUserName
          </Typography>
          <Typography variant="body1">
            {entity.nTUserName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LoginName
          </Typography>
          <Typography variant="body1">
            {entity.loginName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Cpu
          </Typography>
          <Typography variant="body1">
            {entity.cpu}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Reads
          </Typography>
          <Typography variant="body1">
            {entity.reads}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Writes
          </Typography>
          <Typography variant="body1">
            {entity.writes}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Duration
          </Typography>
          <Typography variant="body1">
            {entity.duration}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ClientProcessID
          </Typography>
          <Typography variant="body1">
            {entity.clientProcessID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Spid
          </Typography>
          <Typography variant="body1">
            {entity.spid}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            StartTime
          </Typography>
          <Typography variant="body1">
            {(entity.startTime ? (entity.startTime instanceof Date ? entity.startTime.toLocaleDateString() : new Date(entity.startTime).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EndTime
          </Typography>
          <Typography variant="body1">
            {(entity.endTime ? (entity.endTime instanceof Date ? entity.endTime.toLocaleDateString() : new Date(entity.endTime).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            BinaryData
          </Typography>
          <Typography variant="body1">
            {entity.binaryData}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: _report2
//     Generated: 2025-12-17 21:11:12
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress } from '@mui/material';
import { useReport2, useCreateReport2, useUpdateReport2 } from '../../hooks/useReport2';
import type { CreateReport2Request, UpdateReport2Request } from '../../types/Report2.types';


export const Report2Form: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const report2Id = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!report2Id;

  const { data: existingReport2, isPending: isLoadingEntity } = useReport2(report2Id || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateReport2Request>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingReport2) {
      reset(existingReport2);
    }
  }, [existingReport2, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateReport2();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateReport2();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateReport2Request) => {
    if (isEdit && report2Id) {
      updateEntity(
        { id: report2Id, data: data as UpdateReport2Request },
        { onSuccess: () => navigate('/report2s') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/report2s') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="EventClass"
        type="number"
        fullWidth
        margin="normal"
        {...register('eventClass', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.eventClass}
        helperText={errors.eventClass?.message}
      />

      <TextField
        label="TextData"
        type="text"
        fullWidth
        margin="normal"
        {...register('textData', { maxLength: { value: 16, message: 'Maximum length is 16' } })}
        error={!!errors.textData}
        helperText={errors.textData?.message}
      />

      <TextField
        label="ApplicationName"
        type="text"
        fullWidth
        margin="normal"
        {...register('applicationName', { maxLength: { value: 256, message: 'Maximum length is 256' } })}
        error={!!errors.applicationName}
        helperText={errors.applicationName?.message}
      />

      <TextField
        label="NTUserName"
        type="text"
        fullWidth
        margin="normal"
        {...register('nTUserName', { maxLength: { value: 256, message: 'Maximum length is 256' } })}
        error={!!errors.nTUserName}
        helperText={errors.nTUserName?.message}
      />

      <TextField
        label="LoginName"
        type="text"
        fullWidth
        margin="normal"
        {...register('loginName', { maxLength: { value: 256, message: 'Maximum length is 256' } })}
        error={!!errors.loginName}
        helperText={errors.loginName?.message}
      />

      <TextField
        label="Cpu"
        type="number"
        fullWidth
        margin="normal"
        {...register('cpu', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.cpu}
        helperText={errors.cpu?.message}
      />

      <TextField
        label="Reads"
        type="number"
        fullWidth
        margin="normal"
        {...register('reads', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.reads}
        helperText={errors.reads?.message}
      />

      <TextField
        label="Writes"
        type="number"
        fullWidth
        margin="normal"
        {...register('writes', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.writes}
        helperText={errors.writes?.message}
      />

      <TextField
        label="Duration"
        type="number"
        fullWidth
        margin="normal"
        {...register('duration', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.duration}
        helperText={errors.duration?.message}
      />

      <TextField
        label="ClientProcessID"
        type="number"
        fullWidth
        margin="normal"
        {...register('clientProcessID', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.clientProcessID}
        helperText={errors.clientProcessID?.message}
      />

      <TextField
        label="Spid"
        type="number"
        fullWidth
        margin="normal"
        {...register('spid', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.spid}
        helperText={errors.spid?.message}
      />

      <TextField
        label="StartTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('startTime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.startTime}
        helperText={errors.startTime?.message}
      />

      <TextField
        label="EndTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('endTime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.endTime}
        helperText={errors.endTime?.message}
      />

      <TextField
        label="BinaryData"
        type="text"
        fullWidth
        margin="normal"
        {...register('binaryData', { maxLength: { value: 16, message: 'Maximum length is 16' } })}
        error={!!errors.binaryData}
        helperText={errors.binaryData?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/report2s')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


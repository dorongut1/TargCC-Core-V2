// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: MCCBSecurityCheck
//     Generated: 2025-12-18 07:30:19
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useMCCBSecurityCheck, useCreateMCCBSecurityCheck, useUpdateMCCBSecurityCheck } from '../../hooks/useMCCBSecurityCheck';
import type { CreateMCCBSecurityCheckRequest, UpdateMCCBSecurityCheckRequest } from '../../types/MCCBSecurityCheck.types';


export const MCCBSecurityCheckForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mCCBSecurityCheckId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mCCBSecurityCheckId;

  const { data: existingMCCBSecurityCheck, isPending: isLoadingEntity } = useMCCBSecurityCheck(mCCBSecurityCheckId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMCCBSecurityCheckRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMCCBSecurityCheck) {
      reset(existingMCCBSecurityCheck);
    }
  }, [existingMCCBSecurityCheck, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMCCBSecurityCheck();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMCCBSecurityCheck();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMCCBSecurityCheckRequest) => {
    if (isEdit && mCCBSecurityCheckId) {
      updateEntity(
        { id: mCCBSecurityCheckId, data: data as UpdateMCCBSecurityCheckRequest },
        { onSuccess: () => navigate('/mCCBSecurityChecks') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mCCBSecurityChecks') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameMCCBSecurityCheck')} />}
        label="TableNameMCCBSecurityCheck"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="MCCBReceiverID"
        type="number"
        fullWidth
        margin="normal"
        {...register('mCCBReceiverID', { required: 'MCCBReceiverID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.mCCBReceiverID}
        helperText={errors.mCCBReceiverID?.message}
      />

      <TextField
        label="DestinationCountryISO3166Alpha2"
        type="text"
        fullWidth
        margin="normal"
        {...register('destinationCountryISO3166Alpha2', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.destinationCountryISO3166Alpha2}
        helperText={errors.destinationCountryISO3166Alpha2?.message}
      />

      <TextField
        label="EnmReceiverSecurityClearedSecurityCleared"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmReceiverSecurityClearedSecurityCleared', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmReceiverSecurityClearedSecurityCleared}
        helperText={errors.enmReceiverSecurityClearedSecurityCleared?.message}
      />

      <TextField
        label="ReceiverSecurityAllData"
        type="text"
        fullWidth
        margin="normal"
        {...register('receiverSecurityAllData', {  })}
        error={!!errors.receiverSecurityAllData}
        helperText={errors.receiverSecurityAllData?.message}
      />

      <TextField
        label="EnmPayerSecurityClearedSecurityCleared"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmPayerSecurityClearedSecurityCleared', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmPayerSecurityClearedSecurityCleared}
        helperText={errors.enmPayerSecurityClearedSecurityCleared?.message}
      />

      <TextField
        label="PayerSecurityAllData"
        type="text"
        fullWidth
        margin="normal"
        {...register('payerSecurityAllData', {  })}
        error={!!errors.payerSecurityAllData}
        helperText={errors.payerSecurityAllData?.message}
      />

      <TextField
        label="WhenSecurityDataUpdated"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenSecurityDataUpdated', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenSecurityDataUpdated}
        helperText={errors.whenSecurityDataUpdated?.message}
      />

      <TextField
        label="RequestedTransactionID"
        type="number"
        fullWidth
        margin="normal"
        {...register('requestedTransactionID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.requestedTransactionID}
        helperText={errors.requestedTransactionID?.message}
      />

      <TextField
        label="MCCBTransactionID"
        type="number"
        fullWidth
        margin="normal"
        {...register('mCCBTransactionID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.mCCBTransactionID}
        helperText={errors.mCCBTransactionID?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mCCBSecurityChecks')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


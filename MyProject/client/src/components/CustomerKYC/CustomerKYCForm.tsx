// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CustomerKYC
//     Generated: 2025-12-17 21:45:23
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCustomerKYC, useCreateCustomerKYC, useUpdateCustomerKYC } from '../../hooks/useCustomerKYC';
import type { CreateCustomerKYCRequest, UpdateCustomerKYCRequest } from '../../types/CustomerKYC.types';


export const CustomerKYCForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const customerKYCId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!customerKYCId;

  const { data: existingCustomerKYC, isPending: isLoadingEntity } = useCustomerKYC(customerKYCId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCustomerKYCRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCustomerKYC) {
      reset(existingCustomerKYC);
    }
  }, [existingCustomerKYC, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCustomerKYC();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCustomerKYC();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCustomerKYCRequest) => {
    if (isEdit && customerKYCId) {
      updateEntity(
        { id: customerKYCId, data: data as UpdateCustomerKYCRequest },
        { onSuccess: () => navigate('/customerKYCs') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/customerKYCs') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCustomerKYC')} />}
        label="TableNameCustomerKYC"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="LkpEmploymentStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpEmploymentStatus', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lkpEmploymentStatus}
        helperText={errors.lkpEmploymentStatus?.message}
      />

      <TextField
        label="Employer"
        type="text"
        fullWidth
        margin="normal"
        {...register('employer', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.employer}
        helperText={errors.employer?.message}
      />

      <TextField
        label="EmploymentPosition"
        type="text"
        fullWidth
        margin="normal"
        {...register('employmentPosition', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.employmentPosition}
        helperText={errors.employmentPosition?.message}
      />

      <TextField
        label="PublicPosition"
        type="text"
        fullWidth
        margin="normal"
        {...register('publicPosition', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.publicPosition}
        helperText={errors.publicPosition?.message}
      />

      <TextField
        label="ConnectionWithPublicPerson"
        type="text"
        fullWidth
        margin="normal"
        {...register('connectionWithPublicPerson', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.connectionWithPublicPerson}
        helperText={errors.connectionWithPublicPerson?.message}
      />

      <TextField
        label="LkpAdditionalCitizenshipCountry"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpAdditionalCitizenshipCountry', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.lkpAdditionalCitizenshipCountry}
        helperText={errors.lkpAdditionalCitizenshipCountry?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('wasPreviouslyDeniedBankService')} />}
        label="WasPreviouslyDeniedBankService"
      />

      <FormControlLabel
        control={<Checkbox {...register('isSingleBeneficiary')} />}
        label="IsSingleBeneficiary"
      />

      <TextField
        label="AdditionalBeneficiaries"
        type="text"
        fullWidth
        margin="normal"
        {...register('additionalBeneficiaries', { maxLength: { value: 400, message: 'Maximum length is 400' } })}
        error={!!errors.additionalBeneficiaries}
        helperText={errors.additionalBeneficiaries?.message}
      />

      <TextField
        label="LkpSourceOfFunds"
        type="text"
        fullWidth
        margin="normal"
        {...register('lkpSourceOfFunds', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.lkpSourceOfFunds}
        helperText={errors.lkpSourceOfFunds?.message}
      />

      <TextField
        label="SourceOfFundsOther"
        type="text"
        fullWidth
        margin="normal"
        {...register('sourceOfFundsOther', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.sourceOfFundsOther}
        helperText={errors.sourceOfFundsOther?.message}
      />

      <Box sx={{ my: 2 }}>
        <Button variant="outlined" component="label">
          Upload ManiPremiumBeneficiaryDocument
          <input type="file" hidden {...register('maniPremiumBeneficiaryDocument')} />
        </Button>
      </Box>

      <TextField
        label="EnmManiPremiumBeneficiaryDocumentStatusDocumentationStatus"
        fullWidth
        margin="normal"
        {...register('enmManiPremiumBeneficiaryDocumentStatusDocumentationStatus', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmManiPremiumBeneficiaryDocumentStatusDocumentationStatus}
        helperText={errors.enmManiPremiumBeneficiaryDocumentStatusDocumentationStatus?.message}
      />

      <TextField
        label="ManiPremiumTextExpected"
        type="text"
        fullWidth
        margin="normal"
        {...register('maniPremiumTextExpected', { maxLength: { value: 4000, message: 'Maximum length is 4000' } })}
        error={!!errors.maniPremiumTextExpected}
        helperText={errors.maniPremiumTextExpected?.message}
      />

      <TextField
        label="ManiPremiumTextReceived"
        type="text"
        fullWidth
        margin="normal"
        {...register('maniPremiumTextReceived', { maxLength: { value: 4000, message: 'Maximum length is 4000' } })}
        error={!!errors.maniPremiumTextReceived}
        helperText={errors.maniPremiumTextReceived?.message}
      />

      <TextField
        label="ManiPremiumTextLanguage"
        type="text"
        fullWidth
        margin="normal"
        {...register('maniPremiumTextLanguage', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.maniPremiumTextLanguage}
        helperText={errors.maniPremiumTextLanguage?.message}
      />

      <TextField
        label="ManiPremiumTextScore"
        type="text"
        fullWidth
        margin="normal"
        {...register('maniPremiumTextScore', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.maniPremiumTextScore}
        helperText={errors.maniPremiumTextScore?.message}
      />

      <TextField
        label="ManiPremiumTextThreshold"
        type="text"
        fullWidth
        margin="normal"
        {...register('maniPremiumTextThreshold', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.maniPremiumTextThreshold}
        helperText={errors.maniPremiumTextThreshold?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <Box sx={{ my: 2 }}>
        <Button variant="outlined" component="label">
          Upload ManiPremiumTextVideo
          <input type="file" hidden {...register('maniPremiumTextVideo')} />
        </Button>
      </Box>



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/customerKYCs')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


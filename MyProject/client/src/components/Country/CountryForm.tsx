// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Country
//     Generated: 2025-12-17 09:08:25
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useCountry, useCreateCountry, useUpdateCountry } from '../../hooks/useCountry';
import type { CreateCountryRequest, UpdateCountryRequest } from '../../types/Country.types';


export const CountryForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const countryId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!countryId;

  const { data: existingCountry, isPending: isLoadingEntity } = useCountry(countryId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCountryRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCountry) {
      reset(existingCountry);
    }
  }, [existingCountry, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCountry();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCountry();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCountryRequest) => {
    if (isEdit && countryId) {
      updateEntity(
        { id: countryId, data: data as UpdateCountryRequest },
        { onSuccess: () => navigate('/countrys') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/countrys') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameCountry')} />}
        label="TableNameCountry"
      />

      <TextField
        label="ISO3166Numeric"
        type="text"
        fullWidth
        margin="normal"
        {...register('iSO3166Numeric', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.iSO3166Numeric}
        helperText={errors.iSO3166Numeric?.message}
      />

      <TextField
        label="ISO3166Alpha2"
        type="text"
        fullWidth
        margin="normal"
        {...register('iSO3166Alpha2', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.iSO3166Alpha2}
        helperText={errors.iSO3166Alpha2?.message}
      />

      <TextField
        label="Name"
        type="text"
        fullWidth
        margin="normal"
        {...register('name', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.name}
        helperText={errors.name?.message}
      />

      <TextField
        label="HebrewName"
        type="text"
        fullWidth
        margin="normal"
        {...register('hebrewName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.hebrewName}
        helperText={errors.hebrewName?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="Currency"
        type="text"
        fullWidth
        margin="normal"
        {...register('currency', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.currency}
        helperText={errors.currency?.message}
      />

      <TextField
        label="Region"
        type="text"
        fullWidth
        margin="normal"
        {...register('region', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.region}
        helperText={errors.region?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isGrayCountry')} />}
        label="IsGrayCountry"
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/countrys')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


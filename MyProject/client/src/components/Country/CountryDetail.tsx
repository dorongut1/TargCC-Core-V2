// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Country
//     Generated: 2025-12-16 18:22:29
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useCountry, useDeleteCountry } from '../../hooks/useCountry';
import { useCountryMCCBParticipants } from '../../hooks/useCountry';
import { useCountryMCCBReceivers } from '../../hooks/useCountry';
import { useCountryMCCBSecurityChecks } from '../../hooks/useCountry';


export const CountryDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useCountry(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteCountry();
  const { data: mCCBParticipants, isLoading: mCCBParticipantsLoading } = useCountryMCCBParticipants(id ? parseInt(id, 10) : null);
  const { data: mCCBReceivers, isLoading: mCCBReceiversLoading } = useCountryMCCBReceivers(id ? parseInt(id, 10) : null);
  const { data: mCCBSecurityChecks, isLoading: mCCBSecurityChecksLoading } = useCountryMCCBSecurityChecks(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/countrys'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load country</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/countrys')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/countrys/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            Country Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCountry
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCountry ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ISO3166Numeric
          </Typography>
          <Typography variant="body1">
            {entity.iSO3166Numeric}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ISO3166Alpha2
          </Typography>
          <Typography variant="body1">
            {entity.iSO3166Alpha2}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Name
          </Typography>
          <Typography variant="body1">
            {entity.name}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            HebrewName
          </Typography>
          <Typography variant="body1">
            {entity.hebrewName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? new Date(entity.deletedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Currency
          </Typography>
          <Typography variant="body1">
            {entity.currency}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Region
          </Typography>
          <Typography variant="body1">
            {entity.region}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsGrayCountry
          </Typography>
          <Typography variant="body1">
            {entity.isGrayCountry ? 'Yes' : 'No'}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            MCCBParticipants
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={mCCBParticipants || []}
              columns={[
                { field: 'tableNameMCCBParticipant', headerName: 'TableNameMCCBParticipant', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'enmCorridor', headerName: 'EnmCorridor', width: 150 },
                { field: 'countryISO3166Alpha2', headerName: 'CountryISO3166Alpha2', width: 150 },
                { field: 'payoutCurrency', headerName: 'PayoutCurrency', width: 150 },
              ]}
              loading={mCCBParticipantsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            MCCBReceivers
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={mCCBReceivers || []}
              columns={[
                { field: 'tableNameMCCBReceiver', headerName: 'TableNameMCCBReceiver', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'firstName', headerName: 'FirstName', width: 150 },
                { field: 'lastName', headerName: 'LastName', width: 150 },
              ]}
              loading={mCCBReceiversLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            MCCBSecurityChecks
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={mCCBSecurityChecks || []}
              columns={[
                { field: 'tableNameMCCBSecurityCheck', headerName: 'TableNameMCCBSecurityCheck', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
                { field: 'mCCBReceiverID', headerName: 'MCCBReceiverID', width: 100 },
                { field: 'destinationCountryISO3166Alpha2', headerName: 'DestinationCountryISO3166Alpha2', width: 150 },
              ]}
              loading={mCCBSecurityChecksLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


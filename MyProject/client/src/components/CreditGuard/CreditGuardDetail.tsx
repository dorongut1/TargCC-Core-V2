// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CreditGuard
//     Generated: 2025-12-17 14:36:06
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { useCreditGuard, useDeleteCreditGuard } from '../../hooks/useCreditGuard';


export const CreditGuardDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useCreditGuard(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteCreditGuard();

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/creditGuards'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load creditGuard</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/creditGuards')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/creditGuards/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            CreditGuard Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameCreditGuard
          </Typography>
          <Typography variant="body1">
            {entity.tableNameCreditGuard ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PurchasesMadeByDate01
          </Typography>
          <Typography variant="body1">
            {entity.purchasesMadeByDate01}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentDate01
          </Typography>
          <Typography variant="body1">
            {entity.paymentDate01}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsPaidNextMonth01
          </Typography>
          <Typography variant="body1">
            {entity.isPaidNextMonth01 ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            PaymentDate02
          </Typography>
          <Typography variant="body1">
            {entity.paymentDate02}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WebAPIURL
          </Typography>
          <Typography variant="body1">
            {entity.webAPIURL}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WebAPILoginUserName
          </Typography>
          <Typography variant="body1">
            {entity.webAPILoginUserName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EntWebAPILoginPassword
          </Typography>
          <Typography variant="body1">
            {entity.entWebAPILoginPassword}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WebAPIMainTerminalNumber
          </Typography>
          <Typography variant="body1">
            {entity.webAPIMainTerminalNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WebAPITerminalNumber
          </Typography>
          <Typography variant="body1">
            {entity.webAPITerminalNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RedirectDealerNumber
          </Typography>
          <Typography variant="body1">
            {entity.redirectDealerNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RedirectMainTerminalNumber
          </Typography>
          <Typography variant="body1">
            {entity.redirectMainTerminalNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RedirectTerminalNumber
          </Typography>
          <Typography variant="body1">
            {entity.redirectTerminalNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RedirectURL
          </Typography>
          <Typography variant="body1">
            {entity.redirectURL}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RedirectLoginUserName
          </Typography>
          <Typography variant="body1">
            {entity.redirectLoginUserName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EntRedirectLoginPassword
          </Typography>
          <Typography variant="body1">
            {entity.entRedirectLoginPassword}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RedirectPageDesignMid
          </Typography>
          <Typography variant="body1">
            {entity.redirectPageDesignMid}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            MaximumInterestRate3MonthsAndBelow
          </Typography>
          <Typography variant="body1">
            {entity.maximumInterestRate3MonthsAndBelow?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            MaximumInterestRateAbove3Months
          </Typography>
          <Typography variant="body1">
            {entity.maximumInterestRateAbove3Months?.toFixed(2) ?? 'N/A'}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: CreditGuard
//     Generated: 2025-12-11 21:35:46
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useCreditGuard, useCreateCreditGuard, useUpdateCreditGuard } from '../../hooks/useCreditGuard';
import type { CreateCreditGuardRequest, UpdateCreditGuardRequest } from '../../types/CreditGuard.types';


export const CreditGuardForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const creditGuardId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!creditGuardId;

  const { data: existingCreditGuard, isPending: isLoadingEntity } = useCreditGuard(creditGuardId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateCreditGuardRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingCreditGuard) {
      reset(existingCreditGuard);
    }
  }, [existingCreditGuard, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateCreditGuard();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateCreditGuard();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateCreditGuardRequest) => {
    if (isEdit && creditGuardId) {
      updateEntity(
        { id: creditGuardId, data: data as UpdateCreditGuardRequest },
        { onSuccess: () => navigate('/creditGuards') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/creditGuards') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="PurchasesMadeByDate01"
        type="number"
        fullWidth
        margin="normal"
        {...register('purchasesMadeByDate01', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.purchasesMadeByDate01}
        helperText={errors.purchasesMadeByDate01?.message}
      />

      <TextField
        label="PaymentDate01"
        type="number"
        fullWidth
        margin="normal"
        {...register('paymentDate01', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.paymentDate01}
        helperText={errors.paymentDate01?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('isPaidNextMonth01')} />}
        label="IsPaidNextMonth01"
      />

      <TextField
        label="PaymentDate02"
        type="number"
        fullWidth
        margin="normal"
        {...register('paymentDate02', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.paymentDate02}
        helperText={errors.paymentDate02?.message}
      />

      <TextField
        label="WebAPIURL"
        type="text"
        fullWidth
        margin="normal"
        {...register('webAPIURL', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^https?:\/\/.+/, message: 'Invalid URL' } })}
        error={!!errors.webAPIURL}
        helperText={errors.webAPIURL?.message}
      />

      <TextField
        label="WebAPILoginUserName"
        type="text"
        fullWidth
        margin="normal"
        {...register('webAPILoginUserName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.webAPILoginUserName}
        helperText={errors.webAPILoginUserName?.message}
      />

      <TextField
        label="EntWebAPILoginPassword"
        type="text"
        fullWidth
        margin="normal"
        {...register('entWebAPILoginPassword', {  })}
        error={!!errors.entWebAPILoginPassword}
        helperText={errors.entWebAPILoginPassword?.message}
      />

      <TextField
        label="WebAPIMainTerminalNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('webAPIMainTerminalNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.webAPIMainTerminalNumber}
        helperText={errors.webAPIMainTerminalNumber?.message}
      />

      <TextField
        label="WebAPITerminalNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('webAPITerminalNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.webAPITerminalNumber}
        helperText={errors.webAPITerminalNumber?.message}
      />

      <TextField
        label="RedirectDealerNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('redirectDealerNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.redirectDealerNumber}
        helperText={errors.redirectDealerNumber?.message}
      />

      <TextField
        label="RedirectMainTerminalNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('redirectMainTerminalNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.redirectMainTerminalNumber}
        helperText={errors.redirectMainTerminalNumber?.message}
      />

      <TextField
        label="RedirectTerminalNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('redirectTerminalNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.redirectTerminalNumber}
        helperText={errors.redirectTerminalNumber?.message}
      />

      <TextField
        label="RedirectURL"
        type="text"
        fullWidth
        margin="normal"
        {...register('redirectURL', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^https?:\/\/.+/, message: 'Invalid URL' } })}
        error={!!errors.redirectURL}
        helperText={errors.redirectURL?.message}
      />

      <TextField
        label="RedirectLoginUserName"
        type="text"
        fullWidth
        margin="normal"
        {...register('redirectLoginUserName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.redirectLoginUserName}
        helperText={errors.redirectLoginUserName?.message}
      />

      <TextField
        label="EntRedirectLoginPassword"
        type="text"
        fullWidth
        margin="normal"
        {...register('entRedirectLoginPassword', {  })}
        error={!!errors.entRedirectLoginPassword}
        helperText={errors.entRedirectLoginPassword?.message}
      />

      <TextField
        label="RedirectPageDesignMid"
        type="text"
        fullWidth
        margin="normal"
        {...register('redirectPageDesignMid', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.redirectPageDesignMid}
        helperText={errors.redirectPageDesignMid?.message}
      />

      <TextField
        label="MaximumInterestRate3MonthsAndBelow"
        type="number"
        fullWidth
        margin="normal"
        {...register('maximumInterestRate3MonthsAndBelow', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.maximumInterestRate3MonthsAndBelow}
        helperText={errors.maximumInterestRate3MonthsAndBelow?.message}
      />

      <TextField
        label="MaximumInterestRateAbove3Months"
        type="number"
        fullWidth
        margin="normal"
        {...register('maximumInterestRateAbove3Months', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.maximumInterestRateAbove3Months}
        helperText={errors.maximumInterestRateAbove3Months?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/creditGuards')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


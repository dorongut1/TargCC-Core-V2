// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Process
//     Generated: 2025-12-11 21:35:44
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid, GridColDef } from '@mui/x-data-grid';
import { useProcess, useDeleteProcess } from '../../hooks/useProcess';
import { useProcessPermissions } from '../../hooks/useProcess';
import type { Process } from '../../types/Process.types';
import type { Permission } from '../../types/Permission.types';


export const ProcessDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useProcess(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteProcess();
  const { data: permissions, isLoading: permissionsLoading } = useProcessPermissions(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/processs'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load process</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/processs')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/processs/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            Process Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Name
          </Typography>
          <Typography variant="body1">
            {entity.name}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DateChecked
          </Typography>
          <Typography variant="body1">
            {(entity.dateChecked ? (entity.dateChecked instanceof Date ? entity.dateChecked.toLocaleDateString() : new Date(entity.dateChecked).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            Permissions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={permissions || []}
              columns={[
                { field: 'tableNameCPermission', headerName: 'TableNameCPermission', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'cRoleID', headerName: 'CRoleID', width: 100 },
                { field: 'cProcessID', headerName: 'CProcessID', width: 100 },
                { field: 'canDo', headerName: 'CanDo', width: 150 },
              ]}
              loading={permissionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>
    </Box>
  );
};


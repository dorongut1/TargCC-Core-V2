// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Process
//     Generated: 2025-12-15 22:04:07
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress } from '@mui/material';
import { useProcess, useCreateProcess, useUpdateProcess } from '../../hooks/useProcess';
import type { CreateProcessRequest, UpdateProcessRequest } from '../../types/Process.types';


export const ProcessForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const processId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!processId;

  const { data: existingProcess, isPending: isLoadingEntity } = useProcess(processId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateProcessRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingProcess) {
      reset(existingProcess);
    }
  }, [existingProcess, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateProcess();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateProcess();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateProcessRequest) => {
    if (isEdit && processId) {
      updateEntity(
        { id: processId, data: data as UpdateProcessRequest },
        { onSuccess: () => navigate('/processs') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/processs') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="Name"
        type="text"
        fullWidth
        margin="normal"
        {...register('name', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.name}
        helperText={errors.name?.message}
      />

      <TextField
        label="DateChecked"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('dateChecked', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.dateChecked}
        helperText={errors.dateChecked?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/processs')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: RiskEvent
//     Generated: 2025-12-17 08:24:04
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card as MuiCard, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid } from '@mui/x-data-grid';
import { useRiskEvent, useDeleteRiskEvent } from '../../hooks/useRiskEvent';
import { useRiskEventActiveRestrictionses } from '../../hooks/useRiskEvent';


export const RiskEventDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useRiskEvent(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteRiskEvent();
  const { data: activeRestrictionses, isLoading: activeRestrictionsesLoading } = useRiskEventActiveRestrictionses(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/riskEvents'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load riskEvent</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/riskEvents')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/riskEvents/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <MuiCard>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            RiskEvent Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameRiskEvent
          </Typography>
          <Typography variant="body1">
            {entity.tableNameRiskEvent ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            RiskRuleID
          </Typography>
          <Typography variant="body1">
            {entity.riskRuleID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmEntityType
          </Typography>
          <Typography variant="body1">
            {entity.enmEntityType}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EntityID
          </Typography>
          <Typography variant="body1">
            {entity.entityID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DetectedOn
          </Typography>
          <Typography variant="body1">
            {(entity.detectedOn ? new Date(entity.detectedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ActivatedOn
          </Typography>
          <Typography variant="body1">
            {(entity.activatedOn ? new Date(entity.activatedOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ValidUntil
          </Typography>
          <Typography variant="body1">
            {(entity.validUntil ? new Date(entity.validUntil).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmActionTaken
          </Typography>
          <Typography variant="body1">
            {entity.enmActionTaken}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            EnmStatus
          </Typography>
          <Typography variant="body1">
            {entity.enmStatus}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CLoggedAlertID
          </Typography>
          <Typography variant="body1">
            {entity.cLoggedAlertID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TaskID
          </Typography>
          <Typography variant="body1">
            {entity.taskID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TriggerData
          </Typography>
          <Typography variant="body1">
            {entity.triggerData}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SystemNotes
          </Typography>
          <Typography variant="body1">
            {entity.systemNotes}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ManuallyOverriddenOn
          </Typography>
          <Typography variant="body1">
            {(entity.manuallyOverriddenOn ? new Date(entity.manuallyOverriddenOn).toLocaleDateString() : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ManuallyOverriddenByUserID
          </Typography>
          <Typography variant="body1">
            {entity.manuallyOverriddenByUserID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            ManualNotes
          </Typography>
          <Typography variant="body1">
            {entity.manualNotes}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </MuiCard>

      <MuiCard sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            ActiveRestrictionses
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={activeRestrictionses || []}
              columns={[
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'tableNameActiveRestrictions', headerName: 'TableNameActiveRestrictions', width: 150 },
                { field: 'enmEntityType', headerName: 'EnmEntityType', width: 150 },
                { field: 'entityID', headerName: 'EntityID', width: 100 },
                { field: 'enmBlockedActionType', headerName: 'EnmBlockedActionType', width: 150 },
              ]}
              loading={activeRestrictionsesLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </MuiCard>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: RiskEvent
//     Generated: 2025-12-17 09:02:40
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useRiskEvent, useCreateRiskEvent, useUpdateRiskEvent } from '../../hooks/useRiskEvent';
import type { CreateRiskEventRequest, UpdateRiskEventRequest } from '../../types/RiskEvent.types';


export const RiskEventForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const riskEventId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!riskEventId;

  const { data: existingRiskEvent, isPending: isLoadingEntity } = useRiskEvent(riskEventId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateRiskEventRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingRiskEvent) {
      reset(existingRiskEvent);
    }
  }, [existingRiskEvent, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateRiskEvent();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateRiskEvent();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateRiskEventRequest) => {
    if (isEdit && riskEventId) {
      updateEntity(
        { id: riskEventId, data: data as UpdateRiskEventRequest },
        { onSuccess: () => navigate('/riskEvents') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/riskEvents') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameRiskEvent')} />}
        label="TableNameRiskEvent"
      />

      <TextField
        label="RiskRuleID"
        type="number"
        fullWidth
        margin="normal"
        {...register('riskRuleID', { required: 'RiskRuleID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.riskRuleID}
        helperText={errors.riskRuleID?.message}
      />

      <TextField
        label="EnmEntityType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmEntityType', { required: 'EnmEntityType is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmEntityType}
        helperText={errors.enmEntityType?.message}
      />

      <TextField
        label="EntityID"
        type="number"
        fullWidth
        margin="normal"
        {...register('entityID', { required: 'EntityID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.entityID}
        helperText={errors.entityID?.message}
      />

      <TextField
        label="DetectedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('detectedOn', { required: 'DetectedOn is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.detectedOn}
        helperText={errors.detectedOn?.message}
      />

      <TextField
        label="ActivatedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('activatedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.activatedOn}
        helperText={errors.activatedOn?.message}
      />

      <TextField
        label="ValidUntil"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('validUntil', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.validUntil}
        helperText={errors.validUntil?.message}
      />

      <TextField
        label="EnmActionTaken"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmActionTaken', { required: 'EnmActionTaken is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmActionTaken}
        helperText={errors.enmActionTaken?.message}
      />

      <TextField
        label="EnmStatus"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmStatus', { required: 'EnmStatus is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmStatus}
        helperText={errors.enmStatus?.message}
      />

      <TextField
        label="CLoggedAlertID"
        type="number"
        fullWidth
        margin="normal"
        {...register('cLoggedAlertID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.cLoggedAlertID}
        helperText={errors.cLoggedAlertID?.message}
      />

      <TextField
        label="TaskID"
        type="number"
        fullWidth
        margin="normal"
        {...register('taskID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.taskID}
        helperText={errors.taskID?.message}
      />

      <TextField
        label="TriggerData"
        type="text"
        fullWidth
        margin="normal"
        {...register('triggerData', {  })}
        error={!!errors.triggerData}
        helperText={errors.triggerData?.message}
      />

      <TextField
        label="SystemNotes"
        type="text"
        fullWidth
        margin="normal"
        {...register('systemNotes', {  })}
        error={!!errors.systemNotes}
        helperText={errors.systemNotes?.message}
      />

      <TextField
        label="ManuallyOverriddenOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('manuallyOverriddenOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.manuallyOverriddenOn}
        helperText={errors.manuallyOverriddenOn?.message}
      />

      <TextField
        label="ManuallyOverriddenByUserID"
        type="number"
        fullWidth
        margin="normal"
        {...register('manuallyOverriddenByUserID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.manuallyOverriddenByUserID}
        helperText={errors.manuallyOverriddenByUserID?.message}
      />

      <TextField
        label="ManualNotes"
        type="text"
        fullWidth
        margin="normal"
        {...register('manualNotes', {  })}
        error={!!errors.manualNotes}
        helperText={errors.manualNotes?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/riskEvents')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


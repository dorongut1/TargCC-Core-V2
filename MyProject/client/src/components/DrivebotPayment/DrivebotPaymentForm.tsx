// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: DrivebotPayment
//     Generated: 2025-12-17 08:24:02
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useDrivebotPayment, useCreateDrivebotPayment, useUpdateDrivebotPayment } from '../../hooks/useDrivebotPayment';
import type { CreateDrivebotPaymentRequest, UpdateDrivebotPaymentRequest } from '../../types/DrivebotPayment.types';


export const DrivebotPaymentForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const drivebotPaymentId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!drivebotPaymentId;

  const { data: existingDrivebotPayment, isPending: isLoadingEntity } = useDrivebotPayment(drivebotPaymentId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateDrivebotPaymentRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingDrivebotPayment) {
      reset(existingDrivebotPayment);
    }
  }, [existingDrivebotPayment, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateDrivebotPayment();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateDrivebotPayment();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateDrivebotPaymentRequest) => {
    if (isEdit && drivebotPaymentId) {
      updateEntity(
        { id: drivebotPaymentId, data: data as UpdateDrivebotPaymentRequest },
        { onSuccess: () => navigate('/drivebotPayments') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/drivebotPayments') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameDrivebotPayment')} />}
        label="TableNameDrivebotPayment"
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { required: 'CustomerID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="PartnershipCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('partnershipCode', { required: 'PartnershipCode is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.partnershipCode}
        helperText={errors.partnershipCode?.message}
      />

      <TextField
        label="DriverName"
        type="text"
        fullWidth
        margin="normal"
        {...register('driverName', { required: 'DriverName is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.driverName}
        helperText={errors.driverName?.message}
      />

      <TextField
        label="StationId"
        type="text"
        fullWidth
        margin="normal"
        {...register('stationId', { required: 'StationId is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.stationId}
        helperText={errors.stationId?.message}
      />

      <TextField
        label="StationName"
        type="text"
        fullWidth
        margin="normal"
        {...register('stationName', { required: 'StationName is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.stationName}
        helperText={errors.stationName?.message}
      />

      <TextField
        label="Amount"
        type="number"
        fullWidth
        margin="normal"
        {...register('amount', { required: 'Amount is required', maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.amount}
        helperText={errors.amount?.message}
      />

      <TextField
        label="WalletPhoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('walletPhoneNumber', { required: 'WalletPhoneNumber is required', maxLength: { value: 100, message: 'Maximum length is 100' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.walletPhoneNumber}
        helperText={errors.walletPhoneNumber?.message}
      />

      <TextField
        label="RequestedTransactionID"
        type="number"
        fullWidth
        margin="normal"
        {...register('requestedTransactionID', { required: 'RequestedTransactionID is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.requestedTransactionID}
        helperText={errors.requestedTransactionID?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('maniSuccess')} />}
        label="ManiSuccess"
      />

      <TextField
        label="ManiErrorDescription"
        type="text"
        fullWidth
        margin="normal"
        {...register('maniErrorDescription', { maxLength: { value: 256, message: 'Maximum length is 256' } })}
        error={!!errors.maniErrorDescription}
        helperText={errors.maniErrorDescription?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('drivebotSuccess')} />}
        label="DrivebotSuccess"
      />

      <TextField
        label="DrivebotDescription"
        type="text"
        fullWidth
        margin="normal"
        {...register('drivebotDescription', { maxLength: { value: 256, message: 'Maximum length is 256' } })}
        error={!!errors.drivebotDescription}
        helperText={errors.drivebotDescription?.message}
      />

      <TextField
        label="DrivebotUpdateTime"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('drivebotUpdateTime', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.drivebotUpdateTime}
        helperText={errors.drivebotUpdateTime?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/drivebotPayments')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: NonCustomer
//     Generated: 2025-12-11 22:10:39
// </auto-generated>

import React from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { Box, Typography, Button, CircularProgress, Alert, Card, CardContent, Grid } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon, ArrowBack as ArrowBackIcon } from '@mui/icons-material';
import { DataGrid, GridColDef } from '@mui/x-data-grid';
import { useNonCustomer, useDeleteNonCustomer } from '../../hooks/useNonCustomer';
import { useNonCustomerRequestedTransactions } from '../../hooks/useNonCustomer';
import { useNonCustomerTransactionBits } from '../../hooks/useNonCustomer';
import { useNonCustomerTransactionCreditCards } from '../../hooks/useNonCustomer';
import type { NonCustomer } from '../../types/NonCustomer.types';
import type { RequestedTransaction } from '../../types/RequestedTransaction.types';
import type { TransactionBit } from '../../types/TransactionBit.types';
import type { TransactionCreditCard } from '../../types/TransactionCreditCard.types';


export const NonCustomerDetail: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const { data: entity, isLoading, error } = useNonCustomer(id ? parseInt(id, 10) : null);
  const { mutate: deleteEntity } = useDeleteNonCustomer();
  const { data: requestedTransactions, isLoading: requestedTransactionsLoading } = useNonCustomerRequestedTransactions(id ? parseInt(id, 10) : null);
  const { data: transactionBits, isLoading: transactionBitsLoading } = useNonCustomerTransactionBits(id ? parseInt(id, 10) : null);
  const { data: transactionCreditCards, isLoading: transactionCreditCardsLoading } = useNonCustomerTransactionCreditCards(id ? parseInt(id, 10) : null);

  const handleDelete = () => {
    if (confirm('Are you sure you want to delete this item?')) {
      deleteEntity(parseInt(id!, 10), {
        onSuccess: () => navigate('/nonCustomers'),
      });
    }
  };

  if (isLoading) {
    return <CircularProgress />;
  }

  if (error || !entity) {
    return <Alert severity="error">Failed to load nonCustomer</Alert>;
  }

  return (
    <Box sx={{ maxWidth: 800 }}>
      <Box sx={{ mb: 2, display: 'flex', gap: 2 }}>
        <Button
          variant="outlined"
          startIcon={<ArrowBackIcon />}
          onClick={() => navigate('/nonCustomers')}
        >
          Back
        </Button>
        <Button
          variant="contained"
          startIcon={<EditIcon />}
          onClick={() => navigate(`/nonCustomers/${id}/edit`)}
        >
          Edit
        </Button>
        <Button
          variant="outlined"
          color="error"
          startIcon={<DeleteIcon />}
          onClick={handleDelete}
        >
          Delete
        </Button>
      </Box>

      <Card>
        <CardContent>
          <Typography variant="h5" component="h2" gutterBottom>
            NonCustomer Details
          </Typography>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            TableNameNonCustomer
          </Typography>
          <Typography variant="body1">
            {entity.tableNameNonCustomer ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Id
          </Typography>
          <Typography variant="body1">
            {entity.id}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            FirstName
          </Typography>
          <Typography variant="body1">
            {entity.firstName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            LastName
          </Typography>
          <Typography variant="body1">
            {entity.lastName}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            CellphoneNumber
          </Typography>
          <Typography variant="body1">
            {entity.cellphoneNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IdentityNumber
          </Typography>
          <Typography variant="body1">
            {entity.identityNumber}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenLastReceivedPaymentDemand
          </Typography>
          <Typography variant="body1">
            {(entity.whenLastReceivedPaymentDemand ? (entity.whenLastReceivedPaymentDemand instanceof Date ? entity.whenLastReceivedPaymentDemand.toLocaleDateString() : new Date(entity.whenLastReceivedPaymentDemand).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            WhenLastSentFunds
          </Typography>
          <Typography variant="body1">
            {(entity.whenLastSentFunds ? (entity.whenLastSentFunds instanceof Date ? entity.whenLastSentFunds.toLocaleDateString() : new Date(entity.whenLastSentFunds).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SentTodayAmount
          </Typography>
          <Typography variant="body1">
            {entity.sentTodayAmount}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SentTodayCount
          </Typography>
          <Typography variant="body1">
            {entity.sentTodayCount}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SentLast7DaysAmount
          </Typography>
          <Typography variant="body1">
            {entity.sentLast7DaysAmount}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SentLast7DaysCount
          </Typography>
          <Typography variant="body1">
            {entity.sentLast7DaysCount}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SentLast30DaysAmount
          </Typography>
          <Typography variant="body1">
            {entity.sentLast30DaysAmount}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SentLast30DaysCount
          </Typography>
          <Typography variant="body1">
            {entity.sentLast30DaysCount}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            SentTotalAmount
          </Typography>
          <Typography variant="body1">
            {entity.sentTotalAmount}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            IsBlocked
          </Typography>
          <Typography variant="body1">
            {entity.isBlocked ? 'Yes' : 'No'}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Comments
          </Typography>
          <Typography variant="body1">
            {entity.comments}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedBy
          </Typography>
          <Typography variant="body1">
            {entity.deletedBy}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            DeletedOn
          </Typography>
          <Typography variant="body1">
            {(entity.deletedOn ? (entity.deletedOn instanceof Date ? entity.deletedOn.toLocaleDateString() : new Date(entity.deletedOn).toLocaleDateString()) : 'N/A')}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            UpdatingLoginID
          </Typography>
          <Typography variant="body1">
            {entity.updatingLoginID}
          </Typography>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Typography variant="subtitle2" color="text.secondary">
            Heading
          </Typography>
          <Typography variant="body1">
            {entity.heading}
          </Typography>
        </Grid>

      </Grid>

        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            RequestedTransactions
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={requestedTransactions || []}
              columns={[
                { field: 'tableNameRequestedTransaction', headerName: 'TableNameRequestedTransaction', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'initiatingCustomerID', headerName: 'InitiatingCustomerID', width: 100 },
                { field: 'targetCustomerID', headerName: 'TargetCustomerID', width: 100 },
                { field: 'targetCustomerEmailOrPhone', headerName: 'TargetCustomerEmailOrPhone', width: 150 },
              ]}
              loading={requestedTransactionsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            TransactionBits
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={transactionBits || []}
              columns={[
                { field: 'tableNameTransactionBit', headerName: 'TableNameTransactionBit', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'uniqueIdentifier', headerName: 'UniqueIdentifier', width: 150 },
                { field: 'requestedTransactionID', headerName: 'RequestedTransactionID', width: 100 },
                { field: 'customerID', headerName: 'CustomerID', width: 100 },
              ]}
              loading={transactionBitsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>

      <Card sx={{ mt: 2 }}>
        <CardContent>
          <Typography variant="h6" gutterBottom>
            TransactionCreditCards
          </Typography>
          <Box sx={{ height: 400, width: '100%' }}>
            <DataGrid
              rows={transactionCreditCards || []}
              columns={[
                { field: 'tableNameTransactionCreditCard', headerName: 'TableNameTransactionCreditCard', width: 150 },
                { field: 'id', headerName: 'Id', width: 100 },
                { field: 'uniqueIdentifier', headerName: 'UniqueIdentifier', width: 150 },
                { field: 'whenInitialized', headerName: 'WhenInitialized', width: 150 },
                { field: 'whenValidated', headerName: 'WhenValidated', width: 150 },
              ]}
              loading={transactionCreditCardsLoading}
              pageSizeOptions={[5, 10, 25]}
              initialState={{
                pagination: { paginationModel: { pageSize: 5 } },
              }}
            />
          </Box>
        </CardContent>
      </Card>
    </Box>
  );
};


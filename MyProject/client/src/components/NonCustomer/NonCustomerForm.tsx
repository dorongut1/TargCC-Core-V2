// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: NonCustomer
//     Generated: 2025-12-16 10:02:29
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useNonCustomer, useCreateNonCustomer, useUpdateNonCustomer } from '../../hooks/useNonCustomer';
import type { CreateNonCustomerRequest, UpdateNonCustomerRequest } from '../../types/NonCustomer.types';


export const NonCustomerForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const nonCustomerId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!nonCustomerId;

  const { data: existingNonCustomer, isPending: isLoadingEntity } = useNonCustomer(nonCustomerId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateNonCustomerRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingNonCustomer) {
      reset(existingNonCustomer);
    }
  }, [existingNonCustomer, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateNonCustomer();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateNonCustomer();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateNonCustomerRequest) => {
    if (isEdit && nonCustomerId) {
      updateEntity(
        { id: nonCustomerId, data: data as UpdateNonCustomerRequest },
        { onSuccess: () => navigate('/nonCustomers') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/nonCustomers') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameNonCustomer')} />}
        label="TableNameNonCustomer"
      />

      <TextField
        label="FirstName"
        type="text"
        fullWidth
        margin="normal"
        {...register('firstName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.firstName}
        helperText={errors.firstName?.message}
      />

      <TextField
        label="LastName"
        type="text"
        fullWidth
        margin="normal"
        {...register('lastName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.lastName}
        helperText={errors.lastName?.message}
      />

      <TextField
        label="CellphoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('cellphoneNumber', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.cellphoneNumber}
        helperText={errors.cellphoneNumber?.message}
      />

      <TextField
        label="IdentityNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('identityNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.identityNumber}
        helperText={errors.identityNumber?.message}
      />

      <TextField
        label="WhenLastReceivedPaymentDemand"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenLastReceivedPaymentDemand', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.whenLastReceivedPaymentDemand}
        helperText={errors.whenLastReceivedPaymentDemand?.message}
      />

      <TextField
        label="WhenLastSentFunds"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenLastSentFunds', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.whenLastSentFunds}
        helperText={errors.whenLastSentFunds?.message}
      />

      <TextField
        label="SentTodayAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('sentTodayAmount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.sentTodayAmount}
        helperText={errors.sentTodayAmount?.message}
      />

      <TextField
        label="SentTodayCount"
        type="number"
        fullWidth
        margin="normal"
        {...register('sentTodayCount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.sentTodayCount}
        helperText={errors.sentTodayCount?.message}
      />

      <TextField
        label="SentLast7DaysAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('sentLast7DaysAmount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.sentLast7DaysAmount}
        helperText={errors.sentLast7DaysAmount?.message}
      />

      <TextField
        label="SentLast7DaysCount"
        type="number"
        fullWidth
        margin="normal"
        {...register('sentLast7DaysCount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.sentLast7DaysCount}
        helperText={errors.sentLast7DaysCount?.message}
      />

      <TextField
        label="SentLast30DaysAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('sentLast30DaysAmount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.sentLast30DaysAmount}
        helperText={errors.sentLast30DaysAmount?.message}
      />

      <TextField
        label="SentLast30DaysCount"
        type="number"
        fullWidth
        margin="normal"
        {...register('sentLast30DaysCount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.sentLast30DaysCount}
        helperText={errors.sentLast30DaysCount?.message}
      />

      <TextField
        label="SentTotalAmount"
        type="number"
        fullWidth
        margin="normal"
        {...register('sentTotalAmount', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.sentTotalAmount}
        helperText={errors.sentTotalAmount?.message}
      />

      <TextField
        label="IsBlocked"
        fullWidth
        margin="normal"
        {...register('isBlocked', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.isBlocked}
        helperText={errors.isBlocked?.message}
      />

      <TextField
        label="Comments"
        fullWidth
        margin="normal"
        {...register('comments', { maxLength: { value: 4000, message: 'Maximum length is 4000' } })}
        error={!!errors.comments}
        helperText={errors.comments?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/nonCustomers')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


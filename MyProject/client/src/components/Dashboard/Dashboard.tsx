// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Dashboard Component
// </auto-generated>

import React from 'react';
import { useNavigate } from 'react-router-dom';
import { useQuery } from '@tanstack/react-query';
import {
  Box,
  Grid,
  Card,
  CardContent,
  CardHeader,
  Typography,
  Button,
  CircularProgress,
  Alert,
  Avatar,
  List,
  ListItem,
  ListItemAvatar,
  ListItemText,
} from '@mui/material';
import {
  People as PeopleIcon,
  ShoppingCart as ShoppingCartIcon,
  Inventory as InventoryIcon,
  Add as AddIcon,
  BarChart as BarChartIcon,
  Settings as SettingsIcon,
  AccessTime as AccessTimeIcon,
} from '@mui/icons-material';
import { LineChart, Line, PieChart, Pie, Cell, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from 'recharts';
import { format } from 'date-fns';
import apiClient from '../../api/client';

interface DashboardStats {
  totalReport2s: number;
  report2sGrowth: number;
  totalActiveRestrictionss: number;
  activeRestrictionssGrowth: number;
  totalBackofficeTransactions: number;
  backofficeTransactionsGrowth: number;
}

interface Activity {
  id: number;
  title: string;
  time: string;
  icon: string;
}

export const Dashboard: React.FC = () => {
  const navigate = useNavigate();

  // Fetch dashboard stats
  const { data: stats, isLoading: statsLoading } = useQuery<DashboardStats>({
    queryKey: ['dashboardStats'],
    queryFn: async () => {
      const response = await apiClient.get<DashboardStats>('/dashboard/stats');
      return response.data;
    },
  });

  // Fetch recent activity
  const { data: activities, isLoading: activitiesLoading } = useQuery<Activity[]>({
    queryKey: ['recentActivity'],
    queryFn: async () => {
      const response = await apiClient.get<Activity[]>('/dashboard/recent-activity');
      return response.data;
    },
  });

  // Sample data for charts (replace with real API data)
  const revenueData = [
    { month: 'Jan', revenue: 4000 },
    { month: 'Feb', revenue: 3000 },
    { month: 'Mar', revenue: 5000 },
    { month: 'Apr', revenue: 4500 },
    { month: 'May', revenue: 6000 },
    { month: 'Jun', revenue: 7000 },
  ];

  const statusData = [
    { name: 'Active', value: 60 },
    { name: 'Pending', value: 25 },
    { name: 'Completed', value: 15 },
  ];

  const COLORS = ['#0088FE', '#00C49F', '#FFBB28', '#FF8042'];

  if (statsLoading) {
    return (
      <Box display="flex" justifyContent="center" alignItems="center" minHeight="80vh">
        <CircularProgress />
      </Box>
    );
  }

  return (
    <Box>
      {/* Welcome Section */}
      <Box mb={3}>
        <Typography variant="h4" gutterBottom>
          Welcome back! 👋
        </Typography>
        <Typography variant="body1" color="text.secondary">
          {format(new Date(), 'EEEE, MMMM d, yyyy')}
        </Typography>
      </Box>

      {/* KPI Cards */}
      <Grid container spacing={3} mb={3}>
          <Grid item xs={12} sm={4}>
            <Card sx={{ bgcolor: '#4472C4', color: 'white' }}>
              <CardContent>
                <Box display="flex" alignItems="center" gap={2}>
                  <PeopleIcon sx={{ fontSize: 48 }} />
                  <Box>
                    <Typography variant="h4">{stats?.totalReport2s || 0}</Typography>
                    <Typography variant="body1">Total Report2s</Typography>
                    <Typography variant="caption" sx={{ color: 'lightgreen' }}>
                      +{stats?.report2sGrowth || 0}% vs last month
                    </Typography>
                  </Box>
                </Box>
              </CardContent>
            </Card>
          </Grid>
          <Grid item xs={12} sm={4}>
            <Card sx={{ bgcolor: '#70AD47', color: 'white' }}>
              <CardContent>
                <Box display="flex" alignItems="center" gap={2}>
                  <ShoppingCartIcon sx={{ fontSize: 48 }} />
                  <Box>
                    <Typography variant="h4">{stats?.totalActiveRestrictionss || 0}</Typography>
                    <Typography variant="body1">Total ActiveRestrictionss</Typography>
                    <Typography variant="caption" sx={{ color: 'lightgreen' }}>
                      +{stats?.activeRestrictionssGrowth || 0}% vs last month
                    </Typography>
                  </Box>
                </Box>
              </CardContent>
            </Card>
          </Grid>
          <Grid item xs={12} sm={4}>
            <Card sx={{ bgcolor: '#FFC000', color: 'white' }}>
              <CardContent>
                <Box display="flex" alignItems="center" gap={2}>
                  <InventoryIcon sx={{ fontSize: 48 }} />
                  <Box>
                    <Typography variant="h4">{stats?.totalBackofficeTransactions || 0}</Typography>
                    <Typography variant="body1">Total BackofficeTransactions</Typography>
                    <Typography variant="caption" sx={{ color: 'lightgreen' }}>
                      +{stats?.backofficeTransactionsGrowth || 0}% vs last month
                    </Typography>
                  </Box>
                </Box>
              </CardContent>
            </Card>
          </Grid>
      </Grid>

      {/* Charts */}
      <Grid container spacing={3} mb={3}>
        <Grid item xs={12} md={6}>
          <Card>
            <CardHeader title="Revenue Trend" />
            <CardContent>
              <ResponsiveContainer width="100%" height={300}>
                <LineChart data={revenueData}>
                  <CartesianGrid strokeDasharray="3 3" />
                  <XAxis dataKey="month" />
                  <YAxis />
                  <Tooltip />
                  <Legend />
                  <Line type="monotone" dataKey="revenue" stroke="#4472C4" strokeWidth={2} />
                </LineChart>
              </ResponsiveContainer>
            </CardContent>
          </Card>
        </Grid>

        <Grid item xs={12} md={6}>
          <Card>
            <CardHeader title="Status Distribution" />
            <CardContent>
              <ResponsiveContainer width="100%" height={300}>
                <PieChart>
                  <Pie
                    data={statusData}
                    cx="50%"
                    cy="50%"
                    labelLine={false}
                    label={(entry) => entry.name}
                    outerRadius={100}
                    fill="#8884d8"
                    dataKey="value"
                  >
                    {statusData.map((entry, index) => (
                      <Cell key={`cell-${index}`} fill={COLORS[index % COLORS.length]} />
                    ))}
                  </Pie>
                  <Tooltip />
                </PieChart>
              </ResponsiveContainer>
            </CardContent>
          </Card>
        </Grid>
      </Grid>

      {/* Quick Actions and Recent Activity */}
      <Grid container spacing={3}>
        <Grid item xs={12} md={6}>
          <Card>
            <CardHeader title="Quick Actions" />
            <CardContent>
              <Box display="flex" gap={2} flexWrap="wrap">
                <Button
                  variant="contained"
                  startIcon={<AddIcon />}
                  onClick={() => navigate('/report2s/new')}
                >
                  New Report2
                </Button>
                <Button
                  variant="contained"
                  startIcon={<AddIcon />}
                  onClick={() => navigate('/activeRestrictionss/new')}
                >
                  New ActiveRestrictions
                </Button>
                <Button
                  variant="contained"
                  startIcon={<AddIcon />}
                  onClick={() => navigate('/backofficeTransactions/new')}
                >
                  New BackofficeTransaction
                </Button>
                <Button
                  variant="outlined"
                  startIcon={<BarChartIcon />}
                  onClick={() => navigate('/report2s')}
                >
                  Reports
                </Button>
              </Box>
            </CardContent>
          </Card>
        </Grid>

        <Grid item xs={12} md={6}>
          <Card>
            <CardHeader
              title="Recent Activity"
              avatar={<AccessTimeIcon />}
            />
            <CardContent sx={{ maxHeight: 300, overflow: 'auto' }}>
              {activitiesLoading ? (
                <Box display="flex" justifyContent="center" p={2}>
                  <CircularProgress size={24} />
                </Box>
              ) : activities && activities.length > 0 ? (
                <List>
                  {activities.map((activity) => (
                    <ListItem key={activity.id}>
                      <ListItemAvatar>
                        <Avatar>{activity.icon}</Avatar>
                      </ListItemAvatar>
                      <ListItemText
                        primary={activity.title}
                        secondary={activity.time}
                      />
                    </ListItem>
                  ))}
                </List>
              ) : (
                <Typography color="text.secondary" align="center">
                  No recent activity
                </Typography>
              )}
            </CardContent>
          </Card>
        </Grid>
      </Grid>
    </Box>
  );
};

// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Distributor
//     Generated: 2025-12-11 14:19:35
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useDistributor, useCreateDistributor, useUpdateDistributor } from '../../hooks/useDistributor';
import type { CreateDistributorRequest, UpdateDistributorRequest } from '../../types/Distributor.types';


export const DistributorForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const distributorId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!distributorId;

  const { data: existingDistributor, isPending: isLoadingEntity } = useDistributor(distributorId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateDistributorRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingDistributor) {
      reset(existingDistributor);
    }
  }, [existingDistributor, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateDistributor();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateDistributor();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateDistributorRequest) => {
    if (isEdit && distributorId) {
      updateEntity(
        { id: distributorId, data: data as UpdateDistributorRequest },
        { onSuccess: () => navigate('/distributors') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/distributors') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameDistributor')} />}
        label="TableNameDistributor"
      />

      <TextField
        label="BusinessName"
        type="text"
        fullWidth
        margin="normal"
        {...register('businessName', { required: 'BusinessName is required', maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.businessName}
        helperText={errors.businessName?.message}
      />

      <TextField
        label="BusinessPhoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('businessPhoneNumber', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.businessPhoneNumber}
        helperText={errors.businessPhoneNumber?.message}
      />

      <TextField
        label="ContactName"
        type="text"
        fullWidth
        margin="normal"
        {...register('contactName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.contactName}
        helperText={errors.contactName?.message}
      />

      <TextField
        label="Email"
        type="text"
        fullWidth
        margin="normal"
        {...register('email', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.email}
        helperText={errors.email?.message}
      />

      <TextField
        label="CellphoneNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('cellphoneNumber', { maxLength: { value: 50, message: 'Maximum length is 50' }, pattern: { value: /^[0-9+\-()\s]+$/, message: 'Invalid phone number' } })}
        error={!!errors.cellphoneNumber}
        helperText={errors.cellphoneNumber?.message}
      />

      <TextField
        label="ActualAddressStreetAndNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualAddressStreetAndNumber', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.actualAddressStreetAndNumber}
        helperText={errors.actualAddressStreetAndNumber?.message}
      />

      <TextField
        label="ActualAddressCity"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualAddressCity', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.actualAddressCity}
        helperText={errors.actualAddressCity?.message}
      />

      <TextField
        label="ActualZipCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('actualZipCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.actualZipCode}
        helperText={errors.actualZipCode?.message}
      />

      <TextField
        label="MailAddressStreetAndNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('mailAddressStreetAndNumber', { maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.mailAddressStreetAndNumber}
        helperText={errors.mailAddressStreetAndNumber?.message}
      />

      <TextField
        label="MailAddressCity"
        type="text"
        fullWidth
        margin="normal"
        {...register('mailAddressCity', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.mailAddressCity}
        helperText={errors.mailAddressCity?.message}
      />

      <TextField
        label="MailZipCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('mailZipCode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.mailZipCode}
        helperText={errors.mailZipCode?.message}
      />

      <TextField
        label="GovernmentCompanyNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('governmentCompanyNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.governmentCompanyNumber}
        helperText={errors.governmentCompanyNumber?.message}
      />

      <TextField
        label="EnmPaymentMethod"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmPaymentMethod', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmPaymentMethod}
        helperText={errors.enmPaymentMethod?.message}
      />

      <TextField
        label="BankNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.bankNumber}
        helperText={errors.bankNumber?.message}
      />

      <TextField
        label="BankBranchNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('bankBranchNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.bankBranchNumber}
        helperText={errors.bankBranchNumber?.message}
      />

      <TextField
        label="EntBankAccountNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('entBankAccountNumber', {  })}
        error={!!errors.entBankAccountNumber}
        helperText={errors.entBankAccountNumber?.message}
      />

      <TextField
        label="SAPDebitAccountNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('sAPDebitAccountNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.sAPDebitAccountNumber}
        helperText={errors.sAPDebitAccountNumber?.message}
      />

      <TextField
        label="SAPCreditAccountNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('sAPCreditAccountNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.sAPCreditAccountNumber}
        helperText={errors.sAPCreditAccountNumber?.message}
      />

      <TextField
        label="SAPCreditCommissionAccountNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('sAPCreditCommissionAccountNumber', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.sAPCreditCommissionAccountNumber}
        helperText={errors.sAPCreditCommissionAccountNumber?.message}
      />

      <TextField
        label="Comment"
        fullWidth
        margin="normal"
        {...register('comment', {  })}
        error={!!errors.comment}
        helperText={errors.comment?.message}
      />

      <TextField
        label="IsEnabled"
        fullWidth
        margin="normal"
        {...register('isEnabled', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.isEnabled}
        helperText={errors.isEnabled?.message}
      />

      <TextField
        label="LastActivityTime"
        fullWidth
        margin="normal"
        {...register('lastActivityTime', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.lastActivityTime}
        helperText={errors.lastActivityTime?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <TextField
        label="EnmDistributorType"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmDistributorType', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmDistributorType}
        helperText={errors.enmDistributorType?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('canActivateCards')} />}
        label="CanActivateCards"
      />

      <FormControlLabel
        control={<Checkbox {...register('canUnloadCards')} />}
        label="CanUnloadCards"
      />

      <FormControlLabel
        control={<Checkbox {...register('canSeeCardTransactions')} />}
        label="CanSeeCardTransactions"
      />

      <FormControlLabel
        control={<Checkbox {...register('showBranchesInWeb')} />}
        label="ShowBranchesInWeb"
      />

      <FormControlLabel
        control={<Checkbox {...register('canOnlyLoadOwnedCards')} />}
        label="CanOnlyLoadOwnedCards"
      />

      <FormControlLabel
        control={<Checkbox {...register('canLoadCards')} />}
        label="CanLoadCards"
      />

      <FormControlLabel
        control={<Checkbox {...register('allowZeroMinimumLoadAmount')} />}
        label="AllowZeroMinimumLoadAmount"
      />

      <TextField
        label="BusinessNameLatin"
        type="text"
        fullWidth
        margin="normal"
        {...register('businessNameLatin', { required: 'BusinessNameLatin is required', maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.businessNameLatin}
        helperText={errors.businessNameLatin?.message}
      />

      <TextField
        label="IsNewCardsCashWithdrawalAllowed"
        fullWidth
        margin="normal"
        {...register('isNewCardsCashWithdrawalAllowed', { maxLength: { value: 1, message: 'Maximum length is 1' } })}
        error={!!errors.isNewCardsCashWithdrawalAllowed}
        helperText={errors.isNewCardsCashWithdrawalAllowed?.message}
      />

      <TextField
        label="EnmIsOnTerrorList"
        fullWidth
        margin="normal"
        {...register('enmIsOnTerrorList', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmIsOnTerrorList}
        helperText={errors.enmIsOnTerrorList?.message}
      />

      <TextField
        label="TerrorListUpdatedOn"
        fullWidth
        margin="normal"
        {...register('terrorListUpdatedOn', { maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.terrorListUpdatedOn}
        helperText={errors.terrorListUpdatedOn?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('uniqueCustomerReferenceForSelfOwnedCards')} />}
        label="UniqueCustomerReferenceForSelfOwnedCards"
      />

      <FormControlLabel
        control={<Checkbox {...register('feesPaidByReceiver')} />}
        label="FeesPaidByReceiver"
      />

      <FormControlLabel
        control={<Checkbox {...register('canVerifyActivationDocuments')} />}
        label="CanVerifyActivationDocuments"
      />

      <FormControlLabel
        control={<Checkbox {...register('canWithdrawalCash')} />}
        label="CanWithdrawalCash"
      />

      <FormControlLabel
        control={<Checkbox {...register('canDepositCash')} />}
        label="CanDepositCash"
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/distributors')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_AuditIndexed
//     Generated: 2025-12-11 21:18:52
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, Alert, CircularProgress, Select, MenuItem, FormControl, InputLabel, Checkbox, FormControlLabel } from '@mui/material';
import { useAuditIndexed, useCreateAuditIndexed, useUpdateAuditIndexed } from '../../hooks/useAuditIndexed';
import type { CreateAuditIndexedRequest, UpdateAuditIndexedRequest } from '../../types/AuditIndexed.types';


export const AuditIndexedForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const auditIndexedId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!auditIndexedId;

  const { data: existingAuditIndexed, isPending: isLoadingEntity } = useAuditIndexed(auditIndexedId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateAuditIndexedRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingAuditIndexed) {
      reset(existingAuditIndexed);
    }
  }, [existingAuditIndexed, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateAuditIndexed();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateAuditIndexed();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateAuditIndexedRequest) => {
    if (isEdit && auditIndexedId) {
      updateEntity(
        { id: auditIndexedId, data: data as UpdateAuditIndexedRequest },
        { onSuccess: () => navigate('/auditIndexeds') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/auditIndexeds') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="OriginalID"
        type="number"
        fullWidth
        margin="normal"
        {...register('originalID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.originalID}
        helperText={errors.originalID?.message}
      />

      <TextField
        label="TableName"
        type="text"
        fullWidth
        margin="normal"
        {...register('tableName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.tableName}
        helperText={errors.tableName?.message}
      />

      <TextField
        label="RowID"
        type="number"
        fullWidth
        margin="normal"
        {...register('rowID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.rowID}
        helperText={errors.rowID?.message}
      />

      <TextField
        label="Operation"
        type="text"
        fullWidth
        margin="normal"
        {...register('operation', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.operation}
        helperText={errors.operation?.message}
      />

      <TextField
        label="OccurredAt"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('occurredAt', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.occurredAt}
        helperText={errors.occurredAt?.message}
      />

      <TextField
        label="SqlCurrentUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('sqlCurrentUser', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.sqlCurrentUser}
        helperText={errors.sqlCurrentUser?.message}
      />

      <TextField
        label="FieldName"
        type="text"
        fullWidth
        margin="normal"
        {...register('fieldName', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.fieldName}
        helperText={errors.fieldName?.message}
      />

      <TextField
        label="OldValue"
        type="text"
        fullWidth
        margin="normal"
        {...register('oldValue', { maxLength: { value: 2000, message: 'Maximum length is 2000' } })}
        error={!!errors.oldValue}
        helperText={errors.oldValue?.message}
      />

      <TextField
        label="NewValue"
        type="text"
        fullWidth
        margin="normal"
        {...register('newValue', { maxLength: { value: 2000, message: 'Maximum length is 2000' } })}
        error={!!errors.newValue}
        helperText={errors.newValue?.message}
      />

      <TextField
        label="ChangedByUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('changedByUser', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.changedByUser}
        helperText={errors.changedByUser?.message}
      />

      <TextField
        label="ActiveLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('activeLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.activeLoginID}
        helperText={errors.activeLoginID?.message}
      />

      <TextField
        label="SqlSystemUser"
        type="text"
        fullWidth
        margin="normal"
        {...register('sqlSystemUser', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.sqlSystemUser}
        helperText={errors.sqlSystemUser?.message}
      />

      <TextField
        label="SqlAppName"
        type="text"
        fullWidth
        margin="normal"
        {...register('sqlAppName', { maxLength: { value: 500, message: 'Maximum length is 500' } })}
        error={!!errors.sqlAppName}
        helperText={errors.sqlAppName?.message}
      />

      <TextField
        label="SqlHostName"
        type="text"
        fullWidth
        margin="normal"
        {...register('sqlHostName', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.sqlHostName}
        helperText={errors.sqlHostName?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/auditIndexeds')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: GovtReport99
//     Generated: 2025-12-17 07:52:24
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useGovtReport99, useCreateGovtReport99, useUpdateGovtReport99 } from '../../hooks/useGovtReport99';
import type { CreateGovtReport99Request, UpdateGovtReport99Request } from '../../types/GovtReport99.types';


export const GovtReport99Form: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const govtReport99Id = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!govtReport99Id;

  const { data: existingGovtReport99, isPending: isLoadingEntity } = useGovtReport99(govtReport99Id || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateGovtReport99Request>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingGovtReport99) {
      reset(existingGovtReport99);
    }
  }, [existingGovtReport99, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateGovtReport99();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateGovtReport99();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateGovtReport99Request) => {
    if (isEdit && govtReport99Id) {
      updateEntity(
        { id: govtReport99Id, data: data as UpdateGovtReport99Request },
        { onSuccess: () => navigate('/govtReport99s') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/govtReport99s') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameGovtReport99')} />}
        label="TableNameGovtReport99"
      />

      <TextField
        label="GovtReport00ID"
        type="number"
        fullWidth
        margin="normal"
        {...register('govtReport00ID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.govtReport00ID}
        helperText={errors.govtReport00ID?.message}
      />

      <TextField
        label="ReporterNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterNumber', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.reporterNumber}
        helperText={errors.reporterNumber?.message}
      />

      <TextField
        label="ReporterIDNumber"
        type="text"
        fullWidth
        margin="normal"
        {...register('reporterIDNumber', { maxLength: { value: 15, message: 'Maximum length is 15' } })}
        error={!!errors.reporterIDNumber}
        helperText={errors.reporterIDNumber?.message}
      />

      <TextField
        label="ReportYear"
        type="text"
        fullWidth
        margin="normal"
        {...register('reportYear', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.reportYear}
        helperText={errors.reportYear?.message}
      />

      <TextField
        label="ReportNumber"
        type="number"
        fullWidth
        margin="normal"
        {...register('reportNumber', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.reportNumber}
        helperText={errors.reportNumber?.message}
      />

      <TextField
        label="RecordType"
        type="text"
        fullWidth
        margin="normal"
        {...register('recordType', { maxLength: { value: 2, message: 'Maximum length is 2' } })}
        error={!!errors.recordType}
        helperText={errors.recordType?.message}
      />

      <TextField
        label="ReportMonth"
        type="text"
        fullWidth
        margin="normal"
        {...register('reportMonth', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.reportMonth}
        helperText={errors.reportMonth?.message}
      />

      <TextField
        label="NumberOfRowsIn10"
        type="text"
        fullWidth
        margin="normal"
        {...register('numberOfRowsIn10', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.numberOfRowsIn10}
        helperText={errors.numberOfRowsIn10?.message}
      />

      <TextField
        label="NumberOfRowsIn30"
        type="text"
        fullWidth
        margin="normal"
        {...register('numberOfRowsIn30', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.numberOfRowsIn30}
        helperText={errors.numberOfRowsIn30?.message}
      />

      <TextField
        label="NumberOfRowsIn40"
        type="text"
        fullWidth
        margin="normal"
        {...register('numberOfRowsIn40', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.numberOfRowsIn40}
        helperText={errors.numberOfRowsIn40?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/govtReport99s')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


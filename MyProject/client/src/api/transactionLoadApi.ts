// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: TransactionLoad
//     Generated: 2025-12-17 21:45:25
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  TransactionLoad,
  CreateTransactionLoadRequest,
  UpdateTransactionLoadRequest,
  TransactionLoadFilters,
} from '../types/TransactionLoad.types';

type UseTransactionLoadOptions = UseEntityOptions<TransactionLoadFilters>;
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';
import type { CardActivation } from '../types/CardActivation.types';
import type { ImportTransactionLoadRow } from '../types/ImportTransactionLoadRow.types';

/**
 * API client for TransactionLoad operations.
 * Base URL: /TransactionLoads
 */
export const transactionLoadApi = {
  /**
   * Get TransactionLoad by ID.
   */
  getById: async (id: number): Promise<TransactionLoad> => {
    const response = await api.get<TransactionLoad>(`/TransactionLoads/${id}`);
    return response.data;
  },


  /**
   * Get all TransactionLoads with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseTransactionLoadOptions): Promise<PagedResult<TransactionLoad>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<TransactionLoad>>(`/TransactionLoads?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new TransactionLoad.
   */
  create: async (data: CreateTransactionLoadRequest): Promise<TransactionLoad> => {
    const response = await api.post<TransactionLoad>('/TransactionLoads', data);
    return response.data;
  },


  /**
   * Update existing TransactionLoad.
   */
  update: async (id: number, data: UpdateTransactionLoadRequest): Promise<TransactionLoad> => {
    const response = await api.put<TransactionLoad>(`/TransactionLoads/${id}`, data);
    return response.data;
  },


  /**
   * Delete TransactionLoad.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/TransactionLoads/${id}`);
  },


  /**
   * Get TransactionLoad by RequestedTransactionID.
   */
  getByRequestedTransactionID: async (requestedTransactionID: number): Promise<TransactionLoad> => {
    const response = await api.get<TransactionLoad>(`/TransactionLoads/by-requestedTransactionID/${encodeURIComponent(String(requestedTransactionID))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by Day.
   */
  getByDay: async (day: Date): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-day/${encodeURIComponent(String(day))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by Amount.
   */
  getByAmount: async (amount: number): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-amount/${encodeURIComponent(String(amount))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by CardID.
   */
  getByCardID: async (cardID: number): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-cardID/${encodeURIComponent(String(cardID))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by EnmCurrency.
   */
  getByEnmCurrency: async (enmCurrency: string): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-enmCurrency/${encodeURIComponent(String(enmCurrency))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by DistributorID.
   */
  getByDistributorID: async (distributorID: number): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-distributorID/${encodeURIComponent(String(distributorID))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by FaultNo.
   */
  getByFaultNo: async (faultNo: number): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-faultNo/${encodeURIComponent(String(faultNo))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by EnmLoadResult.
   */
  getByEnmLoadResult: async (enmLoadResult: string): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-enmLoadResult/${encodeURIComponent(String(enmLoadResult))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by EnmLoadType.
   */
  getByEnmLoadType: async (enmLoadType: string): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-enmLoadType/${encodeURIComponent(String(enmLoadType))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by LoggedAlertID.
   */
  getByLoggedAlertID: async (loggedAlertID: number): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-loggedAlertID/${encodeURIComponent(String(loggedAlertID))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by Month.
   */
  getByMonth: async (month: Date): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-month/${encodeURIComponent(String(month))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by UserID.
   */
  getByUserID: async (userID: number): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-userID/${encodeURIComponent(String(userID))}`);
    return response.data;
  },


  /**
   * Get TransactionLoad by WhenRequested.
   */
  getByWhenRequested: async (whenRequested: Date): Promise<TransactionLoad[]> => {
    const response = await api.get<TransactionLoad[]>(`/TransactionLoads/by-whenRequested/${encodeURIComponent(String(whenRequested))}`);
    return response.data;
  },


  /**
   * Get BACKOFFICETRANSACTIONS for this TRANSACTIONLOAD.
   */
  getBackofficeTransactions: async (id: number, skip?: number, take?: number): Promise<BackofficeTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<BackofficeTransaction[]>(`/TransactionLoads/${id}/BACKOFFICETRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDACTIVATIONS for this TRANSACTIONLOAD.
   */
  getCardActivations: async (id: number, skip?: number, take?: number): Promise<CardActivation[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardActivation[]>(`/TransactionLoads/${id}/CARDACTIVATIONS${queryString}`);
    return response.data;
  },


  /**
   * Get IMPORTTRANSACTIONLOADROWS for this TRANSACTIONLOAD.
   */
  getImportTransactionLoadRows: async (id: number, skip?: number, take?: number): Promise<ImportTransactionLoadRow[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ImportTransactionLoadRow[]>(`/TransactionLoads/${id}/IMPORTTRANSACTIONLOADROWS${queryString}`);
    return response.data;
  },


};

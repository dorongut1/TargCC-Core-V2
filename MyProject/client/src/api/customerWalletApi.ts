// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: CustomerWallet
//     Generated: 2025-12-17 17:47:23
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  CustomerWallet,
  CreateCustomerWalletRequest,
  UpdateCustomerWalletRequest,
  CustomerWalletFilters,
} from '../types/CustomerWallet.types';

type UseCustomerWalletOptions = UseEntityOptions<CustomerWalletFilters>;
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';
import type { ExternalTransaction } from '../types/ExternalTransaction.types';
import type { RequestedTransactionFee } from '../types/RequestedTransactionFee.types';
import type { TransactionBit } from '../types/TransactionBit.types';
import type { TransactionCreditCard } from '../types/TransactionCreditCard.types';

/**
 * API client for CustomerWallet operations.
 * Base URL: /CustomerWallets
 */
export const customerWalletApi = {
  /**
   * Get CustomerWallet by ID.
   */
  getById: async (id: number): Promise<CustomerWallet> => {
    const response = await api.get<CustomerWallet>(`/CustomerWallets/${id}`);
    return response.data;
  },


  /**
   * Get all CustomerWallets with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseCustomerWalletOptions): Promise<PagedResult<CustomerWallet>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<CustomerWallet>>(`/CustomerWallets?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new CustomerWallet.
   */
  create: async (data: CreateCustomerWalletRequest): Promise<CustomerWallet> => {
    const response = await api.post<CustomerWallet>('/CustomerWallets', data);
    return response.data;
  },


  /**
   * Update existing CustomerWallet.
   */
  update: async (id: number, data: UpdateCustomerWalletRequest): Promise<CustomerWallet> => {
    const response = await api.put<CustomerWallet>(`/CustomerWallets/${id}`, data);
    return response.data;
  },


  /**
   * Delete CustomerWallet.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/CustomerWallets/${id}`);
  },


  /**
   * Get CustomerWallet by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<CustomerWallet[]> => {
    const response = await api.get<CustomerWallet[]>(`/CustomerWallets/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get CustomerWallet by RequestedTransactionID.
   */
  getByRequestedTransactionID: async (requestedTransactionID: number): Promise<CustomerWallet[]> => {
    const response = await api.get<CustomerWallet[]>(`/CustomerWallets/by-requestedTransactionID/${encodeURIComponent(String(requestedTransactionID))}`);
    return response.data;
  },


  /**
   * Get CustomerWallet by EnmTypeWalletTransactionType.
   */
  getByEnmTypeWalletTransactionType: async (enmTypeWalletTransactionType: string): Promise<CustomerWallet[]> => {
    const response = await api.get<CustomerWallet[]>(`/CustomerWallets/by-enmTypeWalletTransactionType/${encodeURIComponent(String(enmTypeWalletTransactionType))}`);
    return response.data;
  },


  /**
   * Get CustomerWallet by WhoInitiatedID.
   */
  getByWhoInitiatedID: async (whoInitiatedID: number): Promise<CustomerWallet[]> => {
    const response = await api.get<CustomerWallet[]>(`/CustomerWallets/by-whoInitiatedID/${encodeURIComponent(String(whoInitiatedID))}`);
    return response.data;
  },


  /**
   * Get BACKOFFICETRANSACTIONS for this CUSTOMERWALLET.
   */
  getBackofficeTransactions: async (id: number, skip?: number, take?: number): Promise<BackofficeTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<BackofficeTransaction[]>(`/CustomerWallets/${id}/BACKOFFICETRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get EXTERNALTRANSACTIONS for this CUSTOMERWALLET.
   */
  getExternalTransactions: async (id: number, skip?: number, take?: number): Promise<ExternalTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ExternalTransaction[]>(`/CustomerWallets/${id}/EXTERNALTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get REQUESTEDTRANSACTIONFEES for this CUSTOMERWALLET.
   */
  getRequestedTransactionFees: async (id: number, skip?: number, take?: number): Promise<RequestedTransactionFee[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RequestedTransactionFee[]>(`/CustomerWallets/${id}/REQUESTEDTRANSACTIONFEES${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONBITS for this CUSTOMERWALLET.
   */
  getTransactionBits: async (id: number, skip?: number, take?: number): Promise<TransactionBit[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionBit[]>(`/CustomerWallets/${id}/TRANSACTIONBITS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONCREDITCARDS for this CUSTOMERWALLET.
   */
  getTransactionCreditCards: async (id: number, skip?: number, take?: number): Promise<TransactionCreditCard[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionCreditCard[]>(`/CustomerWallets/${id}/TRANSACTIONCREDITCARDS${queryString}`);
    return response.data;
  },


};

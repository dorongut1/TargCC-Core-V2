// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_User
//     Generated: 2025-12-18 08:11:16
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  User,
  CreateUserRequest,
  UpdateUserRequest,
  UserFilters,
} from '../types/User.types';

type UseUserOptions = UseEntityOptions<UserFilters>;
import type { JobAlertRecipient } from '../types/JobAlertRecipient.types';
import type { LoggedAlert } from '../types/LoggedAlert.types';
import type { LoggedRequest } from '../types/LoggedRequest.types';
import type { UserLoginKey } from '../types/UserLoginKey.types';
import type { UserPermission } from '../types/UserPermission.types';
import type { UserStatus } from '../types/UserStatus.types';
import type { CardOrderByDistributor } from '../types/CardOrderByDistributor.types';
import type { CardOrderFromIssuer } from '../types/CardOrderFromIssuer.types';
import type { CustomerWallet } from '../types/CustomerWallet.types';
import type { ImportTransactionLoad } from '../types/ImportTransactionLoad.types';
import type { Task } from '../types/Task.types';
import type { TransactionLoad } from '../types/TransactionLoad.types';
import type { UserApproval } from '../types/UserApproval.types';

/**
 * API client for User operations.
 * Base URL: /Users
 */
export const userApi = {
  /**
   * Get User by ID.
   */
  getById: async (id: number): Promise<User> => {
    const response = await api.get<User>(`/Users/${id}`);
    return response.data;
  },


  /**
   * Get all Users with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseUserOptions): Promise<PagedResult<User>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<User>>(`/Users?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new User.
   */
  create: async (data: CreateUserRequest): Promise<User> => {
    const response = await api.post<User>('/Users', data);
    return response.data;
  },


  /**
   * Update existing User.
   */
  update: async (id: number, data: UpdateUserRequest): Promise<User> => {
    const response = await api.put<User>(`/Users/${id}`, data);
    return response.data;
  },


  /**
   * Delete User.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Users/${id}`);
  },


  /**
   * Get User by UserName.
   */
  getByUserName: async (userName: string): Promise<User> => {
    const response = await api.get<User>(`/Users/by-userName/${encodeURIComponent(String(userName))}`);
    return response.data;
  },


  /**
   * Get User by City.
   */
  getByCity: async (city: string): Promise<User[]> => {
    const response = await api.get<User[]>(`/Users/by-city/${encodeURIComponent(String(city))}`);
    return response.data;
  },


  /**
   * Get User by LastSuccessfulLogin.
   */
  getByLastSuccessfulLogin: async (lastSuccessfulLogin: Date): Promise<User[]> => {
    const response = await api.get<User[]>(`/Users/by-lastSuccessfulLogin/${encodeURIComponent(String(lastSuccessfulLogin))}`);
    return response.data;
  },


  /**
   * Get User by RoleID.
   */
  getByRoleID: async (roleID: number): Promise<User[]> => {
    const response = await api.get<User[]>(`/Users/by-roleID/${encodeURIComponent(String(roleID))}`);
    return response.data;
  },


  /**
   * Get JOBALERTRECIPIENTS for this USER.
   */
  getJobAlertRecipients: async (id: number, skip?: number, take?: number): Promise<JobAlertRecipient[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<JobAlertRecipient[]>(`/Users/${id}/JOBALERTRECIPIENTS${queryString}`);
    return response.data;
  },


  /**
   * Get LOGGEDALERTS for this USER.
   */
  getLoggedAlerts: async (id: number, skip?: number, take?: number): Promise<LoggedAlert[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LoggedAlert[]>(`/Users/${id}/LOGGEDALERTS${queryString}`);
    return response.data;
  },


  /**
   * Get LOGGEDREQUESTS for this USER.
   */
  getLoggedRequests: async (id: number, skip?: number, take?: number): Promise<LoggedRequest[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LoggedRequest[]>(`/Users/${id}/LOGGEDREQUESTS${queryString}`);
    return response.data;
  },


  /**
   * Get USERLOGINKEYS for this USER.
   */
  getUserLoginKeys: async (id: number, skip?: number, take?: number): Promise<UserLoginKey[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<UserLoginKey[]>(`/Users/${id}/USERLOGINKEYS${queryString}`);
    return response.data;
  },


  /**
   * Get USERPERMISSIONS for this USER.
   */
  getUserPermissions: async (id: number, skip?: number, take?: number): Promise<UserPermission[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<UserPermission[]>(`/Users/${id}/USERPERMISSIONS${queryString}`);
    return response.data;
  },


  /**
   * Get USERSTATUSES for this USER.
   */
  getUserStatuses: async (id: number, skip?: number, take?: number): Promise<UserStatus[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<UserStatus[]>(`/Users/${id}/USERSTATUSES${queryString}`);
    return response.data;
  },


  /**
   * Get CARDORDERBYDISTRIBUTORS for this USER.
   */
  getCardOrderByDistributors: async (id: number, skip?: number, take?: number): Promise<CardOrderByDistributor[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardOrderByDistributor[]>(`/Users/${id}/CARDORDERBYDISTRIBUTORS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDORDERFROMISSUERS for this USER.
   */
  getCardOrderFromIssuers: async (id: number, skip?: number, take?: number): Promise<CardOrderFromIssuer[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardOrderFromIssuer[]>(`/Users/${id}/CARDORDERFROMISSUERS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERWALLETS for this USER.
   */
  getCustomerWallets: async (id: number, skip?: number, take?: number): Promise<CustomerWallet[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerWallet[]>(`/Users/${id}/CUSTOMERWALLETS${queryString}`);
    return response.data;
  },


  /**
   * Get IMPORTTRANSACTIONLOADS for this USER.
   */
  getImportTransactionLoads: async (id: number, skip?: number, take?: number): Promise<ImportTransactionLoad[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ImportTransactionLoad[]>(`/Users/${id}/IMPORTTRANSACTIONLOADS${queryString}`);
    return response.data;
  },


  /**
   * Get TASKS for this USER.
   */
  getTasks: async (id: number, skip?: number, take?: number): Promise<Task[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Task[]>(`/Users/${id}/TASKS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONLOADS for this USER.
   */
  getTransactionLoads: async (id: number, skip?: number, take?: number): Promise<TransactionLoad[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionLoad[]>(`/Users/${id}/TRANSACTIONLOADS${queryString}`);
    return response.data;
  },


  /**
   * Get USERAPPROVALS for this USER.
   */
  getUserApprovals: async (id: number, skip?: number, take?: number): Promise<UserApproval[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<UserApproval[]>(`/Users/${id}/USERAPPROVALS${queryString}`);
    return response.data;
  },


};

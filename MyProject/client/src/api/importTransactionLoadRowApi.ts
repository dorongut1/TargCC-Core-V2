// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: ImportTransactionLoadRow
//     Generated: 2025-12-17 21:20:41
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  ImportTransactionLoadRow,
  CreateImportTransactionLoadRowRequest,
  UpdateImportTransactionLoadRowRequest,
  ImportTransactionLoadRowFilters,
} from '../types/ImportTransactionLoadRow.types';

type UseImportTransactionLoadRowOptions = UseEntityOptions<ImportTransactionLoadRowFilters>;

/**
 * API client for ImportTransactionLoadRow operations.
 * Base URL: /ImportTransactionLoadRows
 */
export const importTransactionLoadRowApi = {
  /**
   * Get ImportTransactionLoadRow by ID.
   */
  getById: async (id: number): Promise<ImportTransactionLoadRow> => {
    const response = await api.get<ImportTransactionLoadRow>(`/ImportTransactionLoadRows/${id}`);
    return response.data;
  },


  /**
   * Get all ImportTransactionLoadRows with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseImportTransactionLoadRowOptions): Promise<PagedResult<ImportTransactionLoadRow>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<ImportTransactionLoadRow>>(`/ImportTransactionLoadRows?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new ImportTransactionLoadRow.
   */
  create: async (data: CreateImportTransactionLoadRowRequest): Promise<ImportTransactionLoadRow> => {
    const response = await api.post<ImportTransactionLoadRow>('/ImportTransactionLoadRows', data);
    return response.data;
  },


  /**
   * Update existing ImportTransactionLoadRow.
   */
  update: async (id: number, data: UpdateImportTransactionLoadRowRequest): Promise<ImportTransactionLoadRow> => {
    const response = await api.put<ImportTransactionLoadRow>(`/ImportTransactionLoadRows/${id}`, data);
    return response.data;
  },


  /**
   * Delete ImportTransactionLoadRow.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/ImportTransactionLoadRows/${id}`);
  },


  /**
   * Get ImportTransactionLoadRow by LgCardID.
   */
  getByLgCardID: async (lgCardID: number): Promise<ImportTransactionLoadRow[]> => {
    const response = await api.get<ImportTransactionLoadRow[]>(`/ImportTransactionLoadRows/by-lgCardID/${encodeURIComponent(String(lgCardID))}`);
    return response.data;
  },


  /**
   * Get ImportTransactionLoadRow by LgCustomerID.
   */
  getByLgCustomerID: async (lgCustomerID: number): Promise<ImportTransactionLoadRow[]> => {
    const response = await api.get<ImportTransactionLoadRow[]>(`/ImportTransactionLoadRows/by-lgCustomerID/${encodeURIComponent(String(lgCustomerID))}`);
    return response.data;
  },


  /**
   * Get ImportTransactionLoadRow by ImportTransactionLoadID.
   */
  getByImportTransactionLoadID: async (importTransactionLoadID: number): Promise<ImportTransactionLoadRow[]> => {
    const response = await api.get<ImportTransactionLoadRow[]>(`/ImportTransactionLoadRows/by-importTransactionLoadID/${encodeURIComponent(String(importTransactionLoadID))}`);
    return response.data;
  },


  /**
   * Get ImportTransactionLoadRow by LgLoggedAlertID.
   */
  getByLgLoggedAlertID: async (lgLoggedAlertID: number): Promise<ImportTransactionLoadRow[]> => {
    const response = await api.get<ImportTransactionLoadRow[]>(`/ImportTransactionLoadRows/by-lgLoggedAlertID/${encodeURIComponent(String(lgLoggedAlertID))}`);
    return response.data;
  },


  /**
   * Get ImportTransactionLoadRow by TransactionLoadID.
   */
  getByTransactionLoadID: async (transactionLoadID: number): Promise<ImportTransactionLoadRow[]> => {
    const response = await api.get<ImportTransactionLoadRow[]>(`/ImportTransactionLoadRows/by-transactionLoadID/${encodeURIComponent(String(transactionLoadID))}`);
    return response.data;
  },


  /**
   * Get ImportTransactionLoadRow by WhenCompleted.
   */
  getByWhenCompleted: async (whenCompleted: Date): Promise<ImportTransactionLoadRow[]> => {
    const response = await api.get<ImportTransactionLoadRow[]>(`/ImportTransactionLoadRows/by-whenCompleted/${encodeURIComponent(String(whenCompleted))}`);
    return response.data;
  },


};

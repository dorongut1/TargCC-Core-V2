// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Card
//     Generated: 2025-12-16 10:13:17
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Card,
  CreateCardRequest,
  UpdateCardRequest,
  CardFilters,
} from '../types/Card.types';

type UseCardOptions = UseEntityOptions<CardFilters>;
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';
import type { CardActivation } from '../types/CardActivation.types';
import type { CustomerCardTransaction } from '../types/CustomerCardTransaction.types';
import type { ImportTransactionLoadRow } from '../types/ImportTransactionLoadRow.types';
import type { OnlineApproval } from '../types/OnlineApproval.types';
import type { RequestedTransaction } from '../types/RequestedTransaction.types';
import type { TransactionLoad } from '../types/TransactionLoad.types';

/**
 * API client for Card operations.
 * Base URL: /Cards
 */
export const cardApi = {
  /**
   * Get Card by ID.
   */
  getById: async (id: number): Promise<Card> => {
    const response = await api.get<Card>(`/Cards/${id}`);
    return response.data;
  },


  /**
   * Get all Cards with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseCardOptions): Promise<PagedResult<Card>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Card>>(`/Cards?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Card.
   */
  create: async (data: CreateCardRequest): Promise<Card> => {
    const response = await api.post<Card>('/Cards', data);
    return response.data;
  },


  /**
   * Update existing Card.
   */
  update: async (id: number, data: UpdateCardRequest): Promise<Card> => {
    const response = await api.put<Card>(`/Cards/${id}`, data);
    return response.data;
  },


  /**
   * Delete Card.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Cards/${id}`);
  },


  /**
   * Get Card by EnmIsOnTerrorList.
   */
  getByEnmIsOnTerrorList: async (enmIsOnTerrorList: string): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-enmIsOnTerrorList/${encodeURIComponent(String(enmIsOnTerrorList))}`);
    return response.data;
  },


  /**
   * Get Card by CardOrderByDistributorID.
   */
  getByCardOrderByDistributorID: async (cardOrderByDistributorID: number): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-cardOrderByDistributorID/${encodeURIComponent(String(cardOrderByDistributorID))}`);
    return response.data;
  },


  /**
   * Get Card by CardOrderFromIssuerID.
   */
  getByCardOrderFromIssuerID: async (cardOrderFromIssuerID: number): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-cardOrderFromIssuerID/${encodeURIComponent(String(cardOrderFromIssuerID))}`);
    return response.data;
  },


  /**
   * Get Card by Cid.
   */
  getByCid: async (cid: string): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-cid/${encodeURIComponent(String(cid))}`);
    return response.data;
  },


  /**
   * Get Card by CardClubID.
   */
  getByCardClubID: async (cardClubID: number): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-cardClubID/${encodeURIComponent(String(cardClubID))}`);
    return response.data;
  },


  /**
   * Get Card by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get Card by OwningDistributorID.
   */
  getByOwningDistributorID: async (owningDistributorID: number): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-owningDistributorID/${encodeURIComponent(String(owningDistributorID))}`);
    return response.data;
  },


  /**
   * Get Card by PreviousDistributorID.
   */
  getByPreviousDistributorID: async (previousDistributorID: number): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-previousDistributorID/${encodeURIComponent(String(previousDistributorID))}`);
    return response.data;
  },


  /**
   * Get Card by EnmStatusAtCardIssuer.
   */
  getByEnmStatusAtCardIssuer: async (enmStatusAtCardIssuer: string): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-enmStatusAtCardIssuer/${encodeURIComponent(String(enmStatusAtCardIssuer))}`);
    return response.data;
  },


  /**
   * Get Card by EnmStatusAtICP.
   */
  getByEnmStatusAtICP: async (enmStatusAtICP: string): Promise<Card[]> => {
    const response = await api.get<Card[]>(`/Cards/by-enmStatusAtICP/${encodeURIComponent(String(enmStatusAtICP))}`);
    return response.data;
  },


  /**
   * Get BACKOFFICETRANSACTIONS for this CARD.
   */
  getBackofficeTransactions: async (id: number, skip?: number, take?: number): Promise<BackofficeTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<BackofficeTransaction[]>(`/Cards/${id}/BACKOFFICETRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDACTIVATIONS for this CARD.
   */
  getCardActivations: async (id: number, skip?: number, take?: number): Promise<CardActivation[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardActivation[]>(`/Cards/${id}/CARDACTIVATIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERCARDTRANSACTIONS for this CARD.
   */
  getCustomerCardTransactions: async (id: number, skip?: number, take?: number): Promise<CustomerCardTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerCardTransaction[]>(`/Cards/${id}/CUSTOMERCARDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get IMPORTTRANSACTIONLOADROWS for this CARD.
   */
  getImportTransactionLoadRows: async (id: number, skip?: number, take?: number): Promise<ImportTransactionLoadRow[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ImportTransactionLoadRow[]>(`/Cards/${id}/IMPORTTRANSACTIONLOADROWS${queryString}`);
    return response.data;
  },


  /**
   * Get ONLINEAPPROVALS for this CARD.
   */
  getOnlineApprovals: async (id: number, skip?: number, take?: number): Promise<OnlineApproval[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<OnlineApproval[]>(`/Cards/${id}/ONLINEAPPROVALS${queryString}`);
    return response.data;
  },


  /**
   * Get REQUESTEDTRANSACTIONS for this CARD.
   */
  getRequestedTransactions: async (id: number, skip?: number, take?: number): Promise<RequestedTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RequestedTransaction[]>(`/Cards/${id}/REQUESTEDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONLOADS for this CARD.
   */
  getTransactionLoads: async (id: number, skip?: number, take?: number): Promise<TransactionLoad[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionLoad[]>(`/Cards/${id}/TRANSACTIONLOADS${queryString}`);
    return response.data;
  },


};

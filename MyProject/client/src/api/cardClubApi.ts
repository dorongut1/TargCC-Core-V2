// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: CardClub
//     Generated: 2025-12-18 08:11:17
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  CardClub,
  CreateCardClubRequest,
  UpdateCardClubRequest,
  CardClubFilters,
} from '../types/CardClub.types';

type UseCardClubOptions = UseEntityOptions<CardClubFilters>;
import type { Card } from '../types/Card.types';
import type { CardActivation } from '../types/CardActivation.types';
import type { CardOrderByDistributor } from '../types/CardOrderByDistributor.types';
import type { CardOrderFromIssuer } from '../types/CardOrderFromIssuer.types';
import type { CustomerCardTransaction } from '../types/CustomerCardTransaction.types';
import type { FeeOverride } from '../types/FeeOverride.types';
import type { LimitationOverride } from '../types/LimitationOverride.types';
import type { NewCardRequest } from '../types/NewCardRequest.types';

/**
 * API client for CardClub operations.
 * Base URL: /CardClubs
 */
export const cardClubApi = {
  /**
   * Get CardClub by ID.
   */
  getById: async (id: number): Promise<CardClub> => {
    const response = await api.get<CardClub>(`/CardClubs/${id}`);
    return response.data;
  },


  /**
   * Get all CardClubs with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseCardClubOptions): Promise<PagedResult<CardClub>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<CardClub>>(`/CardClubs?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new CardClub.
   */
  create: async (data: CreateCardClubRequest): Promise<CardClub> => {
    const response = await api.post<CardClub>('/CardClubs', data);
    return response.data;
  },


  /**
   * Update existing CardClub.
   */
  update: async (id: number, data: UpdateCardClubRequest): Promise<CardClub> => {
    const response = await api.put<CardClub>(`/CardClubs/${id}`, data);
    return response.data;
  },


  /**
   * Delete CardClub.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/CardClubs/${id}`);
  },


  /**
   * Get CardClub by UseForUnassignedCustomer.
   */
  getByUseForUnassignedCustomer: async (useForUnassignedCustomer: boolean): Promise<CardClub> => {
    const response = await api.get<CardClub>(`/CardClubs/by-useForUnassignedCustomer/${encodeURIComponent(String(useForUnassignedCustomer))}`);
    return response.data;
  },


  /**
   * Get CardClub by ClubName.
   */
  getByClubName: async (clubName: string): Promise<CardClub> => {
    const response = await api.get<CardClub>(`/CardClubs/by-clubName/${encodeURIComponent(String(clubName))}`);
    return response.data;
  },


  /**
   * Get CardClub by IsRoot.
   */
  getByIsRoot: async (isRoot: boolean): Promise<CardClub> => {
    const response = await api.get<CardClub>(`/CardClubs/by-isRoot/${encodeURIComponent(String(isRoot))}`);
    return response.data;
  },


  /**
   * Get CardClub by AssignedCompanyCustomerID.
   */
  getByAssignedCompanyCustomerID: async (assignedCompanyCustomerID: number): Promise<CardClub[]> => {
    const response = await api.get<CardClub[]>(`/CardClubs/by-assignedCompanyCustomerID/${encodeURIComponent(String(assignedCompanyCustomerID))}`);
    return response.data;
  },


  /**
   * Get CardClub by CardIssuerOrgCode.
   */
  getByCardIssuerOrgCode: async (cardIssuerOrgCode: string): Promise<CardClub[]> => {
    const response = await api.get<CardClub[]>(`/CardClubs/by-cardIssuerOrgCode/${encodeURIComponent(String(cardIssuerOrgCode))}`);
    return response.data;
  },


  /**
   * Get CARDS for this CARDCLUB.
   */
  getCards: async (id: number, skip?: number, take?: number): Promise<Card[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Card[]>(`/CardClubs/${id}/CARDS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDACTIVATIONS for this CARDCLUB.
   */
  getCardActivations: async (id: number, skip?: number, take?: number): Promise<CardActivation[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardActivation[]>(`/CardClubs/${id}/CARDACTIVATIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDORDERBYDISTRIBUTORS for this CARDCLUB.
   */
  getCardOrderByDistributors: async (id: number, skip?: number, take?: number): Promise<CardOrderByDistributor[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardOrderByDistributor[]>(`/CardClubs/${id}/CARDORDERBYDISTRIBUTORS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDORDERFROMISSUERS for this CARDCLUB.
   */
  getCardOrderFromIssuers: async (id: number, skip?: number, take?: number): Promise<CardOrderFromIssuer[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardOrderFromIssuer[]>(`/CardClubs/${id}/CARDORDERFROMISSUERS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERCARDTRANSACTIONS for this CARDCLUB.
   */
  getCustomerCardTransactions: async (id: number, skip?: number, take?: number): Promise<CustomerCardTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerCardTransaction[]>(`/CardClubs/${id}/CUSTOMERCARDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get FEEOVERRIDES for this CARDCLUB.
   */
  getFeeOverrides: async (id: number, skip?: number, take?: number): Promise<FeeOverride[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<FeeOverride[]>(`/CardClubs/${id}/FEEOVERRIDES${queryString}`);
    return response.data;
  },


  /**
   * Get LIMITATIONOVERRIDES for this CARDCLUB.
   */
  getLimitationOverrides: async (id: number, skip?: number, take?: number): Promise<LimitationOverride[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LimitationOverride[]>(`/CardClubs/${id}/LIMITATIONOVERRIDES${queryString}`);
    return response.data;
  },


  /**
   * Get NEWCARDREQUESTS for this CARDCLUB.
   */
  getNewCardRequests: async (id: number, skip?: number, take?: number): Promise<NewCardRequest[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<NewCardRequest[]>(`/CardClubs/${id}/NEWCARDREQUESTS${queryString}`);
    return response.data;
  },


};

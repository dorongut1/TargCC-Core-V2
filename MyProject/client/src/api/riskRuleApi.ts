// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: RiskRule
//     Generated: 2025-12-18 07:42:15
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  RiskRule,
  CreateRiskRuleRequest,
  UpdateRiskRuleRequest,
  RiskRuleFilters,
} from '../types/RiskRule.types';

type UseRiskRuleOptions = UseEntityOptions<RiskRuleFilters>;
import type { ActiveRestrictions } from '../types/ActiveRestrictions.types';
import type { EntityRiskColor } from '../types/EntityRiskColor.types';
import type { RiskEvent } from '../types/RiskEvent.types';
import type { RiskRuleLog } from '../types/RiskRuleLog.types';
import type { RiskRuleOverride } from '../types/RiskRuleOverride.types';

/**
 * API client for RiskRule operations.
 * Base URL: /RiskRules
 */
export const riskRuleApi = {
  /**
   * Get RiskRule by ID.
   */
  getById: async (id: number): Promise<RiskRule> => {
    const response = await api.get<RiskRule>(`/RiskRules/${id}`);
    return response.data;
  },


  /**
   * Get all RiskRules with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseRiskRuleOptions): Promise<PagedResult<RiskRule>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<RiskRule>>(`/RiskRules?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new RiskRule.
   */
  create: async (data: CreateRiskRuleRequest): Promise<RiskRule> => {
    const response = await api.post<RiskRule>('/RiskRules', data);
    return response.data;
  },


  /**
   * Update existing RiskRule.
   */
  update: async (id: number, data: UpdateRiskRuleRequest): Promise<RiskRule> => {
    const response = await api.put<RiskRule>(`/RiskRules/${id}`, data);
    return response.data;
  },


  /**
   * Delete RiskRule.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/RiskRules/${id}`);
  },


  /**
   * Get RiskRule by IsActive.
   */
  getByIsActive: async (isActive: boolean): Promise<RiskRule[]> => {
    const response = await api.get<RiskRule[]>(`/RiskRules/by-isActive/${encodeURIComponent(String(isActive))}`);
    return response.data;
  },


  /**
   * Get ACTIVERESTRICTIONSES for this RISKRULE.
   */
  getActiveRestrictionses: async (id: number, skip?: number, take?: number): Promise<ActiveRestrictions[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ActiveRestrictions[]>(`/RiskRules/${id}/ACTIVERESTRICTIONSES${queryString}`);
    return response.data;
  },


  /**
   * Get ENTITYRISKCOLORS for this RISKRULE.
   */
  getEntityRiskColors: async (id: number, skip?: number, take?: number): Promise<EntityRiskColor[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<EntityRiskColor[]>(`/RiskRules/${id}/ENTITYRISKCOLORS${queryString}`);
    return response.data;
  },


  /**
   * Get RISKEVENTS for this RISKRULE.
   */
  getRiskEvents: async (id: number, skip?: number, take?: number): Promise<RiskEvent[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RiskEvent[]>(`/RiskRules/${id}/RISKEVENTS${queryString}`);
    return response.data;
  },


  /**
   * Get RISKRULELOGS for this RISKRULE.
   */
  getRiskRuleLogs: async (id: number, skip?: number, take?: number): Promise<RiskRuleLog[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RiskRuleLog[]>(`/RiskRules/${id}/RISKRULELOGS${queryString}`);
    return response.data;
  },


  /**
   * Get RISKRULEOVERRIDES for this RISKRULE.
   */
  getRiskRuleOverrides: async (id: number, skip?: number, take?: number): Promise<RiskRuleOverride[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RiskRuleOverride[]>(`/RiskRules/${id}/RISKRULEOVERRIDES${queryString}`);
    return response.data;
  },


};

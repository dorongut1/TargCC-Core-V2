// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: CardOrderFromIssuer
//     Generated: 2025-12-11 20:26:13
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  CardOrderFromIssuer,
  CreateCardOrderFromIssuerRequest,
  UpdateCardOrderFromIssuerRequest,
  CardOrderFromIssuerFilters,
} from '../types/CardOrderFromIssuer.types';
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';
import type { Card } from '../types/Card.types';

/**
 * API client for CardOrderFromIssuer operations.
 * Base URL: /CardOrderFromIssuers
 */
export const cardOrderFromIssuerApi = {
  /**
   * Get CardOrderFromIssuer by ID.
   */
  getById: async (id: number): Promise<CardOrderFromIssuer> => {
    const response = await api.get<CardOrderFromIssuer>(`/CardOrderFromIssuers/${id}`);
    return response.data;
  },


  /**
   * Get all CardOrderFromIssuers with optional filters.
   */
  getAll: async (filters?: CardOrderFromIssuerFilters): Promise<CardOrderFromIssuer[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/CardOrderFromIssuers/filter' : '/CardOrderFromIssuers';
    const response = await api.get<CardOrderFromIssuer[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new CardOrderFromIssuer.
   */
  create: async (data: CreateCardOrderFromIssuerRequest): Promise<CardOrderFromIssuer> => {
    const response = await api.post<CardOrderFromIssuer>('/CardOrderFromIssuers', data);
    return response.data;
  },


  /**
   * Update existing CardOrderFromIssuer.
   */
  update: async (id: number, data: UpdateCardOrderFromIssuerRequest): Promise<CardOrderFromIssuer> => {
    const response = await api.put<CardOrderFromIssuer>(`/CardOrderFromIssuers/${id}`, data);
    return response.data;
  },


  /**
   * Delete CardOrderFromIssuer.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/CardOrderFromIssuers/${id}`);
  },


  /**
   * Get CardOrderFromIssuer by CardIssuerOrderNumber.
   */
  getByCardIssuerOrderNumber: async (cardIssuerOrderNumber: string): Promise<CardOrderFromIssuer> => {
    const response = await api.get<CardOrderFromIssuer>(`/CardOrderFromIssuers/by-cardIssuerOrderNumber/${encodeURIComponent(String(cardIssuerOrderNumber))}`);
    return response.data;
  },


  /**
   * Get CardOrderFromIssuer by CardClubID.
   */
  getByCardClubID: async (cardClubID: number): Promise<CardOrderFromIssuer[]> => {
    const response = await api.get<CardOrderFromIssuer[]>(`/CardOrderFromIssuers/by-cardClubID/${encodeURIComponent(String(cardClubID))}`);
    return response.data;
  },


  /**
   * Get CardOrderFromIssuer by EmbossedText.
   */
  getByEmbossedText: async (embossedText: string): Promise<CardOrderFromIssuer[]> => {
    const response = await api.get<CardOrderFromIssuer[]>(`/CardOrderFromIssuers/by-embossedText/${encodeURIComponent(String(embossedText))}`);
    return response.data;
  },


  /**
   * Get CardOrderFromIssuer by LoggedAlertID.
   */
  getByLoggedAlertID: async (loggedAlertID: number): Promise<CardOrderFromIssuer[]> => {
    const response = await api.get<CardOrderFromIssuer[]>(`/CardOrderFromIssuers/by-loggedAlertID/${encodeURIComponent(String(loggedAlertID))}`);
    return response.data;
  },


  /**
   * Get CardOrderFromIssuer by OrderedByUserID.
   */
  getByOrderedByUserID: async (orderedByUserID: number): Promise<CardOrderFromIssuer[]> => {
    const response = await api.get<CardOrderFromIssuer[]>(`/CardOrderFromIssuers/by-orderedByUserID/${encodeURIComponent(String(orderedByUserID))}`);
    return response.data;
  },


  /**
   * Get CardOrderFromIssuer by EnmStatusCardOrderStatus.
   */
  getByEnmStatusCardOrderStatus: async (enmStatusCardOrderStatus: string): Promise<CardOrderFromIssuer[]> => {
    const response = await api.get<CardOrderFromIssuer[]>(`/CardOrderFromIssuers/by-enmStatusCardOrderStatus/${encodeURIComponent(String(enmStatusCardOrderStatus))}`);
    return response.data;
  },


  /**
   * Get BACKOFFICETRANSACTIONS for this CARDORDERFROMISSUER.
   */
  getBackofficeTransactions: async (id: number, skip?: number, take?: number): Promise<BackofficeTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<BackofficeTransaction[]>(`/CardOrderFromIssuers/${id}/BACKOFFICETRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDS for this CARDORDERFROMISSUER.
   */
  getCards: async (id: number, skip?: number, take?: number): Promise<Card[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Card[]>(`/CardOrderFromIssuers/${id}/CARDS${queryString}`);
    return response.data;
  },


};

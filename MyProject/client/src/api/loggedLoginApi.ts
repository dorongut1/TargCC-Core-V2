// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_LoggedLogin
//     Generated: 2025-12-11 21:58:09
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  LoggedLogin,
  CreateLoggedLoginRequest,
  UpdateLoggedLoginRequest,
  LoggedLoginFilters,
} from '../types/LoggedLogin.types';
import type { LoggedAlert } from '../types/LoggedAlert.types';
import type { LoggedRequest } from '../types/LoggedRequest.types';

/**
 * API client for LoggedLogin operations.
 * Base URL: /LoggedLogins
 */
export const loggedLoginApi = {
  /**
   * Get LoggedLogin by ID.
   */
  getById: async (id: number): Promise<LoggedLogin> => {
    const response = await api.get<LoggedLogin>(`/LoggedLogins/${id}`);
    return response.data;
  },


  /**
   * Get all LoggedLogins with optional filters.
   */
  getAll: async (filters?: LoggedLoginFilters): Promise<LoggedLogin[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/LoggedLogins/filter' : '/LoggedLogins';
    const response = await api.get<LoggedLogin[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new LoggedLogin.
   */
  create: async (data: CreateLoggedLoginRequest): Promise<LoggedLogin> => {
    const response = await api.post<LoggedLogin>('/LoggedLogins', data);
    return response.data;
  },


  /**
   * Update existing LoggedLogin.
   */
  update: async (id: number, data: UpdateLoggedLoginRequest): Promise<LoggedLogin> => {
    const response = await api.put<LoggedLogin>(`/LoggedLogins/${id}`, data);
    return response.data;
  },


  /**
   * Delete LoggedLogin.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/LoggedLogins/${id}`);
  },


  /**
   * Get LoggedLogin by ApplicationName.
   */
  getByApplicationName: async (applicationName: string): Promise<LoggedLogin[]> => {
    const response = await api.get<LoggedLogin[]>(`/LoggedLogins/by-applicationName/${encodeURIComponent(String(applicationName))}`);
    return response.data;
  },


  /**
   * Get LoggedLogin by DateLoggedIn.
   */
  getByDateLoggedIn: async (dateLoggedIn: Date): Promise<LoggedLogin[]> => {
    const response = await api.get<LoggedLogin[]>(`/LoggedLogins/by-dateLoggedIn/${encodeURIComponent(String(dateLoggedIn))}`);
    return response.data;
  },


  /**
   * Get LoggedLogin by LoginFaultNumber.
   */
  getByLoginFaultNumber: async (loginFaultNumber: number): Promise<LoggedLogin[]> => {
    const response = await api.get<LoggedLogin[]>(`/LoggedLogins/by-loginFaultNumber/${encodeURIComponent(String(loginFaultNumber))}`);
    return response.data;
  },


  /**
   * Get LoggedLogin by MonthLoggedIn.
   */
  getByMonthLoggedIn: async (monthLoggedIn: Date): Promise<LoggedLogin[]> => {
    const response = await api.get<LoggedLogin[]>(`/LoggedLogins/by-monthLoggedIn/${encodeURIComponent(String(monthLoggedIn))}`);
    return response.data;
  },


  /**
   * Get LoggedLogin by OriginatingCountry.
   */
  getByOriginatingCountry: async (originatingCountry: string): Promise<LoggedLogin[]> => {
    const response = await api.get<LoggedLogin[]>(`/LoggedLogins/by-originatingCountry/${encodeURIComponent(String(originatingCountry))}`);
    return response.data;
  },


  /**
   * Get LoggedLogin by TimeLoggedIn.
   */
  getByTimeLoggedIn: async (timeLoggedIn: Date): Promise<LoggedLogin[]> => {
    const response = await api.get<LoggedLogin[]>(`/LoggedLogins/by-timeLoggedIn/${encodeURIComponent(String(timeLoggedIn))}`);
    return response.data;
  },


  /**
   * Get LoggedLogin by UserName.
   */
  getByUserName: async (userName: string): Promise<LoggedLogin[]> => {
    const response = await api.get<LoggedLogin[]>(`/LoggedLogins/by-userName/${encodeURIComponent(String(userName))}`);
    return response.data;
  },


  /**
   * Get LOGGEDALERTS for this LOGGEDLOGIN.
   */
  getLoggedAlerts: async (id: number, skip?: number, take?: number): Promise<LoggedAlert[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LoggedAlert[]>(`/LoggedLogins/${id}/LOGGEDALERTS${queryString}`);
    return response.data;
  },


  /**
   * Get LOGGEDREQUESTS for this LOGGEDLOGIN.
   */
  getLoggedRequests: async (id: number, skip?: number, take?: number): Promise<LoggedRequest[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LoggedRequest[]>(`/LoggedLogins/${id}/LOGGEDREQUESTS${queryString}`);
    return response.data;
  },


};

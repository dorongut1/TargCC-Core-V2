// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Country
//     Generated: 2025-12-17 20:20:32
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Country,
  CreateCountryRequest,
  UpdateCountryRequest,
  CountryFilters,
} from '../types/Country.types';

type UseCountryOptions = UseEntityOptions<CountryFilters>;
import type { MCCBParticipant } from '../types/MCCBParticipant.types';
import type { MCCBReceiver } from '../types/MCCBReceiver.types';
import type { MCCBSecurityCheck } from '../types/MCCBSecurityCheck.types';

/**
 * API client for Country operations.
 * Base URL: /Countries
 */
export const countryApi = {
  /**
   * Get Country by ID.
   */
  getById: async (id: number): Promise<Country> => {
    const response = await api.get<Country>(`/Countries/${id}`);
    return response.data;
  },


  /**
   * Get all Countrys with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseCountryOptions): Promise<PagedResult<Country>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Country>>(`/Countries?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Country.
   */
  create: async (data: CreateCountryRequest): Promise<Country> => {
    const response = await api.post<Country>('/Countries', data);
    return response.data;
  },


  /**
   * Update existing Country.
   */
  update: async (id: number, data: UpdateCountryRequest): Promise<Country> => {
    const response = await api.put<Country>(`/Countries/${id}`, data);
    return response.data;
  },


  /**
   * Delete Country.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Countries/${id}`);
  },


  /**
   * Get Country by HebrewName.
   */
  getByHebrewName: async (hebrewName: string): Promise<Country> => {
    const response = await api.get<Country>(`/Countries/by-hebrewName/${encodeURIComponent(String(hebrewName))}`);
    return response.data;
  },


  /**
   * Get Country by ISO3166Alpha2.
   */
  getByISO3166Alpha2: async (iSO3166Alpha2: string): Promise<Country> => {
    const response = await api.get<Country>(`/Countries/by-iSO3166Alpha2/${encodeURIComponent(String(iSO3166Alpha2))}`);
    return response.data;
  },


  /**
   * Get Country by ISO3166Numeric.
   */
  getByISO3166Numeric: async (iSO3166Numeric: string): Promise<Country> => {
    const response = await api.get<Country>(`/Countries/by-iSO3166Numeric/${encodeURIComponent(String(iSO3166Numeric))}`);
    return response.data;
  },


  /**
   * Get Country by Name.
   */
  getByName: async (name: string): Promise<Country> => {
    const response = await api.get<Country>(`/Countries/by-name/${encodeURIComponent(String(name))}`);
    return response.data;
  },


  /**
   * Get MCCBPARTICIPANTS for this COUNTRY.
   */
  getMCCBParticipants: async (id: number, skip?: number, take?: number): Promise<MCCBParticipant[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBParticipant[]>(`/Countries/${id}/MCCBPARTICIPANTS${queryString}`);
    return response.data;
  },


  /**
   * Get MCCBRECEIVERS for this COUNTRY.
   */
  getMCCBReceivers: async (id: number, skip?: number, take?: number): Promise<MCCBReceiver[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBReceiver[]>(`/Countries/${id}/MCCBRECEIVERS${queryString}`);
    return response.data;
  },


  /**
   * Get MCCBSECURITYCHECKS for this COUNTRY.
   */
  getMCCBSecurityChecks: async (id: number, skip?: number, take?: number): Promise<MCCBSecurityCheck[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBSecurityCheck[]>(`/Countries/${id}/MCCBSECURITYCHECKS${queryString}`);
    return response.data;
  },


};

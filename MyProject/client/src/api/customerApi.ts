// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Customer
//     Generated: 2025-12-17 14:36:06
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Customer,
  CreateCustomerRequest,
  UpdateCustomerRequest,
  CustomerFilters,
} from '../types/Customer.types';

type UseCustomerOptions = UseEntityOptions<CustomerFilters>;
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';
import type { Card } from '../types/Card.types';
import type { CardActivation } from '../types/CardActivation.types';
import type { CardClub } from '../types/CardClub.types';
import type { CustomerCardTransaction } from '../types/CustomerCardTransaction.types';
import type { CustomerCreditCard } from '../types/CustomerCreditCard.types';
import type { CustomerKYC } from '../types/CustomerKYC.types';
import type { CustomerPartnership } from '../types/CustomerPartnership.types';
import type { CustomerPerformance } from '../types/CustomerPerformance.types';
import type { CustomerWallet } from '../types/CustomerWallet.types';
import type { DrivebotPayment } from '../types/DrivebotPayment.types';
import type { ExternalCustomer } from '../types/ExternalCustomer.types';
import type { ExternalTransaction } from '../types/ExternalTransaction.types';
import type { ImportTransactionLoadRow } from '../types/ImportTransactionLoadRow.types';
import type { MCCBReceiver } from '../types/MCCBReceiver.types';
import type { MCCBSecurityCheck } from '../types/MCCBSecurityCheck.types';
import type { MCCBTransaction } from '../types/MCCBTransaction.types';
import type { NewCardRequest } from '../types/NewCardRequest.types';
import type { OnlineApproval } from '../types/OnlineApproval.types';
import type { RequestedTransaction } from '../types/RequestedTransaction.types';
import type { Task } from '../types/Task.types';
import type { TransactionBit } from '../types/TransactionBit.types';
import type { TransactionCreditCard } from '../types/TransactionCreditCard.types';
import type { TransactionLoad } from '../types/TransactionLoad.types';

/**
 * API client for Customer operations.
 * Base URL: /Customers
 */
export const customerApi = {
  /**
   * Get Customer by ID.
   */
  getById: async (id: number): Promise<Customer> => {
    const response = await api.get<Customer>(`/Customers/${id}`);
    return response.data;
  },


  /**
   * Get all Customers with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseCustomerOptions): Promise<PagedResult<Customer>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Customer>>(`/Customers?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Customer.
   */
  create: async (data: CreateCustomerRequest): Promise<Customer> => {
    const response = await api.post<Customer>('/Customers', data);
    return response.data;
  },


  /**
   * Update existing Customer.
   */
  update: async (id: number, data: UpdateCustomerRequest): Promise<Customer> => {
    const response = await api.put<Customer>(`/Customers/${id}`, data);
    return response.data;
  },


  /**
   * Delete Customer.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Customers/${id}`);
  },


  /**
   * Get Customer by AssignedDistributorID.
   */
  getByAssignedDistributorID: async (assignedDistributorID: number): Promise<Customer> => {
    const response = await api.get<Customer>(`/Customers/by-assignedDistributorID/${encodeURIComponent(String(assignedDistributorID))}`);
    return response.data;
  },


  /**
   * Get Customer by CellphoneNumber.
   */
  getByCellphoneNumber: async (cellphoneNumber: string): Promise<Customer> => {
    const response = await api.get<Customer>(`/Customers/by-cellphoneNumber/${encodeURIComponent(String(cellphoneNumber))}`);
    return response.data;
  },


  /**
   * Get Customer by CustomerIdentifier.
   */
  getByCustomerIdentifier: async (customerIdentifier: string): Promise<Customer> => {
    const response = await api.get<Customer>(`/Customers/by-customerIdentifier/${encodeURIComponent(String(customerIdentifier))}`);
    return response.data;
  },


  /**
   * Get Customer by WhenRegistered.
   */
  getByWhenRegistered: async (whenRegistered: Date): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-whenRegistered/${encodeURIComponent(String(whenRegistered))}`);
    return response.data;
  },


  /**
   * Get Customer by EnmBankDocumentationStatusDocumentationStatus.
   */
  getByEnmBankDocumentationStatusDocumentationStatus: async (enmBankDocumentationStatusDocumentationStatus: string): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-enmBankDocumentationStatusDocumentationStatus/${encodeURIComponent(String(enmBankDocumentationStatusDocumentationStatus))}`);
    return response.data;
  },


  /**
   * Get Customer by CustomerClubID.
   */
  getByCustomerClubID: async (customerClubID: number): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-customerClubID/${encodeURIComponent(String(customerClubID))}`);
    return response.data;
  },


  /**
   * Get Customer by EnmCustomerType.
   */
  getByEnmCustomerType: async (enmCustomerType: string): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-enmCustomerType/${encodeURIComponent(String(enmCustomerType))}`);
    return response.data;
  },


  /**
   * Get Customer by EnmIsOnTerrorList.
   */
  getByEnmIsOnTerrorList: async (enmIsOnTerrorList: string): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-enmIsOnTerrorList/${encodeURIComponent(String(enmIsOnTerrorList))}`);
    return response.data;
  },


  /**
   * Get Customer by IsEnabled.
   */
  getByIsEnabled: async (isEnabled: boolean): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-isEnabled/${encodeURIComponent(String(isEnabled))}`);
    return response.data;
  },


  /**
   * Get Customer by BusinessLogoNeedsApproval.
   */
  getByBusinessLogoNeedsApproval: async (businessLogoNeedsApproval: boolean): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-businessLogoNeedsApproval/${encodeURIComponent(String(businessLogoNeedsApproval))}`);
    return response.data;
  },


  /**
   * Get Customer by LastName.
   */
  getByLastName: async (lastName: string): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-lastName/${encodeURIComponent(String(lastName))}`);
    return response.data;
  },


  /**
   * Get Customer by LastNameLatin.
   */
  getByLastNameLatin: async (lastNameLatin: string): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-lastNameLatin/${encodeURIComponent(String(lastNameLatin))}`);
    return response.data;
  },


  /**
   * Get Customer by EnmSecurityTier.
   */
  getByEnmSecurityTier: async (enmSecurityTier: string): Promise<Customer[]> => {
    const response = await api.get<Customer[]>(`/Customers/by-enmSecurityTier/${encodeURIComponent(String(enmSecurityTier))}`);
    return response.data;
  },


  /**
   * Get BACKOFFICETRANSACTIONS for this CUSTOMER.
   */
  getBackofficeTransactions: async (id: number, skip?: number, take?: number): Promise<BackofficeTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<BackofficeTransaction[]>(`/Customers/${id}/BACKOFFICETRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDS for this CUSTOMER.
   */
  getCards: async (id: number, skip?: number, take?: number): Promise<Card[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Card[]>(`/Customers/${id}/CARDS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDACTIVATIONS for this CUSTOMER.
   */
  getCardActivations: async (id: number, skip?: number, take?: number): Promise<CardActivation[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardActivation[]>(`/Customers/${id}/CARDACTIVATIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDCLUBS for this CUSTOMER.
   */
  getCardClubs: async (id: number, skip?: number, take?: number): Promise<CardClub[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardClub[]>(`/Customers/${id}/CARDCLUBS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERCARDTRANSACTIONS for this CUSTOMER.
   */
  getCustomerCardTransactions: async (id: number, skip?: number, take?: number): Promise<CustomerCardTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerCardTransaction[]>(`/Customers/${id}/CUSTOMERCARDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERCREDITCARDS for this CUSTOMER.
   */
  getCustomerCreditCards: async (id: number, skip?: number, take?: number): Promise<CustomerCreditCard[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerCreditCard[]>(`/Customers/${id}/CUSTOMERCREDITCARDS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERKYCS for this CUSTOMER.
   */
  getCustomerKYCs: async (id: number, skip?: number, take?: number): Promise<CustomerKYC[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerKYC[]>(`/Customers/${id}/CUSTOMERKYCS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERPARTNERSHIPS for this CUSTOMER.
   */
  getCustomerPartnerships: async (id: number, skip?: number, take?: number): Promise<CustomerPartnership[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerPartnership[]>(`/Customers/${id}/CUSTOMERPARTNERSHIPS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERPERFORMANCES for this CUSTOMER.
   */
  getCustomerPerformances: async (id: number, skip?: number, take?: number): Promise<CustomerPerformance[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerPerformance[]>(`/Customers/${id}/CUSTOMERPERFORMANCES${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERWALLETS for this CUSTOMER.
   */
  getCustomerWallets: async (id: number, skip?: number, take?: number): Promise<CustomerWallet[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerWallet[]>(`/Customers/${id}/CUSTOMERWALLETS${queryString}`);
    return response.data;
  },


  /**
   * Get DRIVEBOTPAYMENTS for this CUSTOMER.
   */
  getDrivebotPayments: async (id: number, skip?: number, take?: number): Promise<DrivebotPayment[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<DrivebotPayment[]>(`/Customers/${id}/DRIVEBOTPAYMENTS${queryString}`);
    return response.data;
  },


  /**
   * Get EXTERNALCUSTOMERS for this CUSTOMER.
   */
  getExternalCustomers: async (id: number, skip?: number, take?: number): Promise<ExternalCustomer[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ExternalCustomer[]>(`/Customers/${id}/EXTERNALCUSTOMERS${queryString}`);
    return response.data;
  },


  /**
   * Get EXTERNALTRANSACTIONS for this CUSTOMER.
   */
  getExternalTransactions: async (id: number, skip?: number, take?: number): Promise<ExternalTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ExternalTransaction[]>(`/Customers/${id}/EXTERNALTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get IMPORTTRANSACTIONLOADROWS for this CUSTOMER.
   */
  getImportTransactionLoadRows: async (id: number, skip?: number, take?: number): Promise<ImportTransactionLoadRow[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ImportTransactionLoadRow[]>(`/Customers/${id}/IMPORTTRANSACTIONLOADROWS${queryString}`);
    return response.data;
  },


  /**
   * Get MCCBRECEIVERS for this CUSTOMER.
   */
  getMCCBReceivers: async (id: number, skip?: number, take?: number): Promise<MCCBReceiver[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBReceiver[]>(`/Customers/${id}/MCCBRECEIVERS${queryString}`);
    return response.data;
  },


  /**
   * Get MCCBSECURITYCHECKS for this CUSTOMER.
   */
  getMCCBSecurityChecks: async (id: number, skip?: number, take?: number): Promise<MCCBSecurityCheck[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBSecurityCheck[]>(`/Customers/${id}/MCCBSECURITYCHECKS${queryString}`);
    return response.data;
  },


  /**
   * Get MCCBTRANSACTIONS for this CUSTOMER.
   */
  getMCCBTransactions: async (id: number, skip?: number, take?: number): Promise<MCCBTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBTransaction[]>(`/Customers/${id}/MCCBTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get NEWCARDREQUESTS for this CUSTOMER.
   */
  getNewCardRequests: async (id: number, skip?: number, take?: number): Promise<NewCardRequest[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<NewCardRequest[]>(`/Customers/${id}/NEWCARDREQUESTS${queryString}`);
    return response.data;
  },


  /**
   * Get ONLINEAPPROVALS for this CUSTOMER.
   */
  getOnlineApprovals: async (id: number, skip?: number, take?: number): Promise<OnlineApproval[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<OnlineApproval[]>(`/Customers/${id}/ONLINEAPPROVALS${queryString}`);
    return response.data;
  },


  /**
   * Get REQUESTEDTRANSACTIONS for this CUSTOMER.
   */
  getRequestedTransactions: async (id: number, skip?: number, take?: number): Promise<RequestedTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RequestedTransaction[]>(`/Customers/${id}/REQUESTEDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get TASKS for this CUSTOMER.
   */
  getTasks: async (id: number, skip?: number, take?: number): Promise<Task[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Task[]>(`/Customers/${id}/TASKS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONBITS for this CUSTOMER.
   */
  getTransactionBits: async (id: number, skip?: number, take?: number): Promise<TransactionBit[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionBit[]>(`/Customers/${id}/TRANSACTIONBITS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONCREDITCARDS for this CUSTOMER.
   */
  getTransactionCreditCards: async (id: number, skip?: number, take?: number): Promise<TransactionCreditCard[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionCreditCard[]>(`/Customers/${id}/TRANSACTIONCREDITCARDS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONLOADS for this CUSTOMER.
   */
  getTransactionLoads: async (id: number, skip?: number, take?: number): Promise<TransactionLoad[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionLoad[]>(`/Customers/${id}/TRANSACTIONLOADS${queryString}`);
    return response.data;
  },


};

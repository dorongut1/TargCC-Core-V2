// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_Table
//     Generated: 2025-12-15 22:04:07
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Table,
  CreateTableRequest,
  UpdateTableRequest,
  TableFilters,
} from '../types/Table.types';

type UseTableOptions = UseEntityOptions<TableFilters>;

/**
 * API client for Table operations.
 * Base URL: /Tables
 */
export const tableApi = {
  /**
   * Get Table by ID.
   */
  getById: async (id: number): Promise<Table> => {
    const response = await api.get<Table>(`/Tables/${id}`);
    return response.data;
  },


  /**
   * Get all Tables with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseTableOptions): Promise<PagedResult<Table>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Table>>(`/Tables?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Table.
   */
  create: async (data: CreateTableRequest): Promise<Table> => {
    const response = await api.post<Table>('/Tables', data);
    return response.data;
  },


  /**
   * Update existing Table.
   */
  update: async (id: number, data: UpdateTableRequest): Promise<Table> => {
    const response = await api.put<Table>(`/Tables/${id}`, data);
    return response.data;
  },


  /**
   * Delete Table.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Tables/${id}`);
  },


  /**
   * Get Table by Name.
   */
  getByName: async (name: string): Promise<Table> => {
    const response = await api.get<Table>(`/Tables/by-name/${encodeURIComponent(String(name))}`);
    return response.data;
  },


};

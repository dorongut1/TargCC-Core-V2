// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: EntityRiskColor
//     Generated: 2025-12-17 21:32:14
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  EntityRiskColor,
  CreateEntityRiskColorRequest,
  UpdateEntityRiskColorRequest,
  EntityRiskColorFilters,
} from '../types/EntityRiskColor.types';

type UseEntityRiskColorOptions = UseEntityOptions<EntityRiskColorFilters>;

/**
 * API client for EntityRiskColor operations.
 * Base URL: /EntityRiskColors
 */
export const entityRiskColorApi = {
  /**
   * Get EntityRiskColor by ID.
   */
  getById: async (id: number): Promise<EntityRiskColor> => {
    const response = await api.get<EntityRiskColor>(`/EntityRiskColors/${id}`);
    return response.data;
  },


  /**
   * Get all EntityRiskColors with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseEntityRiskColorOptions): Promise<PagedResult<EntityRiskColor>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<EntityRiskColor>>(`/EntityRiskColors?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new EntityRiskColor.
   */
  create: async (data: CreateEntityRiskColorRequest): Promise<EntityRiskColor> => {
    const response = await api.post<EntityRiskColor>('/EntityRiskColors', data);
    return response.data;
  },


  /**
   * Update existing EntityRiskColor.
   */
  update: async (id: number, data: UpdateEntityRiskColorRequest): Promise<EntityRiskColor> => {
    const response = await api.put<EntityRiskColor>(`/EntityRiskColors/${id}`, data);
    return response.data;
  },


  /**
   * Delete EntityRiskColor.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/EntityRiskColors/${id}`);
  },


  /**
   * Get EntityRiskColor by EnmCurrentColor.
   */
  getByEnmCurrentColor: async (enmCurrentColor: string): Promise<EntityRiskColor[]> => {
    const response = await api.get<EntityRiskColor[]>(`/EntityRiskColors/by-enmCurrentColor/${encodeURIComponent(String(enmCurrentColor))}`);
    return response.data;
  },


  /**
   * Get EntityRiskColor by UpdatedByRuleID.
   */
  getByUpdatedByRuleID: async (updatedByRuleID: number): Promise<EntityRiskColor[]> => {
    const response = await api.get<EntityRiskColor[]>(`/EntityRiskColors/by-updatedByRuleID/${encodeURIComponent(String(updatedByRuleID))}`);
    return response.data;
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: ActiveRestrictions
//     Generated: 2025-12-16 18:22:26
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  ActiveRestrictions,
  CreateActiveRestrictionsRequest,
  UpdateActiveRestrictionsRequest,
  ActiveRestrictionsFilters,
} from '../types/ActiveRestrictions.types';

type UseActiveRestrictionsOptions = UseEntityOptions<ActiveRestrictionsFilters>;

/**
 * API client for ActiveRestrictions operations.
 * Base URL: /ActiveRestrictionses
 */
export const activeRestrictionsApi = {
  /**
   * Get ActiveRestrictions by ID.
   */
  getById: async (id: number): Promise<ActiveRestrictions> => {
    const response = await api.get<ActiveRestrictions>(`/ActiveRestrictionses/${id}`);
    return response.data;
  },


  /**
   * Get all ActiveRestrictionss with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseActiveRestrictionsOptions): Promise<PagedResult<ActiveRestrictions>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<ActiveRestrictions>>(`/ActiveRestrictionses?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new ActiveRestrictions.
   */
  create: async (data: CreateActiveRestrictionsRequest): Promise<ActiveRestrictions> => {
    const response = await api.post<ActiveRestrictions>('/ActiveRestrictionses', data);
    return response.data;
  },


  /**
   * Update existing ActiveRestrictions.
   */
  update: async (id: number, data: UpdateActiveRestrictionsRequest): Promise<ActiveRestrictions> => {
    const response = await api.put<ActiveRestrictions>(`/ActiveRestrictionses/${id}`, data);
    return response.data;
  },


  /**
   * Delete ActiveRestrictions.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/ActiveRestrictionses/${id}`);
  },


  /**
   * Get ActiveRestrictions by ValidUntil.
   */
  getByValidUntil: async (validUntil: Date): Promise<ActiveRestrictions[]> => {
    const response = await api.get<ActiveRestrictions[]>(`/ActiveRestrictionses/by-validUntil/${encodeURIComponent(String(validUntil))}`);
    return response.data;
  },


  /**
   * Get ActiveRestrictions by TriggerEventID.
   */
  getByTriggerEventID: async (triggerEventID: number): Promise<ActiveRestrictions[]> => {
    const response = await api.get<ActiveRestrictions[]>(`/ActiveRestrictionses/by-triggerEventID/${encodeURIComponent(String(triggerEventID))}`);
    return response.data;
  },


  /**
   * Get ActiveRestrictions by TriggerRuleID.
   */
  getByTriggerRuleID: async (triggerRuleID: number): Promise<ActiveRestrictions[]> => {
    const response = await api.get<ActiveRestrictions[]>(`/ActiveRestrictionses/by-triggerRuleID/${encodeURIComponent(String(triggerRuleID))}`);
    return response.data;
  },


};

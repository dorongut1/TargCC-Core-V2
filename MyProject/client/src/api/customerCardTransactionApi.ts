// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: CustomerCardTransaction
//     Generated: 2025-12-11 20:26:15
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  CustomerCardTransaction,
  CreateCustomerCardTransactionRequest,
  UpdateCustomerCardTransactionRequest,
  CustomerCardTransactionFilters,
} from '../types/CustomerCardTransaction.types';

/**
 * API client for CustomerCardTransaction operations.
 * Base URL: /CustomerCardTransactions
 */
export const customerCardTransactionApi = {
  /**
   * Get CustomerCardTransaction by ID.
   */
  getById: async (id: number): Promise<CustomerCardTransaction> => {
    const response = await api.get<CustomerCardTransaction>(`/CustomerCardTransactions/${id}`);
    return response.data;
  },


  /**
   * Get all CustomerCardTransactions with optional filters.
   */
  getAll: async (filters?: CustomerCardTransactionFilters): Promise<CustomerCardTransaction[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/CustomerCardTransactions/filter' : '/CustomerCardTransactions';
    const response = await api.get<CustomerCardTransaction[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new CustomerCardTransaction.
   */
  create: async (data: CreateCustomerCardTransactionRequest): Promise<CustomerCardTransaction> => {
    const response = await api.post<CustomerCardTransaction>('/CustomerCardTransactions', data);
    return response.data;
  },


  /**
   * Update existing CustomerCardTransaction.
   */
  update: async (id: number, data: UpdateCustomerCardTransactionRequest): Promise<CustomerCardTransaction> => {
    const response = await api.put<CustomerCardTransaction>(`/CustomerCardTransactions/${id}`, data);
    return response.data;
  },


  /**
   * Delete CustomerCardTransaction.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/CustomerCardTransactions/${id}`);
  },


  /**
   * Get CustomerCardTransaction by LgCardClubID.
   */
  getByLgCardClubID: async (lgCardClubID: number): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-lgCardClubID/${encodeURIComponent(String(lgCardClubID))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by CardCid.
   */
  getByCardCid: async (cardCid: string): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-cardCid/${encodeURIComponent(String(cardCid))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by EnmUpayCardTransactionTypeCustomerTransactionType.
   */
  getByEnmUpayCardTransactionTypeCustomerTransactionType: async (enmUpayCardTransactionTypeCustomerTransactionType: string): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-enmUpayCardTransactionTypeCustomerTransactionType/${encodeURIComponent(String(enmUpayCardTransactionTypeCustomerTransactionType))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by AmountInCardCurrency.
   */
  getByAmountInCardCurrency: async (amountInCardCurrency: number): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-amountInCardCurrency/${encodeURIComponent(String(amountInCardCurrency))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by AmountInLocalCurrency.
   */
  getByAmountInLocalCurrency: async (amountInLocalCurrency: number): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-amountInLocalCurrency/${encodeURIComponent(String(amountInLocalCurrency))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by CardID.
   */
  getByCardID: async (cardID: number): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-cardID/${encodeURIComponent(String(cardID))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by Currency.
   */
  getByCurrency: async (currency: string): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-currency/${encodeURIComponent(String(currency))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by Date.
   */
  getByDate: async (date: Date): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-date/${encodeURIComponent(String(date))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by LgOwningDistributorID.
   */
  getByLgOwningDistributorID: async (lgOwningDistributorID: number): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-lgOwningDistributorID/${encodeURIComponent(String(lgOwningDistributorID))}`);
    return response.data;
  },


  /**
   * Get CustomerCardTransaction by TransactionType.
   */
  getByTransactionType: async (transactionType: string): Promise<CustomerCardTransaction[]> => {
    const response = await api.get<CustomerCardTransaction[]>(`/CustomerCardTransactions/by-transactionType/${encodeURIComponent(String(transactionType))}`);
    return response.data;
  },


};

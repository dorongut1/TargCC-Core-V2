// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: MCCBTransaction
//     Generated: 2025-12-17 21:37:04
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  MCCBTransaction,
  CreateMCCBTransactionRequest,
  UpdateMCCBTransactionRequest,
  MCCBTransactionFilters,
} from '../types/MCCBTransaction.types';

type UseMCCBTransactionOptions = UseEntityOptions<MCCBTransactionFilters>;
import type { MCCBSecurityCheck } from '../types/MCCBSecurityCheck.types';

/**
 * API client for MCCBTransaction operations.
 * Base URL: /MCCBTransactions
 */
export const mCCBTransactionApi = {
  /**
   * Get MCCBTransaction by ID.
   */
  getById: async (id: number): Promise<MCCBTransaction> => {
    const response = await api.get<MCCBTransaction>(`/MCCBTransactions/${id}`);
    return response.data;
  },


  /**
   * Get all MCCBTransactions with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseMCCBTransactionOptions): Promise<PagedResult<MCCBTransaction>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<MCCBTransaction>>(`/MCCBTransactions?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new MCCBTransaction.
   */
  create: async (data: CreateMCCBTransactionRequest): Promise<MCCBTransaction> => {
    const response = await api.post<MCCBTransaction>('/MCCBTransactions', data);
    return response.data;
  },


  /**
   * Update existing MCCBTransaction.
   */
  update: async (id: number, data: UpdateMCCBTransactionRequest): Promise<MCCBTransaction> => {
    const response = await api.put<MCCBTransaction>(`/MCCBTransactions/${id}`, data);
    return response.data;
  },


  /**
   * Delete MCCBTransaction.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/MCCBTransactions/${id}`);
  },


  /**
   * Get MCCBTransaction by ProposalId.
   */
  getByProposalId: async (proposalId: string): Promise<MCCBTransaction> => {
    const response = await api.get<MCCBTransaction>(`/MCCBTransactions/by-proposalId/${encodeURIComponent(String(proposalId))}`);
    return response.data;
  },


  /**
   * Get MCCBTransaction by RequestedTransactionID.
   */
  getByRequestedTransactionID: async (requestedTransactionID: number): Promise<MCCBTransaction> => {
    const response = await api.get<MCCBTransaction>(`/MCCBTransactions/by-requestedTransactionID/${encodeURIComponent(String(requestedTransactionID))}`);
    return response.data;
  },


  /**
   * Get MCCBTransaction by TransactionReference.
   */
  getByTransactionReference: async (transactionReference: string): Promise<MCCBTransaction> => {
    const response = await api.get<MCCBTransaction>(`/MCCBTransactions/by-transactionReference/${encodeURIComponent(String(transactionReference))}`);
    return response.data;
  },


  /**
   * Get MCCBTransaction by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<MCCBTransaction[]> => {
    const response = await api.get<MCCBTransaction[]>(`/MCCBTransactions/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get MCCBTransaction by MCCBReceiverID.
   */
  getByMCCBReceiverID: async (mCCBReceiverID: number): Promise<MCCBTransaction[]> => {
    const response = await api.get<MCCBTransaction[]>(`/MCCBTransactions/by-mCCBReceiverID/${encodeURIComponent(String(mCCBReceiverID))}`);
    return response.data;
  },


  /**
   * Get MCCBTransaction by MCCBReceiverAccountID.
   */
  getByMCCBReceiverAccountID: async (mCCBReceiverAccountID: number): Promise<MCCBTransaction[]> => {
    const response = await api.get<MCCBTransaction[]>(`/MCCBTransactions/by-mCCBReceiverAccountID/${encodeURIComponent(String(mCCBReceiverAccountID))}`);
    return response.data;
  },


  /**
   * Get MCCBTransaction by EnmPaymentMode.
   */
  getByEnmPaymentMode: async (enmPaymentMode: string): Promise<MCCBTransaction[]> => {
    const response = await api.get<MCCBTransaction[]>(`/MCCBTransactions/by-enmPaymentMode/${encodeURIComponent(String(enmPaymentMode))}`);
    return response.data;
  },


  /**
   * Get MCCBSECURITYCHECKS for this MCCBTRANSACTION.
   */
  getMCCBSecurityChecks: async (id: number, skip?: number, take?: number): Promise<MCCBSecurityCheck[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBSecurityCheck[]>(`/MCCBTransactions/${id}/MCCBSECURITYCHECKS${queryString}`);
    return response.data;
  },


};

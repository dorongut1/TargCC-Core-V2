// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_MFA
//     Generated: 2025-12-18 08:27:22
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Mfa,
  CreateMfaRequest,
  UpdateMfaRequest,
  MfaFilters,
} from '../types/Mfa.types';

type UseMfaOptions = UseEntityOptions<MfaFilters>;

/**
 * API client for Mfa operations.
 * Base URL: /Mfas
 */
export const mfaApi = {
  /**
   * Get Mfa by ID.
   */
  getById: async (id: number): Promise<Mfa> => {
    const response = await api.get<Mfa>(`/Mfas/${id}`);
    return response.data;
  },


  /**
   * Get all Mfas with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseMfaOptions): Promise<PagedResult<Mfa>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Mfa>>(`/Mfas?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Mfa.
   */
  create: async (data: CreateMfaRequest): Promise<Mfa> => {
    const response = await api.post<Mfa>('/Mfas', data);
    return response.data;
  },


  /**
   * Update existing Mfa.
   */
  update: async (id: number, data: UpdateMfaRequest): Promise<Mfa> => {
    const response = await api.put<Mfa>(`/Mfas/${id}`, data);
    return response.data;
  },


  /**
   * Delete Mfa.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Mfas/${id}`);
  },


  /**
   * Get Mfa by CellOrEmail.
   */
  getByCellOrEmail: async (cellOrEmail: string): Promise<Mfa> => {
    const response = await api.get<Mfa>(`/Mfas/by-cellOrEmail/${encodeURIComponent(String(cellOrEmail))}`);
    return response.data;
  },


};

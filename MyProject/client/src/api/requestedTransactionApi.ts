// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: RequestedTransaction
//     Generated: 2025-12-17 08:16:17
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  RequestedTransaction,
  CreateRequestedTransactionRequest,
  UpdateRequestedTransactionRequest,
  RequestedTransactionFilters,
} from '../types/RequestedTransaction.types';

type UseRequestedTransactionOptions = UseEntityOptions<RequestedTransactionFilters>;
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';
import type { CustomerWallet } from '../types/CustomerWallet.types';
import type { DrivebotPayment } from '../types/DrivebotPayment.types';
import type { MCCBSecurityCheck } from '../types/MCCBSecurityCheck.types';
import type { MCCBTransaction } from '../types/MCCBTransaction.types';
import type { RequestedTransactionFee } from '../types/RequestedTransactionFee.types';
import type { TransactionBit } from '../types/TransactionBit.types';
import type { TransactionCreditCard } from '../types/TransactionCreditCard.types';
import type { TransactionLoad } from '../types/TransactionLoad.types';

/**
 * API client for RequestedTransaction operations.
 * Base URL: /RequestedTransactions
 */
export const requestedTransactionApi = {
  /**
   * Get RequestedTransaction by ID.
   */
  getById: async (id: number): Promise<RequestedTransaction> => {
    const response = await api.get<RequestedTransaction>(`/RequestedTransactions/${id}`);
    return response.data;
  },


  /**
   * Get all RequestedTransactions with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseRequestedTransactionOptions): Promise<PagedResult<RequestedTransaction>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<RequestedTransaction>>(`/RequestedTransactions?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new RequestedTransaction.
   */
  create: async (data: CreateRequestedTransactionRequest): Promise<RequestedTransaction> => {
    const response = await api.post<RequestedTransaction>('/RequestedTransactions', data);
    return response.data;
  },


  /**
   * Update existing RequestedTransaction.
   */
  update: async (id: number, data: UpdateRequestedTransactionRequest): Promise<RequestedTransaction> => {
    const response = await api.put<RequestedTransaction>(`/RequestedTransactions/${id}`, data);
    return response.data;
  },


  /**
   * Delete RequestedTransaction.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/RequestedTransactions/${id}`);
  },


  /**
   * Get RequestedTransaction by CodeToGiveChanger.
   */
  getByCodeToGiveChanger: async (codeToGiveChanger: string): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-codeToGiveChanger/${encodeURIComponent(String(codeToGiveChanger))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by CreditCardID.
   */
  getByCreditCardID: async (creditCardID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-creditCardID/${encodeURIComponent(String(creditCardID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by DateRequested.
   */
  getByDateRequested: async (dateRequested: Date): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-dateRequested/${encodeURIComponent(String(dateRequested))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by EnmDepositLocation.
   */
  getByEnmDepositLocation: async (enmDepositLocation: string): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-enmDepositLocation/${encodeURIComponent(String(enmDepositLocation))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by DistributorID.
   */
  getByDistributorID: async (distributorID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-distributorID/${encodeURIComponent(String(distributorID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by GroupRequestedTransactionID.
   */
  getByGroupRequestedTransactionID: async (groupRequestedTransactionID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-groupRequestedTransactionID/${encodeURIComponent(String(groupRequestedTransactionID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by InitiatingCustomerID.
   */
  getByInitiatingCustomerID: async (initiatingCustomerID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-initiatingCustomerID/${encodeURIComponent(String(initiatingCustomerID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by LoggedAlertID.
   */
  getByLoggedAlertID: async (loggedAlertID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-loggedAlertID/${encodeURIComponent(String(loggedAlertID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by MonthRequested.
   */
  getByMonthRequested: async (monthRequested: Date): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-monthRequested/${encodeURIComponent(String(monthRequested))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by TargetNonCustomerID.
   */
  getByTargetNonCustomerID: async (targetNonCustomerID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-targetNonCustomerID/${encodeURIComponent(String(targetNonCustomerID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by PayingCustomerID.
   */
  getByPayingCustomerID: async (payingCustomerID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-payingCustomerID/${encodeURIComponent(String(payingCustomerID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by PrepaidCardID.
   */
  getByPrepaidCardID: async (prepaidCardID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-prepaidCardID/${encodeURIComponent(String(prepaidCardID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by ReceivingCustomerID.
   */
  getByReceivingCustomerID: async (receivingCustomerID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-receivingCustomerID/${encodeURIComponent(String(receivingCustomerID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by EnmStatusRequestedTransactionStatus.
   */
  getByEnmStatusRequestedTransactionStatus: async (enmStatusRequestedTransactionStatus: string): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-enmStatusRequestedTransactionStatus/${encodeURIComponent(String(enmStatusRequestedTransactionStatus))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by TargetCustomerID.
   */
  getByTargetCustomerID: async (targetCustomerID: number): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-targetCustomerID/${encodeURIComponent(String(targetCustomerID))}`);
    return response.data;
  },


  /**
   * Get RequestedTransaction by EnmDirectionTransactionDirection.
   */
  getByEnmDirectionTransactionDirection: async (enmDirectionTransactionDirection: string): Promise<RequestedTransaction[]> => {
    const response = await api.get<RequestedTransaction[]>(`/RequestedTransactions/by-enmDirectionTransactionDirection/${encodeURIComponent(String(enmDirectionTransactionDirection))}`);
    return response.data;
  },


  /**
   * Get BACKOFFICETRANSACTIONS for this REQUESTEDTRANSACTION.
   */
  getBackofficeTransactions: async (id: number, skip?: number, take?: number): Promise<BackofficeTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<BackofficeTransaction[]>(`/RequestedTransactions/${id}/BACKOFFICETRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERWALLETS for this REQUESTEDTRANSACTION.
   */
  getCustomerWallets: async (id: number, skip?: number, take?: number): Promise<CustomerWallet[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerWallet[]>(`/RequestedTransactions/${id}/CUSTOMERWALLETS${queryString}`);
    return response.data;
  },


  /**
   * Get DRIVEBOTPAYMENTS for this REQUESTEDTRANSACTION.
   */
  getDrivebotPayments: async (id: number, skip?: number, take?: number): Promise<DrivebotPayment[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<DrivebotPayment[]>(`/RequestedTransactions/${id}/DRIVEBOTPAYMENTS${queryString}`);
    return response.data;
  },


  /**
   * Get MCCBSECURITYCHECKS for this REQUESTEDTRANSACTION.
   */
  getMCCBSecurityChecks: async (id: number, skip?: number, take?: number): Promise<MCCBSecurityCheck[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBSecurityCheck[]>(`/RequestedTransactions/${id}/MCCBSECURITYCHECKS${queryString}`);
    return response.data;
  },


  /**
   * Get MCCBTRANSACTIONS for this REQUESTEDTRANSACTION.
   */
  getMCCBTransactions: async (id: number, skip?: number, take?: number): Promise<MCCBTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBTransaction[]>(`/RequestedTransactions/${id}/MCCBTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get REQUESTEDTRANSACTIONFEES for this REQUESTEDTRANSACTION.
   */
  getRequestedTransactionFees: async (id: number, skip?: number, take?: number): Promise<RequestedTransactionFee[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RequestedTransactionFee[]>(`/RequestedTransactions/${id}/REQUESTEDTRANSACTIONFEES${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONBITS for this REQUESTEDTRANSACTION.
   */
  getTransactionBits: async (id: number, skip?: number, take?: number): Promise<TransactionBit[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionBit[]>(`/RequestedTransactions/${id}/TRANSACTIONBITS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONCREDITCARDS for this REQUESTEDTRANSACTION.
   */
  getTransactionCreditCards: async (id: number, skip?: number, take?: number): Promise<TransactionCreditCard[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionCreditCard[]>(`/RequestedTransactions/${id}/TRANSACTIONCREDITCARDS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONLOADS for this REQUESTEDTRANSACTION.
   */
  getTransactionLoads: async (id: number, skip?: number, take?: number): Promise<TransactionLoad[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionLoad[]>(`/RequestedTransactions/${id}/TRANSACTIONLOADS${queryString}`);
    return response.data;
  },


};

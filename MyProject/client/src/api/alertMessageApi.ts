// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_AlertMessage
//     Generated: 2025-12-17 07:52:19
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  AlertMessage,
  CreateAlertMessageRequest,
  UpdateAlertMessageRequest,
  AlertMessageFilters,
} from '../types/AlertMessage.types';

type UseAlertMessageOptions = UseEntityOptions<AlertMessageFilters>;

/**
 * API client for AlertMessage operations.
 * Base URL: /AlertMessages
 */
export const alertMessageApi = {
  /**
   * Get AlertMessage by ID.
   */
  getById: async (id: number): Promise<AlertMessage> => {
    const response = await api.get<AlertMessage>(`/AlertMessages/${id}`);
    return response.data;
  },


  /**
   * Get all AlertMessages with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseAlertMessageOptions): Promise<PagedResult<AlertMessage>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<AlertMessage>>(`/AlertMessages?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new AlertMessage.
   */
  create: async (data: CreateAlertMessageRequest): Promise<AlertMessage> => {
    const response = await api.post<AlertMessage>('/AlertMessages', data);
    return response.data;
  },


  /**
   * Update existing AlertMessage.
   */
  update: async (id: number, data: UpdateAlertMessageRequest): Promise<AlertMessage> => {
    const response = await api.put<AlertMessage>(`/AlertMessages/${id}`, data);
    return response.data;
  },


  /**
   * Delete AlertMessage.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/AlertMessages/${id}`);
  },


  /**
   * Get AlertMessage by Number.
   */
  getByNumber: async (number: number): Promise<AlertMessage> => {
    const response = await api.get<AlertMessage>(`/AlertMessages/by-number/${encodeURIComponent(String(number))}`);
    return response.data;
  },


  /**
   * Get AlertMessage by Description.
   */
  getByDescription: async (description: string): Promise<AlertMessage[]> => {
    const response = await api.get<AlertMessage[]>(`/AlertMessages/by-description/${encodeURIComponent(String(description))}`);
    return response.data;
  },


};

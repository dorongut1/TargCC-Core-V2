// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: ImportTransactionLoad
//     Generated: 2025-12-17 13:13:00
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  ImportTransactionLoad,
  CreateImportTransactionLoadRequest,
  UpdateImportTransactionLoadRequest,
  ImportTransactionLoadFilters,
} from '../types/ImportTransactionLoad.types';

type UseImportTransactionLoadOptions = UseEntityOptions<ImportTransactionLoadFilters>;
import type { ImportTransactionLoadRow } from '../types/ImportTransactionLoadRow.types';

/**
 * API client for ImportTransactionLoad operations.
 * Base URL: /ImportTransactionLoads
 */
export const importTransactionLoadApi = {
  /**
   * Get ImportTransactionLoad by ID.
   */
  getById: async (id: number): Promise<ImportTransactionLoad> => {
    const response = await api.get<ImportTransactionLoad>(`/ImportTransactionLoads/${id}`);
    return response.data;
  },


  /**
   * Get all ImportTransactionLoads with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseImportTransactionLoadOptions): Promise<PagedResult<ImportTransactionLoad>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<ImportTransactionLoad>>(`/ImportTransactionLoads?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new ImportTransactionLoad.
   */
  create: async (data: CreateImportTransactionLoadRequest): Promise<ImportTransactionLoad> => {
    const response = await api.post<ImportTransactionLoad>('/ImportTransactionLoads', data);
    return response.data;
  },


  /**
   * Update existing ImportTransactionLoad.
   */
  update: async (id: number, data: UpdateImportTransactionLoadRequest): Promise<ImportTransactionLoad> => {
    const response = await api.put<ImportTransactionLoad>(`/ImportTransactionLoads/${id}`, data);
    return response.data;
  },


  /**
   * Delete ImportTransactionLoad.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/ImportTransactionLoads/${id}`);
  },


  /**
   * Get ImportTransactionLoad by DistributorID.
   */
  getByDistributorID: async (distributorID: number): Promise<ImportTransactionLoad[]> => {
    const response = await api.get<ImportTransactionLoad[]>(`/ImportTransactionLoads/by-distributorID/${encodeURIComponent(String(distributorID))}`);
    return response.data;
  },


  /**
   * Get ImportTransactionLoad by InitiatedByUserID.
   */
  getByInitiatedByUserID: async (initiatedByUserID: number): Promise<ImportTransactionLoad[]> => {
    const response = await api.get<ImportTransactionLoad[]>(`/ImportTransactionLoads/by-initiatedByUserID/${encodeURIComponent(String(initiatedByUserID))}`);
    return response.data;
  },


  /**
   * Get ImportTransactionLoad by WhenCompleted.
   */
  getByWhenCompleted: async (whenCompleted: Date): Promise<ImportTransactionLoad[]> => {
    const response = await api.get<ImportTransactionLoad[]>(`/ImportTransactionLoads/by-whenCompleted/${encodeURIComponent(String(whenCompleted))}`);
    return response.data;
  },


  /**
   * Get ImportTransactionLoad by WhenInitialized.
   */
  getByWhenInitialized: async (whenInitialized: Date): Promise<ImportTransactionLoad[]> => {
    const response = await api.get<ImportTransactionLoad[]>(`/ImportTransactionLoads/by-whenInitialized/${encodeURIComponent(String(whenInitialized))}`);
    return response.data;
  },


  /**
   * Get IMPORTTRANSACTIONLOADROWS for this IMPORTTRANSACTIONLOAD.
   */
  getImportTransactionLoadRows: async (id: number, skip?: number, take?: number): Promise<ImportTransactionLoadRow[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ImportTransactionLoadRow[]>(`/ImportTransactionLoads/${id}/IMPORTTRANSACTIONLOADROWS${queryString}`);
    return response.data;
  },


};

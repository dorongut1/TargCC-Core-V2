// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: GovtReport00
//     Generated: 2025-12-17 09:34:37
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  GovtReport00,
  CreateGovtReport00Request,
  UpdateGovtReport00Request,
  GovtReport00Filters,
} from '../types/GovtReport00.types';

type UseGovtReport00Options = UseEntityOptions<GovtReport00Filters>;
import type { GovtReport10 } from '../types/GovtReport10.types';
import type { GovtReport30 } from '../types/GovtReport30.types';
import type { GovtReport40 } from '../types/GovtReport40.types';
import type { GovtReport99 } from '../types/GovtReport99.types';
import type { GovtReportPayment } from '../types/GovtReportPayment.types';

/**
 * API client for GovtReport00 operations.
 * Base URL: /GovtReport00s
 */
export const govtReport00Api = {
  /**
   * Get GovtReport00 by ID.
   */
  getById: async (id: number): Promise<GovtReport00> => {
    const response = await api.get<GovtReport00>(`/GovtReport00s/${id}`);
    return response.data;
  },


  /**
   * Get all GovtReport00s with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseGovtReport00Options): Promise<PagedResult<GovtReport00>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<GovtReport00>>(`/GovtReport00s?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new GovtReport00.
   */
  create: async (data: CreateGovtReport00Request): Promise<GovtReport00> => {
    const response = await api.post<GovtReport00>('/GovtReport00s', data);
    return response.data;
  },


  /**
   * Update existing GovtReport00.
   */
  update: async (id: number, data: UpdateGovtReport00Request): Promise<GovtReport00> => {
    const response = await api.put<GovtReport00>(`/GovtReport00s/${id}`, data);
    return response.data;
  },


  /**
   * Delete GovtReport00.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/GovtReport00s/${id}`);
  },


  /**
   * Get GovtReport00 by Name.
   */
  getByName: async (name: string): Promise<GovtReport00> => {
    const response = await api.get<GovtReport00>(`/GovtReport00s/by-name/${encodeURIComponent(String(name))}`);
    return response.data;
  },


  /**
   * Get GovtReport00 by ReportMonth.
   */
  getByReportMonth: async (reportMonth: string): Promise<GovtReport00> => {
    const response = await api.get<GovtReport00>(`/GovtReport00s/by-reportMonth/${encodeURIComponent(String(reportMonth))}`);
    return response.data;
  },


  /**
   * Get GOVTREPORT10S for this GOVTREPORT00.
   */
  getGovtReport10s: async (id: number, skip?: number, take?: number): Promise<GovtReport10[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<GovtReport10[]>(`/GovtReport00s/${id}/GOVTREPORT10S${queryString}`);
    return response.data;
  },


  /**
   * Get GOVTREPORT30S for this GOVTREPORT00.
   */
  getGovtReport30s: async (id: number, skip?: number, take?: number): Promise<GovtReport30[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<GovtReport30[]>(`/GovtReport00s/${id}/GOVTREPORT30S${queryString}`);
    return response.data;
  },


  /**
   * Get GOVTREPORT40S for this GOVTREPORT00.
   */
  getGovtReport40s: async (id: number, skip?: number, take?: number): Promise<GovtReport40[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<GovtReport40[]>(`/GovtReport00s/${id}/GOVTREPORT40S${queryString}`);
    return response.data;
  },


  /**
   * Get GOVTREPORT99S for this GOVTREPORT00.
   */
  getGovtReport99s: async (id: number, skip?: number, take?: number): Promise<GovtReport99[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<GovtReport99[]>(`/GovtReport00s/${id}/GOVTREPORT99S${queryString}`);
    return response.data;
  },


  /**
   * Get GOVTREPORTPAYMENTS for this GOVTREPORT00.
   */
  getGovtReportPayments: async (id: number, skip?: number, take?: number): Promise<GovtReportPayment[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<GovtReportPayment[]>(`/GovtReport00s/${id}/GOVTREPORTPAYMENTS${queryString}`);
    return response.data;
  },


};

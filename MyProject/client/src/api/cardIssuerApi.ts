// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: CardIssuer
//     Generated: 2025-12-17 08:54:22
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  CardIssuer,
  CreateCardIssuerRequest,
  UpdateCardIssuerRequest,
  CardIssuerFilters,
} from '../types/CardIssuer.types';

type UseCardIssuerOptions = UseEntityOptions<CardIssuerFilters>;

/**
 * API client for CardIssuer operations.
 * Base URL: /CardIssuers
 */
export const cardIssuerApi = {
  /**
   * Get CardIssuer by ID.
   */
  getById: async (id: number): Promise<CardIssuer> => {
    const response = await api.get<CardIssuer>(`/CardIssuers/${id}`);
    return response.data;
  },


  /**
   * Get all CardIssuers with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseCardIssuerOptions): Promise<PagedResult<CardIssuer>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<CardIssuer>>(`/CardIssuers?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new CardIssuer.
   */
  create: async (data: CreateCardIssuerRequest): Promise<CardIssuer> => {
    const response = await api.post<CardIssuer>('/CardIssuers', data);
    return response.data;
  },


  /**
   * Update existing CardIssuer.
   */
  update: async (id: number, data: UpdateCardIssuerRequest): Promise<CardIssuer> => {
    const response = await api.put<CardIssuer>(`/CardIssuers/${id}`, data);
    return response.data;
  },


  /**
   * Delete CardIssuer.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/CardIssuers/${id}`);
  },


  /**
   * Get CardIssuer by Name.
   */
  getByName: async (name: string): Promise<CardIssuer> => {
    const response = await api.get<CardIssuer>(`/CardIssuers/by-name/${encodeURIComponent(String(name))}`);
    return response.data;
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_Permission
//     Generated: 2025-12-11 21:58:09
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  Permission,
  CreatePermissionRequest,
  UpdatePermissionRequest,
  PermissionFilters,
} from '../types/Permission.types';

/**
 * API client for Permission operations.
 * Base URL: /Permissions
 */
export const permissionApi = {
  /**
   * Get Permission by ID.
   */
  getById: async (id: number): Promise<Permission> => {
    const response = await api.get<Permission>(`/Permissions/${id}`);
    return response.data;
  },


  /**
   * Get all Permissions with optional filters.
   */
  getAll: async (filters?: PermissionFilters): Promise<Permission[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/Permissions/filter' : '/Permissions';
    const response = await api.get<Permission[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new Permission.
   */
  create: async (data: CreatePermissionRequest): Promise<Permission> => {
    const response = await api.post<Permission>('/Permissions', data);
    return response.data;
  },


  /**
   * Update existing Permission.
   */
  update: async (id: number, data: UpdatePermissionRequest): Promise<Permission> => {
    const response = await api.put<Permission>(`/Permissions/${id}`, data);
    return response.data;
  },


  /**
   * Delete Permission.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Permissions/${id}`);
  },


  /**
   * Get Permission by CProcessID.
   */
  getByCProcessID: async (cProcessID: number): Promise<Permission[]> => {
    const response = await api.get<Permission[]>(`/Permissions/by-cProcessID/${encodeURIComponent(String(cProcessID))}`);
    return response.data;
  },


  /**
   * Get Permission by CRoleID.
   */
  getByCRoleID: async (cRoleID: number): Promise<Permission[]> => {
    const response = await api.get<Permission[]>(`/Permissions/by-cRoleID/${encodeURIComponent(String(cRoleID))}`);
    return response.data;
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: TransactionBit
//     Generated: 2025-12-18 08:27:26
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  TransactionBit,
  CreateTransactionBitRequest,
  UpdateTransactionBitRequest,
  TransactionBitFilters,
} from '../types/TransactionBit.types';

type UseTransactionBitOptions = UseEntityOptions<TransactionBitFilters>;
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';

/**
 * API client for TransactionBit operations.
 * Base URL: /TransactionBits
 */
export const transactionBitApi = {
  /**
   * Get TransactionBit by ID.
   */
  getById: async (id: number): Promise<TransactionBit> => {
    const response = await api.get<TransactionBit>(`/TransactionBits/${id}`);
    return response.data;
  },


  /**
   * Get all TransactionBits with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseTransactionBitOptions): Promise<PagedResult<TransactionBit>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<TransactionBit>>(`/TransactionBits?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new TransactionBit.
   */
  create: async (data: CreateTransactionBitRequest): Promise<TransactionBit> => {
    const response = await api.post<TransactionBit>('/TransactionBits', data);
    return response.data;
  },


  /**
   * Update existing TransactionBit.
   */
  update: async (id: number, data: UpdateTransactionBitRequest): Promise<TransactionBit> => {
    const response = await api.put<TransactionBit>(`/TransactionBits/${id}`, data);
    return response.data;
  },


  /**
   * Delete TransactionBit.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/TransactionBits/${id}`);
  },


  /**
   * Get TransactionBit by RequestedTransactionID.
   */
  getByRequestedTransactionID: async (requestedTransactionID: number): Promise<TransactionBit> => {
    const response = await api.get<TransactionBit>(`/TransactionBits/by-requestedTransactionID/${encodeURIComponent(String(requestedTransactionID))}`);
    return response.data;
  },


  /**
   * Get TransactionBit by CardLast4Numbers.
   */
  getByCardLast4Numbers: async (cardLast4Numbers: string): Promise<TransactionBit[]> => {
    const response = await api.get<TransactionBit[]>(`/TransactionBits/by-cardLast4Numbers/${encodeURIComponent(String(cardLast4Numbers))}`);
    return response.data;
  },


  /**
   * Get TransactionBit by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<TransactionBit[]> => {
    const response = await api.get<TransactionBit[]>(`/TransactionBits/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get TransactionBit by CustomerWalletID.
   */
  getByCustomerWalletID: async (customerWalletID: number): Promise<TransactionBit[]> => {
    const response = await api.get<TransactionBit[]>(`/TransactionBits/by-customerWalletID/${encodeURIComponent(String(customerWalletID))}`);
    return response.data;
  },


  /**
   * Get TransactionBit by NonCustomerID.
   */
  getByNonCustomerID: async (nonCustomerID: number): Promise<TransactionBit[]> => {
    const response = await api.get<TransactionBit[]>(`/TransactionBits/by-nonCustomerID/${encodeURIComponent(String(nonCustomerID))}`);
    return response.data;
  },


  /**
   * Get TransactionBit by WhenInitialized.
   */
  getByWhenInitialized: async (whenInitialized: Date): Promise<TransactionBit[]> => {
    const response = await api.get<TransactionBit[]>(`/TransactionBits/by-whenInitialized/${encodeURIComponent(String(whenInitialized))}`);
    return response.data;
  },


  /**
   * Get BACKOFFICETRANSACTIONS for this TRANSACTIONBIT.
   */
  getBackofficeTransactions: async (id: number, skip?: number, take?: number): Promise<BackofficeTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<BackofficeTransaction[]>(`/TransactionBits/${id}/BACKOFFICETRANSACTIONS${queryString}`);
    return response.data;
  },


};

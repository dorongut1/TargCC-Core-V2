// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: SAP
//     Generated: 2025-12-17 14:36:08
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Sap,
  CreateSapRequest,
  UpdateSapRequest,
  SapFilters,
} from '../types/Sap.types';

type UseSapOptions = UseEntityOptions<SapFilters>;

/**
 * API client for Sap operations.
 * Base URL: /Saps
 */
export const sapApi = {
  /**
   * Get Sap by ID.
   */
  getById: async (id: number): Promise<Sap> => {
    const response = await api.get<Sap>(`/Saps/${id}`);
    return response.data;
  },


  /**
   * Get all Saps with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseSapOptions): Promise<PagedResult<Sap>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Sap>>(`/Saps?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Sap.
   */
  create: async (data: CreateSapRequest): Promise<Sap> => {
    const response = await api.post<Sap>('/Saps', data);
    return response.data;
  },


  /**
   * Update existing Sap.
   */
  update: async (id: number, data: UpdateSapRequest): Promise<Sap> => {
    const response = await api.put<Sap>(`/Saps/${id}`, data);
    return response.data;
  },


  /**
   * Delete Sap.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Saps/${id}`);
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: ExchangeRate
//     Generated: 2025-12-17 09:02:39
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  ExchangeRate,
  CreateExchangeRateRequest,
  UpdateExchangeRateRequest,
  ExchangeRateFilters,
} from '../types/ExchangeRate.types';

type UseExchangeRateOptions = UseEntityOptions<ExchangeRateFilters>;

/**
 * API client for ExchangeRate operations.
 * Base URL: /ExchangeRates
 */
export const exchangeRateApi = {
  /**
   * Get ExchangeRate by ID.
   */
  getById: async (id: number): Promise<ExchangeRate> => {
    const response = await api.get<ExchangeRate>(`/ExchangeRates/${id}`);
    return response.data;
  },


  /**
   * Get all ExchangeRates with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseExchangeRateOptions): Promise<PagedResult<ExchangeRate>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<ExchangeRate>>(`/ExchangeRates?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new ExchangeRate.
   */
  create: async (data: CreateExchangeRateRequest): Promise<ExchangeRate> => {
    const response = await api.post<ExchangeRate>('/ExchangeRates', data);
    return response.data;
  },


  /**
   * Update existing ExchangeRate.
   */
  update: async (id: number, data: UpdateExchangeRateRequest): Promise<ExchangeRate> => {
    const response = await api.put<ExchangeRate>(`/ExchangeRates/${id}`, data);
    return response.data;
  },


  /**
   * Delete ExchangeRate.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/ExchangeRates/${id}`);
  },


  /**
   * Get ExchangeRate by EnmCurrency.
   */
  getByEnmCurrency: async (enmCurrency: string): Promise<ExchangeRate> => {
    const response = await api.get<ExchangeRate>(`/ExchangeRates/by-enmCurrency/${encodeURIComponent(String(enmCurrency))}`);
    return response.data;
  },


};

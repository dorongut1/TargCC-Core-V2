// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: MCCBReceiver
//     Generated: 2025-12-11 22:10:38
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  MCCBReceiver,
  CreateMCCBReceiverRequest,
  UpdateMCCBReceiverRequest,
  MCCBReceiverFilters,
} from '../types/MCCBReceiver.types';
import type { MCCBReceiverAccount } from '../types/MCCBReceiverAccount.types';
import type { MCCBSecurityCheck } from '../types/MCCBSecurityCheck.types';
import type { MCCBTransaction } from '../types/MCCBTransaction.types';

/**
 * API client for MCCBReceiver operations.
 * Base URL: /MCCBReceivers
 */
export const mCCBReceiverApi = {
  /**
   * Get MCCBReceiver by ID.
   */
  getById: async (id: number): Promise<MCCBReceiver> => {
    const response = await api.get<MCCBReceiver>(`/MCCBReceivers/${id}`);
    return response.data;
  },


  /**
   * Get all MCCBReceivers with optional filters.
   */
  getAll: async (filters?: MCCBReceiverFilters): Promise<MCCBReceiver[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/MCCBReceivers/filter' : '/MCCBReceivers';
    const response = await api.get<MCCBReceiver[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new MCCBReceiver.
   */
  create: async (data: CreateMCCBReceiverRequest): Promise<MCCBReceiver> => {
    const response = await api.post<MCCBReceiver>('/MCCBReceivers', data);
    return response.data;
  },


  /**
   * Update existing MCCBReceiver.
   */
  update: async (id: number, data: UpdateMCCBReceiverRequest): Promise<MCCBReceiver> => {
    const response = await api.put<MCCBReceiver>(`/MCCBReceivers/${id}`, data);
    return response.data;
  },


  /**
   * Delete MCCBReceiver.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/MCCBReceivers/${id}`);
  },


  /**
   * Get MCCBReceiver by BirthCountryISO3166Alpha2.
   */
  getByBirthCountryISO3166Alpha2: async (birthCountryISO3166Alpha2: string): Promise<MCCBReceiver[]> => {
    const response = await api.get<MCCBReceiver[]>(`/MCCBReceivers/by-birthCountryISO3166Alpha2/${encodeURIComponent(String(birthCountryISO3166Alpha2))}`);
    return response.data;
  },


  /**
   * Get MCCBReceiver by CitizenshipCountryISO3166Alpha2.
   */
  getByCitizenshipCountryISO3166Alpha2: async (citizenshipCountryISO3166Alpha2: string): Promise<MCCBReceiver[]> => {
    const response = await api.get<MCCBReceiver[]>(`/MCCBReceivers/by-citizenshipCountryISO3166Alpha2/${encodeURIComponent(String(citizenshipCountryISO3166Alpha2))}`);
    return response.data;
  },


  /**
   * Get MCCBReceiver by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<MCCBReceiver[]> => {
    const response = await api.get<MCCBReceiver[]>(`/MCCBReceivers/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get MCCBReceiver by ResidenceCountryISO3166Alpha2.
   */
  getByResidenceCountryISO3166Alpha2: async (residenceCountryISO3166Alpha2: string): Promise<MCCBReceiver[]> => {
    const response = await api.get<MCCBReceiver[]>(`/MCCBReceivers/by-residenceCountryISO3166Alpha2/${encodeURIComponent(String(residenceCountryISO3166Alpha2))}`);
    return response.data;
  },


  /**
   * Get MCCBRECEIVERACCOUNTS for this MCCBRECEIVER.
   */
  getMCCBReceiverAccounts: async (id: number, skip?: number, take?: number): Promise<MCCBReceiverAccount[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBReceiverAccount[]>(`/MCCBReceivers/${id}/MCCBRECEIVERACCOUNTS${queryString}`);
    return response.data;
  },


  /**
   * Get MCCBSECURITYCHECKS for this MCCBRECEIVER.
   */
  getMCCBSecurityChecks: async (id: number, skip?: number, take?: number): Promise<MCCBSecurityCheck[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBSecurityCheck[]>(`/MCCBReceivers/${id}/MCCBSECURITYCHECKS${queryString}`);
    return response.data;
  },


  /**
   * Get MCCBTRANSACTIONS for this MCCBRECEIVER.
   */
  getMCCBTransactions: async (id: number, skip?: number, take?: number): Promise<MCCBTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<MCCBTransaction[]>(`/MCCBReceivers/${id}/MCCBTRANSACTIONS${queryString}`);
    return response.data;
  },


};

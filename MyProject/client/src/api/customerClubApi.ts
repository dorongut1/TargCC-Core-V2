// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: CustomerClub
//     Generated: 2025-12-15 20:38:14
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  CustomerClub,
  CreateCustomerClubRequest,
  UpdateCustomerClubRequest,
  CustomerClubFilters,
} from '../types/CustomerClub.types';

type UseCustomerClubOptions = UseEntityOptions<CustomerClubFilters>;
import type { Customer } from '../types/Customer.types';
import type { FeeOverride } from '../types/FeeOverride.types';
import type { LimitationOverride } from '../types/LimitationOverride.types';

/**
 * API client for CustomerClub operations.
 * Base URL: /CustomerClubs
 */
export const customerClubApi = {
  /**
   * Get CustomerClub by ID.
   */
  getById: async (id: number): Promise<CustomerClub> => {
    const response = await api.get<CustomerClub>(`/CustomerClubs/${id}`);
    return response.data;
  },


  /**
   * Get all CustomerClubs with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseCustomerClubOptions): Promise<PagedResult<CustomerClub>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<CustomerClub>>(`/CustomerClubs?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new CustomerClub.
   */
  create: async (data: CreateCustomerClubRequest): Promise<CustomerClub> => {
    const response = await api.post<CustomerClub>('/CustomerClubs', data);
    return response.data;
  },


  /**
   * Update existing CustomerClub.
   */
  update: async (id: number, data: UpdateCustomerClubRequest): Promise<CustomerClub> => {
    const response = await api.put<CustomerClub>(`/CustomerClubs/${id}`, data);
    return response.data;
  },


  /**
   * Delete CustomerClub.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/CustomerClubs/${id}`);
  },


  /**
   * Get CustomerClub by AppsFlyerFlag.
   */
  getByAppsFlyerFlag: async (appsFlyerFlag: string): Promise<CustomerClub> => {
    const response = await api.get<CustomerClub>(`/CustomerClubs/by-appsFlyerFlag/${encodeURIComponent(String(appsFlyerFlag))}`);
    return response.data;
  },


  /**
   * Get CustomerClub by ClubName.
   */
  getByClubName: async (clubName: string): Promise<CustomerClub> => {
    const response = await api.get<CustomerClub>(`/CustomerClubs/by-clubName/${encodeURIComponent(String(clubName))}`);
    return response.data;
  },


  /**
   * Get CustomerClub by IsRoot.
   */
  getByIsRoot: async (isRoot: boolean): Promise<CustomerClub> => {
    const response = await api.get<CustomerClub>(`/CustomerClubs/by-isRoot/${encodeURIComponent(String(isRoot))}`);
    return response.data;
  },


  /**
   * Get CustomerClub by UseForUnassignedBusinessCustomer.
   */
  getByUseForUnassignedBusinessCustomer: async (useForUnassignedBusinessCustomer: boolean): Promise<CustomerClub> => {
    const response = await api.get<CustomerClub>(`/CustomerClubs/by-useForUnassignedBusinessCustomer/${encodeURIComponent(String(useForUnassignedBusinessCustomer))}`);
    return response.data;
  },


  /**
   * Get CustomerClub by UseForUnassignedCustomer.
   */
  getByUseForUnassignedCustomer: async (useForUnassignedCustomer: boolean): Promise<CustomerClub> => {
    const response = await api.get<CustomerClub>(`/CustomerClubs/by-useForUnassignedCustomer/${encodeURIComponent(String(useForUnassignedCustomer))}`);
    return response.data;
  },


  /**
   * Get CUSTOMERS for this CUSTOMERCLUB.
   */
  getCustomers: async (id: number, skip?: number, take?: number): Promise<Customer[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Customer[]>(`/CustomerClubs/${id}/CUSTOMERS${queryString}`);
    return response.data;
  },


  /**
   * Get FEEOVERRIDES for this CUSTOMERCLUB.
   */
  getFeeOverrides: async (id: number, skip?: number, take?: number): Promise<FeeOverride[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<FeeOverride[]>(`/CustomerClubs/${id}/FEEOVERRIDES${queryString}`);
    return response.data;
  },


  /**
   * Get LIMITATIONOVERRIDES for this CUSTOMERCLUB.
   */
  getLimitationOverrides: async (id: number, skip?: number, take?: number): Promise<LimitationOverride[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LimitationOverride[]>(`/CustomerClubs/${id}/LIMITATIONOVERRIDES${queryString}`);
    return response.data;
  },


};

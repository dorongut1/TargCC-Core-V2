// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: GovtReport30
//     Generated: 2025-12-17 15:31:36
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  GovtReport30,
  CreateGovtReport30Request,
  UpdateGovtReport30Request,
  GovtReport30Filters,
} from '../types/GovtReport30.types';

type UseGovtReport30Options = UseEntityOptions<GovtReport30Filters>;

/**
 * API client for GovtReport30 operations.
 * Base URL: /GovtReport30s
 */
export const govtReport30Api = {
  /**
   * Get GovtReport30 by ID.
   */
  getById: async (id: number): Promise<GovtReport30> => {
    const response = await api.get<GovtReport30>(`/GovtReport30s/${id}`);
    return response.data;
  },


  /**
   * Get all GovtReport30s with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseGovtReport30Options): Promise<PagedResult<GovtReport30>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<GovtReport30>>(`/GovtReport30s?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new GovtReport30.
   */
  create: async (data: CreateGovtReport30Request): Promise<GovtReport30> => {
    const response = await api.post<GovtReport30>('/GovtReport30s', data);
    return response.data;
  },


  /**
   * Update existing GovtReport30.
   */
  update: async (id: number, data: UpdateGovtReport30Request): Promise<GovtReport30> => {
    const response = await api.put<GovtReport30>(`/GovtReport30s/${id}`, data);
    return response.data;
  },


  /**
   * Delete GovtReport30.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/GovtReport30s/${id}`);
  },


  /**
   * Get GovtReport30 by GovtReport00ID.
   */
  getByGovtReport00ID: async (govtReport00ID: number): Promise<GovtReport30[]> => {
    const response = await api.get<GovtReport30[]>(`/GovtReport30s/by-govtReport00ID/${encodeURIComponent(String(govtReport00ID))}`);
    return response.data;
  },


};

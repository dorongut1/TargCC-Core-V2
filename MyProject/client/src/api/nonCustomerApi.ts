// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: NonCustomer
//     Generated: 2025-12-15 09:25:40
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  NonCustomer,
  CreateNonCustomerRequest,
  UpdateNonCustomerRequest,
  NonCustomerFilters,
} from '../types/NonCustomer.types';
import type { RequestedTransaction } from '../types/RequestedTransaction.types';
import type { TransactionBit } from '../types/TransactionBit.types';
import type { TransactionCreditCard } from '../types/TransactionCreditCard.types';

/**
 * API client for NonCustomer operations.
 * Base URL: /NonCustomers
 */
export const nonCustomerApi = {
  /**
   * Get NonCustomer by ID.
   */
  getById: async (id: number): Promise<NonCustomer> => {
    const response = await api.get<NonCustomer>(`/NonCustomers/${id}`);
    return response.data;
  },


  /**
   * Get all NonCustomers with optional filters.
   */
  getAll: async (filters?: NonCustomerFilters): Promise<NonCustomer[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/NonCustomers/filter' : '/NonCustomers';
    const response = await api.get<NonCustomer[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new NonCustomer.
   */
  create: async (data: CreateNonCustomerRequest): Promise<NonCustomer> => {
    const response = await api.post<NonCustomer>('/NonCustomers', data);
    return response.data;
  },


  /**
   * Update existing NonCustomer.
   */
  update: async (id: number, data: UpdateNonCustomerRequest): Promise<NonCustomer> => {
    const response = await api.put<NonCustomer>(`/NonCustomers/${id}`, data);
    return response.data;
  },


  /**
   * Delete NonCustomer.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/NonCustomers/${id}`);
  },


  /**
   * Get NonCustomer by CellphoneNumber.
   */
  getByCellphoneNumber: async (cellphoneNumber: string): Promise<NonCustomer> => {
    const response = await api.get<NonCustomer>(`/NonCustomers/by-cellphoneNumber/${encodeURIComponent(String(cellphoneNumber))}`);
    return response.data;
  },


  /**
   * Get NonCustomer by Heading.
   */
  getByHeading: async (heading: string): Promise<NonCustomer> => {
    const response = await api.get<NonCustomer>(`/NonCustomers/by-heading/${encodeURIComponent(String(heading))}`);
    return response.data;
  },


  /**
   * Get NonCustomer by IdentityNumber.
   */
  getByIdentityNumber: async (identityNumber: string): Promise<NonCustomer[]> => {
    const response = await api.get<NonCustomer[]>(`/NonCustomers/by-identityNumber/${encodeURIComponent(String(identityNumber))}`);
    return response.data;
  },


  /**
   * Get REQUESTEDTRANSACTIONS for this NONCUSTOMER.
   */
  getRequestedTransactions: async (id: number, skip?: number, take?: number): Promise<RequestedTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RequestedTransaction[]>(`/NonCustomers/${id}/REQUESTEDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONBITS for this NONCUSTOMER.
   */
  getTransactionBits: async (id: number, skip?: number, take?: number): Promise<TransactionBit[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionBit[]>(`/NonCustomers/${id}/TRANSACTIONBITS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONCREDITCARDS for this NONCUSTOMER.
   */
  getTransactionCreditCards: async (id: number, skip?: number, take?: number): Promise<TransactionCreditCard[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionCreditCard[]>(`/NonCustomers/${id}/TRANSACTIONCREDITCARDS${queryString}`);
    return response.data;
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_Mail
//     Generated: 2025-12-18 08:27:22
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Mail,
  CreateMailRequest,
  UpdateMailRequest,
  MailFilters,
} from '../types/Mail.types';

type UseMailOptions = UseEntityOptions<MailFilters>;

/**
 * API client for Mail operations.
 * Base URL: /Mails
 */
export const mailApi = {
  /**
   * Get Mail by ID.
   */
  getById: async (id: number): Promise<Mail> => {
    const response = await api.get<Mail>(`/Mails/${id}`);
    return response.data;
  },


  /**
   * Get all Mails with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseMailOptions): Promise<PagedResult<Mail>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Mail>>(`/Mails?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Mail.
   */
  create: async (data: CreateMailRequest): Promise<Mail> => {
    const response = await api.post<Mail>('/Mails', data);
    return response.data;
  },


  /**
   * Update existing Mail.
   */
  update: async (id: number, data: UpdateMailRequest): Promise<Mail> => {
    const response = await api.put<Mail>(`/Mails/${id}`, data);
    return response.data;
  },


  /**
   * Delete Mail.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Mails/${id}`);
  },


  /**
   * Get Mail by WasSeen.
   */
  getByWasSeen: async (wasSeen: boolean): Promise<Mail[]> => {
    const response = await api.get<Mail[]>(`/Mails/by-wasSeen/${encodeURIComponent(String(wasSeen))}`);
    return response.data;
  },


};

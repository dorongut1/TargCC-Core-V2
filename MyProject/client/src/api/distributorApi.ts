// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Distributor
//     Generated: 2025-12-18 08:27:25
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Distributor,
  CreateDistributorRequest,
  UpdateDistributorRequest,
  DistributorFilters,
} from '../types/Distributor.types';

type UseDistributorOptions = UseEntityOptions<DistributorFilters>;
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';
import type { Card } from '../types/Card.types';
import type { CardActivation } from '../types/CardActivation.types';
import type { CardOrderByDistributor } from '../types/CardOrderByDistributor.types';
import type { Customer } from '../types/Customer.types';
import type { CustomerCardTransaction } from '../types/CustomerCardTransaction.types';
import type { DistributorBranch } from '../types/DistributorBranch.types';
import type { DistributorObligo } from '../types/DistributorObligo.types';
import type { FeeOverride } from '../types/FeeOverride.types';
import type { GovtReportDistributor } from '../types/GovtReportDistributor.types';
import type { ImportTransactionLoad } from '../types/ImportTransactionLoad.types';
import type { LimitationOverride } from '../types/LimitationOverride.types';
import type { NewCardRequest } from '../types/NewCardRequest.types';
import type { NewCardRequestAPI } from '../types/NewCardRequestAPI.types';
import type { RequestedTransaction } from '../types/RequestedTransaction.types';
import type { TransactionLoad } from '../types/TransactionLoad.types';

/**
 * API client for Distributor operations.
 * Base URL: /Distributors
 */
export const distributorApi = {
  /**
   * Get Distributor by ID.
   */
  getById: async (id: number): Promise<Distributor> => {
    const response = await api.get<Distributor>(`/Distributors/${id}`);
    return response.data;
  },


  /**
   * Get all Distributors with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseDistributorOptions): Promise<PagedResult<Distributor>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Distributor>>(`/Distributors?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Distributor.
   */
  create: async (data: CreateDistributorRequest): Promise<Distributor> => {
    const response = await api.post<Distributor>('/Distributors', data);
    return response.data;
  },


  /**
   * Update existing Distributor.
   */
  update: async (id: number, data: UpdateDistributorRequest): Promise<Distributor> => {
    const response = await api.put<Distributor>(`/Distributors/${id}`, data);
    return response.data;
  },


  /**
   * Delete Distributor.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Distributors/${id}`);
  },


  /**
   * Get Distributor by BusinessName.
   */
  getByBusinessName: async (businessName: string): Promise<Distributor> => {
    const response = await api.get<Distributor>(`/Distributors/by-businessName/${encodeURIComponent(String(businessName))}`);
    return response.data;
  },


  /**
   * Get Distributor by BusinessNameLatin.
   */
  getByBusinessNameLatin: async (businessNameLatin: string): Promise<Distributor> => {
    const response = await api.get<Distributor>(`/Distributors/by-businessNameLatin/${encodeURIComponent(String(businessNameLatin))}`);
    return response.data;
  },


  /**
   * Get Distributor by GovernmentCompanyNumber.
   */
  getByGovernmentCompanyNumber: async (governmentCompanyNumber: string): Promise<Distributor[]> => {
    const response = await api.get<Distributor[]>(`/Distributors/by-governmentCompanyNumber/${encodeURIComponent(String(governmentCompanyNumber))}`);
    return response.data;
  },


  /**
   * Get Distributor by EnmIsOnTerrorList.
   */
  getByEnmIsOnTerrorList: async (enmIsOnTerrorList: string): Promise<Distributor[]> => {
    const response = await api.get<Distributor[]>(`/Distributors/by-enmIsOnTerrorList/${encodeURIComponent(String(enmIsOnTerrorList))}`);
    return response.data;
  },


  /**
   * Get BACKOFFICETRANSACTIONS for this DISTRIBUTOR.
   */
  getBackofficeTransactions: async (id: number, skip?: number, take?: number): Promise<BackofficeTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<BackofficeTransaction[]>(`/Distributors/${id}/BACKOFFICETRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDS for this DISTRIBUTOR.
   */
  getCards: async (id: number, skip?: number, take?: number): Promise<Card[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Card[]>(`/Distributors/${id}/CARDS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDACTIVATIONS for this DISTRIBUTOR.
   */
  getCardActivations: async (id: number, skip?: number, take?: number): Promise<CardActivation[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardActivation[]>(`/Distributors/${id}/CARDACTIVATIONS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDORDERBYDISTRIBUTORS for this DISTRIBUTOR.
   */
  getCardOrderByDistributors: async (id: number, skip?: number, take?: number): Promise<CardOrderByDistributor[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardOrderByDistributor[]>(`/Distributors/${id}/CARDORDERBYDISTRIBUTORS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERS for this DISTRIBUTOR.
   */
  getCustomers: async (id: number, skip?: number, take?: number): Promise<Customer[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Customer[]>(`/Distributors/${id}/CUSTOMERS${queryString}`);
    return response.data;
  },


  /**
   * Get CUSTOMERCARDTRANSACTIONS for this DISTRIBUTOR.
   */
  getCustomerCardTransactions: async (id: number, skip?: number, take?: number): Promise<CustomerCardTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CustomerCardTransaction[]>(`/Distributors/${id}/CUSTOMERCARDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get DISTRIBUTORBRANCHES for this DISTRIBUTOR.
   */
  getDistributorBranches: async (id: number, skip?: number, take?: number): Promise<DistributorBranch[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<DistributorBranch[]>(`/Distributors/${id}/DISTRIBUTORBRANCHES${queryString}`);
    return response.data;
  },


  /**
   * Get DISTRIBUTOROBLIGOS for this DISTRIBUTOR.
   */
  getDistributorObligos: async (id: number, skip?: number, take?: number): Promise<DistributorObligo[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<DistributorObligo[]>(`/Distributors/${id}/DISTRIBUTOROBLIGOS${queryString}`);
    return response.data;
  },


  /**
   * Get FEEOVERRIDES for this DISTRIBUTOR.
   */
  getFeeOverrides: async (id: number, skip?: number, take?: number): Promise<FeeOverride[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<FeeOverride[]>(`/Distributors/${id}/FEEOVERRIDES${queryString}`);
    return response.data;
  },


  /**
   * Get GOVTREPORTDISTRIBUTORS for this DISTRIBUTOR.
   */
  getGovtReportDistributors: async (id: number, skip?: number, take?: number): Promise<GovtReportDistributor[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<GovtReportDistributor[]>(`/Distributors/${id}/GOVTREPORTDISTRIBUTORS${queryString}`);
    return response.data;
  },


  /**
   * Get IMPORTTRANSACTIONLOADS for this DISTRIBUTOR.
   */
  getImportTransactionLoads: async (id: number, skip?: number, take?: number): Promise<ImportTransactionLoad[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ImportTransactionLoad[]>(`/Distributors/${id}/IMPORTTRANSACTIONLOADS${queryString}`);
    return response.data;
  },


  /**
   * Get LIMITATIONOVERRIDES for this DISTRIBUTOR.
   */
  getLimitationOverrides: async (id: number, skip?: number, take?: number): Promise<LimitationOverride[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LimitationOverride[]>(`/Distributors/${id}/LIMITATIONOVERRIDES${queryString}`);
    return response.data;
  },


  /**
   * Get NEWCARDREQUESTS for this DISTRIBUTOR.
   */
  getNewCardRequests: async (id: number, skip?: number, take?: number): Promise<NewCardRequest[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<NewCardRequest[]>(`/Distributors/${id}/NEWCARDREQUESTS${queryString}`);
    return response.data;
  },


  /**
   * Get NEWCARDREQUESTAPIS for this DISTRIBUTOR.
   */
  getNewCardRequestAPIs: async (id: number, skip?: number, take?: number): Promise<NewCardRequestAPI[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<NewCardRequestAPI[]>(`/Distributors/${id}/NEWCARDREQUESTAPIS${queryString}`);
    return response.data;
  },


  /**
   * Get REQUESTEDTRANSACTIONS for this DISTRIBUTOR.
   */
  getRequestedTransactions: async (id: number, skip?: number, take?: number): Promise<RequestedTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RequestedTransaction[]>(`/Distributors/${id}/REQUESTEDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONLOADS for this DISTRIBUTOR.
   */
  getTransactionLoads: async (id: number, skip?: number, take?: number): Promise<TransactionLoad[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionLoad[]>(`/Distributors/${id}/TRANSACTIONLOADS${queryString}`);
    return response.data;
  },


};

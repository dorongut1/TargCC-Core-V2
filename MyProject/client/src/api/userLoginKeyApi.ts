// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_UserLoginKey
//     Generated: 2025-12-17 09:34:34
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  UserLoginKey,
  CreateUserLoginKeyRequest,
  UpdateUserLoginKeyRequest,
  UserLoginKeyFilters,
} from '../types/UserLoginKey.types';

type UseUserLoginKeyOptions = UseEntityOptions<UserLoginKeyFilters>;

/**
 * API client for UserLoginKey operations.
 * Base URL: /UserLoginKeys
 */
export const userLoginKeyApi = {
  /**
   * Get UserLoginKey by ID.
   */
  getById: async (id: number): Promise<UserLoginKey> => {
    const response = await api.get<UserLoginKey>(`/UserLoginKeys/${id}`);
    return response.data;
  },


  /**
   * Get all UserLoginKeys with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseUserLoginKeyOptions): Promise<PagedResult<UserLoginKey>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<UserLoginKey>>(`/UserLoginKeys?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new UserLoginKey.
   */
  create: async (data: CreateUserLoginKeyRequest): Promise<UserLoginKey> => {
    const response = await api.post<UserLoginKey>('/UserLoginKeys', data);
    return response.data;
  },


  /**
   * Update existing UserLoginKey.
   */
  update: async (id: number, data: UpdateUserLoginKeyRequest): Promise<UserLoginKey> => {
    const response = await api.put<UserLoginKey>(`/UserLoginKeys/${id}`, data);
    return response.data;
  },


  /**
   * Delete UserLoginKey.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/UserLoginKeys/${id}`);
  },


  /**
   * Get UserLoginKey by CUserID.
   */
  getByCUserID: async (cUserID: number): Promise<UserLoginKey[]> => {
    const response = await api.get<UserLoginKey[]>(`/UserLoginKeys/by-cUserID/${encodeURIComponent(String(cUserID))}`);
    return response.data;
  },


};

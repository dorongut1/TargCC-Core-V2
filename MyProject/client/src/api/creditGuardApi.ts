// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: CreditGuard
//     Generated: 2025-12-15 22:04:09
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  CreditGuard,
  CreateCreditGuardRequest,
  UpdateCreditGuardRequest,
  CreditGuardFilters,
} from '../types/CreditGuard.types';

type UseCreditGuardOptions = UseEntityOptions<CreditGuardFilters>;

/**
 * API client for CreditGuard operations.
 * Base URL: /CreditGuards
 */
export const creditGuardApi = {
  /**
   * Get CreditGuard by ID.
   */
  getById: async (id: number): Promise<CreditGuard> => {
    const response = await api.get<CreditGuard>(`/CreditGuards/${id}`);
    return response.data;
  },


  /**
   * Get all CreditGuards with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseCreditGuardOptions): Promise<PagedResult<CreditGuard>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<CreditGuard>>(`/CreditGuards?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new CreditGuard.
   */
  create: async (data: CreateCreditGuardRequest): Promise<CreditGuard> => {
    const response = await api.post<CreditGuard>('/CreditGuards', data);
    return response.data;
  },


  /**
   * Update existing CreditGuard.
   */
  update: async (id: number, data: UpdateCreditGuardRequest): Promise<CreditGuard> => {
    const response = await api.put<CreditGuard>(`/CreditGuards/${id}`, data);
    return response.data;
  },


  /**
   * Delete CreditGuard.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/CreditGuards/${id}`);
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Limitation
//     Generated: 2025-12-17 14:08:26
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Limitation,
  CreateLimitationRequest,
  UpdateLimitationRequest,
  LimitationFilters,
} from '../types/Limitation.types';

type UseLimitationOptions = UseEntityOptions<LimitationFilters>;
import type { LimitationOverride } from '../types/LimitationOverride.types';

/**
 * API client for Limitation operations.
 * Base URL: /Limitations
 */
export const limitationApi = {
  /**
   * Get Limitation by ID.
   */
  getById: async (id: number): Promise<Limitation> => {
    const response = await api.get<Limitation>(`/Limitations/${id}`);
    return response.data;
  },


  /**
   * Get all Limitations with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseLimitationOptions): Promise<PagedResult<Limitation>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Limitation>>(`/Limitations?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Limitation.
   */
  create: async (data: CreateLimitationRequest): Promise<Limitation> => {
    const response = await api.post<Limitation>('/Limitations', data);
    return response.data;
  },


  /**
   * Update existing Limitation.
   */
  update: async (id: number, data: UpdateLimitationRequest): Promise<Limitation> => {
    const response = await api.put<Limitation>(`/Limitations/${id}`, data);
    return response.data;
  },


  /**
   * Delete Limitation.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Limitations/${id}`);
  },


  /**
   * Get Limitation by EnmLimitedEntityEntity.
   */
  getByEnmLimitedEntityEntity: async (enmLimitedEntityEntity: string): Promise<Limitation[]> => {
    const response = await api.get<Limitation[]>(`/Limitations/by-enmLimitedEntityEntity/${encodeURIComponent(String(enmLimitedEntityEntity))}`);
    return response.data;
  },


  /**
   * Get LIMITATIONOVERRIDES for this LIMITATION.
   */
  getLimitationOverrides: async (id: number, skip?: number, take?: number): Promise<LimitationOverride[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LimitationOverride[]>(`/Limitations/${id}/LIMITATIONOVERRIDES${queryString}`);
    return response.data;
  },


};

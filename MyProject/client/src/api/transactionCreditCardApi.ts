// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: TransactionCreditCard
//     Generated: 2025-12-15 22:18:52
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  TransactionCreditCard,
  CreateTransactionCreditCardRequest,
  UpdateTransactionCreditCardRequest,
  TransactionCreditCardFilters,
} from '../types/TransactionCreditCard.types';

type UseTransactionCreditCardOptions = UseEntityOptions<TransactionCreditCardFilters>;
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';

/**
 * API client for TransactionCreditCard operations.
 * Base URL: /TransactionCreditCards
 */
export const transactionCreditCardApi = {
  /**
   * Get TransactionCreditCard by ID.
   */
  getById: async (id: number): Promise<TransactionCreditCard> => {
    const response = await api.get<TransactionCreditCard>(`/TransactionCreditCards/${id}`);
    return response.data;
  },


  /**
   * Get all TransactionCreditCards with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseTransactionCreditCardOptions): Promise<PagedResult<TransactionCreditCard>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<TransactionCreditCard>>(`/TransactionCreditCards?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new TransactionCreditCard.
   */
  create: async (data: CreateTransactionCreditCardRequest): Promise<TransactionCreditCard> => {
    const response = await api.post<TransactionCreditCard>('/TransactionCreditCards', data);
    return response.data;
  },


  /**
   * Update existing TransactionCreditCard.
   */
  update: async (id: number, data: UpdateTransactionCreditCardRequest): Promise<TransactionCreditCard> => {
    const response = await api.put<TransactionCreditCard>(`/TransactionCreditCards/${id}`, data);
    return response.data;
  },


  /**
   * Delete TransactionCreditCard.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/TransactionCreditCards/${id}`);
  },


  /**
   * Get TransactionCreditCard by RequestedTransactionID.
   */
  getByRequestedTransactionID: async (requestedTransactionID: number): Promise<TransactionCreditCard> => {
    const response = await api.get<TransactionCreditCard>(`/TransactionCreditCards/by-requestedTransactionID/${encodeURIComponent(String(requestedTransactionID))}`);
    return response.data;
  },


  /**
   * Get TransactionCreditCard by UniqueIdentifier.
   */
  getByUniqueIdentifier: async (uniqueIdentifier: string): Promise<TransactionCreditCard> => {
    const response = await api.get<TransactionCreditCard>(`/TransactionCreditCards/by-uniqueIdentifier/${encodeURIComponent(String(uniqueIdentifier))}`);
    return response.data;
  },


  /**
   * Get TransactionCreditCard by CardLast4Numbers.
   */
  getByCardLast4Numbers: async (cardLast4Numbers: string): Promise<TransactionCreditCard[]> => {
    const response = await api.get<TransactionCreditCard[]>(`/TransactionCreditCards/by-cardLast4Numbers/${encodeURIComponent(String(cardLast4Numbers))}`);
    return response.data;
  },


  /**
   * Get TransactionCreditCard by CGResult.
   */
  getByCGResult: async (cGResult: string): Promise<TransactionCreditCard[]> => {
    const response = await api.get<TransactionCreditCard[]>(`/TransactionCreditCards/by-cGResult/${encodeURIComponent(String(cGResult))}`);
    return response.data;
  },


  /**
   * Get TransactionCreditCard by LkpClearingHouse.
   */
  getByLkpClearingHouse: async (lkpClearingHouse: number): Promise<TransactionCreditCard[]> => {
    const response = await api.get<TransactionCreditCard[]>(`/TransactionCreditCards/by-lkpClearingHouse/${encodeURIComponent(String(lkpClearingHouse))}`);
    return response.data;
  },


  /**
   * Get TransactionCreditCard by CustomerCreditCardID.
   */
  getByCustomerCreditCardID: async (customerCreditCardID: number): Promise<TransactionCreditCard[]> => {
    const response = await api.get<TransactionCreditCard[]>(`/TransactionCreditCards/by-customerCreditCardID/${encodeURIComponent(String(customerCreditCardID))}`);
    return response.data;
  },


  /**
   * Get TransactionCreditCard by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<TransactionCreditCard[]> => {
    const response = await api.get<TransactionCreditCard[]>(`/TransactionCreditCards/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get TransactionCreditCard by CustomerWalletID.
   */
  getByCustomerWalletID: async (customerWalletID: number): Promise<TransactionCreditCard[]> => {
    const response = await api.get<TransactionCreditCard[]>(`/TransactionCreditCards/by-customerWalletID/${encodeURIComponent(String(customerWalletID))}`);
    return response.data;
  },


  /**
   * Get TransactionCreditCard by NonCustomerID.
   */
  getByNonCustomerID: async (nonCustomerID: number): Promise<TransactionCreditCard[]> => {
    const response = await api.get<TransactionCreditCard[]>(`/TransactionCreditCards/by-nonCustomerID/${encodeURIComponent(String(nonCustomerID))}`);
    return response.data;
  },


  /**
   * Get TransactionCreditCard by WhenInitialized.
   */
  getByWhenInitialized: async (whenInitialized: Date): Promise<TransactionCreditCard[]> => {
    const response = await api.get<TransactionCreditCard[]>(`/TransactionCreditCards/by-whenInitialized/${encodeURIComponent(String(whenInitialized))}`);
    return response.data;
  },


  /**
   * Get BACKOFFICETRANSACTIONS for this TRANSACTIONCREDITCARD.
   */
  getBackofficeTransactions: async (id: number, skip?: number, take?: number): Promise<BackofficeTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<BackofficeTransaction[]>(`/TransactionCreditCards/${id}/BACKOFFICETRANSACTIONS${queryString}`);
    return response.data;
  },


};

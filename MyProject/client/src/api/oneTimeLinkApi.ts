// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: OneTimeLink
//     Generated: 2025-12-17 07:52:25
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  OneTimeLink,
  CreateOneTimeLinkRequest,
  UpdateOneTimeLinkRequest,
  OneTimeLinkFilters,
} from '../types/OneTimeLink.types';

type UseOneTimeLinkOptions = UseEntityOptions<OneTimeLinkFilters>;

/**
 * API client for OneTimeLink operations.
 * Base URL: /OneTimeLinks
 */
export const oneTimeLinkApi = {
  /**
   * Get OneTimeLink by ID.
   */
  getById: async (id: number): Promise<OneTimeLink> => {
    const response = await api.get<OneTimeLink>(`/OneTimeLinks/${id}`);
    return response.data;
  },


  /**
   * Get all OneTimeLinks with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseOneTimeLinkOptions): Promise<PagedResult<OneTimeLink>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<OneTimeLink>>(`/OneTimeLinks?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new OneTimeLink.
   */
  create: async (data: CreateOneTimeLinkRequest): Promise<OneTimeLink> => {
    const response = await api.post<OneTimeLink>('/OneTimeLinks', data);
    return response.data;
  },


  /**
   * Update existing OneTimeLink.
   */
  update: async (id: number, data: UpdateOneTimeLinkRequest): Promise<OneTimeLink> => {
    const response = await api.put<OneTimeLink>(`/OneTimeLinks/${id}`, data);
    return response.data;
  },


  /**
   * Delete OneTimeLink.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/OneTimeLinks/${id}`);
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Lead
//     Generated: 2025-12-17 21:37:04
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Lead,
  CreateLeadRequest,
  UpdateLeadRequest,
  LeadFilters,
} from '../types/Lead.types';

type UseLeadOptions = UseEntityOptions<LeadFilters>;

/**
 * API client for Lead operations.
 * Base URL: /Leads
 */
export const leadApi = {
  /**
   * Get Lead by ID.
   */
  getById: async (id: number): Promise<Lead> => {
    const response = await api.get<Lead>(`/Leads/${id}`);
    return response.data;
  },


  /**
   * Get all Leads with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseLeadOptions): Promise<PagedResult<Lead>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Lead>>(`/Leads?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Lead.
   */
  create: async (data: CreateLeadRequest): Promise<Lead> => {
    const response = await api.post<Lead>('/Leads', data);
    return response.data;
  },


  /**
   * Update existing Lead.
   */
  update: async (id: number, data: UpdateLeadRequest): Promise<Lead> => {
    const response = await api.put<Lead>(`/Leads/${id}`, data);
    return response.data;
  },


  /**
   * Delete Lead.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Leads/${id}`);
  },


  /**
   * Get Lead by EnmStatus.
   */
  getByEnmStatus: async (enmStatus: string): Promise<Lead[]> => {
    const response = await api.get<Lead[]>(`/Leads/by-enmStatus/${encodeURIComponent(String(enmStatus))}`);
    return response.data;
  },


};

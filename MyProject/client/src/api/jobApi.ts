// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_Job
//     Generated: 2025-12-11 13:48:44
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  Job,
  CreateJobRequest,
  UpdateJobRequest,
  JobFilters,
} from '../types/Job.types';
import type { JobAlertRecipient } from '../types/JobAlertRecipient.types';
import type { LoggedJob } from '../types/LoggedJob.types';

/**
 * API client for Job operations.
 * Base URL: /Jobs
 */
export const jobApi = {
  /**
   * Get Job by ID.
   */
  getById: async (id: number): Promise<Job> => {
    const response = await api.get<Job>(`/Jobs/${id}`);
    return response.data;
  },


  /**
   * Get all Jobs with optional filters.
   */
  getAll: async (filters?: JobFilters): Promise<Job[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/Jobs/filter' : '/Jobs';
    const response = await api.get<Job[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new Job.
   */
  create: async (data: CreateJobRequest): Promise<Job> => {
    const response = await api.post<Job>('/Jobs', data);
    return response.data;
  },


  /**
   * Update existing Job.
   */
  update: async (id: number, data: UpdateJobRequest): Promise<Job> => {
    const response = await api.put<Job>(`/Jobs/${id}`, data);
    return response.data;
  },


  /**
   * Delete Job.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Jobs/${id}`);
  },


  /**
   * Get Job by Active.
   */
  getByActive: async (active: boolean): Promise<Job[]> => {
    const response = await api.get<Job[]>(`/Jobs/by-active/${encodeURIComponent(String(active))}`);
    return response.data;
  },


  /**
   * Get Job by LkpJob.
   */
  getByLkpJob: async (lkpJob: string): Promise<Job[]> => {
    const response = await api.get<Job[]>(`/Jobs/by-lkpJob/${encodeURIComponent(String(lkpJob))}`);
    return response.data;
  },


  /**
   * Get Job by LkpJobRunner.
   */
  getByLkpJobRunner: async (lkpJobRunner: string): Promise<Job[]> => {
    const response = await api.get<Job[]>(`/Jobs/by-lkpJobRunner/${encodeURIComponent(String(lkpJobRunner))}`);
    return response.data;
  },


  /**
   * Get JOBALERTRECIPIENTS for this JOB.
   */
  getJobAlertRecipients: async (id: number, skip?: number, take?: number): Promise<JobAlertRecipient[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<JobAlertRecipient[]>(`/Jobs/${id}/JOBALERTRECIPIENTS${queryString}`);
    return response.data;
  },


  /**
   * Get LOGGEDJOBS for this JOB.
   */
  getLoggedJobs: async (id: number, skip?: number, take?: number): Promise<LoggedJob[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LoggedJob[]>(`/Jobs/${id}/LOGGEDJOBS${queryString}`);
    return response.data;
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: CustomerCreditCard
//     Generated: 2025-12-15 13:48:32
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type {
  CustomerCreditCard,
  CreateCustomerCreditCardRequest,
  UpdateCustomerCreditCardRequest,
  CustomerCreditCardFilters,
} from '../types/CustomerCreditCard.types';
import type { RequestedTransaction } from '../types/RequestedTransaction.types';
import type { TransactionCreditCard } from '../types/TransactionCreditCard.types';

/**
 * API client for CustomerCreditCard operations.
 * Base URL: /CustomerCreditCards
 */
export const customerCreditCardApi = {
  /**
   * Get CustomerCreditCard by ID.
   */
  getById: async (id: number): Promise<CustomerCreditCard> => {
    const response = await api.get<CustomerCreditCard>(`/CustomerCreditCards/${id}`);
    return response.data;
  },


  /**
   * Get all CustomerCreditCards with optional filters.
   */
  getAll: async (filters?: CustomerCreditCardFilters): Promise<CustomerCreditCard[]> => {
    // Use /filter endpoint when filters are provided
    const endpoint = filters && Object.keys(filters).length > 0 ? '/CustomerCreditCards/filter' : '/CustomerCreditCards';
    const response = await api.get<CustomerCreditCard[]>(endpoint, {
      params: filters,
    });
    return response.data;
  },


  /**
   * Create new CustomerCreditCard.
   */
  create: async (data: CreateCustomerCreditCardRequest): Promise<CustomerCreditCard> => {
    const response = await api.post<CustomerCreditCard>('/CustomerCreditCards', data);
    return response.data;
  },


  /**
   * Update existing CustomerCreditCard.
   */
  update: async (id: number, data: UpdateCustomerCreditCardRequest): Promise<CustomerCreditCard> => {
    const response = await api.put<CustomerCreditCard>(`/CustomerCreditCards/${id}`, data);
    return response.data;
  },


  /**
   * Delete CustomerCreditCard.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/CustomerCreditCards/${id}`);
  },


  /**
   * Get CustomerCreditCard by CardToken.
   */
  getByCardToken: async (cardToken: string): Promise<CustomerCreditCard> => {
    const response = await api.get<CustomerCreditCard>(`/CustomerCreditCards/by-cardToken/${encodeURIComponent(String(cardToken))}`);
    return response.data;
  },


  /**
   * Get CustomerCreditCard by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<CustomerCreditCard[]> => {
    const response = await api.get<CustomerCreditCard[]>(`/CustomerCreditCards/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get REQUESTEDTRANSACTIONS for this CUSTOMERCREDITCARD.
   */
  getRequestedTransactions: async (id: number, skip?: number, take?: number): Promise<RequestedTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RequestedTransaction[]>(`/CustomerCreditCards/${id}/REQUESTEDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONCREDITCARDS for this CUSTOMERCREDITCARD.
   */
  getTransactionCreditCards: async (id: number, skip?: number, take?: number): Promise<TransactionCreditCard[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionCreditCard[]>(`/CustomerCreditCards/${id}/TRANSACTIONCREDITCARDS${queryString}`);
    return response.data;
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: UIText
//     Generated: 2025-12-16 08:57:51
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  UIText,
  CreateUITextRequest,
  UpdateUITextRequest,
  UITextFilters,
} from '../types/UIText.types';

type UseUITextOptions = UseEntityOptions<UITextFilters>;

/**
 * API client for UIText operations.
 * Base URL: /UITexts
 */
export const uITextApi = {
  /**
   * Get UIText by ID.
   */
  getById: async (id: number): Promise<UIText> => {
    const response = await api.get<UIText>(`/UITexts/${id}`);
    return response.data;
  },


  /**
   * Get all UITexts with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseUITextOptions): Promise<PagedResult<UIText>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<UIText>>(`/UITexts?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new UIText.
   */
  create: async (data: CreateUITextRequest): Promise<UIText> => {
    const response = await api.post<UIText>('/UITexts', data);
    return response.data;
  },


  /**
   * Update existing UIText.
   */
  update: async (id: number, data: UpdateUITextRequest): Promise<UIText> => {
    const response = await api.put<UIText>(`/UITexts/${id}`, data);
    return response.data;
  },


  /**
   * Delete UIText.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/UITexts/${id}`);
  },


  /**
   * Get UIText by Page.
   */
  getByPage: async (page: string): Promise<UIText[]> => {
    const response = await api.get<UIText[]>(`/UITexts/by-page/${encodeURIComponent(String(page))}`);
    return response.data;
  },


};

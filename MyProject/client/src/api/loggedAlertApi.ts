// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_LoggedAlert
//     Generated: 2025-12-16 10:13:17
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  LoggedAlert,
  CreateLoggedAlertRequest,
  UpdateLoggedAlertRequest,
  LoggedAlertFilters,
} from '../types/LoggedAlert.types';

type UseLoggedAlertOptions = UseEntityOptions<LoggedAlertFilters>;
import type { LoggedJob } from '../types/LoggedJob.types';
import type { CardOrderFromIssuer } from '../types/CardOrderFromIssuer.types';
import type { ImportTransactionLoadRow } from '../types/ImportTransactionLoadRow.types';
import type { NewCardRequest } from '../types/NewCardRequest.types';
import type { NewCardRequestAPI } from '../types/NewCardRequestAPI.types';
import type { RequestedTransaction } from '../types/RequestedTransaction.types';
import type { TransactionLoad } from '../types/TransactionLoad.types';

/**
 * API client for LoggedAlert operations.
 * Base URL: /LoggedAlerts
 */
export const loggedAlertApi = {
  /**
   * Get LoggedAlert by ID.
   */
  getById: async (id: number): Promise<LoggedAlert> => {
    const response = await api.get<LoggedAlert>(`/LoggedAlerts/${id}`);
    return response.data;
  },


  /**
   * Get all LoggedAlerts with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseLoggedAlertOptions): Promise<PagedResult<LoggedAlert>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<LoggedAlert>>(`/LoggedAlerts?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new LoggedAlert.
   */
  create: async (data: CreateLoggedAlertRequest): Promise<LoggedAlert> => {
    const response = await api.post<LoggedAlert>('/LoggedAlerts', data);
    return response.data;
  },


  /**
   * Update existing LoggedAlert.
   */
  update: async (id: number, data: UpdateLoggedAlertRequest): Promise<LoggedAlert> => {
    const response = await api.put<LoggedAlert>(`/LoggedAlerts/${id}`, data);
    return response.data;
  },


  /**
   * Delete LoggedAlert.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/LoggedAlerts/${id}`);
  },


  /**
   * Get LoggedAlert by CallingApplication.
   */
  getByCallingApplication: async (callingApplication: string): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-callingApplication/${encodeURIComponent(String(callingApplication))}`);
    return response.data;
  },


  /**
   * Get LoggedAlert by DateOccurred.
   */
  getByDateOccurred: async (dateOccurred: Date): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-dateOccurred/${encodeURIComponent(String(dateOccurred))}`);
    return response.data;
  },


  /**
   * Get LoggedAlert by FaultNumber.
   */
  getByFaultNumber: async (faultNumber: number): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-faultNumber/${encodeURIComponent(String(faultNumber))}`);
    return response.data;
  },


  /**
   * Get LoggedAlert by EnmFaultSeverityFaultSeverity.
   */
  getByEnmFaultSeverityFaultSeverity: async (enmFaultSeverityFaultSeverity: string): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-enmFaultSeverityFaultSeverity/${encodeURIComponent(String(enmFaultSeverityFaultSeverity))}`);
    return response.data;
  },


  /**
   * Get LoggedAlert by EnmFaultTypeFaultType.
   */
  getByEnmFaultTypeFaultType: async (enmFaultTypeFaultType: string): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-enmFaultTypeFaultType/${encodeURIComponent(String(enmFaultTypeFaultType))}`);
    return response.data;
  },


  /**
   * Get LoggedAlert by CLoggedLoginID.
   */
  getByCLoggedLoginID: async (cLoggedLoginID: number): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-cLoggedLoginID/${encodeURIComponent(String(cLoggedLoginID))}`);
    return response.data;
  },


  /**
   * Get LoggedAlert by MonthOccurred.
   */
  getByMonthOccurred: async (monthOccurred: Date): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-monthOccurred/${encodeURIComponent(String(monthOccurred))}`);
    return response.data;
  },


  /**
   * Get LoggedAlert by SystemName.
   */
  getBySystemName: async (systemName: string): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-systemName/${encodeURIComponent(String(systemName))}`);
    return response.data;
  },


  /**
   * Get LoggedAlert by TimeOccurred.
   */
  getByTimeOccurred: async (timeOccurred: Date): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-timeOccurred/${encodeURIComponent(String(timeOccurred))}`);
    return response.data;
  },


  /**
   * Get LoggedAlert by AffectedUserID.
   */
  getByAffectedUserID: async (affectedUserID: number): Promise<LoggedAlert[]> => {
    const response = await api.get<LoggedAlert[]>(`/LoggedAlerts/by-affectedUserID/${encodeURIComponent(String(affectedUserID))}`);
    return response.data;
  },


  /**
   * Get LOGGEDJOBS for this LOGGEDALERT.
   */
  getLoggedJobs: async (id: number, skip?: number, take?: number): Promise<LoggedJob[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<LoggedJob[]>(`/LoggedAlerts/${id}/LOGGEDJOBS${queryString}`);
    return response.data;
  },


  /**
   * Get CARDORDERFROMISSUERS for this LOGGEDALERT.
   */
  getCardOrderFromIssuers: async (id: number, skip?: number, take?: number): Promise<CardOrderFromIssuer[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<CardOrderFromIssuer[]>(`/LoggedAlerts/${id}/CARDORDERFROMISSUERS${queryString}`);
    return response.data;
  },


  /**
   * Get IMPORTTRANSACTIONLOADROWS for this LOGGEDALERT.
   */
  getImportTransactionLoadRows: async (id: number, skip?: number, take?: number): Promise<ImportTransactionLoadRow[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<ImportTransactionLoadRow[]>(`/LoggedAlerts/${id}/IMPORTTRANSACTIONLOADROWS${queryString}`);
    return response.data;
  },


  /**
   * Get NEWCARDREQUESTS for this LOGGEDALERT.
   */
  getNewCardRequests: async (id: number, skip?: number, take?: number): Promise<NewCardRequest[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<NewCardRequest[]>(`/LoggedAlerts/${id}/NEWCARDREQUESTS${queryString}`);
    return response.data;
  },


  /**
   * Get NEWCARDREQUESTAPIS for this LOGGEDALERT.
   */
  getNewCardRequestAPIs: async (id: number, skip?: number, take?: number): Promise<NewCardRequestAPI[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<NewCardRequestAPI[]>(`/LoggedAlerts/${id}/NEWCARDREQUESTAPIS${queryString}`);
    return response.data;
  },


  /**
   * Get REQUESTEDTRANSACTIONS for this LOGGEDALERT.
   */
  getRequestedTransactions: async (id: number, skip?: number, take?: number): Promise<RequestedTransaction[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<RequestedTransaction[]>(`/LoggedAlerts/${id}/REQUESTEDTRANSACTIONS${queryString}`);
    return response.data;
  },


  /**
   * Get TRANSACTIONLOADS for this LOGGEDALERT.
   */
  getTransactionLoads: async (id: number, skip?: number, take?: number): Promise<TransactionLoad[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<TransactionLoad[]>(`/LoggedAlerts/${id}/TRANSACTIONLOADS${queryString}`);
    return response.data;
  },


};

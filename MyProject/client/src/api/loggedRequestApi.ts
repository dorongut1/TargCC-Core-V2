// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_LoggedRequest
//     Generated: 2025-12-17 21:11:12
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  LoggedRequest,
  CreateLoggedRequestRequest,
  UpdateLoggedRequestRequest,
  LoggedRequestFilters,
} from '../types/LoggedRequest.types';

type UseLoggedRequestOptions = UseEntityOptions<LoggedRequestFilters>;

/**
 * API client for LoggedRequest operations.
 * Base URL: /LoggedRequests
 */
export const loggedRequestApi = {
  /**
   * Get LoggedRequest by ID.
   */
  getById: async (id: number): Promise<LoggedRequest> => {
    const response = await api.get<LoggedRequest>(`/LoggedRequests/${id}`);
    return response.data;
  },


  /**
   * Get all LoggedRequests with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseLoggedRequestOptions): Promise<PagedResult<LoggedRequest>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<LoggedRequest>>(`/LoggedRequests?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new LoggedRequest.
   */
  create: async (data: CreateLoggedRequestRequest): Promise<LoggedRequest> => {
    const response = await api.post<LoggedRequest>('/LoggedRequests', data);
    return response.data;
  },


  /**
   * Update existing LoggedRequest.
   */
  update: async (id: number, data: UpdateLoggedRequestRequest): Promise<LoggedRequest> => {
    const response = await api.put<LoggedRequest>(`/LoggedRequests/${id}`, data);
    return response.data;
  },


  /**
   * Delete LoggedRequest.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/LoggedRequests/${id}`);
  },


  /**
   * Get LoggedRequest by CLoggedLoginID.
   */
  getByCLoggedLoginID: async (cLoggedLoginID: number): Promise<LoggedRequest[]> => {
    const response = await api.get<LoggedRequest[]>(`/LoggedRequests/by-cLoggedLoginID/${encodeURIComponent(String(cLoggedLoginID))}`);
    return response.data;
  },


  /**
   * Get LoggedRequest by CUserID.
   */
  getByCUserID: async (cUserID: number): Promise<LoggedRequest[]> => {
    const response = await api.get<LoggedRequest[]>(`/LoggedRequests/by-cUserID/${encodeURIComponent(String(cUserID))}`);
    return response.data;
  },


};

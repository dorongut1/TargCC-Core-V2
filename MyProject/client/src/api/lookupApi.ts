// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_Lookup
//     Generated: 2025-12-17 20:20:28
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Lookup,
  CreateLookupRequest,
  UpdateLookupRequest,
  LookupFilters,
} from '../types/Lookup.types';

type UseLookupOptions = UseEntityOptions<LookupFilters>;

/**
 * API client for Lookup operations.
 * Base URL: /Lookups
 */
export const lookupApi = {
  /**
   * Get Lookup by ID.
   */
  getById: async (id: number): Promise<Lookup> => {
    const response = await api.get<Lookup>(`/Lookups/${id}`);
    return response.data;
  },


  /**
   * Get all Lookups with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseLookupOptions): Promise<PagedResult<Lookup>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Lookup>>(`/Lookups?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Lookup.
   */
  create: async (data: CreateLookupRequest): Promise<Lookup> => {
    const response = await api.post<Lookup>('/Lookups', data);
    return response.data;
  },


  /**
   * Update existing Lookup.
   */
  update: async (id: number, data: UpdateLookupRequest): Promise<Lookup> => {
    const response = await api.put<Lookup>(`/Lookups/${id}`, data);
    return response.data;
  },


  /**
   * Delete Lookup.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Lookups/${id}`);
  },


  /**
   * Get Lookup by EnmLookupTypeLookup.
   */
  getByEnmLookupTypeLookup: async (enmLookupTypeLookup: string): Promise<Lookup[]> => {
    const response = await api.get<Lookup[]>(`/Lookups/by-enmLookupTypeLookup/${encodeURIComponent(String(enmLookupTypeLookup))}`);
    return response.data;
  },


};

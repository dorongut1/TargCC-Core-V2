// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: Country
//     Generated: 2025-12-15 22:04:09
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { countryApi } from '../api/countryApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateCountryRequest, UpdateCountryRequest, CountryFilters } from '../types/Country.types';

type UseCountryOptions = UseEntityOptions<CountryFilters>;

/**
 * Hook to fetch single Country by ID.
 */
export const useCountry = (id: number | null) => {
  return useQuery({
    queryKey: ['country', id],
    queryFn: () => countryApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Countrys with server-side filtering, sorting, and pagination.
 */
export const useCountrys = (options?: UseCountryOptions) => {
  return useQuery({
    queryKey: ['countrys', options],
    queryFn: () => countryApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch mCCBParticipants for a specific COUNTRY.
 */
export const useCountryMCCBParticipants = (
  countryId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['country', countryId, 'mCCBParticipants', skip, take],
    queryFn: () => countryApi.getMCCBParticipants(countryId!, skip, take),
    enabled: countryId !== null,
  });
};

/**
 * Hook to fetch mCCBReceivers for a specific COUNTRY.
 */
export const useCountryMCCBReceivers = (
  countryId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['country', countryId, 'mCCBReceivers', skip, take],
    queryFn: () => countryApi.getMCCBReceivers(countryId!, skip, take),
    enabled: countryId !== null,
  });
};

/**
 * Hook to fetch mCCBSecurityChecks for a specific COUNTRY.
 */
export const useCountryMCCBSecurityChecks = (
  countryId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['country', countryId, 'mCCBSecurityChecks', skip, take],
    queryFn: () => countryApi.getMCCBSecurityChecks(countryId!, skip, take),
    enabled: countryId !== null,
  });
};

/**
 * Hook to create Country.
 */
export const useCreateCountry = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateCountryRequest) => countryApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['countrys'] });
    },
  });
};


/**
 * Hook to update Country.
 */
export const useUpdateCountry = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateCountryRequest }) =>
      countryApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['country', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['countrys'] });
    },
  });
};


/**
 * Hook to delete Country.
 */
export const useDeleteCountry = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => countryApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['countrys'] });
    },
  });
};



// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: CardIssuer
//     Generated: 2025-12-16 08:57:46
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { cardIssuerApi } from '../api/cardIssuerApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateCardIssuerRequest, UpdateCardIssuerRequest, CardIssuerFilters } from '../types/CardIssuer.types';

type UseCardIssuerOptions = UseEntityOptions<CardIssuerFilters>;

/**
 * Hook to fetch single CardIssuer by ID.
 */
export const useCardIssuer = (id: number | null) => {
  return useQuery({
    queryKey: ['cardIssuer', id],
    queryFn: () => cardIssuerApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all CardIssuers with server-side filtering, sorting, and pagination.
 */
export const useCardIssuers = (options?: UseCardIssuerOptions) => {
  return useQuery({
    queryKey: ['cardIssuers', options],
    queryFn: () => cardIssuerApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to create CardIssuer.
 */
export const useCreateCardIssuer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateCardIssuerRequest) => cardIssuerApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cardIssuers'] });
    },
  });
};


/**
 * Hook to update CardIssuer.
 */
export const useUpdateCardIssuer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateCardIssuerRequest }) =>
      cardIssuerApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['cardIssuer', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['cardIssuers'] });
    },
  });
};


/**
 * Hook to delete CardIssuer.
 */
export const useDeleteCardIssuer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => cardIssuerApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cardIssuers'] });
    },
  });
};



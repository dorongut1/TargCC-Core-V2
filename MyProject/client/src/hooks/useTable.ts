// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: c_Table
//     Generated: 2025-12-15 22:55:55
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { tableApi } from '../api/tableApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateTableRequest, UpdateTableRequest, TableFilters } from '../types/Table.types';

type UseTableOptions = UseEntityOptions<TableFilters>;

/**
 * Hook to fetch single Table by ID.
 */
export const useTable = (id: number | null) => {
  return useQuery({
    queryKey: ['table', id],
    queryFn: () => tableApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Tables with server-side filtering, sorting, and pagination.
 */
export const useTables = (options?: UseTableOptions) => {
  return useQuery({
    queryKey: ['tables', options],
    queryFn: () => tableApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to create Table.
 */
export const useCreateTable = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateTableRequest) => tableApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['tables'] });
    },
  });
};


/**
 * Hook to update Table.
 */
export const useUpdateTable = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateTableRequest }) =>
      tableApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['table', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['tables'] });
    },
  });
};


/**
 * Hook to delete Table.
 */
export const useDeleteTable = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => tableApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['tables'] });
    },
  });
};



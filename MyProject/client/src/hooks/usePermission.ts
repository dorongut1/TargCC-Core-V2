// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: c_Permission
//     Generated: 2025-12-18 07:57:40
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { permissionApi } from '../api/permissionApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreatePermissionRequest, UpdatePermissionRequest, PermissionFilters } from '../types/Permission.types';

type UsePermissionOptions = UseEntityOptions<PermissionFilters>;

/**
 * Hook to fetch single Permission by ID.
 */
export const usePermission = (id: number | null) => {
  return useQuery({
    queryKey: ['permission', id],
    queryFn: () => permissionApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Permissions with server-side filtering, sorting, and pagination.
 */
export const usePermissions = (options?: UsePermissionOptions) => {
  return useQuery({
    queryKey: ['permissions', options],
    queryFn: () => permissionApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to create Permission.
 */
export const useCreatePermission = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreatePermissionRequest) => permissionApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['permissions'] });
    },
  });
};


/**
 * Hook to update Permission.
 */
export const useUpdatePermission = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdatePermissionRequest }) =>
      permissionApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['permission', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['permissions'] });
    },
  });
};


/**
 * Hook to delete Permission.
 */
export const useDeletePermission = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => permissionApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['permissions'] });
    },
  });
};



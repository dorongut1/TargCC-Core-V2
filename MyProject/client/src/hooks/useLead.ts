// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: Lead
//     Generated: 2025-12-15 22:55:58
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { leadApi } from '../api/leadApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateLeadRequest, UpdateLeadRequest, LeadFilters } from '../types/Lead.types';

type UseLeadOptions = UseEntityOptions<LeadFilters>;

/**
 * Hook to fetch single Lead by ID.
 */
export const useLead = (id: number | null) => {
  return useQuery({
    queryKey: ['lead', id],
    queryFn: () => leadApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Leads with server-side filtering, sorting, and pagination.
 */
export const useLeads = (options?: UseLeadOptions) => {
  return useQuery({
    queryKey: ['leads', options],
    queryFn: () => leadApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to create Lead.
 */
export const useCreateLead = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateLeadRequest) => leadApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['leads'] });
    },
  });
};


/**
 * Hook to update Lead.
 */
export const useUpdateLead = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateLeadRequest }) =>
      leadApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['lead', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['leads'] });
    },
  });
};


/**
 * Hook to delete Lead.
 */
export const useDeleteLead = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => leadApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['leads'] });
    },
  });
};



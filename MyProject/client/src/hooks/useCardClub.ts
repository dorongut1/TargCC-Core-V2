// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: CardClub
//     Generated: 2025-12-16 18:22:27
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { cardClubApi } from '../api/cardClubApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateCardClubRequest, UpdateCardClubRequest, CardClubFilters } from '../types/CardClub.types';

type UseCardClubOptions = UseEntityOptions<CardClubFilters>;

/**
 * Hook to fetch single CardClub by ID.
 */
export const useCardClub = (id: number | null) => {
  return useQuery({
    queryKey: ['cardClub', id],
    queryFn: () => cardClubApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all CardClubs with server-side filtering, sorting, and pagination.
 */
export const useCardClubs = (options?: UseCardClubOptions) => {
  return useQuery({
    queryKey: ['cardClubs', options],
    queryFn: () => cardClubApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch cards for a specific CARDCLUB.
 */
export const useCardClubCards = (
  cardClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardClub', cardClubId, 'cards', skip, take],
    queryFn: () => cardClubApi.getCards(cardClubId!, skip, take),
    enabled: cardClubId !== null,
  });
};

/**
 * Hook to fetch cardActivations for a specific CARDCLUB.
 */
export const useCardClubCardActivations = (
  cardClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardClub', cardClubId, 'cardActivations', skip, take],
    queryFn: () => cardClubApi.getCardActivations(cardClubId!, skip, take),
    enabled: cardClubId !== null,
  });
};

/**
 * Hook to fetch cardOrderByDistributors for a specific CARDCLUB.
 */
export const useCardClubCardOrderByDistributors = (
  cardClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardClub', cardClubId, 'cardOrderByDistributors', skip, take],
    queryFn: () => cardClubApi.getCardOrderByDistributors(cardClubId!, skip, take),
    enabled: cardClubId !== null,
  });
};

/**
 * Hook to fetch cardOrderFromIssuers for a specific CARDCLUB.
 */
export const useCardClubCardOrderFromIssuers = (
  cardClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardClub', cardClubId, 'cardOrderFromIssuers', skip, take],
    queryFn: () => cardClubApi.getCardOrderFromIssuers(cardClubId!, skip, take),
    enabled: cardClubId !== null,
  });
};

/**
 * Hook to fetch customerCardTransactions for a specific CARDCLUB.
 */
export const useCardClubCustomerCardTransactions = (
  cardClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardClub', cardClubId, 'customerCardTransactions', skip, take],
    queryFn: () => cardClubApi.getCustomerCardTransactions(cardClubId!, skip, take),
    enabled: cardClubId !== null,
  });
};

/**
 * Hook to fetch feeOverrides for a specific CARDCLUB.
 */
export const useCardClubFeeOverrides = (
  cardClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardClub', cardClubId, 'feeOverrides', skip, take],
    queryFn: () => cardClubApi.getFeeOverrides(cardClubId!, skip, take),
    enabled: cardClubId !== null,
  });
};

/**
 * Hook to fetch limitationOverrides for a specific CARDCLUB.
 */
export const useCardClubLimitationOverrides = (
  cardClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardClub', cardClubId, 'limitationOverrides', skip, take],
    queryFn: () => cardClubApi.getLimitationOverrides(cardClubId!, skip, take),
    enabled: cardClubId !== null,
  });
};

/**
 * Hook to fetch newCardRequests for a specific CARDCLUB.
 */
export const useCardClubNewCardRequests = (
  cardClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardClub', cardClubId, 'newCardRequests', skip, take],
    queryFn: () => cardClubApi.getNewCardRequests(cardClubId!, skip, take),
    enabled: cardClubId !== null,
  });
};

/**
 * Hook to create CardClub.
 */
export const useCreateCardClub = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateCardClubRequest) => cardClubApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cardClubs'] });
    },
  });
};


/**
 * Hook to update CardClub.
 */
export const useUpdateCardClub = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateCardClubRequest }) =>
      cardClubApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['cardClub', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['cardClubs'] });
    },
  });
};


/**
 * Hook to delete CardClub.
 */
export const useDeleteCardClub = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => cardClubApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cardClubs'] });
    },
  });
};



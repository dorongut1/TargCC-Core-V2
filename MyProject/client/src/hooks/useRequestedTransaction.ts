// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: RequestedTransaction
//     Generated: 2025-12-17 09:34:38
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { requestedTransactionApi } from '../api/requestedTransactionApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateRequestedTransactionRequest, UpdateRequestedTransactionRequest, RequestedTransactionFilters } from '../types/RequestedTransaction.types';

type UseRequestedTransactionOptions = UseEntityOptions<RequestedTransactionFilters>;

/**
 * Hook to fetch single RequestedTransaction by ID.
 */
export const useRequestedTransaction = (id: number | null) => {
  return useQuery({
    queryKey: ['requestedTransaction', id],
    queryFn: () => requestedTransactionApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all RequestedTransactions with server-side filtering, sorting, and pagination.
 */
export const useRequestedTransactions = (options?: UseRequestedTransactionOptions) => {
  return useQuery({
    queryKey: ['requestedTransactions', options],
    queryFn: () => requestedTransactionApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch backofficeTransactions for a specific REQUESTEDTRANSACTION.
 */
export const useRequestedTransactionBackofficeTransactions = (
  requestedTransactionId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['requestedTransaction', requestedTransactionId, 'backofficeTransactions', skip, take],
    queryFn: () => requestedTransactionApi.getBackofficeTransactions(requestedTransactionId!, skip, take),
    enabled: requestedTransactionId !== null,
  });
};

/**
 * Hook to fetch customerWallets for a specific REQUESTEDTRANSACTION.
 */
export const useRequestedTransactionCustomerWallets = (
  requestedTransactionId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['requestedTransaction', requestedTransactionId, 'customerWallets', skip, take],
    queryFn: () => requestedTransactionApi.getCustomerWallets(requestedTransactionId!, skip, take),
    enabled: requestedTransactionId !== null,
  });
};

/**
 * Hook to fetch drivebotPayments for a specific REQUESTEDTRANSACTION.
 */
export const useRequestedTransactionDrivebotPayments = (
  requestedTransactionId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['requestedTransaction', requestedTransactionId, 'drivebotPayments', skip, take],
    queryFn: () => requestedTransactionApi.getDrivebotPayments(requestedTransactionId!, skip, take),
    enabled: requestedTransactionId !== null,
  });
};

/**
 * Hook to fetch mCCBSecurityChecks for a specific REQUESTEDTRANSACTION.
 */
export const useRequestedTransactionMCCBSecurityChecks = (
  requestedTransactionId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['requestedTransaction', requestedTransactionId, 'mCCBSecurityChecks', skip, take],
    queryFn: () => requestedTransactionApi.getMCCBSecurityChecks(requestedTransactionId!, skip, take),
    enabled: requestedTransactionId !== null,
  });
};

/**
 * Hook to fetch mCCBTransactions for a specific REQUESTEDTRANSACTION.
 */
export const useRequestedTransactionMCCBTransactions = (
  requestedTransactionId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['requestedTransaction', requestedTransactionId, 'mCCBTransactions', skip, take],
    queryFn: () => requestedTransactionApi.getMCCBTransactions(requestedTransactionId!, skip, take),
    enabled: requestedTransactionId !== null,
  });
};

/**
 * Hook to fetch requestedTransactionFees for a specific REQUESTEDTRANSACTION.
 */
export const useRequestedTransactionRequestedTransactionFees = (
  requestedTransactionId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['requestedTransaction', requestedTransactionId, 'requestedTransactionFees', skip, take],
    queryFn: () => requestedTransactionApi.getRequestedTransactionFees(requestedTransactionId!, skip, take),
    enabled: requestedTransactionId !== null,
  });
};

/**
 * Hook to fetch transactionBits for a specific REQUESTEDTRANSACTION.
 */
export const useRequestedTransactionTransactionBits = (
  requestedTransactionId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['requestedTransaction', requestedTransactionId, 'transactionBits', skip, take],
    queryFn: () => requestedTransactionApi.getTransactionBits(requestedTransactionId!, skip, take),
    enabled: requestedTransactionId !== null,
  });
};

/**
 * Hook to fetch transactionCreditCards for a specific REQUESTEDTRANSACTION.
 */
export const useRequestedTransactionTransactionCreditCards = (
  requestedTransactionId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['requestedTransaction', requestedTransactionId, 'transactionCreditCards', skip, take],
    queryFn: () => requestedTransactionApi.getTransactionCreditCards(requestedTransactionId!, skip, take),
    enabled: requestedTransactionId !== null,
  });
};

/**
 * Hook to fetch transactionLoads for a specific REQUESTEDTRANSACTION.
 */
export const useRequestedTransactionTransactionLoads = (
  requestedTransactionId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['requestedTransaction', requestedTransactionId, 'transactionLoads', skip, take],
    queryFn: () => requestedTransactionApi.getTransactionLoads(requestedTransactionId!, skip, take),
    enabled: requestedTransactionId !== null,
  });
};

/**
 * Hook to create RequestedTransaction.
 */
export const useCreateRequestedTransaction = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateRequestedTransactionRequest) => requestedTransactionApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['requestedTransactions'] });
    },
  });
};


/**
 * Hook to update RequestedTransaction.
 */
export const useUpdateRequestedTransaction = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateRequestedTransactionRequest }) =>
      requestedTransactionApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['requestedTransaction', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['requestedTransactions'] });
    },
  });
};


/**
 * Hook to delete RequestedTransaction.
 */
export const useDeleteRequestedTransaction = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => requestedTransactionApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['requestedTransactions'] });
    },
  });
};



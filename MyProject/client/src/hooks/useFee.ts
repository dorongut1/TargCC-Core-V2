// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: Fee
//     Generated: 2025-12-11 21:35:46
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { feeApi } from '../api/feeApi';
import type {
  Fee,
  CreateFeeRequest,
  UpdateFeeRequest,
  FeeFilters,
} from '../types/Fee.types';
import type { FeeOverride } from '../types/FeeOverride.types';

/**
 * Hook to fetch single Fee by ID.
 */
export const useFee = (id: number | null) => {
  return useQuery({
    queryKey: ['fee', id],
    queryFn: () => feeApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Fees with optional filters.
 */
export const useFees = (filters?: FeeFilters) => {
  return useQuery({
    queryKey: ['fees', filters],
    queryFn: () => feeApi.getAll(filters),
  });
};


/**
 * Hook to fetch feeOverrides for a specific FEE.
 */
export const useFeeFeeOverrides = (
  feeId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['fee', feeId, 'feeOverrides', skip, take],
    queryFn: () => feeApi.getFeeOverrides(feeId!, skip, take),
    enabled: feeId !== null,
  });
};

/**
 * Hook to create Fee.
 */
export const useCreateFee = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateFeeRequest) => feeApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['fees'] });
    },
  });
};


/**
 * Hook to update Fee.
 */
export const useUpdateFee = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateFeeRequest }) =>
      feeApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['fee', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['fees'] });
    },
  });
};


/**
 * Hook to delete Fee.
 */
export const useDeleteFee = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => feeApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['fees'] });
    },
  });
};



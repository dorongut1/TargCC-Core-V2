// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: c_Lookup
//     Generated: 2025-12-17 21:32:11
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { lookupApi } from '../api/lookupApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateLookupRequest, UpdateLookupRequest, LookupFilters } from '../types/Lookup.types';

type UseLookupOptions = UseEntityOptions<LookupFilters>;

/**
 * Hook to fetch single Lookup by ID.
 */
export const useLookup = (id: number | null) => {
  return useQuery({
    queryKey: ['lookup', id],
    queryFn: () => lookupApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Lookups with server-side filtering, sorting, and pagination.
 */
export const useLookups = (options?: UseLookupOptions) => {
  return useQuery({
    queryKey: ['lookups', options],
    queryFn: () => lookupApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to create Lookup.
 */
export const useCreateLookup = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateLookupRequest) => lookupApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['lookups'] });
    },
  });
};


/**
 * Hook to update Lookup.
 */
export const useUpdateLookup = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateLookupRequest }) =>
      lookupApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['lookup', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['lookups'] });
    },
  });
};


/**
 * Hook to delete Lookup.
 */
export const useDeleteLookup = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => lookupApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['lookups'] });
    },
  });
};



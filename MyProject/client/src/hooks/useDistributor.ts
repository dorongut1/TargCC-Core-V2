// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: Distributor
//     Generated: 2025-12-17 21:45:23
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { distributorApi } from '../api/distributorApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateDistributorRequest, UpdateDistributorRequest, DistributorFilters } from '../types/Distributor.types';

type UseDistributorOptions = UseEntityOptions<DistributorFilters>;

/**
 * Hook to fetch single Distributor by ID.
 */
export const useDistributor = (id: number | null) => {
  return useQuery({
    queryKey: ['distributor', id],
    queryFn: () => distributorApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Distributors with server-side filtering, sorting, and pagination.
 */
export const useDistributors = (options?: UseDistributorOptions) => {
  return useQuery({
    queryKey: ['distributors', options],
    queryFn: () => distributorApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch backofficeTransactions for a specific DISTRIBUTOR.
 */
export const useDistributorBackofficeTransactions = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'backofficeTransactions', skip, take],
    queryFn: () => distributorApi.getBackofficeTransactions(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch cards for a specific DISTRIBUTOR.
 */
export const useDistributorCards = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'cards', skip, take],
    queryFn: () => distributorApi.getCards(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch cardActivations for a specific DISTRIBUTOR.
 */
export const useDistributorCardActivations = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'cardActivations', skip, take],
    queryFn: () => distributorApi.getCardActivations(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch cardOrderByDistributors for a specific DISTRIBUTOR.
 */
export const useDistributorCardOrderByDistributors = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'cardOrderByDistributors', skip, take],
    queryFn: () => distributorApi.getCardOrderByDistributors(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch customers for a specific DISTRIBUTOR.
 */
export const useDistributorCustomers = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'customers', skip, take],
    queryFn: () => distributorApi.getCustomers(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch customerCardTransactions for a specific DISTRIBUTOR.
 */
export const useDistributorCustomerCardTransactions = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'customerCardTransactions', skip, take],
    queryFn: () => distributorApi.getCustomerCardTransactions(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch distributorBranches for a specific DISTRIBUTOR.
 */
export const useDistributorDistributorBranches = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'distributorBranches', skip, take],
    queryFn: () => distributorApi.getDistributorBranches(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch distributorObligos for a specific DISTRIBUTOR.
 */
export const useDistributorDistributorObligos = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'distributorObligos', skip, take],
    queryFn: () => distributorApi.getDistributorObligos(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch feeOverrides for a specific DISTRIBUTOR.
 */
export const useDistributorFeeOverrides = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'feeOverrides', skip, take],
    queryFn: () => distributorApi.getFeeOverrides(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch govtReportDistributors for a specific DISTRIBUTOR.
 */
export const useDistributorGovtReportDistributors = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'govtReportDistributors', skip, take],
    queryFn: () => distributorApi.getGovtReportDistributors(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch importTransactionLoads for a specific DISTRIBUTOR.
 */
export const useDistributorImportTransactionLoads = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'importTransactionLoads', skip, take],
    queryFn: () => distributorApi.getImportTransactionLoads(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch limitationOverrides for a specific DISTRIBUTOR.
 */
export const useDistributorLimitationOverrides = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'limitationOverrides', skip, take],
    queryFn: () => distributorApi.getLimitationOverrides(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch newCardRequests for a specific DISTRIBUTOR.
 */
export const useDistributorNewCardRequests = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'newCardRequests', skip, take],
    queryFn: () => distributorApi.getNewCardRequests(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch newCardRequestAPIs for a specific DISTRIBUTOR.
 */
export const useDistributorNewCardRequestAPIs = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'newCardRequestAPIs', skip, take],
    queryFn: () => distributorApi.getNewCardRequestAPIs(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch requestedTransactions for a specific DISTRIBUTOR.
 */
export const useDistributorRequestedTransactions = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'requestedTransactions', skip, take],
    queryFn: () => distributorApi.getRequestedTransactions(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to fetch transactionLoads for a specific DISTRIBUTOR.
 */
export const useDistributorTransactionLoads = (
  distributorId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['distributor', distributorId, 'transactionLoads', skip, take],
    queryFn: () => distributorApi.getTransactionLoads(distributorId!, skip, take),
    enabled: distributorId !== null,
  });
};

/**
 * Hook to create Distributor.
 */
export const useCreateDistributor = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateDistributorRequest) => distributorApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['distributors'] });
    },
  });
};


/**
 * Hook to update Distributor.
 */
export const useUpdateDistributor = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateDistributorRequest }) =>
      distributorApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['distributor', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['distributors'] });
    },
  });
};


/**
 * Hook to delete Distributor.
 */
export const useDeleteDistributor = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => distributorApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['distributors'] });
    },
  });
};



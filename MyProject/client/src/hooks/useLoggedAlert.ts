// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: c_LoggedAlert
//     Generated: 2025-12-17 09:08:22
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { loggedAlertApi } from '../api/loggedAlertApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateLoggedAlertRequest, UpdateLoggedAlertRequest, LoggedAlertFilters } from '../types/LoggedAlert.types';

type UseLoggedAlertOptions = UseEntityOptions<LoggedAlertFilters>;

/**
 * Hook to fetch single LoggedAlert by ID.
 */
export const useLoggedAlert = (id: number | null) => {
  return useQuery({
    queryKey: ['loggedAlert', id],
    queryFn: () => loggedAlertApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all LoggedAlerts with server-side filtering, sorting, and pagination.
 */
export const useLoggedAlerts = (options?: UseLoggedAlertOptions) => {
  return useQuery({
    queryKey: ['loggedAlerts', options],
    queryFn: () => loggedAlertApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch loggedJobs for a specific LOGGEDALERT.
 */
export const useLoggedAlertLoggedJobs = (
  loggedAlertId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['loggedAlert', loggedAlertId, 'loggedJobs', skip, take],
    queryFn: () => loggedAlertApi.getLoggedJobs(loggedAlertId!, skip, take),
    enabled: loggedAlertId !== null,
  });
};

/**
 * Hook to fetch cardOrderFromIssuers for a specific LOGGEDALERT.
 */
export const useLoggedAlertCardOrderFromIssuers = (
  loggedAlertId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['loggedAlert', loggedAlertId, 'cardOrderFromIssuers', skip, take],
    queryFn: () => loggedAlertApi.getCardOrderFromIssuers(loggedAlertId!, skip, take),
    enabled: loggedAlertId !== null,
  });
};

/**
 * Hook to fetch importTransactionLoadRows for a specific LOGGEDALERT.
 */
export const useLoggedAlertImportTransactionLoadRows = (
  loggedAlertId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['loggedAlert', loggedAlertId, 'importTransactionLoadRows', skip, take],
    queryFn: () => loggedAlertApi.getImportTransactionLoadRows(loggedAlertId!, skip, take),
    enabled: loggedAlertId !== null,
  });
};

/**
 * Hook to fetch newCardRequests for a specific LOGGEDALERT.
 */
export const useLoggedAlertNewCardRequests = (
  loggedAlertId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['loggedAlert', loggedAlertId, 'newCardRequests', skip, take],
    queryFn: () => loggedAlertApi.getNewCardRequests(loggedAlertId!, skip, take),
    enabled: loggedAlertId !== null,
  });
};

/**
 * Hook to fetch newCardRequestAPIs for a specific LOGGEDALERT.
 */
export const useLoggedAlertNewCardRequestAPIs = (
  loggedAlertId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['loggedAlert', loggedAlertId, 'newCardRequestAPIs', skip, take],
    queryFn: () => loggedAlertApi.getNewCardRequestAPIs(loggedAlertId!, skip, take),
    enabled: loggedAlertId !== null,
  });
};

/**
 * Hook to fetch requestedTransactions for a specific LOGGEDALERT.
 */
export const useLoggedAlertRequestedTransactions = (
  loggedAlertId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['loggedAlert', loggedAlertId, 'requestedTransactions', skip, take],
    queryFn: () => loggedAlertApi.getRequestedTransactions(loggedAlertId!, skip, take),
    enabled: loggedAlertId !== null,
  });
};

/**
 * Hook to fetch transactionLoads for a specific LOGGEDALERT.
 */
export const useLoggedAlertTransactionLoads = (
  loggedAlertId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['loggedAlert', loggedAlertId, 'transactionLoads', skip, take],
    queryFn: () => loggedAlertApi.getTransactionLoads(loggedAlertId!, skip, take),
    enabled: loggedAlertId !== null,
  });
};

/**
 * Hook to create LoggedAlert.
 */
export const useCreateLoggedAlert = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateLoggedAlertRequest) => loggedAlertApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['loggedAlerts'] });
    },
  });
};


/**
 * Hook to update LoggedAlert.
 */
export const useUpdateLoggedAlert = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateLoggedAlertRequest }) =>
      loggedAlertApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['loggedAlert', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['loggedAlerts'] });
    },
  });
};


/**
 * Hook to delete LoggedAlert.
 */
export const useDeleteLoggedAlert = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => loggedAlertApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['loggedAlerts'] });
    },
  });
};



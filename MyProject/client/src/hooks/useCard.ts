// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: Card
//     Generated: 2025-12-15 09:25:37
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { cardApi } from '../api/cardApi';
import type {
  Card,
  CreateCardRequest,
  UpdateCardRequest,
  CardFilters,
} from '../types/Card.types';
import type { BackofficeTransaction } from '../types/BackofficeTransaction.types';
import type { CardActivation } from '../types/CardActivation.types';
import type { CustomerCardTransaction } from '../types/CustomerCardTransaction.types';
import type { ImportTransactionLoadRow } from '../types/ImportTransactionLoadRow.types';
import type { OnlineApproval } from '../types/OnlineApproval.types';
import type { RequestedTransaction } from '../types/RequestedTransaction.types';
import type { TransactionLoad } from '../types/TransactionLoad.types';

/**
 * Hook to fetch single Card by ID.
 */
export const useCard = (id: number | null) => {
  return useQuery({
    queryKey: ['card', id],
    queryFn: () => cardApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Cards with optional filters.
 */
export const useCards = (filters?: CardFilters) => {
  return useQuery({
    queryKey: ['cards', filters],
    queryFn: () => cardApi.getAll(filters),
  });
};


/**
 * Hook to fetch backofficeTransactions for a specific CARD.
 */
export const useCardBackofficeTransactions = (
  cardId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['card', cardId, 'backofficeTransactions', skip, take],
    queryFn: () => cardApi.getBackofficeTransactions(cardId!, skip, take),
    enabled: cardId !== null,
  });
};

/**
 * Hook to fetch cardActivations for a specific CARD.
 */
export const useCardCardActivations = (
  cardId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['card', cardId, 'cardActivations', skip, take],
    queryFn: () => cardApi.getCardActivations(cardId!, skip, take),
    enabled: cardId !== null,
  });
};

/**
 * Hook to fetch customerCardTransactions for a specific CARD.
 */
export const useCardCustomerCardTransactions = (
  cardId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['card', cardId, 'customerCardTransactions', skip, take],
    queryFn: () => cardApi.getCustomerCardTransactions(cardId!, skip, take),
    enabled: cardId !== null,
  });
};

/**
 * Hook to fetch importTransactionLoadRows for a specific CARD.
 */
export const useCardImportTransactionLoadRows = (
  cardId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['card', cardId, 'importTransactionLoadRows', skip, take],
    queryFn: () => cardApi.getImportTransactionLoadRows(cardId!, skip, take),
    enabled: cardId !== null,
  });
};

/**
 * Hook to fetch onlineApprovals for a specific CARD.
 */
export const useCardOnlineApprovals = (
  cardId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['card', cardId, 'onlineApprovals', skip, take],
    queryFn: () => cardApi.getOnlineApprovals(cardId!, skip, take),
    enabled: cardId !== null,
  });
};

/**
 * Hook to fetch requestedTransactions for a specific CARD.
 */
export const useCardRequestedTransactions = (
  cardId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['card', cardId, 'requestedTransactions', skip, take],
    queryFn: () => cardApi.getRequestedTransactions(cardId!, skip, take),
    enabled: cardId !== null,
  });
};

/**
 * Hook to fetch transactionLoads for a specific CARD.
 */
export const useCardTransactionLoads = (
  cardId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['card', cardId, 'transactionLoads', skip, take],
    queryFn: () => cardApi.getTransactionLoads(cardId!, skip, take),
    enabled: cardId !== null,
  });
};

/**
 * Hook to create Card.
 */
export const useCreateCard = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateCardRequest) => cardApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cards'] });
    },
  });
};


/**
 * Hook to update Card.
 */
export const useUpdateCard = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateCardRequest }) =>
      cardApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['card', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['cards'] });
    },
  });
};


/**
 * Hook to delete Card.
 */
export const useDeleteCard = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => cardApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cards'] });
    },
  });
};



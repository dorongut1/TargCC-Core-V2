// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: CustomerWallet
//     Generated: 2025-12-16 18:22:29
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { customerWalletApi } from '../api/customerWalletApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateCustomerWalletRequest, UpdateCustomerWalletRequest, CustomerWalletFilters } from '../types/CustomerWallet.types';

type UseCustomerWalletOptions = UseEntityOptions<CustomerWalletFilters>;

/**
 * Hook to fetch single CustomerWallet by ID.
 */
export const useCustomerWallet = (id: number | null) => {
  return useQuery({
    queryKey: ['customerWallet', id],
    queryFn: () => customerWalletApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all CustomerWallets with server-side filtering, sorting, and pagination.
 */
export const useCustomerWallets = (options?: UseCustomerWalletOptions) => {
  return useQuery({
    queryKey: ['customerWallets', options],
    queryFn: () => customerWalletApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch backofficeTransactions for a specific CUSTOMERWALLET.
 */
export const useCustomerWalletBackofficeTransactions = (
  customerWalletId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customerWallet', customerWalletId, 'backofficeTransactions', skip, take],
    queryFn: () => customerWalletApi.getBackofficeTransactions(customerWalletId!, skip, take),
    enabled: customerWalletId !== null,
  });
};

/**
 * Hook to fetch externalTransactions for a specific CUSTOMERWALLET.
 */
export const useCustomerWalletExternalTransactions = (
  customerWalletId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customerWallet', customerWalletId, 'externalTransactions', skip, take],
    queryFn: () => customerWalletApi.getExternalTransactions(customerWalletId!, skip, take),
    enabled: customerWalletId !== null,
  });
};

/**
 * Hook to fetch requestedTransactionFees for a specific CUSTOMERWALLET.
 */
export const useCustomerWalletRequestedTransactionFees = (
  customerWalletId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customerWallet', customerWalletId, 'requestedTransactionFees', skip, take],
    queryFn: () => customerWalletApi.getRequestedTransactionFees(customerWalletId!, skip, take),
    enabled: customerWalletId !== null,
  });
};

/**
 * Hook to fetch transactionBits for a specific CUSTOMERWALLET.
 */
export const useCustomerWalletTransactionBits = (
  customerWalletId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customerWallet', customerWalletId, 'transactionBits', skip, take],
    queryFn: () => customerWalletApi.getTransactionBits(customerWalletId!, skip, take),
    enabled: customerWalletId !== null,
  });
};

/**
 * Hook to fetch transactionCreditCards for a specific CUSTOMERWALLET.
 */
export const useCustomerWalletTransactionCreditCards = (
  customerWalletId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customerWallet', customerWalletId, 'transactionCreditCards', skip, take],
    queryFn: () => customerWalletApi.getTransactionCreditCards(customerWalletId!, skip, take),
    enabled: customerWalletId !== null,
  });
};

/**
 * Hook to create CustomerWallet.
 */
export const useCreateCustomerWallet = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateCustomerWalletRequest) => customerWalletApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['customerWallets'] });
    },
  });
};


/**
 * Hook to update CustomerWallet.
 */
export const useUpdateCustomerWallet = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateCustomerWalletRequest }) =>
      customerWalletApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['customerWallet', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['customerWallets'] });
    },
  });
};


/**
 * Hook to delete CustomerWallet.
 */
export const useDeleteCustomerWallet = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => customerWalletApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['customerWallets'] });
    },
  });
};



// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: c_Role
//     Generated: 2025-12-17 08:49:55
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { roleApi } from '../api/roleApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateRoleRequest, UpdateRoleRequest, RoleFilters } from '../types/Role.types';

type UseRoleOptions = UseEntityOptions<RoleFilters>;

/**
 * Hook to fetch single Role by ID.
 */
export const useRole = (id: number | null) => {
  return useQuery({
    queryKey: ['role', id],
    queryFn: () => roleApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Roles with server-side filtering, sorting, and pagination.
 */
export const useRoles = (options?: UseRoleOptions) => {
  return useQuery({
    queryKey: ['roles', options],
    queryFn: () => roleApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch permissions for a specific ROLE.
 */
export const useRolePermissions = (
  roleId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['role', roleId, 'permissions', skip, take],
    queryFn: () => roleApi.getPermissions(roleId!, skip, take),
    enabled: roleId !== null,
  });
};

/**
 * Hook to fetch roles for a specific ROLE.
 */
export const useRoleRoles = (
  roleId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['role', roleId, 'roles', skip, take],
    queryFn: () => roleApi.getRoles(roleId!, skip, take),
    enabled: roleId !== null,
  });
};

/**
 * Hook to fetch users for a specific ROLE.
 */
export const useRoleUsers = (
  roleId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['role', roleId, 'users', skip, take],
    queryFn: () => roleApi.getUsers(roleId!, skip, take),
    enabled: roleId !== null,
  });
};

/**
 * Hook to create Role.
 */
export const useCreateRole = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateRoleRequest) => roleApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['roles'] });
    },
  });
};


/**
 * Hook to update Role.
 */
export const useUpdateRole = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateRoleRequest }) =>
      roleApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['role', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['roles'] });
    },
  });
};


/**
 * Hook to delete Role.
 */
export const useDeleteRole = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => roleApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['roles'] });
    },
  });
};



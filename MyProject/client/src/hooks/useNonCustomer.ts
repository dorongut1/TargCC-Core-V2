// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: NonCustomer
//     Generated: 2025-12-15 22:04:10
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { nonCustomerApi } from '../api/nonCustomerApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateNonCustomerRequest, UpdateNonCustomerRequest, NonCustomerFilters } from '../types/NonCustomer.types';

type UseNonCustomerOptions = UseEntityOptions<NonCustomerFilters>;

/**
 * Hook to fetch single NonCustomer by ID.
 */
export const useNonCustomer = (id: number | null) => {
  return useQuery({
    queryKey: ['nonCustomer', id],
    queryFn: () => nonCustomerApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all NonCustomers with server-side filtering, sorting, and pagination.
 */
export const useNonCustomers = (options?: UseNonCustomerOptions) => {
  return useQuery({
    queryKey: ['nonCustomers', options],
    queryFn: () => nonCustomerApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch requestedTransactions for a specific NONCUSTOMER.
 */
export const useNonCustomerRequestedTransactions = (
  nonCustomerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['nonCustomer', nonCustomerId, 'requestedTransactions', skip, take],
    queryFn: () => nonCustomerApi.getRequestedTransactions(nonCustomerId!, skip, take),
    enabled: nonCustomerId !== null,
  });
};

/**
 * Hook to fetch transactionBits for a specific NONCUSTOMER.
 */
export const useNonCustomerTransactionBits = (
  nonCustomerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['nonCustomer', nonCustomerId, 'transactionBits', skip, take],
    queryFn: () => nonCustomerApi.getTransactionBits(nonCustomerId!, skip, take),
    enabled: nonCustomerId !== null,
  });
};

/**
 * Hook to fetch transactionCreditCards for a specific NONCUSTOMER.
 */
export const useNonCustomerTransactionCreditCards = (
  nonCustomerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['nonCustomer', nonCustomerId, 'transactionCreditCards', skip, take],
    queryFn: () => nonCustomerApi.getTransactionCreditCards(nonCustomerId!, skip, take),
    enabled: nonCustomerId !== null,
  });
};

/**
 * Hook to create NonCustomer.
 */
export const useCreateNonCustomer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateNonCustomerRequest) => nonCustomerApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['nonCustomers'] });
    },
  });
};


/**
 * Hook to update NonCustomer.
 */
export const useUpdateNonCustomer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateNonCustomerRequest }) =>
      nonCustomerApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['nonCustomer', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['nonCustomers'] });
    },
  });
};


/**
 * Hook to delete NonCustomer.
 */
export const useDeleteNonCustomer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => nonCustomerApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['nonCustomers'] });
    },
  });
};



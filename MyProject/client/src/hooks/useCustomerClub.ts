// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: CustomerClub
//     Generated: 2025-12-11 13:48:46
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { customerClubApi } from '../api/customerClubApi';
import type {
  CustomerClub,
  CreateCustomerClubRequest,
  UpdateCustomerClubRequest,
  CustomerClubFilters,
} from '../types/CustomerClub.types';
import type { Customer } from '../types/Customer.types';
import type { FeeOverride } from '../types/FeeOverride.types';
import type { LimitationOverride } from '../types/LimitationOverride.types';

/**
 * Hook to fetch single CustomerClub by ID.
 */
export const useCustomerClub = (id: number | null) => {
  return useQuery({
    queryKey: ['customerClub', id],
    queryFn: () => customerClubApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all CustomerClubs with optional filters.
 */
export const useCustomerClubs = (filters?: CustomerClubFilters) => {
  return useQuery({
    queryKey: ['customerClubs', filters],
    queryFn: () => customerClubApi.getAll(filters),
  });
};


/**
 * Hook to fetch customers for a specific CUSTOMERCLUB.
 */
export const useCustomerClubCustomers = (
  customerClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customerClub', customerClubId, 'customers', skip, take],
    queryFn: () => customerClubApi.getCustomers(customerClubId!, skip, take),
    enabled: customerClubId !== null,
  });
};

/**
 * Hook to fetch feeOverrides for a specific CUSTOMERCLUB.
 */
export const useCustomerClubFeeOverrides = (
  customerClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customerClub', customerClubId, 'feeOverrides', skip, take],
    queryFn: () => customerClubApi.getFeeOverrides(customerClubId!, skip, take),
    enabled: customerClubId !== null,
  });
};

/**
 * Hook to fetch limitationOverrides for a specific CUSTOMERCLUB.
 */
export const useCustomerClubLimitationOverrides = (
  customerClubId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customerClub', customerClubId, 'limitationOverrides', skip, take],
    queryFn: () => customerClubApi.getLimitationOverrides(customerClubId!, skip, take),
    enabled: customerClubId !== null,
  });
};

/**
 * Hook to create CustomerClub.
 */
export const useCreateCustomerClub = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateCustomerClubRequest) => customerClubApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['customerClubs'] });
    },
  });
};


/**
 * Hook to update CustomerClub.
 */
export const useUpdateCustomerClub = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateCustomerClubRequest }) =>
      customerClubApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['customerClub', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['customerClubs'] });
    },
  });
};


/**
 * Hook to delete CustomerClub.
 */
export const useDeleteCustomerClub = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => customerClubApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['customerClubs'] });
    },
  });
};



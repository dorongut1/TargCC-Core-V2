// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: Customer
//     Generated: 2025-12-15 20:38:14
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { customerApi } from '../api/customerApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateCustomerRequest, UpdateCustomerRequest, CustomerFilters } from '../types/Customer.types';

type UseCustomerOptions = UseEntityOptions<CustomerFilters>;

/**
 * Hook to fetch single Customer by ID.
 */
export const useCustomer = (id: number | null) => {
  return useQuery({
    queryKey: ['customer', id],
    queryFn: () => customerApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Customers with server-side filtering, sorting, and pagination.
 */
export const useCustomers = (options?: UseCustomerOptions) => {
  return useQuery({
    queryKey: ['customers', options],
    queryFn: () => customerApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch backofficeTransactions for a specific CUSTOMER.
 */
export const useCustomerBackofficeTransactions = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'backofficeTransactions', skip, take],
    queryFn: () => customerApi.getBackofficeTransactions(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch cards for a specific CUSTOMER.
 */
export const useCustomerCards = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'cards', skip, take],
    queryFn: () => customerApi.getCards(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch cardActivations for a specific CUSTOMER.
 */
export const useCustomerCardActivations = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'cardActivations', skip, take],
    queryFn: () => customerApi.getCardActivations(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch cardClubs for a specific CUSTOMER.
 */
export const useCustomerCardClubs = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'cardClubs', skip, take],
    queryFn: () => customerApi.getCardClubs(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch customerCardTransactions for a specific CUSTOMER.
 */
export const useCustomerCustomerCardTransactions = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'customerCardTransactions', skip, take],
    queryFn: () => customerApi.getCustomerCardTransactions(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch customerCreditCards for a specific CUSTOMER.
 */
export const useCustomerCustomerCreditCards = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'customerCreditCards', skip, take],
    queryFn: () => customerApi.getCustomerCreditCards(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch customerKYCs for a specific CUSTOMER.
 */
export const useCustomerCustomerKYCs = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'customerKYCs', skip, take],
    queryFn: () => customerApi.getCustomerKYCs(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch customerPartnerships for a specific CUSTOMER.
 */
export const useCustomerCustomerPartnerships = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'customerPartnerships', skip, take],
    queryFn: () => customerApi.getCustomerPartnerships(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch customerPerformances for a specific CUSTOMER.
 */
export const useCustomerCustomerPerformances = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'customerPerformances', skip, take],
    queryFn: () => customerApi.getCustomerPerformances(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch customerWallets for a specific CUSTOMER.
 */
export const useCustomerCustomerWallets = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'customerWallets', skip, take],
    queryFn: () => customerApi.getCustomerWallets(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch drivebotPayments for a specific CUSTOMER.
 */
export const useCustomerDrivebotPayments = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'drivebotPayments', skip, take],
    queryFn: () => customerApi.getDrivebotPayments(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch externalCustomers for a specific CUSTOMER.
 */
export const useCustomerExternalCustomers = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'externalCustomers', skip, take],
    queryFn: () => customerApi.getExternalCustomers(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch externalTransactions for a specific CUSTOMER.
 */
export const useCustomerExternalTransactions = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'externalTransactions', skip, take],
    queryFn: () => customerApi.getExternalTransactions(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch importTransactionLoadRows for a specific CUSTOMER.
 */
export const useCustomerImportTransactionLoadRows = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'importTransactionLoadRows', skip, take],
    queryFn: () => customerApi.getImportTransactionLoadRows(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch mCCBReceivers for a specific CUSTOMER.
 */
export const useCustomerMCCBReceivers = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'mCCBReceivers', skip, take],
    queryFn: () => customerApi.getMCCBReceivers(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch mCCBSecurityChecks for a specific CUSTOMER.
 */
export const useCustomerMCCBSecurityChecks = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'mCCBSecurityChecks', skip, take],
    queryFn: () => customerApi.getMCCBSecurityChecks(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch mCCBTransactions for a specific CUSTOMER.
 */
export const useCustomerMCCBTransactions = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'mCCBTransactions', skip, take],
    queryFn: () => customerApi.getMCCBTransactions(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch newCardRequests for a specific CUSTOMER.
 */
export const useCustomerNewCardRequests = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'newCardRequests', skip, take],
    queryFn: () => customerApi.getNewCardRequests(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch onlineApprovals for a specific CUSTOMER.
 */
export const useCustomerOnlineApprovals = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'onlineApprovals', skip, take],
    queryFn: () => customerApi.getOnlineApprovals(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch requestedTransactions for a specific CUSTOMER.
 */
export const useCustomerRequestedTransactions = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'requestedTransactions', skip, take],
    queryFn: () => customerApi.getRequestedTransactions(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch tasks for a specific CUSTOMER.
 */
export const useCustomerTasks = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'tasks', skip, take],
    queryFn: () => customerApi.getTasks(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch transactionBits for a specific CUSTOMER.
 */
export const useCustomerTransactionBits = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'transactionBits', skip, take],
    queryFn: () => customerApi.getTransactionBits(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch transactionCreditCards for a specific CUSTOMER.
 */
export const useCustomerTransactionCreditCards = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'transactionCreditCards', skip, take],
    queryFn: () => customerApi.getTransactionCreditCards(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to fetch transactionLoads for a specific CUSTOMER.
 */
export const useCustomerTransactionLoads = (
  customerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['customer', customerId, 'transactionLoads', skip, take],
    queryFn: () => customerApi.getTransactionLoads(customerId!, skip, take),
    enabled: customerId !== null,
  });
};

/**
 * Hook to create Customer.
 */
export const useCreateCustomer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateCustomerRequest) => customerApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['customers'] });
    },
  });
};


/**
 * Hook to update Customer.
 */
export const useUpdateCustomer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateCustomerRequest }) =>
      customerApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['customer', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['customers'] });
    },
  });
};


/**
 * Hook to delete Customer.
 */
export const useDeleteCustomer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => customerApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['customers'] });
    },
  });
};



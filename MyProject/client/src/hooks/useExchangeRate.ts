// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: ExchangeRate
//     Generated: 2025-12-11 11:52:13
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { exchangeRateApi } from '../api/exchangeRateApi';
import type {
  ExchangeRate,
  CreateExchangeRateRequest,
  UpdateExchangeRateRequest,
  ExchangeRateFilters,
} from '../types/ExchangeRate.types';

/**
 * Hook to fetch single ExchangeRate by ID.
 */
export const useExchangeRate = (id: number | null) => {
  return useQuery({
    queryKey: ['exchangeRate', id],
    queryFn: () => exchangeRateApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all ExchangeRates with optional filters.
 */
export const useExchangeRates = (filters?: ExchangeRateFilters) => {
  return useQuery({
    queryKey: ['exchangeRates', filters],
    queryFn: () => exchangeRateApi.getAll(filters),
  });
};


/**
 * Hook to create ExchangeRate.
 */
export const useCreateExchangeRate = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateExchangeRateRequest) => exchangeRateApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['exchangeRates'] });
    },
  });
};


/**
 * Hook to update ExchangeRate.
 */
export const useUpdateExchangeRate = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateExchangeRateRequest }) =>
      exchangeRateApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['exchangeRate', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['exchangeRates'] });
    },
  });
};


/**
 * Hook to delete ExchangeRate.
 */
export const useDeleteExchangeRate = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => exchangeRateApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['exchangeRates'] });
    },
  });
};



// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: c_Job
//     Generated: 2025-12-15 22:18:47
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { jobApi } from '../api/jobApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateJobRequest, UpdateJobRequest, JobFilters } from '../types/Job.types';

type UseJobOptions = UseEntityOptions<JobFilters>;

/**
 * Hook to fetch single Job by ID.
 */
export const useJob = (id: number | null) => {
  return useQuery({
    queryKey: ['job', id],
    queryFn: () => jobApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Jobs with server-side filtering, sorting, and pagination.
 */
export const useJobs = (options?: UseJobOptions) => {
  return useQuery({
    queryKey: ['jobs', options],
    queryFn: () => jobApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch jobAlertRecipients for a specific JOB.
 */
export const useJobJobAlertRecipients = (
  jobId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['job', jobId, 'jobAlertRecipients', skip, take],
    queryFn: () => jobApi.getJobAlertRecipients(jobId!, skip, take),
    enabled: jobId !== null,
  });
};

/**
 * Hook to fetch loggedJobs for a specific JOB.
 */
export const useJobLoggedJobs = (
  jobId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['job', jobId, 'loggedJobs', skip, take],
    queryFn: () => jobApi.getLoggedJobs(jobId!, skip, take),
    enabled: jobId !== null,
  });
};

/**
 * Hook to create Job.
 */
export const useCreateJob = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateJobRequest) => jobApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['jobs'] });
    },
  });
};


/**
 * Hook to update Job.
 */
export const useUpdateJob = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateJobRequest }) =>
      jobApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['job', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['jobs'] });
    },
  });
};


/**
 * Hook to delete Job.
 */
export const useDeleteJob = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => jobApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['jobs'] });
    },
  });
};



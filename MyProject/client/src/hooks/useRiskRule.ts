// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: RiskRule
//     Generated: 2025-12-11 21:35:47
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { riskRuleApi } from '../api/riskRuleApi';
import type {
  RiskRule,
  CreateRiskRuleRequest,
  UpdateRiskRuleRequest,
  RiskRuleFilters,
} from '../types/RiskRule.types';
import type { ActiveRestrictions } from '../types/ActiveRestrictions.types';
import type { EntityRiskColor } from '../types/EntityRiskColor.types';
import type { RiskEvent } from '../types/RiskEvent.types';
import type { RiskRuleLog } from '../types/RiskRuleLog.types';
import type { RiskRuleOverride } from '../types/RiskRuleOverride.types';

/**
 * Hook to fetch single RiskRule by ID.
 */
export const useRiskRule = (id: number | null) => {
  return useQuery({
    queryKey: ['riskRule', id],
    queryFn: () => riskRuleApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all RiskRules with optional filters.
 */
export const useRiskRules = (filters?: RiskRuleFilters) => {
  return useQuery({
    queryKey: ['riskRules', filters],
    queryFn: () => riskRuleApi.getAll(filters),
  });
};


/**
 * Hook to fetch activeRestrictionses for a specific RISKRULE.
 */
export const useRiskRuleActiveRestrictionses = (
  riskRuleId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['riskRule', riskRuleId, 'activeRestrictionses', skip, take],
    queryFn: () => riskRuleApi.getActiveRestrictionses(riskRuleId!, skip, take),
    enabled: riskRuleId !== null,
  });
};

/**
 * Hook to fetch entityRiskColors for a specific RISKRULE.
 */
export const useRiskRuleEntityRiskColors = (
  riskRuleId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['riskRule', riskRuleId, 'entityRiskColors', skip, take],
    queryFn: () => riskRuleApi.getEntityRiskColors(riskRuleId!, skip, take),
    enabled: riskRuleId !== null,
  });
};

/**
 * Hook to fetch riskEvents for a specific RISKRULE.
 */
export const useRiskRuleRiskEvents = (
  riskRuleId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['riskRule', riskRuleId, 'riskEvents', skip, take],
    queryFn: () => riskRuleApi.getRiskEvents(riskRuleId!, skip, take),
    enabled: riskRuleId !== null,
  });
};

/**
 * Hook to fetch riskRuleLogs for a specific RISKRULE.
 */
export const useRiskRuleRiskRuleLogs = (
  riskRuleId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['riskRule', riskRuleId, 'riskRuleLogs', skip, take],
    queryFn: () => riskRuleApi.getRiskRuleLogs(riskRuleId!, skip, take),
    enabled: riskRuleId !== null,
  });
};

/**
 * Hook to fetch riskRuleOverrides for a specific RISKRULE.
 */
export const useRiskRuleRiskRuleOverrides = (
  riskRuleId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['riskRule', riskRuleId, 'riskRuleOverrides', skip, take],
    queryFn: () => riskRuleApi.getRiskRuleOverrides(riskRuleId!, skip, take),
    enabled: riskRuleId !== null,
  });
};

/**
 * Hook to create RiskRule.
 */
export const useCreateRiskRule = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateRiskRuleRequest) => riskRuleApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['riskRules'] });
    },
  });
};


/**
 * Hook to update RiskRule.
 */
export const useUpdateRiskRule = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateRiskRuleRequest }) =>
      riskRuleApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['riskRule', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['riskRules'] });
    },
  });
};


/**
 * Hook to delete RiskRule.
 */
export const useDeleteRiskRule = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => riskRuleApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['riskRules'] });
    },
  });
};



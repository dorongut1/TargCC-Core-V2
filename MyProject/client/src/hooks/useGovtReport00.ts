// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: GovtReport00
//     Generated: 2025-12-17 21:45:24
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { govtReport00Api } from '../api/govtReport00Api';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateGovtReport00Request, UpdateGovtReport00Request, GovtReport00Filters } from '../types/GovtReport00.types';

type UseGovtReport00Options = UseEntityOptions<GovtReport00Filters>;

/**
 * Hook to fetch single GovtReport00 by ID.
 */
export const useGovtReport00 = (id: number | null) => {
  return useQuery({
    queryKey: ['govtReport00', id],
    queryFn: () => govtReport00Api.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all GovtReport00s with server-side filtering, sorting, and pagination.
 */
export const useGovtReport00s = (options?: UseGovtReport00Options) => {
  return useQuery({
    queryKey: ['govtReport00s', options],
    queryFn: () => govtReport00Api.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch govtReport10s for a specific GOVTREPORT00.
 */
export const useGovtReport00GovtReport10s = (
  govtReport00Id: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['govtReport00', govtReport00Id, 'govtReport10s', skip, take],
    queryFn: () => govtReport00Api.getGovtReport10s(govtReport00Id!, skip, take),
    enabled: govtReport00Id !== null,
  });
};

/**
 * Hook to fetch govtReport30s for a specific GOVTREPORT00.
 */
export const useGovtReport00GovtReport30s = (
  govtReport00Id: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['govtReport00', govtReport00Id, 'govtReport30s', skip, take],
    queryFn: () => govtReport00Api.getGovtReport30s(govtReport00Id!, skip, take),
    enabled: govtReport00Id !== null,
  });
};

/**
 * Hook to fetch govtReport40s for a specific GOVTREPORT00.
 */
export const useGovtReport00GovtReport40s = (
  govtReport00Id: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['govtReport00', govtReport00Id, 'govtReport40s', skip, take],
    queryFn: () => govtReport00Api.getGovtReport40s(govtReport00Id!, skip, take),
    enabled: govtReport00Id !== null,
  });
};

/**
 * Hook to fetch govtReport99s for a specific GOVTREPORT00.
 */
export const useGovtReport00GovtReport99s = (
  govtReport00Id: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['govtReport00', govtReport00Id, 'govtReport99s', skip, take],
    queryFn: () => govtReport00Api.getGovtReport99s(govtReport00Id!, skip, take),
    enabled: govtReport00Id !== null,
  });
};

/**
 * Hook to fetch govtReportPayments for a specific GOVTREPORT00.
 */
export const useGovtReport00GovtReportPayments = (
  govtReport00Id: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['govtReport00', govtReport00Id, 'govtReportPayments', skip, take],
    queryFn: () => govtReport00Api.getGovtReportPayments(govtReport00Id!, skip, take),
    enabled: govtReport00Id !== null,
  });
};

/**
 * Hook to create GovtReport00.
 */
export const useCreateGovtReport00 = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateGovtReport00Request) => govtReport00Api.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['govtReport00s'] });
    },
  });
};


/**
 * Hook to update GovtReport00.
 */
export const useUpdateGovtReport00 = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateGovtReport00Request }) =>
      govtReport00Api.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['govtReport00', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['govtReport00s'] });
    },
  });
};


/**
 * Hook to delete GovtReport00.
 */
export const useDeleteGovtReport00 = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => govtReport00Api.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['govtReport00s'] });
    },
  });
};



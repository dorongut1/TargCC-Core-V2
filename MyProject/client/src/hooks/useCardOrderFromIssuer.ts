// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: CardOrderFromIssuer
//     Generated: 2025-12-17 08:23:59
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { cardOrderFromIssuerApi } from '../api/cardOrderFromIssuerApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateCardOrderFromIssuerRequest, UpdateCardOrderFromIssuerRequest, CardOrderFromIssuerFilters } from '../types/CardOrderFromIssuer.types';

type UseCardOrderFromIssuerOptions = UseEntityOptions<CardOrderFromIssuerFilters>;

/**
 * Hook to fetch single CardOrderFromIssuer by ID.
 */
export const useCardOrderFromIssuer = (id: number | null) => {
  return useQuery({
    queryKey: ['cardOrderFromIssuer', id],
    queryFn: () => cardOrderFromIssuerApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all CardOrderFromIssuers with server-side filtering, sorting, and pagination.
 */
export const useCardOrderFromIssuers = (options?: UseCardOrderFromIssuerOptions) => {
  return useQuery({
    queryKey: ['cardOrderFromIssuers', options],
    queryFn: () => cardOrderFromIssuerApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch backofficeTransactions for a specific CARDORDERFROMISSUER.
 */
export const useCardOrderFromIssuerBackofficeTransactions = (
  cardOrderFromIssuerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardOrderFromIssuer', cardOrderFromIssuerId, 'backofficeTransactions', skip, take],
    queryFn: () => cardOrderFromIssuerApi.getBackofficeTransactions(cardOrderFromIssuerId!, skip, take),
    enabled: cardOrderFromIssuerId !== null,
  });
};

/**
 * Hook to fetch cards for a specific CARDORDERFROMISSUER.
 */
export const useCardOrderFromIssuerCards = (
  cardOrderFromIssuerId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['cardOrderFromIssuer', cardOrderFromIssuerId, 'cards', skip, take],
    queryFn: () => cardOrderFromIssuerApi.getCards(cardOrderFromIssuerId!, skip, take),
    enabled: cardOrderFromIssuerId !== null,
  });
};

/**
 * Hook to create CardOrderFromIssuer.
 */
export const useCreateCardOrderFromIssuer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateCardOrderFromIssuerRequest) => cardOrderFromIssuerApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cardOrderFromIssuers'] });
    },
  });
};


/**
 * Hook to update CardOrderFromIssuer.
 */
export const useUpdateCardOrderFromIssuer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateCardOrderFromIssuerRequest }) =>
      cardOrderFromIssuerApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['cardOrderFromIssuer', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['cardOrderFromIssuers'] });
    },
  });
};


/**
 * Hook to delete CardOrderFromIssuer.
 */
export const useDeleteCardOrderFromIssuer = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => cardOrderFromIssuerApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cardOrderFromIssuers'] });
    },
  });
};



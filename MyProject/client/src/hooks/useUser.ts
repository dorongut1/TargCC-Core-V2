// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: c_User
//     Generated: 2025-12-17 20:20:28
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { userApi } from '../api/userApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateUserRequest, UpdateUserRequest, UserFilters } from '../types/User.types';

type UseUserOptions = UseEntityOptions<UserFilters>;

/**
 * Hook to fetch single User by ID.
 */
export const useUser = (id: number | null) => {
  return useQuery({
    queryKey: ['user', id],
    queryFn: () => userApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Users with server-side filtering, sorting, and pagination.
 */
export const useUsers = (options?: UseUserOptions) => {
  return useQuery({
    queryKey: ['users', options],
    queryFn: () => userApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch jobAlertRecipients for a specific USER.
 */
export const useUserJobAlertRecipients = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'jobAlertRecipients', skip, take],
    queryFn: () => userApi.getJobAlertRecipients(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch loggedAlerts for a specific USER.
 */
export const useUserLoggedAlerts = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'loggedAlerts', skip, take],
    queryFn: () => userApi.getLoggedAlerts(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch loggedRequests for a specific USER.
 */
export const useUserLoggedRequests = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'loggedRequests', skip, take],
    queryFn: () => userApi.getLoggedRequests(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch userLoginKeys for a specific USER.
 */
export const useUserUserLoginKeys = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'userLoginKeys', skip, take],
    queryFn: () => userApi.getUserLoginKeys(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch userPermissions for a specific USER.
 */
export const useUserUserPermissions = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'userPermissions', skip, take],
    queryFn: () => userApi.getUserPermissions(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch userStatuses for a specific USER.
 */
export const useUserUserStatuses = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'userStatuses', skip, take],
    queryFn: () => userApi.getUserStatuses(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch cardOrderByDistributors for a specific USER.
 */
export const useUserCardOrderByDistributors = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'cardOrderByDistributors', skip, take],
    queryFn: () => userApi.getCardOrderByDistributors(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch cardOrderFromIssuers for a specific USER.
 */
export const useUserCardOrderFromIssuers = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'cardOrderFromIssuers', skip, take],
    queryFn: () => userApi.getCardOrderFromIssuers(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch customerWallets for a specific USER.
 */
export const useUserCustomerWallets = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'customerWallets', skip, take],
    queryFn: () => userApi.getCustomerWallets(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch importTransactionLoads for a specific USER.
 */
export const useUserImportTransactionLoads = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'importTransactionLoads', skip, take],
    queryFn: () => userApi.getImportTransactionLoads(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch tasks for a specific USER.
 */
export const useUserTasks = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'tasks', skip, take],
    queryFn: () => userApi.getTasks(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch transactionLoads for a specific USER.
 */
export const useUserTransactionLoads = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'transactionLoads', skip, take],
    queryFn: () => userApi.getTransactionLoads(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to fetch userApprovals for a specific USER.
 */
export const useUserUserApprovals = (
  userId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['user', userId, 'userApprovals', skip, take],
    queryFn: () => userApi.getUserApprovals(userId!, skip, take),
    enabled: userId !== null,
  });
};

/**
 * Hook to create User.
 */
export const useCreateUser = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateUserRequest) => userApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['users'] });
    },
  });
};


/**
 * Hook to update User.
 */
export const useUpdateUser = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateUserRequest }) =>
      userApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['user', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['users'] });
    },
  });
};


/**
 * Hook to delete User.
 */
export const useDeleteUser = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => userApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['users'] });
    },
  });
};



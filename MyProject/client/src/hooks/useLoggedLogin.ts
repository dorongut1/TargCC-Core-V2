// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: c_LoggedLogin
//     Generated: 2025-12-15 13:53:26
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { loggedLoginApi } from '../api/loggedLoginApi';
import type { CreateLoggedLoginRequest, UpdateLoggedLoginRequest, LoggedLoginFilters } from '../types/LoggedLogin.types';

/**
 * Hook to fetch single LoggedLogin by ID.
 */
export const useLoggedLogin = (id: number | null) => {
  return useQuery({
    queryKey: ['loggedLogin', id],
    queryFn: () => loggedLoginApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all LoggedLogins with optional filters.
 */
export const useLoggedLogins = (filters?: LoggedLoginFilters) => {
  return useQuery({
    queryKey: ['loggedLogins', filters],
    queryFn: () => loggedLoginApi.getAll(filters),
  });
};


/**
 * Hook to fetch loggedAlerts for a specific LOGGEDLOGIN.
 */
export const useLoggedLoginLoggedAlerts = (
  loggedLoginId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['loggedLogin', loggedLoginId, 'loggedAlerts', skip, take],
    queryFn: () => loggedLoginApi.getLoggedAlerts(loggedLoginId!, skip, take),
    enabled: loggedLoginId !== null,
  });
};

/**
 * Hook to fetch loggedRequests for a specific LOGGEDLOGIN.
 */
export const useLoggedLoginLoggedRequests = (
  loggedLoginId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['loggedLogin', loggedLoginId, 'loggedRequests', skip, take],
    queryFn: () => loggedLoginApi.getLoggedRequests(loggedLoginId!, skip, take),
    enabled: loggedLoginId !== null,
  });
};

/**
 * Hook to create LoggedLogin.
 */
export const useCreateLoggedLogin = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateLoggedLoginRequest) => loggedLoginApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['loggedLogins'] });
    },
  });
};


/**
 * Hook to update LoggedLogin.
 */
export const useUpdateLoggedLogin = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateLoggedLoginRequest }) =>
      loggedLoginApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['loggedLogin', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['loggedLogins'] });
    },
  });
};


/**
 * Hook to delete LoggedLogin.
 */
export const useDeleteLoggedLogin = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => loggedLoginApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['loggedLogins'] });
    },
  });
};



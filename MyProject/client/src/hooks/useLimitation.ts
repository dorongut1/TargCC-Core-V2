// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: Limitation
//     Generated: 2025-12-17 15:31:36
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { limitationApi } from '../api/limitationApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateLimitationRequest, UpdateLimitationRequest, LimitationFilters } from '../types/Limitation.types';

type UseLimitationOptions = UseEntityOptions<LimitationFilters>;

/**
 * Hook to fetch single Limitation by ID.
 */
export const useLimitation = (id: number | null) => {
  return useQuery({
    queryKey: ['limitation', id],
    queryFn: () => limitationApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all Limitations with server-side filtering, sorting, and pagination.
 */
export const useLimitations = (options?: UseLimitationOptions) => {
  return useQuery({
    queryKey: ['limitations', options],
    queryFn: () => limitationApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to fetch limitationOverrides for a specific LIMITATION.
 */
export const useLimitationLimitationOverrides = (
  limitationId: number | null,
  skip?: number,
  take?: number
) => {
  return useQuery({
    queryKey: ['limitation', limitationId, 'limitationOverrides', skip, take],
    queryFn: () => limitationApi.getLimitationOverrides(limitationId!, skip, take),
    enabled: limitationId !== null,
  });
};

/**
 * Hook to create Limitation.
 */
export const useCreateLimitation = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateLimitationRequest) => limitationApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['limitations'] });
    },
  });
};


/**
 * Hook to update Limitation.
 */
export const useUpdateLimitation = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateLimitationRequest }) =>
      limitationApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['limitation', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['limitations'] });
    },
  });
};


/**
 * Hook to delete Limitation.
 */
export const useDeleteLimitation = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => limitationApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['limitations'] });
    },
  });
};



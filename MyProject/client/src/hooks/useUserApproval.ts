// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactHooks
//     Table: UserApproval
//     Generated: 2025-12-17 07:52:25
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { userApprovalApi } from '../api/userApprovalApi';
import type { UseEntityOptions } from '../types/common.types';
import type { CreateUserApprovalRequest, UpdateUserApprovalRequest, UserApprovalFilters } from '../types/UserApproval.types';

type UseUserApprovalOptions = UseEntityOptions<UserApprovalFilters>;

/**
 * Hook to fetch single UserApproval by ID.
 */
export const useUserApproval = (id: number | null) => {
  return useQuery({
    queryKey: ['userApproval', id],
    queryFn: () => userApprovalApi.getById(id!),
    enabled: id !== null,
  });
};


/**
 * Hook to fetch all UserApprovals with server-side filtering, sorting, and pagination.
 */
export const useUserApprovals = (options?: UseUserApprovalOptions) => {
  return useQuery({
    queryKey: ['userApprovals', options],
    queryFn: () => userApprovalApi.getAll(options),
    enabled: options?.enabled !== false,
    staleTime: 30000, // 30 seconds
  });
};


/**
 * Hook to create UserApproval.
 */
export const useCreateUserApproval = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateUserApprovalRequest) => userApprovalApi.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['userApprovals'] });
    },
  });
};


/**
 * Hook to update UserApproval.
 */
export const useUpdateUserApproval = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: UpdateUserApprovalRequest }) =>
      userApprovalApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['userApproval', variables.id] });
      queryClient.invalidateQueries({ queryKey: ['userApprovals'] });
    },
  });
};


/**
 * Hook to delete UserApproval.
 */
export const useDeleteUserApproval = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: number) => userApprovalApi.delete(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['userApprovals'] });
    },
  });
};



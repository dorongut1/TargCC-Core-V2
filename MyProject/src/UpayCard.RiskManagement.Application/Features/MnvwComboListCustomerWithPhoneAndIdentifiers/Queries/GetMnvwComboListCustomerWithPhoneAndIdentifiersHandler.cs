// <auto-generated>
//     This code was generated by TargCC
//     Generated for: mnvwComboList_CustomerWithPhoneAndIdentifier (Handler)
//     Generation date: 2025-12-16 08:57:50
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

using AutoMapper;
using MediatR;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Logging;
using UpayCard.RiskManagement.Application.Common.Models;
using UpayCard.RiskManagement.Application.DTOs;
using UpayCard.RiskManagement.Domain.Common;
using UpayCard.RiskManagement.Domain.Entities;
using UpayCard.RiskManagement.Domain.Interfaces;

namespace UpayCard.RiskManagement.Application.Features.mnvwComboList_CustomerWithPhoneAndIdentifiers.Queries;

/// <summary>
/// Handles the GetMnvwComboListCustomerWithPhoneAndIdentifiersQuery request with server-side filtering, sorting, and pagination.
/// </summary>
public class GetMnvwComboListCustomerWithPhoneAndIdentifiersHandler : IRequestHandler<GetMnvwComboListCustomerWithPhoneAndIdentifiersQuery, Result<PagedResult<MnvwComboListCustomerWithPhoneAndIdentifierDto>>>
{
    private readonly IApplicationDbContext _context;
    private readonly IMapper _mapper;
    private readonly ILogger<GetMnvwComboListCustomerWithPhoneAndIdentifiersHandler> _logger;

    /// <summary>
    /// Initializes a new instance of the <see cref="GetMnvwComboListCustomerWithPhoneAndIdentifiersHandler"/> class.
    /// </summary>
    public GetMnvwComboListCustomerWithPhoneAndIdentifiersHandler(
        IApplicationDbContext context,
        IMapper mapper,
        ILogger<GetMnvwComboListCustomerWithPhoneAndIdentifiersHandler> logger)
    {
        _context = context ?? throw new ArgumentNullException(nameof(context));
        _mapper = mapper ?? throw new ArgumentNullException(nameof(mapper));
        _logger = logger ?? throw new ArgumentNullException(nameof(logger));
    }

    /// <inheritdoc/>
    public async Task<Result<PagedResult<MnvwComboListCustomerWithPhoneAndIdentifierDto>>> Handle(GetMnvwComboListCustomerWithPhoneAndIdentifiersQuery request, CancellationToken cancellationToken)
    {
        ArgumentNullException.ThrowIfNull(request);

        _logger.LogDebug("Getting mnvwComboList_CustomerWithPhoneAndIdentifiers - Page: {Page}, Size: {Size}, SortBy: {SortBy}", request.Page, request.PageSize, request.SortBy);

        try
        {
            var query = _context.mnvwComboList_CustomerWithPhoneAndIdentifiers.AsQueryable();

            // Apply filters
            query = ApplyFilters(query, request.Filters);

            // Get total count BEFORE pagination
            var totalCount = await query.CountAsync(cancellationToken);

            // Apply sorting
            query = ApplySorting(query, request.SortBy, request.SortDirection);

            // Apply pagination
            query = query
                .Skip((request.Page - 1) * request.PageSize)
                .Take(request.PageSize);

            var entities = await query.ToListAsync(cancellationToken);
            var dtos = _mapper.Map<List<MnvwComboListCustomerWithPhoneAndIdentifierDto>>(entities);

            var result = new PagedResult<MnvwComboListCustomerWithPhoneAndIdentifierDto>
            {
                Items = dtos,
                TotalCount = totalCount,
                Page = request.Page,
                PageSize = request.PageSize
            };

            _logger.LogDebug("Successfully retrieved {Count} of {Total} mnvwComboList_CustomerWithPhoneAndIdentifiers", dtos.Count, totalCount);

            return Result<PagedResult<MnvwComboListCustomerWithPhoneAndIdentifierDto>>.Success(result);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error retrieving mnvwComboList_CustomerWithPhoneAndIdentifiers");
            throw;
        }
    }

    private static IQueryable<MnvwComboListCustomerWithPhoneAndIdentifier> ApplyFilters(IQueryable<MnvwComboListCustomerWithPhoneAndIdentifier> query, MnvwComboListCustomerWithPhoneAndIdentifierFilters? filters)
    {
        if (filters == null)
        {
            return query;
        }

        // TODO: Add filter logic based on table columns
        // Example:
        // if (!string.IsNullOrEmpty(filters.Name))
        // {
        //     query = query.Where(e => e.Name.Contains(filters.Name));
        // }

        return query;
    }

    private static IQueryable<MnvwComboListCustomerWithPhoneAndIdentifier> ApplySorting(IQueryable<MnvwComboListCustomerWithPhoneAndIdentifier> query, string? sortBy, string? sortDirection)
    {
        if (string.IsNullOrEmpty(sortBy))
        {
            return query.OrderBy(e => e.Id); // Default sort
        }

        var isDescending = sortDirection?.ToLower() == "desc";

        // TODO: Add sorting logic based on table columns
        return sortBy.ToLower() switch
        {
            "id" => isDescending ? query.OrderByDescending(e => e.Id) : query.OrderBy(e => e.Id),
            // Add more sortable columns here
            _ => query.OrderBy(e => e.Id) // Default to Id if unknown field
        };
    }
}

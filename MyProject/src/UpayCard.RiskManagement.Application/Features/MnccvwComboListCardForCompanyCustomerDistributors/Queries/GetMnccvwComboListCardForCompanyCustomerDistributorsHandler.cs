// <auto-generated>
//     This code was generated by TargCC
//     Generated for: mnccvwComboList_CardForCompanyCustomerDistributor (Handler)
//     Generation date: 2025-12-17 08:50:00
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

using AutoMapper;
using MediatR;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Logging;
using UpayCard.RiskManagement.Application.Common.Interfaces;
using UpayCard.RiskManagement.Application.Common.Models;
using UpayCard.RiskManagement.Application.DTOs;
using UpayCard.RiskManagement.Domain.Common;
using UpayCard.RiskManagement.Domain.Entities;
using UpayCard.RiskManagement.Domain.Interfaces;

namespace UpayCard.RiskManagement.Application.Features.mnccvwComboList_CardForCompanyCustomerDistributors.Queries;

/// <summary>
/// Handles the GetMnccvwComboListCardForCompanyCustomerDistributorsQuery request with server-side filtering, sorting, and pagination.
/// </summary>
public class GetMnccvwComboListCardForCompanyCustomerDistributorsHandler : IRequestHandler<GetMnccvwComboListCardForCompanyCustomerDistributorsQuery, Result<PagedResult<MnccvwComboListCardForCompanyCustomerDistributorDto>>>
{
    private readonly IApplicationDbContext _context;
    private readonly IMapper _mapper;
    private readonly ILogger<GetMnccvwComboListCardForCompanyCustomerDistributorsHandler> _logger;

    /// <summary>
    /// Initializes a new instance of the <see cref="GetMnccvwComboListCardForCompanyCustomerDistributorsHandler"/> class.
    /// </summary>
    public GetMnccvwComboListCardForCompanyCustomerDistributorsHandler(
        IApplicationDbContext context,
        IMapper mapper,
        ILogger<GetMnccvwComboListCardForCompanyCustomerDistributorsHandler> logger)
    {
        _context = context ?? throw new ArgumentNullException(nameof(context));
        _mapper = mapper ?? throw new ArgumentNullException(nameof(mapper));
        _logger = logger ?? throw new ArgumentNullException(nameof(logger));
    }

    /// <inheritdoc/>
    public async Task<Result<PagedResult<MnccvwComboListCardForCompanyCustomerDistributorDto>>> Handle(GetMnccvwComboListCardForCompanyCustomerDistributorsQuery request, CancellationToken cancellationToken)
    {
        ArgumentNullException.ThrowIfNull(request);

        _logger.LogDebug("Getting mnccvwComboList_CardForCompanyCustomerDistributors - Page: {Page}, Size: {Size}, SortBy: {SortBy}", request.Page, request.PageSize, request.SortBy);

        try
        {
            var query = _context.MnccvwComboListCardForCompanyCustomerDistributors.AsQueryable();

            // Apply filters
            query = ApplyFilters(query, request.Filters);

            // Get total count BEFORE pagination
            var totalCount = await query.CountAsync(cancellationToken);

            // Apply sorting
            query = ApplySorting(query, request.SortBy, request.SortDirection);

            // Apply pagination
            query = query
                .Skip((request.Page - 1) * request.PageSize)
                .Take(request.PageSize);

            var entities = await query.ToListAsync(cancellationToken);
            var dtos = _mapper.Map<List<MnccvwComboListCardForCompanyCustomerDistributorDto>>(entities);

            var result = new PagedResult<MnccvwComboListCardForCompanyCustomerDistributorDto>
            {
                Items = dtos,
                TotalCount = totalCount,
                Page = request.Page,
                PageSize = request.PageSize
            };

            _logger.LogDebug("Successfully retrieved {Count} of {Total} mnccvwComboList_CardForCompanyCustomerDistributors", dtos.Count, totalCount);

            return Result<PagedResult<MnccvwComboListCardForCompanyCustomerDistributorDto>>.Success(result);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error retrieving mnccvwComboList_CardForCompanyCustomerDistributors");
            throw;
        }
    }

    private static IQueryable<MnccvwComboListCardForCompanyCustomerDistributor> ApplyFilters(IQueryable<MnccvwComboListCardForCompanyCustomerDistributor> query, MnccvwComboListCardForCompanyCustomerDistributorFilters? filters)
    {
        if (filters == null)
        {
            return query;
        }

        // TODO: Add filter logic based on table columns
        // Example:
        // if (!string.IsNullOrEmpty(filters.Name))
        // {
        //     query = query.Where(e => e.Name.Contains(filters.Name));
        // }

        return query;
    }

    private static IQueryable<MnccvwComboListCardForCompanyCustomerDistributor> ApplySorting(IQueryable<MnccvwComboListCardForCompanyCustomerDistributor> query, string? sortBy, string? sortDirection)
    {
        if (string.IsNullOrEmpty(sortBy))
        {
            return query.OrderBy(e => e.ID); // Default sort
        }

        var isDescending = sortDirection?.ToLower() == "desc";

        // TODO: Add sorting logic based on table columns
        return sortBy.ToLower() switch
        {
            "id" => isDescending ? query.OrderByDescending(e => e.ID) : query.OrderBy(e => e.ID),
            // Add more sortable columns here
            _ => query.OrderBy(e => e.ID) // Default to ID if unknown field
        };
    }
}

// <auto-generated>
//     This code was generated by TargCC
//     Generated for: NewCardRequestAPI (Handler)
//     Generation date: 2025-12-18 07:42:14
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

using AutoMapper;
using MediatR;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Logging;
using UpayCard.RiskManagement.Application.Common.Interfaces;
using UpayCard.RiskManagement.Application.Common.Models;
using UpayCard.RiskManagement.Application.DTOs;
using UpayCard.RiskManagement.Domain.Common;
using UpayCard.RiskManagement.Domain.Entities;
using UpayCard.RiskManagement.Domain.Interfaces;

namespace UpayCard.RiskManagement.Application.Features.NewCardRequestAPIs.Queries;

/// <summary>
/// Handles the GetNewCardRequestAPIsQuery request with server-side filtering, sorting, and pagination.
/// </summary>
public class GetNewCardRequestAPIsHandler : IRequestHandler<GetNewCardRequestAPIsQuery, Result<PagedResult<NewCardRequestAPIDto>>>
{
    private readonly IApplicationDbContext _context;
    private readonly IMapper _mapper;
    private readonly ILogger<GetNewCardRequestAPIsHandler> _logger;

    /// <summary>
    /// Initializes a new instance of the <see cref="GetNewCardRequestAPIsHandler"/> class.
    /// </summary>
    public GetNewCardRequestAPIsHandler(
        IApplicationDbContext context,
        IMapper mapper,
        ILogger<GetNewCardRequestAPIsHandler> logger)
    {
        _context = context ?? throw new ArgumentNullException(nameof(context));
        _mapper = mapper ?? throw new ArgumentNullException(nameof(mapper));
        _logger = logger ?? throw new ArgumentNullException(nameof(logger));
    }

    /// <inheritdoc/>
    public async Task<Result<PagedResult<NewCardRequestAPIDto>>> Handle(GetNewCardRequestAPIsQuery request, CancellationToken cancellationToken)
    {
        ArgumentNullException.ThrowIfNull(request);

        _logger.LogDebug("Getting NewCardRequestAPIs - Page: {Page}, Size: {Size}, SortBy: {SortBy}", request.Page, request.PageSize, request.SortBy);

        try
        {
            var query = _context.NewCardRequestAPIs.AsQueryable();

            // Apply filters
            query = ApplyFilters(query, request.Filters);

            // Get total count BEFORE pagination
            var totalCount = await query.CountAsync(cancellationToken);

            // Apply sorting
            query = ApplySorting(query, request.SortBy, request.SortDirection);

            // Apply pagination
            query = query
                .Skip((request.Page - 1) * request.PageSize)
                .Take(request.PageSize);

            var entities = await query.ToListAsync(cancellationToken);
            var dtos = _mapper.Map<List<NewCardRequestAPIDto>>(entities);

            var result = new PagedResult<NewCardRequestAPIDto>
            {
                Items = dtos,
                TotalCount = totalCount,
                Page = request.Page,
                PageSize = request.PageSize
            };

            _logger.LogDebug("Successfully retrieved {Count} of {Total} NewCardRequestAPIs", dtos.Count, totalCount);

            return Result<PagedResult<NewCardRequestAPIDto>>.Success(result);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error retrieving NewCardRequestAPIs");
            throw;
        }
    }

    private static IQueryable<NewCardRequestAPI> ApplyFilters(IQueryable<NewCardRequestAPI> query, NewCardRequestAPIFilters? filters)
    {
        if (filters == null)
        {
            return query;
        }

        // TODO: Add filter logic based on table columns
        // Example:
        // if (!string.IsNullOrEmpty(filters.Name))
        // {
        //     query = query.Where(e => e.Name.Contains(filters.Name));
        // }

        return query;
    }

    private static IQueryable<NewCardRequestAPI> ApplySorting(IQueryable<NewCardRequestAPI> query, string? sortBy, string? sortDirection)
    {
        if (string.IsNullOrEmpty(sortBy))
        {
            return query.OrderBy(e => e.ID); // Default sort
        }

        var isDescending = sortDirection?.ToLower() == "desc";

        // TODO: Add sorting logic based on table columns
        return sortBy.ToLower() switch
        {
            "id" => isDescending ? query.OrderByDescending(e => e.ID) : query.OrderBy(e => e.ID),
            // Add more sortable columns here
            _ => query.OrderBy(e => e.ID) // Default to ID if unknown field
        };
    }
}

// <auto-generated>
//     This code was generated by TargCC
//     Generated for: c_SystemAudit (Handler)
//     Generation date: 2025-12-16 08:57:45
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

using AutoMapper;
using MediatR;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Logging;
using UpayCard.RiskManagement.Application.Common.Models;
using UpayCard.RiskManagement.Application.DTOs;
using UpayCard.RiskManagement.Domain.Common;
using UpayCard.RiskManagement.Domain.Entities;
using UpayCard.RiskManagement.Domain.Interfaces;

namespace UpayCard.RiskManagement.Application.Features.c_SystemAudits.Queries;

/// <summary>
/// Handles the GetSystemAuditsQuery request with server-side filtering, sorting, and pagination.
/// </summary>
public class GetSystemAuditsHandler : IRequestHandler<GetSystemAuditsQuery, Result<PagedResult<SystemAuditDto>>>
{
    private readonly IApplicationDbContext _context;
    private readonly IMapper _mapper;
    private readonly ILogger<GetSystemAuditsHandler> _logger;

    /// <summary>
    /// Initializes a new instance of the <see cref="GetSystemAuditsHandler"/> class.
    /// </summary>
    public GetSystemAuditsHandler(
        IApplicationDbContext context,
        IMapper mapper,
        ILogger<GetSystemAuditsHandler> logger)
    {
        _context = context ?? throw new ArgumentNullException(nameof(context));
        _mapper = mapper ?? throw new ArgumentNullException(nameof(mapper));
        _logger = logger ?? throw new ArgumentNullException(nameof(logger));
    }

    /// <inheritdoc/>
    public async Task<Result<PagedResult<SystemAuditDto>>> Handle(GetSystemAuditsQuery request, CancellationToken cancellationToken)
    {
        ArgumentNullException.ThrowIfNull(request);

        _logger.LogDebug("Getting c_SystemAudits - Page: {Page}, Size: {Size}, SortBy: {SortBy}", request.Page, request.PageSize, request.SortBy);

        try
        {
            var query = _context.c_SystemAudits.AsQueryable();

            // Apply filters
            query = ApplyFilters(query, request.Filters);

            // Get total count BEFORE pagination
            var totalCount = await query.CountAsync(cancellationToken);

            // Apply sorting
            query = ApplySorting(query, request.SortBy, request.SortDirection);

            // Apply pagination
            query = query
                .Skip((request.Page - 1) * request.PageSize)
                .Take(request.PageSize);

            var entities = await query.ToListAsync(cancellationToken);
            var dtos = _mapper.Map<List<SystemAuditDto>>(entities);

            var result = new PagedResult<SystemAuditDto>
            {
                Items = dtos,
                TotalCount = totalCount,
                Page = request.Page,
                PageSize = request.PageSize
            };

            _logger.LogDebug("Successfully retrieved {Count} of {Total} c_SystemAudits", dtos.Count, totalCount);

            return Result<PagedResult<SystemAuditDto>>.Success(result);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error retrieving c_SystemAudits");
            throw;
        }
    }

    private static IQueryable<SystemAudit> ApplyFilters(IQueryable<SystemAudit> query, SystemAuditFilters? filters)
    {
        if (filters == null)
        {
            return query;
        }

        // TODO: Add filter logic based on table columns
        // Example:
        // if (!string.IsNullOrEmpty(filters.Name))
        // {
        //     query = query.Where(e => e.Name.Contains(filters.Name));
        // }

        return query;
    }

    private static IQueryable<SystemAudit> ApplySorting(IQueryable<SystemAudit> query, string? sortBy, string? sortDirection)
    {
        if (string.IsNullOrEmpty(sortBy))
        {
            return query.OrderBy(e => e.Id); // Default sort
        }

        var isDescending = sortDirection?.ToLower() == "desc";

        // TODO: Add sorting logic based on table columns
        return sortBy.ToLower() switch
        {
            "id" => isDescending ? query.OrderByDescending(e => e.Id) : query.OrderBy(e => e.Id),
            // Add more sortable columns here
            _ => query.OrderBy(e => e.Id) // Default to Id if unknown field
        };
    }
}

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: Invoice
//     Generated: 2025-12-27 21:28:29
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Invoice,
  CreateInvoiceRequest,
  UpdateInvoiceRequest,
  InvoiceFilters,
} from '../types/Invoice.types';

type UseInvoiceOptions = UseEntityOptions<InvoiceFilters>;

/**
 * API client for Invoice operations.
 * Base URL: /Invoices
 */
export const invoiceApi = {
  /**
   * Get Invoice by ID.
   */
  getById: async (id: number): Promise<Invoice> => {
    const response = await api.get<Invoice>(`/Invoices/${id}`);
    return response.data;
  },


  /**
   * Get all Invoices with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseInvoiceOptions): Promise<PagedResult<Invoice>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Invoice>>(`/Invoices?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Invoice.
   */
  create: async (data: CreateInvoiceRequest): Promise<Invoice> => {
    const response = await api.post<Invoice>('/Invoices', data);
    return response.data;
  },


  /**
   * Update existing Invoice.
   */
  update: async (id: number, data: UpdateInvoiceRequest): Promise<Invoice> => {
    const response = await api.put<Invoice>(`/Invoices/${id}`, data);
    return response.data;
  },


  /**
   * Delete Invoice.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Invoices/${id}`);
  },


  /**
   * Get Invoice by InvoiceCode.
   */
  getByInvoiceCode: async (invoiceCode: string): Promise<Invoice> => {
    const response = await api.get<Invoice>(`/Invoices/by-invoiceCode/${encodeURIComponent(String(invoiceCode))}`);
    return response.data;
  },


  /**
   * Get Invoice by CustomerID.
   */
  getByCustomerID: async (customerID: number): Promise<Invoice[]> => {
    const response = await api.get<Invoice[]>(`/Invoices/by-customerID/${encodeURIComponent(String(customerID))}`);
    return response.data;
  },


  /**
   * Get Invoice by DistributorID.
   */
  getByDistributorID: async (distributorID: number): Promise<Invoice[]> => {
    const response = await api.get<Invoice[]>(`/Invoices/by-distributorID/${encodeURIComponent(String(distributorID))}`);
    return response.data;
  },


  /**
   * Get Invoice by Month.
   */
  getByMonth: async (month: Date): Promise<Invoice[]> => {
    const response = await api.get<Invoice[]>(`/Invoices/by-month/${encodeURIComponent(String(month))}`);
    return response.data;
  },


  /**
   * Get Invoice by Url.
   */
  getByUrl: async (url: string): Promise<Invoice[]> => {
    const response = await api.get<Invoice[]>(`/Invoices/by-url/${encodeURIComponent(String(url))}`);
    return response.data;
  },


};

// <auto-generated>
//     This code was generated by TargCC UI Generator.
//     Generator: ReactApi
//     Table: c_Process
//     Generated: 2025-12-27 21:28:27
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

import { api } from './client';
import type { PagedResult, UseEntityOptions } from '../types/common.types';
import type {
  Process,
  CreateProcessRequest,
  UpdateProcessRequest,
  ProcessFilters,
} from '../types/Process.types';

type UseProcessOptions = UseEntityOptions<ProcessFilters>;
import type { Permission } from '../types/Permission.types';

/**
 * API client for Process operations.
 * Base URL: /Processes
 */
export const processApi = {
  /**
   * Get Process by ID.
   */
  getById: async (id: number): Promise<Process> => {
    const response = await api.get<Process>(`/Processes/${id}`);
    return response.data;
  },


  /**
   * Get all Processs with server-side filtering, sorting, and pagination.
   */
  getAll: async (options?: UseProcessOptions): Promise<PagedResult<Process>> => {
    const params = new URLSearchParams();

    // Add pagination params
    if (options?.page) params.append('page', options.page.toString());
    if (options?.pageSize) params.append('pageSize', options.pageSize.toString());
    if (options?.sortBy) params.append('sortBy', options.sortBy);
    if (options?.sortDirection) params.append('sortDirection', options.sortDirection);

    // Add filter params
    if (options?.filters) {
      Object.entries(options.filters).forEach(([key, value]) => {
        if (value !== undefined && value !== null && value !== '') {
          params.append(key, value.toString());
        }
      });
    }

    const response = await api.get<PagedResult<Process>>(`/Processes?${params.toString()}`);
    return response.data;
  },


  /**
   * Create new Process.
   */
  create: async (data: CreateProcessRequest): Promise<Process> => {
    const response = await api.post<Process>('/Processes', data);
    return response.data;
  },


  /**
   * Update existing Process.
   */
  update: async (id: number, data: UpdateProcessRequest): Promise<Process> => {
    const response = await api.put<Process>(`/Processes/${id}`, data);
    return response.data;
  },


  /**
   * Delete Process.
   */
  delete: async (id: number): Promise<void> => {
    await api.delete(`/Processes/${id}`);
  },


  /**
   * Get Process by Name.
   */
  getByName: async (name: string): Promise<Process> => {
    const response = await api.get<Process>(`/Processes/by-name/${encodeURIComponent(String(name))}`);
    return response.data;
  },


  /**
   * Get PERMISSIONS for this PROCESS.
   */
  getPermissions: async (id: number, skip?: number, take?: number): Promise<Permission[]> => {
    const params = new URLSearchParams();
    if (skip !== undefined) params.append('skip', skip.toString());
    if (take !== undefined) params.append('take', take.toString());
    const queryString = params.toString() ? `?${params.toString()}` : '';
    const response = await api.get<Permission[]>(`/Processes/${id}/PERMISSIONS${queryString}`);
    return response.data;
  },


};

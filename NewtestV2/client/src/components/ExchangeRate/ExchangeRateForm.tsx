// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: ExchangeRate
//     Generated: 2025-12-27 21:28:28
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useExchangeRate, useCreateExchangeRate, useUpdateExchangeRate } from '../../hooks/useExchangeRate';
import type { CreateExchangeRateRequest, UpdateExchangeRateRequest } from '../../types/ExchangeRate.types';


export const ExchangeRateForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const exchangeRateId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!exchangeRateId;

  const { data: existingExchangeRate, isPending: isLoadingEntity } = useExchangeRate(exchangeRateId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateExchangeRateRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingExchangeRate) {
      reset(existingExchangeRate);
    }
  }, [existingExchangeRate, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateExchangeRate();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateExchangeRate();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateExchangeRateRequest) => {
    if (isEdit && exchangeRateId) {
      updateEntity(
        { id: exchangeRateId, data: data as UpdateExchangeRateRequest },
        { onSuccess: () => navigate('/exchangeRates') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/exchangeRates') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameExchangeRate')} />}
        label="TableNameExchangeRate"
      />

      <TextField
        label="Date"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('date', { required: 'Date is required', maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.date}
        helperText={errors.date?.message}
      />

      <TextField
        label="EnmCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmCurrency', { required: 'EnmCurrency is required', maxLength: { value: 3, message: 'Maximum length is 3' } })}
        error={!!errors.enmCurrency}
        helperText={errors.enmCurrency?.message}
      />

      <TextField
        label="ExchangeRateToIls"
        type="number"
        fullWidth
        margin="normal"
        {...register('exchangeRateToIls', { maxLength: { value: 9, message: 'Maximum length is 9' } })}
        error={!!errors.exchangeRateToIls}
        helperText={errors.exchangeRateToIls?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/exchangeRates')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


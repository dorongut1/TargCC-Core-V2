// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: MCCBFPMobileWallet
//     Generated: 2025-12-27 21:28:29
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useMCCBFPMobileWallet, useCreateMCCBFPMobileWallet, useUpdateMCCBFPMobileWallet } from '../../hooks/useMCCBFPMobileWallet';
import type { CreateMCCBFPMobileWalletRequest, UpdateMCCBFPMobileWalletRequest } from '../../types/MCCBFPMobileWallet.types';


export const MCCBFPMobileWalletForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mCCBFPMobileWalletId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mCCBFPMobileWalletId;

  const { data: existingMCCBFPMobileWallet, isPending: isLoadingEntity } = useMCCBFPMobileWallet(mCCBFPMobileWalletId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMCCBFPMobileWalletRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMCCBFPMobileWallet) {
      reset(existingMCCBFPMobileWallet);
    }
  }, [existingMCCBFPMobileWallet, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMCCBFPMobileWallet();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMCCBFPMobileWallet();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMCCBFPMobileWalletRequest) => {
    if (isEdit && mCCBFPMobileWalletId) {
      updateEntity(
        { id: mCCBFPMobileWalletId, data: data as UpdateMCCBFPMobileWalletRequest },
        { onSuccess: () => navigate('/mCCBFPMobileWallets') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mCCBFPMobileWallets') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <FormControlLabel
        control={<Checkbox {...register('tableNameMCCBFPMobileWallet')} />}
        label="TableNameMCCBFPMobileWallet"
      />

      <TextField
        label="Region"
        type="text"
        fullWidth
        margin="normal"
        {...register('region', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.region}
        helperText={errors.region?.message}
      />

      <TextField
        label="Country"
        type="text"
        fullWidth
        margin="normal"
        {...register('country', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.country}
        helperText={errors.country?.message}
      />

      <TextField
        label="Brand"
        type="text"
        fullWidth
        margin="normal"
        {...register('brand', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.brand}
        helperText={errors.brand?.message}
      />

      <TextField
        label="PayoutCurrency"
        type="text"
        fullWidth
        margin="normal"
        {...register('payoutCurrency', { maxLength: { value: 4, message: 'Maximum length is 4' } })}
        error={!!errors.payoutCurrency}
        helperText={errors.payoutCurrency?.message}
      />

      <TextField
        label="Channel"
        type="text"
        fullWidth
        margin="normal"
        {...register('channel', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.channel}
        helperText={errors.channel?.message}
      />

      <TextField
        label="Corridors"
        type="text"
        fullWidth
        margin="normal"
        {...register('corridors', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.corridors}
        helperText={errors.corridors?.message}
      />

      <TextField
        label="MinimumTransactionAmount"
        type="text"
        fullWidth
        margin="normal"
        {...register('minimumTransactionAmount', { maxLength: { value: 250, message: 'Maximum length is 250' } })}
        error={!!errors.minimumTransactionAmount}
        helperText={errors.minimumTransactionAmount?.message}
      />

      <TextField
        label="MaximumTransactionAmount"
        type="text"
        fullWidth
        margin="normal"
        {...register('maximumTransactionAmount', { maxLength: { value: 1000, message: 'Maximum length is 1000' } })}
        error={!!errors.maximumTransactionAmount}
        helperText={errors.maximumTransactionAmount?.message}
      />

      <TextField
        label="OnTheFlyWalletCreation"
        type="text"
        fullWidth
        margin="normal"
        {...register('onTheFlyWalletCreation', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.onTheFlyWalletCreation}
        helperText={errors.onTheFlyWalletCreation?.message}
      />

      <TextField
        label="DeliveryTime"
        type="text"
        fullWidth
        margin="normal"
        {...register('deliveryTime', { maxLength: { value: 60, message: 'Maximum length is 60' } })}
        error={!!errors.deliveryTime}
        helperText={errors.deliveryTime?.message}
      />

      <TextField
        label="OIPreapprovalRequired"
        type="text"
        fullWidth
        margin="normal"
        {...register('oIPreapprovalRequired', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.oIPreapprovalRequired}
        helperText={errors.oIPreapprovalRequired?.message}
      />

      <TextField
        label="RequiredInformation"
        type="text"
        fullWidth
        margin="normal"
        {...register('requiredInformation', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.requiredInformation}
        helperText={errors.requiredInformation?.message}
      />

      <TextField
        label="AverageResponseTimeFromRSP"
        type="text"
        fullWidth
        margin="normal"
        {...register('averageResponseTimeFromRSP', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.averageResponseTimeFromRSP}
        helperText={errors.averageResponseTimeFromRSP?.message}
      />

      <TextField
        label="DeletedBy"
        type="text"
        fullWidth
        margin="normal"
        {...register('deletedBy', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.deletedBy}
        helperText={errors.deletedBy?.message}
      />

      <TextField
        label="DeletedOn"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('deletedOn', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.deletedOn}
        helperText={errors.deletedOn?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mCCBFPMobileWallets')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


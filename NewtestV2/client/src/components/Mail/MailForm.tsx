// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: c_Mail
//     Generated: 2025-12-27 21:28:27
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useMail, useCreateMail, useUpdateMail } from '../../hooks/useMail';
import type { CreateMailRequest, UpdateMailRequest } from '../../types/Mail.types';


export const MailForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const mailId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!mailId;

  const { data: existingMail, isPending: isLoadingEntity } = useMail(mailId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateMailRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingMail) {
      reset(existingMail);
    }
  }, [existingMail, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateMail();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateMail();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateMailRequest) => {
    if (isEdit && mailId) {
      updateEntity(
        { id: mailId, data: data as UpdateMailRequest },
        { onSuccess: () => navigate('/mails') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/mails') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="EnmMessagingMode"
        type="text"
        fullWidth
        margin="normal"
        {...register('enmMessagingMode', { maxLength: { value: 50, message: 'Maximum length is 50' } })}
        error={!!errors.enmMessagingMode}
        helperText={errors.enmMessagingMode?.message}
      />

      <TextField
        label="RecipientEmail"
        type="text"
        fullWidth
        margin="normal"
        {...register('recipientEmail', { maxLength: { value: 100, message: 'Maximum length is 100' }, pattern: { value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i, message: 'Invalid email address' } })}
        error={!!errors.recipientEmail}
        helperText={errors.recipientEmail?.message}
      />

      <TextField
        label="WhenSent"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenSent', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenSent}
        helperText={errors.whenSent?.message}
      />

      <TextField
        label="Subject"
        type="text"
        fullWidth
        margin="normal"
        {...register('subject', { maxLength: { value: 100, message: 'Maximum length is 100' } })}
        error={!!errors.subject}
        helperText={errors.subject?.message}
      />

      <TextField
        label="Body"
        type="text"
        fullWidth
        margin="normal"
        {...register('body', {  })}
        error={!!errors.body}
        helperText={errors.body?.message}
      />

      <TextField
        label="WhenSeen"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('whenSeen', { maxLength: { value: 10, message: 'Maximum length is 10' } })}
        error={!!errors.whenSeen}
        helperText={errors.whenSeen?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('wasSeen')} />}
        label="WasSeen"
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/mails')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};


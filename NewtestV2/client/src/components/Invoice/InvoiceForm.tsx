// <auto-generated>
//     This code was generated by TargCC Component Generator.
//     Table: Invoice
//     Generated: 2025-12-27 21:28:29
// </auto-generated>

import React, { useEffect } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { useForm } from 'react-hook-form';
import { TextField, Button, Box, CircularProgress, Checkbox, FormControlLabel } from '@mui/material';
import { useInvoice, useCreateInvoice, useUpdateInvoice } from '../../hooks/useInvoice';
import type { CreateInvoiceRequest, UpdateInvoiceRequest } from '../../types/Invoice.types';


export const InvoiceForm: React.FC = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const invoiceId = id && id !== 'new' ? Number(id) : undefined;
  const isEdit = !!invoiceId;

  const { data: existingInvoice, isPending: isLoadingEntity } = useInvoice(invoiceId || null);

  const {
    register,
    handleSubmit,
    reset,
    formState: { errors },
  } = useForm<CreateInvoiceRequest>({
    defaultValues: {
    },
  });

  // Update form when data loads
  useEffect(() => {
    if (existingInvoice) {
      reset(existingInvoice);
    }
  }, [existingInvoice, reset]);

  const { mutate: createEntity, isPending: isCreating } = useCreateInvoice();
  const { mutate: updateEntity, isPending: isUpdating } = useUpdateInvoice();

  const isSubmitting = isCreating || isUpdating;

  const onSubmit = (data: CreateInvoiceRequest) => {
    if (isEdit && invoiceId) {
      updateEntity(
        { id: invoiceId, data: data as UpdateInvoiceRequest },
        { onSuccess: () => navigate('/invoices') }
      );
    } else {
      createEntity(data, { onSuccess: () => navigate('/invoices') });
    }
  };

  if (isEdit && isLoadingEntity) {
    return <CircularProgress />;
  }

  return (
    <Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{ maxWidth: 600 }}>
      <TextField
        label="Month"
        type="date"
        fullWidth
        margin="normal"
        InputLabelProps={{ shrink: true }}
        {...register('month', { required: 'Month is required', maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.month}
        helperText={errors.month?.message}
      />

      <TextField
        label="CustomerID"
        type="number"
        fullWidth
        margin="normal"
        {...register('customerID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.customerID}
        helperText={errors.customerID?.message}
      />

      <TextField
        label="DistributorID"
        type="number"
        fullWidth
        margin="normal"
        {...register('distributorID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.distributorID}
        helperText={errors.distributorID?.message}
      />

      <TextField
        label="InvoiceCode"
        type="text"
        fullWidth
        margin="normal"
        {...register('invoiceCode', { required: 'InvoiceCode is required', maxLength: { value: 200, message: 'Maximum length is 200' } })}
        error={!!errors.invoiceCode}
        helperText={errors.invoiceCode?.message}
      />

      <TextField
        label="Url"
        type="text"
        fullWidth
        margin="normal"
        {...register('url', { maxLength: { value: 1000, message: 'Maximum length is 1000' }, pattern: { value: /^https?:\/\/.+/, message: 'Invalid URL' } })}
        error={!!errors.url}
        helperText={errors.url?.message}
      />

      <TextField
        label="UpdatingLoginID"
        type="number"
        fullWidth
        margin="normal"
        {...register('updatingLoginID', { maxLength: { value: 8, message: 'Maximum length is 8' } })}
        error={!!errors.updatingLoginID}
        helperText={errors.updatingLoginID?.message}
      />

      <FormControlLabel
        control={<Checkbox {...register('tableNameInvoice')} />}
        label="TableNameInvoice"
      />



      <Box sx={{ mt: 2, display: 'flex', gap: 2 }}>
        <Button type="submit" variant="contained" disabled={isSubmitting}>
          {isEdit ? 'Update' : 'Create'}
        </Button>
        <Button variant="outlined" onClick={() => navigate('/invoices')}>
          Cancel
        </Button>
      </Box>
    </Box>
  );
};

